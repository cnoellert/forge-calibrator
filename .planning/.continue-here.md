---
status: paused-mid-debug
session: aim-rig-roundtrip-offset
created: 2026-04-25
last_commit: 6d7c4d4
context_reason: live UAT bug investigation hit context limit before round-trip verified
---

# Continue Here — Aim-Rig Round-Trip Debug, Probe 4 Crash

## TL;DR

User is debugging a Flame↔Blender aim-rig round-trip bug live. We had **localized the bug** (stale Blender addon v1.1.1, fixed by rebuilt v1.2.0 zip — see commit `6d7c4d4`). User installed v1.2.0 in Blender, retried the round-trip, and hit a **NEW SIGSEGV** at the same address `0x00001508`.

The new error pattern is **different** from earlier probes (1, 2, 3) and may be an independent Flame hook-loader issue exposed when the stale-addon bug was fixed.

## Open Question Routed to Next Session

Is the new SIGSEGV (Probe 4) caused by:

**Hypothesis A (install-layout residual):** A stray `/opt/Autodesk/shared/python/__init__.py` survived the cleanup and is making Flame's hook scanner treat the whole tree as one namespace, causing the duplicate-module warnings, the ImportError on `forge_core/solver/solver.py`'s relative imports, and ultimately the SIGSEGV.

**Hypothesis B (Flame hook scanner pre-existing):** Flame's hook loader has always walked the entire `/opt/Autodesk/shared/python/` tree, the duplicate-init warnings and ImportErrors have always been tolerated noise, and Phase 04.3's code introduced a new code path (perhaps something in `forge_core/solver/solver.py` or its dependencies) that pushes Flame from "tolerated noise" to "actual SIGSEGV" when triggered by import_fbx.

The first command in §"User-side data needed" below distinguishes A from B.

## Probe 4 Evidence (live-UAT 2026-04-25 post-v1.2.0 install)

```
Python encountered ImportError('attempted relative import with no known parent package') in unknown code
[PYTHON HOOK] An error occurred. Ignoring python hooks from /opt/Autodesk/shared/python/forge_core/solver/solver.py
[PYTHON HOOK] Duplicate module name found __init__ (/opt/Autodesk/shared/python/forge_core/solver/__init__.py vs /opt/Autodesk/shared/python/forge_flame/__init__.py).
[PYTHON HOOK] Duplicate module name found __init__ (/opt/Autodesk/shared/python/forge_core/math/__init__.py vs /opt/Autodesk/shared/python/forge_flame/__init__.py).
[PYTHON HOOK] Duplicate module name found __init__ (/opt/Autodesk/shared/python/forge_core/image/__init__.py vs /opt/Autodesk/shared/python/forge_flame/__init__.py).
[PYTHON HOOK] Duplicate module name found __init__ (/opt/Autodesk/shared/python/forge_core/colour/__init__.py vs /opt/Autodesk/shared/python/forge_flame/__init__.py).
[PYTHON HOOK] Duplicate module name found __init__ (/opt/Autodesk/shared/python/camera_match/__init__.py vs /opt/Autodesk/shared/python/forge_flame/__init__.py).
[PYTHON HOOK] Duplicate module name found __init__ (/opt/Autodesk/shared/python/forge_core/__init__.py vs /opt/Autodesk/shared/python/forge_flame/__init__.py).
(repeats once)
Application: flame 2026.2.1 arm64
Pid: 37328
Error: abnormal termination, signal = 11
SIGSEGV - Segmentation Fault
Signal was generated internally:
invalid permissions for mapped object at address 0x00001508
```

## Pre-Probe-4 Findings (still in force)

Probe 1: bpy ModuleNotFoundError + crash → caused by bad rsync deploying `tools/blender/` into Flame's hook tree. Fixed by manual cleanup + install.sh.

Probe 2: `'flame' is not a package` ModuleNotFoundError + SIGSEGV → caused by same bad rsync flattening four packages' contents at `/opt/Autodesk/shared/python/` top level, shadowing Flame's bundled `flame` module. Fixed by manual cleanup of top-level files: action_export.py, apply_solve.py, camera_match_hook.py, rotation_diagnostic.py, solve_and_update.py, adapter.py, blender_bridge.py, camera_io.py, fbx_ascii.py, fbx_io.py, wiretap.py, fspy_import.py, plus subdirs blender/, colour/, image/, math/, solver/, smoke-test/, templates/, and `__init__.py` (top-level). Phase 01-VERIFICATION.md Test 1 doc also fixed (commit `ea36515`) so the catastrophic rsync command isn't recommended again.

Probe 3: round-trip completed without crash but returned-camera rz was sign-flipped (Camera1 came back as Camera3 with rotation `(1.8°, 1.1°, -1.3°)` instead of the expected `(1.814°, 1.058°, +1.252°)`). Root cause: stale Blender addon v1.1.1 — pre-Phase-04.3 decomposer running against post-Phase-04.3 cam_rot from the bake. Fixed by rebuilding the addon as v1.2.0 (commit `6d7c4d4`); user installed it in Blender; reproduction script `/tmp/aim_rig_roundtrip_repro.py` confirms variant A (current source on both ends) round-trips correctly.

Probe 4 (current): user retried the round-trip with v1.2.0 Blender addon. Hard crash. SIGSEGV again, but the error signature is DIFFERENT (relative-import error in solver.py, plus a flood of duplicate-init warnings).

## User-Side Data Needed (Probe 4 triage)

When user is back, run on the production stack:

```bash
# 1. Is there a stray top-level __init__.py?
ls -la /opt/Autodesk/shared/python/__init__.py 2>&1

# 2. What top-level .py files exist? (should be NONE — install.sh deploys only into subdirs)
find /opt/Autodesk/shared/python/ -maxdepth 1 -name "*.py" 2>&1

# 3. What __init__.py files exist at depth 1-2?
find /opt/Autodesk/shared/python/ -maxdepth 2 -name "__init__.py" 2>&1

# Expected output for #3 (correct install state):
#   /opt/Autodesk/shared/python/camera_match/__init__.py    (install.sh marker)
#   /opt/Autodesk/shared/python/forge_core/__init__.py
#   /opt/Autodesk/shared/python/forge_flame/__init__.py
#   /opt/Autodesk/shared/python/forge_bridge/__init__.py    (if forge-bridge has one)
#
# Anything else at top level (e.g. /opt/Autodesk/shared/python/__init__.py) is residual
# and should be deleted with `rm -f`.
```

If output #1 shows a file → Hypothesis A confirmed. `rm -f /opt/Autodesk/shared/python/__init__.py` and retry.

If output #1 says "No such file" but the duplicate warnings persist → Hypothesis B in play. Need to investigate `forge_core/solver/solver.py` and what about Phase 04.3 might have made it crash where it tolerated before. Possibly add a `# fmt: hook-shield` mechanism or move solver.py to a deeper path Flame's scanner doesn't walk. Also worth comparing pre-04.3 git revisions of solver.py and surrounding files to see if anything changed.

## Codebase State at Pause

Branch: `main` at `6d7c4d4 fix(04.2-debug): bump forge_sender addon to v1.2.0; add anti-oracle hand-built tests`

Test suite: 400 passed, 2 skipped (mathutils-gated). All Phase 04.3 + Probe 3 fixes committed.

Uncommitted (unrelated to this debug):
- `M .planning/phases/04.1-.../04.1-HUMAN-UAT.md` (left over from session start, unrelated)
- `?? .claude/` (agent state)
- `?? .planning/phases/04.2-.../04.2-PATTERNS.md` (Phase 04.2 leftover, unrelated)

## Active Debug Session

`.planning/debug/aim-rig-roundtrip-offset.md` — status `fixed-needs-uat` for Probe 3. Probe 4 should be appended as a new evidence/escalation entry, NOT a new session, because it's the same round-trip flow and may share root cause with the install-layout corruption history.

Continue command: `/gsd-debug continue aim-rig-roundtrip-offset`

## Reproduction script

`/tmp/aim_rig_roundtrip_repro.py` — reproduces variant A (correct), variant B (Phase 04.3 stale-bake), variant C (stale-addon = pre-Phase-04.3 extract on Phase-04.3 bake → matches user's Probe 3 symptom). Re-runnable on dev box:

```bash
source ~/miniconda3/etc/profile.d/conda.sh && conda activate forge && python /tmp/aim_rig_roundtrip_repro.py
```

## Outstanding Phase 04.2 UAT State

`.planning/phases/04.2-aim-target-rig-camera-orientation-round-trip/04.2-HUMAN-UAT.md`:
- Test 1 (Flame→Blender): pending (live-Flame required, unblocked once round-trip works)
- Test 2 (Blender→Flame round-trip): issue, blocker, with evolving Probes 1-4 evidence trail
- Test 3 (D-10 apply-path smoke): pending

Once Probe 4 is resolved and the round-trip is verifiably clean, all three of these become testable.

## Outstanding Phase-Level Work

Phase 04.3 is marked complete in ROADMAP.md as of session start. The Probe 3 fix (forge_sender v1.2.0 addon) ships in commit `6d7c4d4`. If Probe 4 is a Phase 04.3 fallout, may need a Phase 04.4 (insert) to handle. If Probe 4 is install-layout-residual (Hypothesis A), it's a one-line cleanup, not a new phase.

## Next Suggested Actions

1. Get the user's output from the three commands in §"User-Side Data Needed"
2. Resolve Probe 4 per Hypothesis A or B path
3. Re-run round-trip; if Camera3 (or whatever the new returned camera is named) shows rotation aligned with Camera1's frustum, mark Test 2 finally `pass` (this time legitimately)
4. Continue with Phase 01/04.1/04.2 Test 1/Test 3 (the live-Flame UAT items previously blocked)
5. Optional polish: surface the install-layout fragility as a backlog item — install.sh should either auto-install the Blender addon OR detect/warn on stale addon installs OR check for stray top-level files in /opt/Autodesk/shared/python/ on every run

## Key Files for Cold Pickup

Read in this order:
1. `.planning/.continue-here.md` (this file)
2. `.planning/debug/aim-rig-roundtrip-offset.md` (full debug session — Probes 1-3 evidence)
3. `.planning/phases/04.2-aim-target-rig-camera-orientation-round-trip/04.2-HUMAN-UAT.md` (UAT trail)
4. `tools/blender/forge_sender/__init__.py` + `flame_math.py` (Phase 04.3 addon source)
5. `forge_core/math/rotations.py` (parser side, untouched by Probe 3 fix)

## Open File on User's Side

User has Flame open with Camera1 (aim-rig original) + Camera3 (returned, sign-flipped pre-fix). After Probe 4 cleanup, they'll send the camera back via the v1.2.0 forge-sender addon and produce a new Camera (probably Camera4). That's the test gate.
