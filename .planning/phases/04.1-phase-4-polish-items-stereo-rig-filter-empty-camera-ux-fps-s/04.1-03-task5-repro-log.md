---
phase: "04.1"
plan: "03"
task: "2 — N=5 repro attempt for Task 5 Flame crash"
outcome: "no-crash (informal)"
date: "2026-04-23"
---

# Task 5 Flame Crash — Repro Session Log

## Outcome: no-crash (informal)

The originally scoped N=5 roundtrip loop was **not run exhaustively**. The session was redirected to resolve an upstream blocker before roundtripping was possible, and once the blocker was cleared the user confirmed a working roundtrip by inspection rather than running the full 5-iteration protocol.

## What happened

1. User launched Flame 2026.2.1 and attempted step (a) of the protocol: `Action → Camera Match → Export Camera to Blender`.
2. Export failed with `Blender bake failed (exit 1): baked.json: no frames in JSON` on a **static** (zero-keyframe) camera. This is the Flame→Blender direction — not the Blender→Flame crash that this plan was instrumenting.
3. Investigation via `/gsd-debug` (session `baked-json-no-frames`, now in `.planning/debug/resolved/`) identified the root cause as a **deployment gap**: the installed hook at `/opt/Autodesk/shared/python/camera_match/camera_match.py` (modified 2026-04-22 13:10) predated the 04.1-02 merge commit `e708944` (16:13) by 3 hours. `install.sh` had not been re-run after wave 1 merged.
4. User ran `./install.sh --force` and restarted Flame.
5. User re-tested export on the static camera. Reply: **"approved. Appears to be working"**.

## Closure rationale (D-08 / D-09 invariant)

Per the plan's decision log:

- **D-08:** instrumentation ships regardless of repro outcome — "cheap insurance for future crashes, not just this one".
- **D-09:** the Task 5 crash is **not a v6.3 ship gate**. Informal "no crash observed during the working-state validation" is acceptable closure when coupled with shipped instrumentation.

The informal roundtrip validation produced no crash. If a future Send-to-Flame crash fires, `forge_send_debug.log` will self-capture the phase tag at the point of failure, which is the value this plan's instrumentation delivers.

## What was NOT done

- The full N=5 (or escalation to N=10) roundtrip loop was not mechanically executed with each iteration logged.
- No `forge_send_debug.log` artifact from a successful roundtrip was collected and attached to this log.

Neither is a ship gate per D-09. A dedicated repro session is still possible in a future milestone if crash-behaviour data becomes load-bearing.

## Task 5 closure

- **Status:** closed via D-08 (instrumentation shipped) + D-09 (crash not a ship gate) + informal no-crash validation.
- **Phase 4.1 Item 3 (GA-4):** resolved.
- **Follow-up:** none required for v6.3. The installed hook is now current; `forge_send_debug.log` will activate on the next `_FLAME_SIDE_TEMPLATE` invocation.
