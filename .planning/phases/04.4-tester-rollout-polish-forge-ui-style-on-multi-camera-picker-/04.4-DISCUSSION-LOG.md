# Phase 04.4: Tester-rollout polish — Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-25
**Phase:** 04.4-tester-rollout-polish-forge-ui-style-on-multi-camera-picker
**Areas discussed:** Multi-camera picker scope, Right-click on camera nodes inside Action, Bidirectional Blender import, Menu reorganization

---

## Multi-camera picker scope

| Option | Description | Selected |
|--------|-------------|----------|
| (a) Palette-only | Replace `QInputDialog.getItem` with a styled `QDialog` using the FORGE palette + em-dash title. Same data shape (label list). 999.1 polish items deferred. | ✓ |
| (b) Palette + sort order | Palette plus deterministic sort (alphabetical / creation order / most-recently-solved). | |
| (c) Palette + sort + active-cam indicator | Palette, sort, and a marker on the currently-active-in-viewport camera. | |
| (d) Full 999.1 | Palette, sort, indicator, thumbnail previews, last-picked memory. | |

**User's choice:** (a) Palette-only.
**Notes:** 999.1 fold-in is partial — only the styling bullet closes; thumbnails, sort order, active indicator, name disambiguation, and last-picked memory all stay deferred.

---

## Right-click on camera nodes inside Action

| Option | Description | Selected |
|--------|-------------|----------|
| Two entry points, one handler | Action-scope right-click keeps picker (selection has no implicit camera). Camera-node-scope right-click bypasses picker (selection IS the camera). Both menu items visible. | ✓ |
| Hide Action menu when camera-node menu applies | Conditional visibility based on Flame's selection model. | |
| Camera-node-scope only | Drop the Action-scope right-click entirely. | |

**User's choice:** Two entry points, one handler.
**Notes:** "These are just two different entry points to the same function. One is just scoped as selection, the other requires a selection as one is not implicit." Probe deferred to planner: does Flame 2026.2.1's `isVisible` callback fire on camera-node selection inside an Action's schematic?

---

## Bidirectional Blender import — overall direction

| Option | Description | Selected |
|--------|-------------|----------|
| (a) Pull-from-live | Flame queries an HTTP endpoint inside a running Blender for its current camera. Reuses v5 JSON shape. | |
| (b) Reuse existing Send-to-Flame push only | Drop the Flame-side "Import from live Blender" idea. Document the addon's existing button as the live-session path. Flame's import menu only handles `.blend` files. | |
| (c) Hybrid | Flame's "Import from Blender" presents a sub-choice: file / live Blender. | |
| (d) Pivot — drop Flame import, extend Blender push | Remove "Import Camera from Blender" from Flame's menu entirely. Extend `forge_sender` addon to handle cameras without forge metadata by pushing into a new (or chosen) Flame Action. | ✓ |

**User's choice:** (d) — proposed by the user as "Perhaps a better approach is just a push from Blender that allows for non forge cameras to be push to a new action and we remove the import from Blender?"
**Notes:** This collapses the bidirectional-import gray area into a Blender-side scope expansion plus a Flame-side menu deletion.

---

## Bidirectional Blender import — sub-decisions

### 3a. Target-Action selection mechanism in addon (for non-forge cameras)

| Option | Description | Selected |
|--------|-------------|----------|
| (i) Text field for new Action name | Bridge creates an Action with the user-supplied name. | |
| (ii) Dropdown of existing Actions + create-new option | Addon queries forge-bridge for existing Actions, presents dropdown plus "Create new" option. | ✓ |
| (iii) Always create a new Action with auto-name | E.g. `Action_<camera_name>`; user renames in Flame after. | |

**User's choice:** (ii).
**Notes:** Action enumeration scope is the **currently-loaded Batch only** — `flame.batch.nodes` filtered by `type == "Action"`.

### 3b. Detection of non-forge camera in addon

**User's choice:** Free-form — "I think we check meta data, if non exists we throw the dialog from 3a."
**Notes:** Existing path (metadata present → use stamped Action) stays. Metadata absent → trigger 3a dropdown dialog rather than erroring.

### 3c. Final menu shape after dropping Flame-side import

**User's choice:** Free-form — "Open Camera Calibrator is fine to keep as is, as is Export Camera to Blender."
**Notes:** Final menu shape is `FORGE / Camera / {Open Camera Calibrator, Export Camera to Blender}`. "Import Camera from Blender" removed.

---

## Menu reorganization (FORGE / Camera / X)

### FORGE scope — all FORGE tools or just camera-calibrator?

**User's choice:** Free-form — "FORGE is indeed the parent for all FORGE tools in my mind."
**Notes:** This phase only registers the camera-calibrator items; future FORGE tools (forge_cv_align migration etc.) land here in their own phases.

### Hook rename — rename `camera_match_hook.py` and module / install paths?

| Option | Description | Selected |
|--------|-------------|----------|
| Rename internal paths | `camera_match_hook.py` → `camera_calibrator_hook.py` plus module / install-dir renames. Riskier — touches imports, install.sh, and Flame's hook loader. | |
| Keep internal names, change labels + docs only | Internal naming unchanged. Only user-facing labels and menu hierarchy change. Docs updated. Safer. | ✓ |

**User's choice:** Keep internal names — "I think the renaming of the hook is less important. If it's easier to leave these as they are currently named, update the menustructure and documentation than I think that's acceptable as well."
**Notes:** Hook rename deferred to v2 if naming drift becomes painful.

### Backwards compatibility — keep old `Camera Match` submenu visible during transition?

| Option | Description | Selected |
|--------|-------------|----------|
| Hard cut | Old menu disappears entirely on next install. | ✓ |
| One-cycle transition | Keep old menu visible alongside the new one for one tester cycle. | |

**User's choice:** Hard cut — "No one else has tested. We're trying to get this going before hand."
**Notes:** No testers have used the tool yet; the rollout itself is the introduction.

### Action enumeration scope — currently-loaded Batch only?

**User's choice:** Free-form — "That is correct. We'll be looking only in batch."
**Notes:** `flame.batch.nodes` filtered by `type == "Action"`. Not Actions across other Desktops or Reels.

---

## Claude's Discretion

- Exact widget composition of the styled `QDialog` picker (`QListWidget` vs `QComboBox` + button row). Constrained by the FORGE palette / em-dash title; implementation shape is open.
- Whether the addon's "list Actions in Batch" call rides as inline Python via forge-bridge `/exec` or as a structured RPC method — both equivalent under the REPL contract.
- Naming of camera-node-scope menu leaves (e.g. "Export Camera to Blender" vs "Export Selected Camera to Blender"), pending the Flame submenu API probe.

## Deferred Ideas

- Hook rename (`camera_match_hook.py` and install paths).
- 999.1 polish bullets — thumbnail previews, sort order, active-camera indicator, name disambiguation across Actions, last-picked memory.
- `FORGE` parent menu populated with non-camera-calibrator tools (forge_cv_align migration etc.).
- Pull-from-live Blender direction (Flame querying a running Blender) — rejected this phase.
- Multi-camera round-trip / lights / axis nodes / null targets — already in REQUIREMENTS.md v2 / Out of Scope.
