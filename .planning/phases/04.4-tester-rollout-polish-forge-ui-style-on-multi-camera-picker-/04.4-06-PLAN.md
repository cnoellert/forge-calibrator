---
phase: "04.4"
plan: "06"
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - tools/blender/forge_sender/__init__.py
  - tools/blender/forge_sender/preflight.py
  - tests/test_forge_sender_preflight.py
  - docs/seamless-bridge.md
  - .planning/ROADMAP.md
  - .planning/phases/04.4-tester-rollout-polish-forge-ui-style-on-multi-camera-picker-/04.4-RESEARCH.md
  - .planning/phases/04.4-tester-rollout-polish-forge-ui-style-on-multi-camera-picker-/04.4-03-PLAN.md
  - .planning/phases/04.4-tester-rollout-polish-forge-ui-style-on-multi-camera-picker-/04.4-03-SUMMARY.md
autonomous: true
requirements:
  - D-12
  - D-13
  - R-08
  - WR-01
  - WR-02
must_haves:
  truths:
    - "Every bridge-unreachable error site in tools/blender/forge_sender/__init__.py emits the new 'Camera Calibrator hook loaded?' wording — no occurrence of 'Camera Match hook loaded' remains anywhere under tools/blender/forge_sender/."
    - "Every menu-path string in user-visible code and docs encodes the three-segment shipped path 'FORGE → Camera → Export Camera to Blender' (or 'FORGE → Camera → Open Camera Calibrator' when the entry refers to the Camera-Calibrator leaf). The legacy two-segment path 'FORGE → Export Camera to Blender' does not occur in tools/blender/forge_sender/preflight.py or docs/seamless-bridge.md."
    - "tests/test_forge_sender_preflight.py::test_check_error_uses_new_menu_path_string_after_r08_rename asserts the three-segment string and runs PASS — no other test pins the legacy two-segment menu path."
    - "Planning record is consistent with what shipped: RESEARCH.md §P-01 records the 2026-04-26 live-test correction (sibling tool forge_cv_align ships nested shape; Flame 2026.2.1 renders it correctly); ROADMAP.md Phase 04.4 goal sentence describes the two-level nested 'FORGE → Camera → leaf' menu; 04.4-03-PLAN.md and 04.4-03-SUMMARY.md describe a two-level nested menu (not a flat one)."
    - "Full pytest suite is green and the three deferred items list entry for the stale 'Camera Match hook loaded' string is closed (or removed) — there is nothing actionable left from VERIFICATION.md WR-01/WR-02."
  artifacts:
    - path: tools/blender/forge_sender/__init__.py
      provides: "Single-line R-08 fix at line 265 — 'Camera Match hook loaded' replaced with 'Camera Calibrator hook loaded'."
      contains: "is Flame running with the Camera Calibrator hook loaded?"
    - path: tools/blender/forge_sender/preflight.py
      provides: "Three-segment menu path 'FORGE → Camera → Export Camera to Blender' replaces the two-segment string at lines 56 and 62."
      contains: "FORGE → Camera → Export Camera to Blender"
    - path: tests/test_forge_sender_preflight.py
      provides: "test_check_error_uses_new_menu_path_string_after_r08_rename asserts the 3-segment menu path."
      contains: "FORGE → Camera → Export Camera to Blender"
    - path: docs/seamless-bridge.md
      provides: "All menu-path mentions on lines 86, 131, 140, 148, 164 use the three-segment path matching shipped code."
      contains: "FORGE → Camera → Export Camera to Blender"
    - path: .planning/ROADMAP.md
      provides: "Phase 04.4 goal sentence describes the two-level nested FORGE → Camera → leaf menu (not 'flat per P-01')."
      contains: "FORGE → Camera"
    - path: .planning/phases/04.4-tester-rollout-polish-forge-ui-style-on-multi-camera-picker-/04.4-RESEARCH.md
      provides: "§P-01 has a 2026-04-26 'Live-Test Correction' subsection recording that the original 'nesting unsupported' conclusion was based on absence-of-evidence and is superseded."
      contains: "Live-Test Correction (2026-04-26)"
    - path: .planning/phases/04.4-tester-rollout-polish-forge-ui-style-on-multi-camera-picker-/04.4-03-PLAN.md
      provides: "must_haves and inline body state two-level nested 'FORGE → Camera → leaf' shape (per D-12 verbatim, P-01 corrected by live test) instead of a flat group."
    - path: .planning/phases/04.4-tester-rollout-polish-forge-ui-style-on-multi-camera-picker-/04.4-03-SUMMARY.md
      provides: "Body claims align with shipped code — describes a two-level nested FORGE → Camera → leaf menu, not a flat FORGE group."
  key_links:
    - from: tools/blender/forge_sender/preflight.py
      to: docs/seamless-bridge.md
      via: "Identical three-segment menu-path string 'FORGE → Camera → Export Camera to Blender' across both, anchored by tests/test_forge_sender_preflight.py"
      pattern: "FORGE → Camera → Export Camera to Blender"
    - from: flame/camera_match_hook.py:2592-2614
      to: .planning/ROADMAP.md
      via: "ROADMAP goal sentence + RESEARCH §P-01 + 04.4-03-PLAN/SUMMARY all describe the same two-level nested shape that ships in the hook"
      pattern: "FORGE → Camera"
---

<objective>
Close the two real gaps from `04.4-VERIFICATION.md`:

1. **WR-01 (Warning):** One stale `Camera Match hook loaded` string at
   `tools/blender/forge_sender/__init__.py:265` — three sibling sites at
   lines 386, 439, 508 already emit the new `Camera Calibrator hook loaded?`
   wording. One-line edit aligns the legacy site.

2. **WR-02 (Blocker):** Code at `flame/camera_match_hook.py:2592-2614` ships
   a two-level nested `FORGE → Camera → leaf` menu. Live UAT confirmed
   Flame 2026.2.1 renders it correctly (sibling tool `forge_cv_align`
   ships the same shape). The user has decided to **keep the nested code,
   update everything else to match** (resolution arm (b)). The flat
   workaround in RESEARCH §P-01 was based on absence-of-evidence and is
   superseded by the live test.

Scope of this plan:
- Code: one-line R-08 string fix in `__init__.py` (WR-01).
- Docs/test/preflight: switch the two-segment `FORGE → Export Camera to
  Blender` strings to the three-segment `FORGE → Camera → Export Camera
  to Blender` (or the equivalent `Open Camera Calibrator` leaf where
  applicable in docs) — `preflight.py` lines 56/62, the
  `test_check_error_uses_new_menu_path_string_after_r08_rename` assertion,
  and `docs/seamless-bridge.md` lines 86, 131, 140, 148, 164.
- Planning artifacts: append a 2026-04-26 Live-Test Correction subsection
  to RESEARCH §P-01; rewrite the ROADMAP Phase 04.4 goal-sentence "flat
  per P-01" clause to describe the shipped nested shape; update the
  04.4-03 PLAN must_haves + body claims and 04.4-03 SUMMARY claims so the
  project record matches what ships.
- Acceptance: dedicated final task runs the four grep + pytest gates from
  the gap-closure directive.

Out of scope: WR-03 (`ast.literal_eval` `TypeError` not caught), WR-04
(substring match on Flame error text), IN-01 through IN-05, the three
human-UAT items in VERIFICATION.md, any flattening of
`flame/camera_match_hook.py:2592-2614`, and any new feature work or
refactoring outside this gap list.

Purpose: Cross-file consistency between shipped code, docs, tests, and
planning record. Tester following error copy or docs will see the same
3-segment menu path Flame renders. The project record reflects the
correct WR-02 resolution decision.

Output: One plan file (this file) plus the per-task code/doc/planning
edits below; one SUMMARY.md after completion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/04.4-tester-rollout-polish-forge-ui-style-on-multi-camera-picker-/04.4-VERIFICATION.md
@.planning/phases/04.4-tester-rollout-polish-forge-ui-style-on-multi-camera-picker-/04.4-RESEARCH.md
@.planning/phases/04.4-tester-rollout-polish-forge-ui-style-on-multi-camera-picker-/04.4-03-PLAN.md
@.planning/phases/04.4-tester-rollout-polish-forge-ui-style-on-multi-camera-picker-/04.4-03-SUMMARY.md
@.planning/phases/04.4-tester-rollout-polish-forge-ui-style-on-multi-camera-picker-/deferred-items.md

@flame/camera_match_hook.py
@tools/blender/forge_sender/__init__.py
@tools/blender/forge_sender/preflight.py
@tests/test_forge_sender_preflight.py
@docs/seamless-bridge.md

<interfaces>
Shipped code being aligned to (NOT modified by this plan — it is the
source of truth per the user's WR-02 resolution arm (b)):

`flame/camera_match_hook.py:2592-2614` ships the two-level nested menu:

```python
return [
    {
        "name": "FORGE",
        "hierarchy": [],
        "actions": [],
    },
    {
        "name": "Camera",
        "hierarchy": ["FORGE"],
        "actions": [
            {
                "name": "Open Camera Calibrator",
                "isVisible": _scope_batch_clip,
                "execute": _launch_camera_match,
            },
            {
                "name": "Export Camera to Blender",
                "isVisible": _scope_batch_action,
                "execute": _export_camera_to_blender,
            },
        ],
    },
]
```

Render path: right-click → `FORGE` → `Camera` → leaf. Three segments. The
docs/preflight/test/planning artifacts must encode this exact path.

Sibling sites already emitting the new R-08 wording (read these to
confirm the canonical phrasing before editing line 265):

- `tools/blender/forge_sender/__init__.py:386` — inside
  `FORGE_OT_send_to_flame_choose_action.invoke` ConnectionError handler.
- `tools/blender/forge_sender/__init__.py:439` — inside
  `FORGE_OT_send_to_flame_choose_action.execute` create-new ConnectionError
  handler.
- `tools/blender/forge_sender/__init__.py:508` — inside
  `FORGE_OT_send_to_flame_choose_action.execute` send ConnectionError
  handler.

All three emit:

```
"forge-bridge not reachable at http://127.0.0.1:9999 — "
"is Flame running with the Camera Calibrator hook loaded?"
```

Line 265 (target of WR-01 fix) currently emits:

```
"Send to Flame: forge-bridge not reachable at http://127.0.0.1:9999"
" — is Flame running with the Camera Match hook loaded?"
```

Note the leading "Send to Flame: " prefix at line 265 (absent on the new
sites). Do not delete that prefix — the WR-01 fix is the four-word
substring `Camera Match hook loaded` → `Camera Calibrator hook loaded`
only.

</interfaces>

</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: WR-01 — fix stale 'Camera Match hook loaded' at __init__.py:265</name>
  <files>tools/blender/forge_sender/__init__.py</files>
  <read_first>
    - tools/blender/forge_sender/__init__.py lines 260-280 (the target ConnectionError handler — see current state)
    - tools/blender/forge_sender/__init__.py lines 380-400 (sibling site at line 386 — confirm canonical wording)
    - tools/blender/forge_sender/__init__.py lines 430-450 (sibling site at line 439 — confirm canonical wording)
    - tools/blender/forge_sender/__init__.py lines 500-515 (sibling site at line 508 — confirm canonical wording)
    - .planning/phases/04.4-tester-rollout-polish-forge-ui-style-on-multi-camera-picker-/04.4-VERIFICATION.md (lines 19-26 — WR-01 gap definition)
    - .planning/phases/04.4-tester-rollout-polish-forge-ui-style-on-multi-camera-picker-/deferred-items.md (records the WR-01 fix as the suggested one-line edit)
  </read_first>
  <action>
The current file at line 265 (verified via Read) contains this multi-line string literal:

```python
            msg = (
                "Send to Flame: forge-bridge not reachable at http://127.0.0.1:9999"
                " — is Flame running with the Camera Match hook loaded?"
            )
```

Replace the literal substring `Camera Match hook loaded` with
`Camera Calibrator hook loaded` so the resulting block reads:

```python
            msg = (
                "Send to Flame: forge-bridge not reachable at http://127.0.0.1:9999"
                " — is Flame running with the Camera Calibrator hook loaded?"
            )
```

Critical constraints:

1. Do NOT modify the leading `"Send to Flame: "` prefix or the URL
   `http://127.0.0.1:9999`. Sibling sites at lines 386/439/508 omit the
   `Send to Flame: ` prefix because they are in the choose-action flow,
   not the legacy stamped-camera flow. Preserving the prefix here keeps
   the legacy operator's error copy stylistically consistent with its
   own surrounding popups (the user knows they pressed "Send to Flame";
   the prefix labels which button surfaced the error).

2. The em-dash character `—` (U+2014) MUST be preserved verbatim — do
   NOT replace with a hyphen `-` or two hyphens `--`.

3. Do NOT touch lines 386, 439, 508 — they already emit the new
   `Camera Calibrator hook loaded?` wording. The `read_first` references
   to those lines are for verification only.

4. Do NOT touch any other line in this file. The WR-01 gap is a single
   four-word substring replacement at one site.
  </action>
  <verify>
    <automated>cd /Users/cnoellert/Documents/GitHub/forge-calibrator && python -c "import ast, pathlib; ast.parse(pathlib.Path('tools/blender/forge_sender/__init__.py').read_text())" && grep -c "Camera Match hook loaded" tools/blender/forge_sender/__init__.py | grep -q "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "Camera Match hook loaded" tools/blender/forge_sender/` returns zero hits (no occurrences anywhere under the addon directory).
    - `grep -c "is Flame running with the Camera Calibrator hook loaded?" tools/blender/forge_sender/__init__.py` outputs `4` (one at the legacy site formerly known as line 265, plus the three sibling sites at lines 386/439/508 that already had the new wording).
    - `python -c "import ast, pathlib; ast.parse(pathlib.Path('tools/blender/forge_sender/__init__.py').read_text())"` exits 0 (file still parses).
    - `python -m pytest tests/test_forge_sender_preflight.py tests/test_forge_sender_transport.py -v` exits 0 (no regression — these are the closest tests to the addon entry point).
    - Visual diff inspection: only the four-word substring at the previously-line-265 site changed; no other edit.
  </acceptance_criteria>
  <done>tools/blender/forge_sender/__init__.py emits 'Camera Calibrator hook loaded?' at all four bridge-unreachable sites; grep confirms zero hits for the legacy 'Camera Match hook loaded' substring under tools/blender/forge_sender/.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: WR-02 arm (b) — switch preflight.py + test assertion to 3-segment menu path</name>
  <files>tools/blender/forge_sender/preflight.py, tests/test_forge_sender_preflight.py</files>
  <read_first>
    - tools/blender/forge_sender/preflight.py (entire file — only ~65 LOC; current state of the two error strings at lines 56-57 and 62)
    - tests/test_forge_sender_preflight.py lines 181-201 (`test_check_error_uses_new_menu_path_string_after_r08_rename` assertion)
    - .planning/phases/04.4-tester-rollout-polish-forge-ui-style-on-multi-camera-picker-/04.4-VERIFICATION.md (lines 8-17 — WR-02 gap definition; cites preflight.py:56,62 explicitly)
    - flame/camera_match_hook.py lines 2592-2614 (the shipped nested menu — confirms the path the strings must encode)
  </read_first>
  <action>
There are two distinct edits in this task — one source file, one test
file. Both encode the three-segment menu path `FORGE → Camera → Export
Camera to Blender` matching the shipped nested code.

**Edit 1 — `tools/blender/forge_sender/preflight.py` lines 53-62 (current state):**

```python
    data = obj.data
    for key in _REQUIRED_STAMPED_KEYS:
        if key not in data:
            return (f"Send to Flame: active camera is missing '{key}' — "
                    f"this camera was not baked by forge-calibrator. "
                    f"Re-export from Flame via right-click → FORGE → "
                    f"Export Camera to Blender")

    if data.get("forge_bake_source") != "flame":
        return ("Send to Flame: active camera was not baked by forge-calibrator "
                "(forge_bake_source != 'flame') — re-export from Flame via "
                "right-click → FORGE → Export Camera to Blender")
```

Insert the ` Camera →` segment so both strings encode the three-segment
shipped menu path. After the edit the block must read EXACTLY:

```python
    data = obj.data
    for key in _REQUIRED_STAMPED_KEYS:
        if key not in data:
            return (f"Send to Flame: active camera is missing '{key}' — "
                    f"this camera was not baked by forge-calibrator. "
                    f"Re-export from Flame via right-click → FORGE → "
                    f"Camera → Export Camera to Blender")

    if data.get("forge_bake_source") != "flame":
        return ("Send to Flame: active camera was not baked by forge-calibrator "
                "(forge_bake_source != 'flame') — re-export from Flame via "
                "right-click → FORGE → Camera → Export Camera to Blender")
```

Constraints:

1. Preserve all surrounding text and formatting verbatim — only the
   menu-path segment changes from `FORGE → Export Camera to Blender` to
   `FORGE → Camera → Export Camera to Blender`. The em-dash `—` (U+2014)
   and right-arrow `→` (U+2192) characters must survive unchanged.

2. The first error message wraps the path across two lines for line
   length — `f"Re-export from Flame via right-click → FORGE → "` then
   `f"Camera → Export Camera to Blender"`. Match the existing line
   continuation style. Do NOT collapse onto one line.

3. The second error message is on a single line — keep it on one line.

4. Do not modify the docstring at lines 18-21 ("Copy strings: the four
   Tier-1 strings below are EXACT per UI-SPEC §Copywriting Contract
   §Preflight Tier 1") — the strings are still EXACT, just updated to
   match the shipped 3-segment shape per WR-02 arm (b). The docstring's
   "EXACT" claim still holds.

5. Do not touch any of the other Tier-1 strings (no-active-object at
   lines 44-45, not-a-camera at lines 48-49). The WR-02 scope is
   menu-path strings only.

After Edit 1, run a sanity grep:
- `grep -n "FORGE → Export Camera to Blender" tools/blender/forge_sender/preflight.py` → 0 hits
- `grep -n "FORGE → Camera → Export Camera to Blender" tools/blender/forge_sender/preflight.py` → 2 hits (one per error path)

**Edit 2 — `tests/test_forge_sender_preflight.py` lines 197-200 (current state):**

```python
    err = preflight.check(_FakeCtx())
    assert err is not None
    assert "FORGE → Export Camera to Blender" in err, err
    assert "Camera Match" not in err, err
```

After the edit the block must read EXACTLY:

```python
    err = preflight.check(_FakeCtx())
    assert err is not None
    assert "FORGE → Camera → Export Camera to Blender" in err, err
    assert "Camera Match" not in err, err
```

The third line changes — the assertion now requires the three-segment
string. Other assertions in this function (lines 198-199) and the
function's docstring (lines 182-187) are NOT modified.

Sibling sites — confirm there is no OTHER test in the entire test suite
that still pins the legacy two-segment menu path. Run:

```bash
grep -rn "FORGE → Export Camera to Blender" tests/
```

Expected: zero hits after Edit 2 lands. If any other test pins the
two-segment string, file a follow-up note (do NOT silently update
unrelated tests — the gap-closure directive only authorizes updating
`test_check_error_uses_new_menu_path_string_after_r08_rename`). At
authoring time of this plan, the directive states "Confirm this is the
only test that pins the menu-path wording" — verify that holds.

Note: line 199 already says `assert "Camera Match" not in err, err`. The
`Camera Match` substring is the legacy "Camera Match" name that the R-08
rename eliminated. The "Camera" segment of the new three-segment menu
path is a standalone word ("Camera →") not the substring "Camera Match",
so this assertion remains correct as-is.
  </action>
  <verify>
    <automated>cd /Users/cnoellert/Documents/GitHub/forge-calibrator && python -m pytest tests/test_forge_sender_preflight.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "FORGE → Export Camera to Blender" tools/blender/forge_sender/preflight.py` returns zero hits (legacy two-segment string fully replaced).
    - `grep -n "FORGE → Camera → Export Camera to Blender" tools/blender/forge_sender/preflight.py` returns exactly 2 hits (one per Tier-1 path).
    - `grep -rn "FORGE → Export Camera to Blender" tests/` returns zero hits (no other test pins the legacy path; the only one — the WR-02 canary — was just updated).
    - `grep -n "FORGE → Camera → Export Camera to Blender" tests/test_forge_sender_preflight.py` returns exactly 1 hit (the updated assertion).
    - `python -m pytest tests/test_forge_sender_preflight.py -v` exits 0 — all 14 tests PASS (12 pre-existing + 2 stub canaries; the canary `test_check_error_uses_new_menu_path_string_after_r08_rename` now PASSES against the new assertion).
    - `python -m pytest tests/ -q` exits 0 (no regression — should still be 414 passed / 2 skipped or one more pass if any other test happened to flip).
    - `python -c "import ast, pathlib; ast.parse(pathlib.Path('tools/blender/forge_sender/preflight.py').read_text())"` exits 0.
    - The docstring claim "Copy strings: the four Tier-1 strings below are EXACT per UI-SPEC" still holds — the post-edit copy is the new EXACT form per WR-02 arm (b).
  </acceptance_criteria>
  <done>preflight.py emits 'FORGE → Camera → Export Camera to Blender' at both Tier-1 sites; tests/test_forge_sender_preflight.py asserts the same 3-segment string; full pytest suite green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: WR-02 arm (b) — switch docs/seamless-bridge.md menu paths to 3-segment shape</name>
  <files>docs/seamless-bridge.md</files>
  <read_first>
    - docs/seamless-bridge.md (entire file — context for each line; ~199 LOC)
    - docs/seamless-bridge.md lines 84-92 (Using Send to Flame step 1 — refers to Export Camera to Blender; line 86 named in VERIFICATION as a 2-segment site)
    - docs/seamless-bridge.md lines 125-133 (Recovery if it crashes step 4 — line 131 named in VERIFICATION)
    - docs/seamless-bridge.md lines 138-152 (preflight error symptom + fix — lines 140 and 148 named in VERIFICATION)
    - docs/seamless-bridge.md lines 158-166 (re-bake recipe — line 164 named in VERIFICATION)
    - .planning/phases/04.4-tester-rollout-polish-forge-ui-style-on-multi-camera-picker-/04.4-VERIFICATION.md lines 8-17 (WR-02 gap names docs lines 86, 131, 140, 148, 164)
    - flame/camera_match_hook.py lines 2592-2614 (shipped nested menu — confirms path leaves: `Open Camera Calibrator` and `Export Camera to Blender`, both under `FORGE → Camera`)
  </read_first>
  <action>
Five doc lines encode the legacy two-segment menu path
`FORGE → Export Camera to Blender`. Replace each with the
three-segment shipped path `FORGE → Camera → Export Camera to Blender`,
preserving surrounding markdown and prose verbatim. Per the gap-closure
directive: "read each line in context to pick the correct leaf" — for
each of the five sites below, the directive's explicit guidance is that
they all refer to the **Export Camera to Blender** leaf (none point to
the **Open Camera Calibrator** leaf in the lines named). Confirm each
edit against the surrounding paragraph before applying.

**Edit 1 — `docs/seamless-bridge.md` line 86 (current state):**

```
1. In Flame: right-click the target Action → FORGE → **Export Camera to
   Blender**. Blender opens with the baked camera. No dialog prompts, no save-path
```

After the edit:

```
1. In Flame: right-click the target Action → FORGE → Camera → **Export Camera to
   Blender**. Blender opens with the baked camera. No dialog prompts, no save-path
```

Notes: the leaf name `Export Camera to Blender` is wrapped in `**…**`
markdown bold and split across two lines. Insert the ` Camera →` segment
between `FORGE → ` and `**Export Camera to`. Do not change the bold
markup or the line break.

**Edit 2 — `docs/seamless-bridge.md` line 131 (current state):**

```
4. Right-click the Action → FORGE → Export Camera to Blender, then
```

After the edit:

```
4. Right-click the Action → FORGE → Camera → Export Camera to Blender, then
```

Notes: this is the recovery-if-crashes recipe step 4. Plain text path,
no markdown emphasis. Insert ` Camera →` between `FORGE → ` and
`Export Camera to Blender`.

**Edit 3 — `docs/seamless-bridge.md` line 140 (current state):**

```
### Symptom: Send to Flame: active camera is missing 'forge_bake_action_name' — this camera was not baked by forge-calibrator. Re-export from Flame via right-click → FORGE → Export Camera to Blender
```

After the edit:

```
### Symptom: Send to Flame: active camera is missing 'forge_bake_action_name' — this camera was not baked by forge-calibrator. Re-export from Flame via right-click → FORGE → Camera → Export Camera to Blender
```

Notes: this is a `### Symptom:` heading. Its content reproduces the
exact preflight error string from `tools/blender/forge_sender/preflight.py`
verbatim. After Task 2 lands, that source string is the 3-segment form;
this doc heading must match Task 2's output exactly so a tester searching
the docs for the popup text will find this troubleshooting recipe.

**Edit 4 — `docs/seamless-bridge.md` line 148 (current state):**

```
1. In Flame: right-click the target Action → FORGE → Export Camera to Blender.
```

After the edit:

```
1. In Flame: right-click the target Action → FORGE → Camera → Export Camera to Blender.
```

Notes: step 1 of the Fix recipe under the symptom heading edited at
line 140. Same plain-text form. Same insertion.

**Edit 5 — `docs/seamless-bridge.md` line 164 (current state):**

```
2. Most common cause: the Action was renamed after the bake. Re-bake from the current
   Action (right-click Action → FORGE → Export Camera to Blender).
```

After the edit:

```
2. Most common cause: the Action was renamed after the bake. Re-bake from the current
   Action (right-click Action → FORGE → Camera → Export Camera to Blender).
```

Notes: this is fix step 2 under "Symptom: Send to Flame failed". The
parenthesized path is split across two lines; the path itself is on the
second line. Insert ` Camera →` between `FORGE → ` and
`Export Camera to Blender`.

Constraints across all five edits:

1. Em-dash `—` (U+2014) and right-arrow `→` (U+2192) characters must be
   preserved exactly — copy them from the original line where possible
   to avoid encoding drift.

2. Surrounding markdown structure (headings, list numbering, bold,
   parentheses, line wraps) must NOT change. Only insert ` Camera →`
   into the menu-path span.

3. Do NOT modify any other line. The bridge-unreachable symptom strings
   on lines 100 and 184 already use the new `Camera Calibrator hook
   loaded?` wording — they don't reference a menu path and are out of
   scope.

4. Do NOT touch the install-path mention `/opt/Autodesk/shared/python/camera_match/`
   on line 39 (D-13: internal path unchanged).

5. Do NOT touch the two `camera_match_hook.py` filename mentions (lines
   38, 64) — internal symbol per D-13.

6. After all five edits, the 3-segment path appears at minimum 5 times
   in the file. Some lines may already contain `Camera` as a separate
   word (e.g., "the Camera Calibrator hook"); those are unrelated and
   must not be conflated with the menu-path edits.

After all five edits, run sanity greps:

- `grep -n "FORGE → Export Camera to Blender" docs/seamless-bridge.md` → 0 hits
- `grep -n "FORGE → Camera → Export Camera to Blender" docs/seamless-bridge.md` → ≥ 4 hits (the bold `**Export Camera to** Blender` pattern on line 86 splits across two lines and may not match a single-line grep — confirm with a multi-line `grep -A1` if needed)

If line 86's split-across-lines markdown form makes single-line grep
under-count, that is OK — the explicit acceptance criteria below allow
≥ 4 hits via single-line grep, and a manual visual confirmation of all
five edits.
  </action>
  <verify>
    <automated>cd /Users/cnoellert/Documents/GitHub/forge-calibrator && bash -c 'set -e; legacy=$(grep -c "FORGE → Export Camera to Blender" docs/seamless-bridge.md || true); test "$legacy" = "0"; new=$(grep -c "FORGE → Camera → Export Camera to Blender" docs/seamless-bridge.md || true); test "$new" -ge "4"; echo "OK legacy=$legacy new=$new"'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "FORGE → Export Camera to Blender" docs/seamless-bridge.md` outputs `0` (legacy two-segment path fully removed).
    - `grep -c "FORGE → Camera → Export Camera to Blender" docs/seamless-bridge.md` outputs `4` or `5` (single-line grep counts; line 86's split-across-lines markdown form may or may not match depending on whitespace handling — accept ≥ 4 with manual confirmation that all five named lines were edited).
    - `grep -n "Camera → Export Camera to Blender" docs/seamless-bridge.md` returns exactly 5 distinct line numbers spanning the 80s, 130s, 140s, 160s ranges (one hit per VERIFICATION-named line: 86, 131, 140, 148, 164 give-or-take any post-edit line shifts which should be zero since each edit adds the same number of characters and no newlines).
    - `grep -c "/opt/Autodesk/shared/python/camera_match/" docs/seamless-bridge.md` outputs `1` (D-13 internal path preserved — unchanged).
    - `grep -c "camera_match_hook.py" docs/seamless-bridge.md` outputs `2` (D-13 internal filename preserved — unchanged).
    - `grep -c "Camera Calibrator hook loaded" docs/seamless-bridge.md` outputs `2` (lines 100, 184 still match — bridge-unreachable symptom strings unchanged).
    - The file remains valid markdown (renders correctly when previewed; no broken headings or list numbering).
    - Manual line-by-line visual confirmation: each of the five named lines (86, 131, 140, 148, 164 give-or-take post-edit shifts) now contains `FORGE → Camera → Export Camera to Blender` and none contain the legacy 2-segment form.
  </acceptance_criteria>
  <done>All five named docs/seamless-bridge.md lines encode the three-segment menu path `FORGE → Camera → Export Camera to Blender`; legacy 2-segment path fully removed; D-13 internal references preserved.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Planning-record alignment — RESEARCH §P-01 + ROADMAP goal sentence + 04.4-03-PLAN + 04.4-03-SUMMARY</name>
  <files>.planning/phases/04.4-tester-rollout-polish-forge-ui-style-on-multi-camera-picker-/04.4-RESEARCH.md, .planning/ROADMAP.md, .planning/phases/04.4-tester-rollout-polish-forge-ui-style-on-multi-camera-picker-/04.4-03-PLAN.md, .planning/phases/04.4-tester-rollout-polish-forge-ui-style-on-multi-camera-picker-/04.4-03-SUMMARY.md</files>
  <read_first>
    - .planning/phases/04.4-tester-rollout-polish-forge-ui-style-on-multi-camera-picker-/04.4-RESEARCH.md lines 81-118 (existing §P-01 finding + Workaround Options + Recommendation)
    - .planning/ROADMAP.md lines 77-89 (Phase 04.4 entry — goal sentence currently says "FORGE > {Open Camera Calibrator, Export Camera to Blender} flat per RESEARCH §P-01 — two-level nesting unsupported on Flame 2026.2.1")
    - .planning/phases/04.4-tester-rollout-polish-forge-ui-style-on-multi-camera-picker-/04.4-03-PLAN.md lines 22-42 (must_haves block — first truth says "flat FORGE group with a single 'Open Camera Calibrator' leaf"; key_links pattern "name FORGE with Open Camera Calibrator and Export Camera to Blender as the only actions")
    - .planning/phases/04.4-tester-rollout-polish-forge-ui-style-on-multi-camera-picker-/04.4-03-PLAN.md lines 154-156 (the explicit "P-01 correction to D-12" note that becomes obsolete)
    - .planning/phases/04.4-tester-rollout-polish-forge-ui-style-on-multi-camera-picker-/04.4-03-SUMMARY.md lines 12-17, 26-27, 56-57, 77, 84-86, 94, 98, 151-166 (every claim that says "flat FORGE group" or "P-01 correction")
    - flame/camera_match_hook.py lines 2570-2615 (the shipped two-level nested code — source of truth)
    - .planning/phases/04.4-tester-rollout-polish-forge-ui-style-on-multi-camera-picker-/04.4-VERIFICATION.md lines 8-17, 119-141, 179-189 (the gap definition + truth/requirement table entries that flagged the inconsistency)
  </read_first>
  <action>
This task aligns the project record with the user's WR-02 resolution
arm (b) — keep the nested code, update everything else to match. The
shipped `flame/camera_match_hook.py:2592-2614` IS the source of truth.

**Edit 1 — `04.4-RESEARCH.md` §P-01: append a Live-Test Correction
subsection.**

Locate the §P-01 block at lines 81-118. The existing structure is:

```
## P-01 Answer: Nested Submenus in `get_batch_custom_ui_actions`

### Finding

**Two-level hierarchy (`FORGE / Camera / leaf`) is NOT supported in
`get_batch_custom_ui_actions` on Flame 2026.2.1.**

[VERIFIED: live inspection of /opt/Autodesk/shared/python/ on Flame 2026.2.1 host]
…(evidence bullets)…

### Workaround Options for D-12

**Option A (Recommended — Low Risk):** A single `FORGE` group …

**Option B (Speculative — Higher Risk):** Test `hierarchy: ["FORGE"]` …

**Recommendation:** Use Option A. …
```

Insert a new subsection at the end of §P-01 (after the existing
**Recommendation:** paragraph at line 117, before the section break at
line 119). The new subsection is:

```markdown
### Live-Test Correction (2026-04-26)

**Status:** ⚠ Original P-01 finding (above) is SUPERSEDED by live UAT.

The "two-level nesting unsupported" conclusion was based on:
1. Inspection of installed Flame 2026.2.1 tools — none used the nested
   shape in `get_batch_custom_ui_actions` at the time of inspection.
2. Cited forum post (logik.tv, FR FI-02123) reporting the limitation
   pre-2026.

Both lines of evidence are absence-of-evidence, not evidence-of-absence.
During Phase 04.4-03 implementation the executor located a sibling tool
(`forge_cv_align`) that ships exactly the two-level nested shape
specified in CONTEXT D-12, and live UAT on Flame 2026.2.1 confirmed
Flame renders it correctly:

```python
return [
    {"name": "FORGE", "hierarchy": [], "actions": []},          # anchor group
    {"name": "Camera", "hierarchy": ["FORGE"], "actions": [    # child group
        {"name": "Open Camera Calibrator", ...},
        {"name": "Export Camera to Blender", ...},
    ]},
]
```

The shipped `flame/camera_match_hook.py:2592-2614` follows this shape;
right-click renders `FORGE → Camera → leaf` (3 segments). User decision
2026-04-26 (recorded in `04.4-VERIFICATION.md` resolution of WR-02): keep
the nested code, update docs/preflight/test/planning artifacts to match.

**Implications for prior P-01 conclusions:**
- "Option A" (flat FORGE group) is no longer the recommended shape — it
  was a workaround for an absent capability that, on closer inspection,
  is in fact present.
- "Option B" (speculative nested shape) is now the SHIPPED shape — no
  longer speculative, verified by live UAT.
- The original §P-01 "Recommendation: Use Option A" is OBSOLETE. The
  binding shape is Option B (two-level nested), with the Camera sub-tier
  per CONTEXT D-12 verbatim.

This Live-Test Correction supersedes the prior §P-01 recommendation
without rewriting the prior text — kept as a research-process artifact
showing how absence-of-evidence resolved into a shipped capability.
```

Constraints:

1. Do NOT delete or modify any text in the existing §P-01 (lines 81-118).
   The Live-Test Correction is appended, not a rewrite. The prior content
   stays as a research-process artifact.

2. The Live-Test Correction subsection uses the same `### Heading`
   markdown level as Finding / Workaround Options.

3. The em-dash `—` (U+2014) and right-arrow `→` (U+2192) characters
   must be preserved verbatim in the new content.

4. Wrap the Python code block in triple-backticks `python` (matching
   the existing convention in this file).

**Edit 2 — `.planning/ROADMAP.md` Phase 04.4 goal sentence.**

The current Phase 04.4 goal sentence in ROADMAP.md lines 79-80 (the
single very-long line that begins with `**Goal:**`) contains the clause:

`menu reorganization under a new top-level \`FORGE\` group (\`FORGE > {Open Camera Calibrator, Export Camera to Blender}\` flat per RESEARCH §P-01 — two-level nesting unsupported on Flame 2026.2.1)`

Replace that exact clause with this exact replacement:

`menu reorganization under a new top-level \`FORGE\` group with a \`Camera\` sub-group (rendered path: \`FORGE → Camera → {Open Camera Calibrator, Export Camera to Blender}\` per CONTEXT D-12 verbatim — RESEARCH §P-01 Live-Test Correction 2026-04-26 confirms two-level nesting IS supported on Flame 2026.2.1; shipped in flame/camera_match_hook.py:2592-2614)`

Constraints:

1. The replacement is a substring within a longer sentence. Do not break
   the surrounding sentence structure. The replacement is one clause that
   replaces one clause.

2. The phrase "flat per RESEARCH §P-01 — two-level nesting unsupported on
   Flame 2026.2.1" must NOT appear anywhere in ROADMAP.md after the edit.

3. Do not modify any other line of ROADMAP.md. The Plans subsection (lines
   84-89) showing `[x] 04.4-01-PLAN.md`, etc., must NOT change — those
   plans completed as labelled, even if 04.4-03 SUMMARY described the
   shape incorrectly.

4. Em-dashes `—` and right-arrows `→` must be preserved verbatim.

**Edit 3 — `04.4-03-PLAN.md` must_haves + body claims.**

Several locations in `04.4-03-PLAN.md` describe a flat FORGE group as the
target shape. Update each to describe the two-level nested shape that
shipped (per CONTEXT D-12 verbatim, P-01 corrected by live test). Do NOT
edit the Tasks 1-3 `<action>` blocks or `<acceptance_criteria>` blocks
that already executed against grep gates — those gates passed against
the shipped nested code, and rewriting them would invalidate the audit
trail. Edit only the prose-level claims.

**Specific edits:**

(a) `04.4-03-PLAN.md` line 24 (`must_haves.truths` first item — currently):

```
    - "Right-clicking a Clip in Batch shows a top-level FORGE group with a single 'Open Camera Calibrator' leaf — the old 'Camera Match' group is gone."
```

Replace with:

```
    - "Right-clicking a Clip in Batch shows the FORGE → Camera nested menu with a single 'Open Camera Calibrator' leaf under it — the old 'Camera Match' group is gone. Two-level nesting is supported on Flame 2026.2.1 per RESEARCH §P-01 Live-Test Correction 2026-04-26."
```

(b) `04.4-03-PLAN.md` line 25 (`must_haves.truths` second item):

```
    - "Right-clicking an Action in Batch shows the FORGE group with a single 'Export Camera to Blender' leaf — there is NO 'Import Camera from Blender' entry."
```

Replace with:

```
    - "Right-clicking an Action in Batch shows the FORGE → Camera nested menu with a single 'Export Camera to Blender' leaf under it — there is NO 'Import Camera from Blender' entry."
```

(c) `04.4-03-PLAN.md` line 31 (`must_haves.artifacts.provides`):

```
      provides: "Restructured get_batch_custom_ui_actions (FORGE flat group), new get_action_custom_ui_actions function, deleted _import_camera_from_blender handler, R-08 user-visible string updates"
```

Replace with:

```
      provides: "Restructured get_batch_custom_ui_actions (two-level nested FORGE → Camera group per D-12 verbatim, P-01 corrected by live test), new get_action_custom_ui_actions function, deleted _import_camera_from_blender handler, R-08 user-visible string updates"
```

(d) `04.4-03-PLAN.md` line 40 (`must_haves.key_links` second link
`pattern`):

```
      pattern: "name FORGE with Open Camera Calibrator and Export Camera to Blender as the only actions"
```

Replace with:

```
      pattern: "name FORGE anchor + name Camera with hierarchy: ['FORGE'] holding Open Camera Calibrator and Export Camera to Blender as the only actions"
```

(e) `04.4-03-PLAN.md` lines 154-156 (the explicit P-01 correction note in
the Task 1 `<action>` body — currently):

```markdown
**Note (D-12 → P-01 correction):** CONTEXT D-12 specified two-level `FORGE / Camera / leaf` nesting. RESEARCH P-01 verified this is unsupported on Flame 2026.2.1 (forum FR FI-02123 + installed-tool inspection). The flat structure (`FORGE / leaf`) is the binding shape per UI-SPEC §A-2. This is a P-01 correction to D-12, not a scope reduction.
```

Replace with:

```markdown
**Note (D-12 verbatim, P-01 Live-Test Correction 2026-04-26):** CONTEXT D-12 specified two-level `FORGE / Camera / leaf` nesting. The original RESEARCH §P-01 finding (two-level nesting unsupported) was based on absence-of-evidence — installed tools at the time of research did not demonstrate the shape. The Phase 04.4-03 executor located sibling tool `forge_cv_align` shipping exactly the nested shape, and live UAT on Flame 2026.2.1 confirmed Flame renders it correctly. The two-level nested structure (`FORGE → Camera → leaf`) is the binding shape per CONTEXT D-12 verbatim. See RESEARCH §P-01 Live-Test Correction 2026-04-26 for the full record. (Plan body originally prescribed a flat group; that prescription is superseded by the live-test outcome — Tasks 1-2 below ship the nested shape; the existing acceptance criteria are intentionally permissive about the inner dict shape.)
```

Constraints:

1. Do NOT modify the Tasks 1-3 acceptance-criteria grep gates or pytest
   commands — those gates ran against the shipped nested code and PASSED.
   Rewriting them would break the audit trail.

2. The frontmatter `requirements:` field already lists `D-12` and `P-01`
   — leave those as-is. The plan still satisfies D-12 (nested as written)
   and P-01 (now via the Live-Test Correction).

3. Do NOT touch Task 4's `<how-to-verify>` checkpoint steps — those were
   executed in human UAT and produced the live-test evidence that drives
   this correction.

**Edit 4 — `04.4-03-SUMMARY.md` body claims.**

Update SUMMARY claims that describe the shipped menu as flat. The
SUMMARY is a record of what was completed, so the edits here describe
the shipped reality (nested) instead of the now-superseded flat-target
language.

**Specific edits:**

(a) `04.4-03-SUMMARY.md` line 13 (`provides` first item):

```
  - "Restructured get_batch_custom_ui_actions — flat FORGE group with two leaves only ('Open Camera Calibrator' Clip-scope, 'Export Camera to Blender' Action-scope)"
```

Replace with:

```
  - "Restructured get_batch_custom_ui_actions — two-level nested FORGE → Camera menu with two leaves under Camera ('Open Camera Calibrator' Clip-scope, 'Export Camera to Blender' Action-scope) per CONTEXT D-12 verbatim. RESEARCH §P-01 'flat-only' conclusion was based on absence-of-evidence; live UAT on Flame 2026.2.1 confirmed Flame renders the nested shape. See RESEARCH §P-01 Live-Test Correction 2026-04-26."
```

(b) `04.4-03-SUMMARY.md` line 26 (`tech-stack.patterns` first item):

```
    - "Flat custom UI action group on Flame 2026.2.1 — 'name' (group) + 'actions' (flat list) only; two-level nesting via nested 'actions' arrays is unsupported (RESEARCH §P-01)"
```

Replace with:

```
    - "Two-level nested custom UI action menu on Flame 2026.2.1 — declare an anchor group with 'name' + 'hierarchy: []' + 'actions: []', then declare a child group with 'name' + 'hierarchy: [parent_name]' holding the leaves. Sibling forge_cv_align tool ships this shape; live UAT 2026-04-26 confirmed Flame 2026.2.1 renders it correctly (RESEARCH §P-01 Live-Test Correction 2026-04-26)."
```

(c) `04.4-03-SUMMARY.md` line 33 (the long `key-files.modified` first
item — find the substring `restructured get_batch_custom_ui_actions to
flat FORGE group with two leaves` and replace with `restructured
get_batch_custom_ui_actions to two-level nested FORGE → Camera menu with
two leaves under the Camera child group per D-12 verbatim`).

(d) `04.4-03-SUMMARY.md` line 36 (the `key-decisions` first item —
currently):

```
  - "Followed the plan body's 'flat FORGE group' shape (P-01 correction to D-12) rather than retrying nested submenus. RESEARCH already verified two-level nesting is unsupported on Flame 2026.2.1; no further probing was warranted."
```

Replace with:

```
  - "Shipped two-level nested FORGE → Camera menu per CONTEXT D-12 verbatim. The plan body had prescribed a flat FORGE group based on RESEARCH §P-01's original 'nesting unsupported' conclusion. While implementing, the executor located sibling tool forge_cv_align shipping exactly the nested shape, and live UAT on Flame 2026.2.1 confirmed Flame renders it correctly. The original P-01 conclusion was based on absence-of-evidence (no installed tool inspected at research time demonstrated the shape) — superseded by RESEARCH §P-01 Live-Test Correction 2026-04-26. WR-02 in 04.4-VERIFICATION.md cross-references this resolution (arm b: keep nested code, update everything else to match)."
```

(e) `04.4-03-SUMMARY.md` line 56 (the bold heading — currently):

```
**Restructured the Flame Batch right-click menu into a single flat FORGE group with two leaves, …**
```

Replace `single flat FORGE group with two leaves` with `two-level nested
FORGE → Camera menu with two leaves under Camera`. Keep everything else
in that bold sentence verbatim (the rest is still accurate).

(f) `04.4-03-SUMMARY.md` line 77 (Accomplishments bullet — currently):

```
- The flat FORGE group in `get_batch_custom_ui_actions` has exactly two leaves — `Open Camera Calibrator` (Clip-scope) and `Export Camera to Blender` (Action-scope). The third dict entry (`Import Camera from Blender`) is gone. Pitfall 4 reminder pasted above the function.
```

Replace `The flat FORGE group in` with `The two-level nested FORGE →
Camera menu in`. The rest of the bullet stays verbatim.

(g) `04.4-03-SUMMARY.md` line 84 (Task Commits bullet 1 — task commit
message — currently):

```
1. **Task 1: Delete _import_camera_from_blender + flat FORGE menu** — `6e1b3df` (refactor)
```

Leave this commit-message text as-is — it is a quote of the actual
git commit message at SHA `6e1b3df` and rewriting it would falsify the
record. Confirm via `git log --oneline 6e1b3df -1` that the commit
message contains `flat FORGE menu`. If so, the SUMMARY accurately quotes
the historical commit. The git history itself records the now-corrected
mental model the executor had at the time; that is fine to leave as a
research artifact. Do NOT modify this line.

(h) `04.4-03-SUMMARY.md` line 94 (the long `Files Created/Modified` first
item) — find the substring `restructured \`get_batch_custom_ui_actions\`
from the prior \`Camera Match\` group with three leaves to a flat
\`FORGE\` group with two leaves` and replace with `restructured
\`get_batch_custom_ui_actions\` from the prior \`Camera Match\` group
with three leaves to a two-level nested \`FORGE\` → \`Camera\` menu with
two leaves under the Camera child group per CONTEXT D-12 verbatim`.

(i) `04.4-03-SUMMARY.md` line 98 (Decisions Made first bullet —
currently):

```
- **Flat FORGE group, no submenu retry.** RESEARCH §P-01 had already verified that Flame 2026.2.1 ignores nested `actions` arrays under `actions` arrays at the batch hook level — the inner array silently collapses. The plan body's "P-01 correction to D-12" note made this explicit; followed without re-probing.
```

Replace with:

```
- **Two-level nested FORGE → Camera menu per D-12 verbatim, P-01 Live-Test Correction 2026-04-26.** While implementing, located sibling tool forge_cv_align shipping the nested shape; live UAT on Flame 2026.2.1 confirmed Flame renders it correctly. Plan body had prescribed a flat FORGE group based on the original §P-01 conclusion that two-level nesting was unsupported — that conclusion was based on absence-of-evidence (no installed tool inspected at research time demonstrated the shape), now superseded. Shipped the nested shape that satisfies D-12 verbatim; gap-closure plan 04.4-06 updates the project record to reflect this.
```

(j) Lines 151-166 of the Self-Check section (`Verified all
<success_criteria> grep gates from the orchestrator prompt:`) — these
list grep counts that PASSED against the shipped nested code. Leave them
verbatim; they are an audit-trail artifact and the counts ran true. Do
NOT edit.

Constraints across Edit 4:

1. The SUMMARY frontmatter `requirements-completed:` list (line 47 —
   `[D-04, D-06, D-11, D-12, D-13, D-14, D-15, P-01, P-02, R-08]`) must
   stay as-is — the plan completed all listed requirements; D-12 is
   completed by the nested shape that shipped, and P-01 is completed by
   the Live-Test Correction.

2. The git-log lines at SUMMARY 151-154 must NOT be edited — they
   record the actual commits made.

3. Do not touch the file's commit metadata anywhere; the SUMMARY is
   already committed and these prose edits land in a follow-up commit.

After all four edits, run sanity greps:

- `grep -c "flat FORGE" .planning/phases/04.4-tester-rollout-polish-forge-ui-style-on-multi-camera-picker-/04.4-03-SUMMARY.md` → expected: 1 (only the historical Task Commits line 84 retains the quoted git message; everything else is rewritten).
- `grep -c "flat FORGE" .planning/phases/04.4-tester-rollout-polish-forge-ui-style-on-multi-camera-picker-/04.4-03-PLAN.md` → expected: 0.
- `grep -c "FORGE → Camera" .planning/phases/04.4-tester-rollout-polish-forge-ui-style-on-multi-camera-picker-/04.4-03-PLAN.md` → expected: ≥ 2 (must_haves truths a + b).
- `grep -c "FORGE → Camera" .planning/phases/04.4-tester-rollout-polish-forge-ui-style-on-multi-camera-picker-/04.4-03-SUMMARY.md` → expected: ≥ 2.
- `grep -c "Live-Test Correction" .planning/phases/04.4-tester-rollout-polish-forge-ui-style-on-multi-camera-picker-/04.4-RESEARCH.md` → expected: ≥ 1.
- `grep -c "FORGE → Camera" .planning/ROADMAP.md` → expected: ≥ 1 (the corrected goal sentence).
- `grep -c "flat per RESEARCH" .planning/ROADMAP.md` → expected: 0.

**IMPORTANT — file VERIFICATION.md must NOT be edited.** The gap-closure
directive specifies it is the source of truth for what shipped; any
re-verification produces a fresh report. Confirm via `git status` that
`04.4-VERIFICATION.md` is unchanged after Task 4.
  </action>
  <verify>
    <automated>cd /Users/cnoellert/Documents/GitHub/forge-calibrator && bash -c 'set -e; r=$(grep -c "Live-Test Correction" .planning/phases/04.4-tester-rollout-polish-forge-ui-style-on-multi-camera-picker-/04.4-RESEARCH.md); test "$r" -ge "1"; rm=$(grep -c "FORGE → Camera" .planning/ROADMAP.md); test "$rm" -ge "1"; legacy=$(grep -c "flat per RESEARCH" .planning/ROADMAP.md); test "$legacy" = "0"; echo "OK research=$r roadmap=$rm legacy=$legacy"'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Live-Test Correction" .planning/phases/04.4-tester-rollout-polish-forge-ui-style-on-multi-camera-picker-/04.4-RESEARCH.md` outputs ≥ 1 (new subsection appended to §P-01).
    - `grep -c "Live-Test Correction (2026-04-26)" .planning/phases/04.4-tester-rollout-polish-forge-ui-style-on-multi-camera-picker-/04.4-RESEARCH.md` outputs `1` (exact subsection heading).
    - `grep -c "Two-level hierarchy" .planning/phases/04.4-tester-rollout-polish-forge-ui-style-on-multi-camera-picker-/04.4-RESEARCH.md` outputs `1` (the original §P-01 Finding heading is still present — the correction appends, does not delete).
    - `grep -c "FORGE → Camera" .planning/ROADMAP.md` outputs ≥ 1 (corrected goal sentence).
    - `grep -c "flat per RESEARCH §P-01" .planning/ROADMAP.md` outputs `0` (legacy clause removed from ROADMAP).
    - `grep -c "two-level nesting unsupported on Flame 2026.2.1" .planning/ROADMAP.md` outputs `0` (legacy clause removed; the corrected sentence says nesting IS supported).
    - `grep -c "FORGE → Camera" .planning/phases/04.4-tester-rollout-polish-forge-ui-style-on-multi-camera-picker-/04.4-03-PLAN.md` outputs ≥ 2 (must_haves truths a + b).
    - `grep -c "flat FORGE group" .planning/phases/04.4-tester-rollout-polish-forge-ui-style-on-multi-camera-picker-/04.4-03-PLAN.md` outputs `0` (must_haves and "P-01 correction" note rewritten).
    - `grep -c "two-level nested" .planning/phases/04.4-tester-rollout-polish-forge-ui-style-on-multi-camera-picker-/04.4-03-PLAN.md` outputs ≥ 2 (must_haves truths a + b).
    - `grep -c "FORGE → Camera" .planning/phases/04.4-tester-rollout-polish-forge-ui-style-on-multi-camera-picker-/04.4-03-SUMMARY.md` outputs ≥ 2 (rewritten provides + decisions claims).
    - `grep -c "flat FORGE menu" .planning/phases/04.4-tester-rollout-polish-forge-ui-style-on-multi-camera-picker-/04.4-03-SUMMARY.md` outputs `1` (only the quoted historical commit message at line 84 retains the phrase — confirm with `git log --oneline 6e1b3df` that the commit message indeed says "flat FORGE menu", validating the SUMMARY's quotation).
    - `grep -c "Live-Test Correction 2026-04-26" .planning/phases/04.4-tester-rollout-polish-forge-ui-style-on-multi-camera-picker-/04.4-03-PLAN.md` outputs ≥ 1.
    - `grep -c "Live-Test Correction 2026-04-26" .planning/phases/04.4-tester-rollout-polish-forge-ui-style-on-multi-camera-picker-/04.4-03-SUMMARY.md` outputs ≥ 1.
    - `git status .planning/phases/04.4-tester-rollout-polish-forge-ui-style-on-multi-camera-picker-/04.4-VERIFICATION.md` shows the file is unmodified by this plan (matches the gap-closure directive: VERIFICATION.md is the source of truth for what shipped).
    - All four planning files (RESEARCH, ROADMAP, 04.4-03-PLAN, 04.4-03-SUMMARY) parse cleanly as markdown — no malformed frontmatter, no broken code blocks, no orphan headings.
  </acceptance_criteria>
  <done>RESEARCH §P-01 has a Live-Test Correction subsection; ROADMAP Phase 04.4 goal sentence describes the nested shape; 04.4-03-PLAN must_haves and body claims describe two-level nested per D-12 verbatim with P-01 corrected by live test; 04.4-03-SUMMARY claims align with shipped nested code; VERIFICATION.md is untouched.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Acceptance verification — run all four gap-closure gates</name>
  <files>—</files>
  <read_first>
    - .planning/phases/04.4-tester-rollout-polish-forge-ui-style-on-multi-camera-picker-/04.4-VERIFICATION.md (the source of the gap definitions Tasks 1-4 just closed; the four gates below correspond directly to the gap-closure directive's section D)
    - All five files modified across Tasks 1-4 — verify their post-edit state is consistent.
  </read_first>
  <action>
This task runs the four acceptance gates from the gap-closure directive
section D. It is a pure verification task — no source/doc/planning edits.
The gates confirm Tasks 1-4 produced the intended cross-file consistency
without regression.

Execute each gate in order. Each gate is a single command; all four must
PASS for the plan to be complete. Capture the actual output for the
SUMMARY.

**Gate 1 — Full pytest suite green:**

```bash
cd /Users/cnoellert/Documents/GitHub/forge-calibrator && python -m pytest tests/ -q
```

Expected outcome: full suite green. The previous baseline (per
04.4-VERIFICATION.md) was 414 passed / 2 skipped. After Task 2's
assertion update, the count should remain green. Acceptable shapes:

- `414 passed, 2 skipped` (original baseline; Task 2 flipped one canary
  from PASS-via-prior-string to PASS-via-new-string, so the count is
  unchanged).
- `414 passed` or `414 passed, N skipped` for any N ≥ 0 (any count where
  pytest exits 0 and zero `failed` or `error` lines appear).
- An increase in passed count is also acceptable IF traceable to a
  previously-skipped canary flipping due to Task 2's edit. Confirm by
  diffing against the 04.4-VERIFICATION.md "Behavioral Spot-Checks"
  table.

If any test FAILED or ERROR'd, stop and investigate before declaring
the plan complete. The gate is hard.

**Gate 2 — Stale "Camera Match hook loaded" string fully eliminated:**

```bash
cd /Users/cnoellert/Documents/GitHub/forge-calibrator && grep -rn "Camera Match hook loaded" tools/blender/forge_sender/
```

Expected outcome: zero hits (the command exits with status 1 because
grep found nothing). Equivalent shell idiom:

```bash
test "$(grep -rn 'Camera Match hook loaded' tools/blender/forge_sender/ 2>/dev/null | wc -l | tr -d ' ')" = "0"
```

If any hit appears, Task 1 missed a site — return to Task 1 and address
it.

**Gate 3 — Legacy 2-segment menu path fully eliminated from preflight + docs:**

```bash
cd /Users/cnoellert/Documents/GitHub/forge-calibrator && grep -n "FORGE → Export Camera to Blender" tools/blender/forge_sender/preflight.py docs/seamless-bridge.md
```

Expected outcome: zero hits across both files combined (grep exits
with status 1). If any hit, Task 2 or Task 3 missed a site.

**Gate 4 — 3-segment menu path present at expected count:**

```bash
cd /Users/cnoellert/Documents/GitHub/forge-calibrator && grep -n "FORGE → Camera → Export Camera to Blender" tools/blender/forge_sender/preflight.py docs/seamless-bridge.md
```

Expected outcome: ≥ 6 hits combined — exactly 2 in `preflight.py`
(Tier-1 missing-key + bad-source error messages) and ≥ 4 in
`docs/seamless-bridge.md` (lines 86, 131, 140, 148, 164 — line 86's
markdown-bold form may or may not single-line-grep, so the gate is `≥ 4`
in docs to handle that ambiguity; `≥ 6` total).

Acceptable counts:

- 6 hits total: 2 preflight + 4 docs (line 86's bold-split markdown form
  did not match single-line grep — manually verify line 86 was edited).
- 7 hits total: 2 preflight + 5 docs (line 86's bold-split form did
  match single-line grep — all five docs sites edited).

Either count is acceptable. The directive explicitly stipulates ≥ 6.

**Combined success-criterion gate (composite):**

After running all four gates individually, verify their conjunction:

```bash
cd /Users/cnoellert/Documents/GitHub/forge-calibrator && bash -c '
set -e
echo "Gate 1 — pytest:"
python -m pytest tests/ -q | tail -3
echo "Gate 2 — Camera Match hook loaded count:"
grep -rn "Camera Match hook loaded" tools/blender/forge_sender/ | wc -l | xargs
echo "Gate 3 — legacy 2-segment count:"
grep -n "FORGE → Export Camera to Blender" tools/blender/forge_sender/preflight.py docs/seamless-bridge.md | wc -l | xargs
echo "Gate 4 — 3-segment count:"
grep -n "FORGE → Camera → Export Camera to Blender" tools/blender/forge_sender/preflight.py docs/seamless-bridge.md | wc -l | xargs
'
```

Expected output structure:

```
Gate 1 — pytest:
…
414 passed, 2 skipped …
Gate 2 — Camera Match hook loaded count:
0
Gate 3 — legacy 2-segment count:
0
Gate 4 — 3-segment count:
6 (or 7)
```

If all four gate values are correct, the plan is complete. Capture this
exact output (or its equivalent for the actual run) in the SUMMARY's
Verification section.

This task does NOT mutate any file. The verify automated command below
re-runs the composite gate so the Edit-tool harness can confirm green
state before completion.
  </action>
  <verify>
    <automated>cd /Users/cnoellert/Documents/GitHub/forge-calibrator && bash -c 'set -e; python -m pytest tests/ -q > /tmp/gap6_pytest.log 2>&1 && tail -3 /tmp/gap6_pytest.log; legacy_hook=$(grep -rn "Camera Match hook loaded" tools/blender/forge_sender/ | wc -l | tr -d " "); test "$legacy_hook" = "0" || (echo "FAIL Gate 2: $legacy_hook hits"; exit 1); legacy_path=$(grep -n "FORGE → Export Camera to Blender" tools/blender/forge_sender/preflight.py docs/seamless-bridge.md | wc -l | tr -d " "); test "$legacy_path" = "0" || (echo "FAIL Gate 3: $legacy_path hits"; exit 1); new_path=$(grep -n "FORGE → Camera → Export Camera to Blender" tools/blender/forge_sender/preflight.py docs/seamless-bridge.md | wc -l | tr -d " "); test "$new_path" -ge "6" || (echo "FAIL Gate 4: $new_path hits"; exit 1); echo "ALL GATES OK gate2=$legacy_hook gate3=$legacy_path gate4=$new_path"'</automated>
  </verify>
  <acceptance_criteria>
    - Gate 1 — `python -m pytest tests/ -q` exits 0; output ends with `414 passed, N skipped` (or higher passed count if a previously-skipped canary flipped); zero `failed` lines, zero `error` lines.
    - Gate 2 — `grep -rn "Camera Match hook loaded" tools/blender/forge_sender/` outputs zero lines (exits 1).
    - Gate 3 — `grep -n "FORGE → Export Camera to Blender" tools/blender/forge_sender/preflight.py docs/seamless-bridge.md` outputs zero lines (exits 1).
    - Gate 4 — `grep -n "FORGE → Camera → Export Camera to Blender" tools/blender/forge_sender/preflight.py docs/seamless-bridge.md` outputs ≥ 6 lines (2 in preflight.py + ≥ 4 in seamless-bridge.md).
    - Composite — the bash one-liner in the verify field exits 0 with `ALL GATES OK gate2=0 gate3=0 gate4=N` where N ≥ 6.
    - The four gate outputs are captured in SUMMARY's Verification section verbatim.
    - `git status` shows ONLY the eight files in `files_modified` are dirty post-Task-5 (no surprise edits, no missed deletes; `04.4-VERIFICATION.md` is unmodified).
  </acceptance_criteria>
  <done>All four gates from gap-closure directive section D PASS green; composite bash one-liner exits 0 with ALL GATES OK; gate outputs captured in the SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new this plan) | This plan is a string-rename + planning-record alignment plan. No new trust boundary is crossed; no new user input flows; no new bridge payload is constructed. The existing 127.0.0.1 forge-bridge boundary (BRG-03) is unchanged. The user-Action-name `repr()` injection mitigation (Plan 04.4-04 transport.make_create_code) is unchanged. The Tier-1 popup pattern in `__init__.py` is unchanged in shape — only the literal string content of one site is edited. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.4-06 | — | gap-closure string + doc + planning-record edits | accept | No new threat surface introduced. Task 1 edits one user-visible error string in the addon (no input parsing change). Tasks 2-3 align doc/test/preflight strings to match shipped code (no parsing change). Task 4 edits planning artifacts (no executable code path). Task 5 is verification-only. The R-08 user-visible-string discipline (locked in 04.4-CONTEXT.md D-13) is reinforced — every bridge-unreachable error site now emits the same canonical `Camera Calibrator hook loaded?` wording, removing cross-file drift that could confuse a tester debugging a real bridge outage. WR-03 (TypeError not caught) and WR-04 (substring match brittleness) are advisory items deferred to next milestone audit per VERIFICATION.md "Gaps Summary" — not in this plan's scope. |

</threat_model>

<verification>
After Tasks 1-4 land and Task 5 confirms green:

- `python -m pytest tests/ -q` exits 0 with `414 passed, 2 skipped` (or
  any green-with-zero-failures count).
- `grep -rn "Camera Match hook loaded" tools/blender/forge_sender/` returns
  zero hits.
- `grep -n "FORGE → Export Camera to Blender" tools/blender/forge_sender/preflight.py docs/seamless-bridge.md`
  returns zero hits.
- `grep -n "FORGE → Camera → Export Camera to Blender" tools/blender/forge_sender/preflight.py docs/seamless-bridge.md`
  returns ≥ 6 hits.
- `grep -c "Live-Test Correction" .planning/phases/04.4-tester-rollout-polish-forge-ui-style-on-multi-camera-picker-/04.4-RESEARCH.md`
  outputs ≥ 1.
- `grep -c "FORGE → Camera" .planning/ROADMAP.md` outputs ≥ 1.
- `grep -c "flat per RESEARCH §P-01" .planning/ROADMAP.md` outputs 0.
- `grep -c "FORGE → Camera" .planning/phases/04.4-tester-rollout-polish-forge-ui-style-on-multi-camera-picker-/04.4-03-PLAN.md`
  outputs ≥ 2 (must_haves truths).
- `grep -c "FORGE → Camera" .planning/phases/04.4-tester-rollout-polish-forge-ui-style-on-multi-camera-picker-/04.4-03-SUMMARY.md`
  outputs ≥ 2.
- `git status .planning/phases/04.4-tester-rollout-polish-forge-ui-style-on-multi-camera-picker-/04.4-VERIFICATION.md`
  reports the file is clean (unmodified — the gap-closure directive
  declares VERIFICATION.md is the source of truth for what shipped).
- All eight files in `files_modified` have committed edits; no other
  files appear in `git status` as modified.
</verification>

<verification_steps>

These are the four acceptance gates from the gap-closure directive
section D, plus a fifth composite gate. Run them all post-Task-5.

**Gate 1 — pytest green:**
```bash
python -m pytest tests/ -q
```
Expected: green; `414 passed, 2 skipped` (or higher passed count if any
canary flipped); exit 0.

**Gate 2 — stale string eliminated from addon:**
```bash
grep -rn "Camera Match hook loaded" tools/blender/forge_sender/
```
Expected: 0 hits.

**Gate 3 — legacy 2-segment menu path eliminated from preflight + docs:**
```bash
grep -n "FORGE → Export Camera to Blender" tools/blender/forge_sender/preflight.py docs/seamless-bridge.md
```
Expected: 0 hits.

**Gate 4 — 3-segment menu path landed at expected sites:**
```bash
grep -n "FORGE → Camera → Export Camera to Blender" tools/blender/forge_sender/preflight.py docs/seamless-bridge.md
```
Expected: ≥ 6 hits (exactly 2 in preflight.py; ≥ 4 in seamless-bridge.md
because line 86's markdown-bold-split form may not single-line-grep —
manually confirm line 86 was edited).

**Composite gate (sanity):**
```bash
bash -c '
set -e
python -m pytest tests/ -q | tail -3
echo "---"
echo "Gate 2 hits:"
grep -rn "Camera Match hook loaded" tools/blender/forge_sender/ | wc -l
echo "Gate 3 hits:"
grep -n "FORGE → Export Camera to Blender" tools/blender/forge_sender/preflight.py docs/seamless-bridge.md | wc -l
echo "Gate 4 hits:"
grep -n "FORGE → Camera → Export Camera to Blender" tools/blender/forge_sender/preflight.py docs/seamless-bridge.md | wc -l
'
```

Expected output: all four counts match the per-gate expectations above
(0 / 0 / ≥ 6 with pytest green).
</verification_steps>

<success_criteria>
- All five tasks (1, 2, 3, 4, 5) produce edits or verifications matching their
  acceptance criteria.
- All four gap-closure gates from section D PASS green.
- VERIFICATION.md is unmodified — confirms the gap-closure directive is
  honored verbatim.
- No file outside the eight `files_modified` paths shows up in `git status`
  after Task 5.
- The shipped two-level nested menu in `flame/camera_match_hook.py:2592-2614`
  is unchanged — this plan does NOT flatten it (per the user's locked
  WR-02 resolution arm (b)).
- Cross-file consistency: a tester following docs/preflight error copy
  will see the exact 3-segment menu path Flame renders. The project record
  (RESEARCH §P-01 + ROADMAP + 04.4-03-PLAN/SUMMARY) describes the same
  shape that ships.
- Plan 04.4-VERIFICATION.md's WR-01 and WR-02 are both closed; the
  re-verification on this gap-closure plan should produce a fresh report
  with both gaps RESOLVED.
</success_criteria>

<output>
After completion, create `.planning/phases/04.4-tester-rollout-polish-forge-ui-style-on-multi-camera-picker-/04.4-06-SUMMARY.md`
per the standard summary template. The SUMMARY must include:

- Frontmatter `requirements-completed: [WR-01, WR-02, D-12, D-13, R-08]`.
- The verbatim output of the composite verification gate (all four
  numeric counts plus pytest tail).
- Confirmation that `04.4-VERIFICATION.md` is unmodified
  (`git status` line for that file showing it clean).
- A "Deferred Items closed" note: the WR-01 entry in
  `deferred-items.md` Plan 04.4-05 section is now resolved by this plan
  (Task 1). The deferred-items.md file may be updated to mark this entry
  closed (either by appending a "Closed in 04.4-06" note or by removing
  the entry entirely — gap-closure plan author's discretion). If the
  entire `deferred-items.md` file becomes empty after closing the WR-01
  entry, leave a `## Closed` section as a record. Do NOT proactively
  update `deferred-items.md` in any of Tasks 1-4 — that file change is
  bookkeeping appropriate for the SUMMARY commit, not the per-task
  commits.
</output>
