---
phase: 04.4
slug: tester-rollout-polish-forge-ui-style-on-multi-camera-picker
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-04-25
---

# Phase 04.4 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest 7.x (existing) |
| **Config file** | none — bare pytest at repo root |
| **Quick run command** | `pytest tests/test_<module>.py -x` |
| **Full suite command** | `pytest tests/ -q` |
| **Estimated runtime** | ~5 seconds (unit only); manual UAT in Flame is the primary signal |

---

## Sampling Rate

- **After every task commit:** Run focused unit tests for the changed module (`pytest tests/test_<module>.py -x`)
- **After every plan wave:** Run full suite (`pytest tests/ -q`)
- **Before `/gsd-verify-work`:** Full suite must be green AND manual UAT items signed off
- **Max feedback latency:** ~5 seconds for automated; manual UAT requires Flame restart per `memory/flame_module_reload.md`

---

## Per-Task Verification Map

> **To be filled by planner.** Each task in PLAN.md gets a row mapping it to a unit test (Wave 0 stub if needed) or a manual UAT entry. Most of this phase's tasks land in the Manual-Only table below — Flame UI/menu work, Blender addon dialog flow, and visual styling are not unit-testable.

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 04.4-01-01 | TBD | 1 | TBD | — | N/A | manual | see Manual-Only table | — | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `tests/test_forge_sender_metadata_absent.py` — stubs for new addon "send to chosen Action" branch (payload shape, error tier classification)
- [ ] `tests/test_camera_picker_dialog.py` — stubs for the FORGE-styled QDialog data contract (input cameras list, output selection tuple)

*Filled by planner; this phase's automated surface is small — most tasks are UI-visual or Flame-API-bound.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| FORGE menu structure renders correctly in Batch right-click | D-11, D-12 | Flame menu rendering happens inside the host process; no headless API to introspect rendered hierarchy | Install via `install.sh`; restart Flame; right-click Clip in Batch; verify `FORGE` group appears with `Open Camera Calibrator` and `Export Camera to Blender` (flat per RESEARCH P-01); verify old `Camera Match` group is gone |
| Multi-camera picker uses FORGE palette | D-01 | Visual styling check; pixel comparison brittle vs. real | Open an Action with ≥2 cameras; right-click → Export Camera to Blender; picker dialog appears with `#282c34` background, `#E87E24` accent, em-dash window title `FORGE — <title>`; cancellable returns None |
| Camera-node-scope right-click bypasses picker | D-03, D-04, P-02 | Requires Flame Action schematic interaction | Open an Action with ≥2 cameras; right-click a specific Camera node *inside* the Action's schematic; verify export menu appears and resolves to that camera (no picker shown) |
| Blender addon "send camera without metadata" flow — pick existing Action | D-07, D-08 | Cross-process flow (Blender → forge-bridge → Flame); modal dialog UX | In Blender, select a camera with NO `forge_bake_action_name` custom prop; trigger forge_sender; verify dropdown lists Actions in current Flame Batch; pick one; verify camera lands in chosen Action |
| Blender addon "send camera without metadata" flow — create new Action | D-09 (corrected per RESEARCH) | Cross-process; tests Flame `flame.batch.create_node("Action")` + collision RuntimeError handling | In Blender, with metadata-absent camera, pick "Create new Action"; enter unique name; verify Action appears in Flame Batch with that name; repeat with same name; verify error popup surfaces Flame's `Could not set Batch node name` RuntimeError (no auto-suffix) |
| Old `Camera Match` menu items are gone | D-14 | Flame restart + visual menu check | After install + restart, right-click Clip and Action in Batch; verify no `Camera Match` group exists anywhere |
| `Import Camera from Blender` menu item is gone | D-06 | Flame restart + visual menu check | After install + restart, right-click Action; verify no `Import Camera from Blender` entry exists; verify Blender-side push is the only inbound path |
| `Open Camera Calibrator` rename — user-visible labels updated | D-13, R-08 | Spans dialog titles, window titles, error messages, README, docs | grep verifies no user-visible "Camera Match" string remains in `flame/camera_match_hook.py` user-facing strings, `tools/blender/forge_sender/preflight.py`, `docs/seamless-bridge.md`, `README.md`; internal symbols (`camera_match_hook.py`, `forge_flame/`, install path) UNCHANGED |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies (or sit in the Manual-Only table with explicit UAT instructions)
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify (acceptable here — phase is dominated by Flame-UI-bound work; manual UAT carries the weight)
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 5s for automated path; manual UAT bounded by Flame restart cycle
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
