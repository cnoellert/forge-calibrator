---
status: partial
phase: 04.4-tester-rollout-polish-forge-ui-style-on-multi-camera-picker-
source: [04.4-VERIFICATION.md]
started: 2026-04-27T00:00:00Z
updated: 2026-04-27T00:00:00Z
---

## Current Test

[Test 2 partially exercised — Camera-node-scope path failed; Test 3 still awaiting]

## Tests

### 1. forge_sender v1.3.0 addon repackage + 11 UAT scenarios in Blender
expected: |
  Repackage `tools/blender/forge_sender/` as v1.3.0 zip, uninstall existing v1.2.0 in
  Blender Preferences, install the new zip, restart Blender, then run the 11 UAT scenarios
  from Plan 04.4-04 Task 4 §<how-to-verify> steps 5-11 (pick-existing, create-new happy
  path, name-collision, empty-name, bridge-unreachable, empty-Batch, stamped-camera
  regression).
  Each scenario behaves as Plan 04.4-04 prescribes: dialog dropdown lists live Actions
  + '-- Create New --'; collision surfaces clear "An Action named X already exists"
  Tier-1 popup; bridge-unreachable surfaces immediately without showing dialog;
  stamped-camera path remains unchanged.
result: pass
notes: |
  PASS with caveats — the addon was rebuilt as v1.3.4 (not v1.3.0) after live UAT
  surfaced three EnumProperty items-callback regressions in v1.3.0..1.3.2:
    - v1.3.1: cache moved from `self._cached_actions` to module-level (didn't fix it)
    - v1.3.2: cache the items-tuple list object, not just the names (didn't fix it)
    - v1.3.3: diagnostic build with stdout prints — exposed the actual root cause
    - v1.3.4: switched the items callback from a method-bound lambda to a plain
              module-level function. Blender's EnumProperty items callback receives
              the operator's RNA struct as `self`, NOT the Python class instance, so
              `self._method` raises AttributeError and the dropdown silently empties.
  Shipped zip is `tools/blender/forge_sender-v1.3.4.zip` (commit 424657d).

  All 11 scenarios verified against v1.3.4 in live Blender 4.5 + Flame 2026.2.1:
    - 5  pick-existing happy path        — pass (after clean restart of both apps;
                                            the prior crashes were Flame state
                                            corruption per memory/flame_sigsegv_cascade_lesson.md)
    - 6  create-new happy path           — pass (Action 'BLENDER' created, camera
                                            sent, bridge returned success)
    - 7  name collision                  — pass (popup wording correct)
    - 8  empty name                      — pass (popup wording correct, no bridge POST)
    - 9  bridge unreachable              — pass (immediate popup, no dialog shown)
    - 10 empty Batch                     — pass
    - 11 stamped-camera regression       — pass

  Two new issues found during this UAT (both NOT regressions of v1.3.4 itself,
  both in adjacent code paths) — see ## Gaps below: GAP-1, GAP-2.

### 2. Hook install + Flame restart + four right-click contexts
expected: |
  Install the v04.4 hook (`bash install.sh`), fully quit and restart Flame, then verify
  each of the four right-click contexts:
  (1) Clip → menu shows FORGE → Camera → "Open Camera Calibrator";
  (2) Action → menu shows FORGE → Camera → "Export Camera to Blender" (NOT
      "Import Camera from Blender" — that leaf must be gone);
  (3) Camera node inside Action schematic → root-level "Export Camera to Blender" that
      bypasses the picker and runs the export pipeline directly;
  (4) non-Camera node inside Action schematic → no Export Camera entry.
  All four contexts behave per Plan 04.4-03 Task 4 §<how-to-verify> steps 3-7. Two-level
  nested FORGE → Camera shape renders correctly on Flame 2026.2.1 (already confirmed in
  live UAT 2026-04-26 — that confirmation is what drove the WR-02 arm (b) decision).
result: issue
notes: |
  Sub-tests (1), (2), (4) implicitly verified — the FORGE → Camera menu renders correctly
  on Flame 2026.2.1, the user successfully exercised the Action-scope right-click during
  UAT Test 1 setup (no "Import Camera from Blender" leaf observed).

  Sub-test (3) FAILED with Tier-1 dialog "Failed to write FBX: 'NoneType' object is
  not callable" when right-clicking a Camera node inside an Action's schematic and
  choosing Export Camera to Blender. Intermediate files preserved at
  /var/folders/30/sdkv1lw160v8bq48pwrlbqwm0000gn/T/forge_bake_xqrsfkzk (per D-14).

  Plan 04.4-03 explicitly flagged this as RESEARCH §P-02 Open Question OQ-2:
  "cam.parent returning the containing PyActionNode is verified via bridge probe
  but NOT in the actual hook callback context. … Defer until UAT shows the parent
  path failing." UAT just tripped it. See GAP-1 for the fix scope.
why_human: |
  Flame menu callback dispatch, custom hook UI rendering, and the cam.parent resolution
  in hook callback context (vs. bridge context — RESEARCH §P-02 OQ-2 explicitly notes
  this hasn't been verified in hook context).

### 3. FORGE-styled multi-camera picker visual confirmation
expected: |
  Open the Apply Camera flow inside the line-tool calibration window with 2+ non-Perspective
  cameras present. Verify dialog uses #282c34 background, #E87E24 accent, em-dash window
  title "FORGE — Select Camera", double-click-to-accept, Enter accepts, Escape cancels.
  Dialog matches FORGE palette per memory/forge_ui_style.md. No QInputDialog appears
  anywhere in the hook flow.
result: pending
why_human: |
  Visual styling check; pixel comparison brittle vs. real rendering. PySide6 QDialog
  cannot be exercised without a running PySide6 event loop and a host process.

## Summary

total: 3
passed: 1
issues: 1
pending: 1
skipped: 0
blocked: 0

## Gaps

- **GAP-04.4-UAT-01 (`OQ-2 fallback unimplemented`):** The Camera-node-scope right-click
  path (Plan 04.4-03 / `_first_camera_in_action_selection`) trusts `cam.parent` to
  return the containing `PyActionNode`. In the actual Flame hook callback context
  (Flame 2026.2.1 arm64, macOS), `cam.parent` returns an object whose chained API
  (likely `.export_fbx` or one of its callees) resolves to `None`, so the call from
  `forge_flame.fbx_io.export_action_cameras_to_fbx` raises
  `'NoneType' object is not callable` and the user gets a Tier-1 "Failed to write
  FBX" dialog. Plan 04.4-03 already documented the fallback (scan
  `flame.batch.nodes` for the Action whose `.nodes` contains `cam_node`) but
  deferred implementation pending UAT. UAT has now confirmed the parent path fails.
  Scope: rewrite `_first_camera_in_action_selection` in
  `flame/camera_match_hook.py` to scan `flame.batch.nodes` (with `cam.parent`
  preserved as the fast path inside try/except) so the export pipeline sees the
  same PyActionNode that the Action-scope right-click path produces.
- **GAP-04.4-UAT-02 (`forge_bake_camera_name not applied on import`):** The Blender →
  Flame send path (`FORGE_OT_send_to_flame_choose_action.execute` and the existing
  v1.2.0 stamped path both stamp `forge_bake_camera_name` into the v5 payload's
  `custom_properties`. The bridge-side `_forge_send` consumes
  `forge_bake_action_name` to find the target Action but DROPS
  `forge_bake_camera_name` — the imported camera takes whatever name the FBX
  template carries (observed: 'Camera1', not the Blender-side name). End-to-end
  effect: a tester who renames a camera in Blender and sends it to Flame sees
  the Flame-side camera ignore that rename and ship as the FBX default. Scope:
  read `forge_bake_camera_name` in `_forge_send`, then either rename the imported
  camera nodes via `created[i].name.set_value(...)` after `import_fbx_to_action`
  returns, or patch the FBX template's camera name field before write.

Both gaps are found-during-UAT and post-date plan 04.4-06. They are NOT regressions
of v1.3.4 (the EnumProperty items-callback fix) — both predate it and live in
adjacent code paths. Fix path: run `/gsd-plan-phase 04.4 --gaps` to spawn a Plan
04.4-07 gap-closure plan that covers both, then `/gsd-execute-phase 04.4 --gaps-only`.
