---
phase: "04.4"
plan: "05"
type: execute
wave: 3
depends_on:
  - "04.4-01"
files_modified:
  - docs/seamless-bridge.md
autonomous: true
requirements:
  - D-06
  - D-11
  - D-12
  - D-13
  - R-08
must_haves:
  truths:
    - "docs/seamless-bridge.md uses 'FORGE → Export Camera to Blender' and 'FORGE → Open Camera Calibrator' for every user-visible menu path."
    - "docs/seamless-bridge.md does NOT reference 'Import Camera from Blender' as a Flame menu path (it was deleted in Plan 04.4-03)."
    - "Tool name in prose is 'Camera Calibrator' wherever the user-visible label appears; INTERNAL references (camera_match_hook.py filename, /opt/Autodesk/shared/python/camera_match/ install path) are unchanged per D-13."
    - "Error symptom strings reproduced from the addon stay byte-for-byte identical to the strings the addon actually emits (the doc reproduces 'is Flame running with the Camera Calibrator hook loaded?' to match Plan 04.4-04's updated bridge-unreachable message; the addon's preflight error message uses the new 'FORGE → Export Camera to Blender' menu fragment from Plan 04.4-04 Task 3)."
    - "If a README.md tool-reference section exists in repo root, it is reviewed and any user-visible Camera Match string is updated; if no such section exists, the README is left unchanged (this plan ONLY touches docs/seamless-bridge.md)."
  artifacts:
    - path: docs/seamless-bridge.md
      provides: "Updated user-facing documentation reflecting the new FORGE menu structure and Camera Calibrator brand"
      contains: "FORGE → Export Camera to Blender"
  key_links:
    - from: docs/seamless-bridge.md
      to: tools/blender/forge_sender/__init__.py
      via: "Bridge-unreachable error symptom string in docs MUST match the operator's actual report() copy"
      pattern: "is Flame running with the Camera Calibrator hook loaded"
    - from: docs/seamless-bridge.md
      to: tools/blender/forge_sender/preflight.py
      via: "Preflight error symptom string in docs MUST match preflight.check()'s actual return value"
      pattern: "FORGE → Export Camera to Blender preflight echo"
---

<objective>
Wave 3 (parallel) — apply RESEARCH §R-08 user-visible string updates to
`docs/seamless-bridge.md`. This is a string-replacement-only plan, scoped
to a single docs file. It runs in parallel with Plan 04.4-04 (the Blender
addon work) because they touch disjoint files.

Concretely, this plan:
1. Replaces every user-visible occurrence of "Camera Match" in the doc
   with either "Camera Calibrator" (when referring to the tool name) or
   "FORGE" (when referring to the menu group), per RESEARCH §R-08 audit.
2. Drops references to "Import Camera from Blender" as a Flame menu item
   — the Blender push (forge_sender) is now the sole inbound path per
   CONTEXT D-06 / Plan 04.4-03.
3. Updates the menu-path strings: `Camera Match → Export Camera to Blender`
   → `FORGE → Export Camera to Blender` (no Camera sub-tier per
   RESEARCH §P-01 / Plan 04.4-03's flat group structure).
4. Keeps INTERNAL references unchanged: `camera_match_hook.py` filename,
   `/opt/Autodesk/shared/python/camera_match/` install path, the
   `camera_match` package name (per CONTEXT D-13).
5. Synchronizes the error-symptom strings reproduced in the troubleshooting
   section with the actual strings emitted by Plan 04.4-04's updated
   addon — `"is Flame running with the Camera Match hook loaded?"` →
   `"is Flame running with the Camera Calibrator hook loaded?"`.

Purpose: Close the user-facing rebrand by updating the only user-facing
docs file in the repo. After this plan ships, a tester reading
`docs/seamless-bridge.md` sees the new menu structure and brand
consistently — no orphaned "Camera Match" mentions or stale "Import Camera
from Blender" workflow references.

Output: `docs/seamless-bridge.md` with all R-08 updates applied. No code
files modified; no tests added (this is doc copy only).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/04.4-tester-rollout-polish-forge-ui-style-on-multi-camera-picker-/04.4-CONTEXT.md
@.planning/phases/04.4-tester-rollout-polish-forge-ui-style-on-multi-camera-picker-/04.4-RESEARCH.md
@.planning/phases/04.4-tester-rollout-polish-forge-ui-style-on-multi-camera-picker-/04.4-PATTERNS.md
@.planning/phases/04.4-tester-rollout-polish-forge-ui-style-on-multi-camera-picker-/04.4-UI-SPEC.md

@docs/seamless-bridge.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply R-08 string updates to docs/seamless-bridge.md</name>
  <files>docs/seamless-bridge.md</files>
  <read_first>
    - docs/seamless-bridge.md (entire file — preserve everything except the audited strings)
    - .planning/phases/04.4-tester-rollout-polish-forge-ui-style-on-multi-camera-picker-/04.4-RESEARCH.md §R-08 (the audit table with all known string locations)
    - .planning/phases/04.4-tester-rollout-polish-forge-ui-style-on-multi-camera-picker-/04.4-PATTERNS.md (`docs/seamless-bridge.md` MODIFY block — line-by-line target list)
    - .planning/phases/04.4-tester-rollout-polish-forge-ui-style-on-multi-camera-picker-/04.4-UI-SPEC.md §Copywriting (rename rules and exact substrings)
    - .planning/phases/04.4-tester-rollout-polish-forge-ui-style-on-multi-camera-picker-/04.4-CONTEXT.md (D-13: user-facing label rename only; internal names unchanged)
    - tools/blender/forge_sender/preflight.py (will be updated in Plan 04.4-04 Task 3 — the doc must echo the same strings the addon actually emits AFTER that task lands)
    - tools/blender/forge_sender/__init__.py (will be updated in Plan 04.4-04 Task 2 — the bridge-unreachable message in the new operator must match the doc's troubleshooting symptom string)
  </read_first>
  <behavior>
    - Test 1 (manual review): A tester reading the doc top-to-bottom never encounters "Camera Match" as a user-visible tool name or menu group; sees "FORGE" for the menu group and "Camera Calibrator" for the tool name; sees "FORGE → Export Camera to Blender" for the menu path; sees no instructions to use "Import Camera from Blender" from Flame's menu.
    - Test 2 (grep): `grep -n "Camera Match" docs/seamless-bridge.md` returns zero hits, OR returns ONLY hits inside historical/changelog sections that explicitly reference the old name (this plan does NOT add a changelog; if the file has no changelog section, the count must be zero).
    - Test 3 (cross-file synchronization): `grep -n "is Flame running with" docs/seamless-bridge.md` returns hits whose surrounding text matches the bridge-unreachable error string the addon actually emits AFTER Plan 04.4-04's updates ("Camera Calibrator hook loaded", not "Camera Match hook loaded").
  </behavior>
  <action>
Apply the following exact line-by-line replacements per RESEARCH §R-08 audit table. Line numbers are approximate (from the audit) — use Read tool first to confirm current state, then Edit each occurrence.

| Target line | Old text fragment | New text fragment | Notes |
|-------------|------------------|------------------|-------|
| 38 | `the Camera Match hook at` | `the Camera Calibrator hook at` | Prose reference to the tool name |
| 64 | `loads the Camera Match hook (camera_match_hook.py)` | `loads the Camera Calibrator hook (camera_match_hook.py)` | Tool name updates; FILENAME stays unchanged per D-13 |
| 86 | `right-click the target Action → Camera Match → **Export Camera to` | `right-click the target Action → FORGE → **Export Camera to` | Menu path; flat FORGE group per Plan 04.4-03 |
| 100 | `is Flame running with the Camera Match hook loaded?` | `is Flame running with the Camera Calibrator hook loaded?` | Error symptom string — MUST match the operator's actual report() copy in __init__.py after Plan 04.4-04 |
| 131 | `Camera Match → Export Camera to Blender` | `FORGE → Export Camera to Blender` | Menu path |
| 140 | (whichever line is the addon-error symptom — check file carefully) | replace `Camera Match` substring per UI-SPEC §Copywriting | Symptom strings reproduced from the addon — sync with preflight.py |
| 148 | `Camera Match → Export Camera to Blender` | `FORGE → Export Camera to Blender` | Menu path |
| 164 | `right-click Action → Camera Match → Export Camera to Blender` | `right-click Action → FORGE → Export Camera to Blender` | Menu path |
| 184 | `is Flame running with the Camera Match hook loaded?` | `is Flame running with the Camera Calibrator hook loaded?` | Error symptom string — sync with addon |

After the table-driven replacements, also scan for and update any of these less-obvious mentions:

1. **"Import Camera from Blender" workflow descriptions** — if the doc describes a workflow that involves the Flame-side Import Camera from Blender menu item, that entire workflow paragraph must be REWRITTEN or REMOVED. The Blender push (forge_sender's "Send to Flame") is now the sole inbound path. Check sections about returning a camera from Blender to Flame; the description should say "click 'Send to Flame' in Blender's N-panel" — never "right-click Action → Import Camera from Blender" (that menu item no longer exists).

2. **Tool-name prose in headings or section labels** — if any heading contains "Camera Match" as the tool-name reference (e.g. "Camera Match installation"), update to "Camera Calibrator". DO NOT update a heading that references the file/install-path internals (e.g. "the camera_match_hook.py file").

3. **Bridge-unreachable error symptom strings in troubleshooting** — these MUST match the Blender addon's actual output. Plan 04.4-04 Task 2 updates the operator's bridge-unreachable message to use "Camera Calibrator hook loaded"; this doc must echo the EXACT same string in the troubleshooting symptom heading. If the strings drift, the doc's grep-anchored troubleshooting recipes break.

4. **Preflight error symptom strings** — Plan 04.4-04 Task 3 updates preflight.py to use `"FORGE → Export Camera to Blender"` in the metadata-absent error copy. The doc's "Symptom: ... missing 'forge_bake_action_name'" recipe (around line 140 per audit) must reproduce the same string. After updates, `grep -F "FORGE → Export Camera to Blender" docs/seamless-bridge.md` should match the addon's preflight error verbatim.

INTERNAL references that MUST stay unchanged per D-13:

- `camera_match_hook.py` (filename — every mention stays).
- `/opt/Autodesk/shared/python/camera_match/` (install path — every mention stays).
- `import camera_match` (Python import — every mention stays).
- The package directory name `camera_match` (every mention stays).

Audit pass after replacements:

```
grep -n "Camera Match" docs/seamless-bridge.md
```

This grep should return ZERO hits. If any hit remains, inspect its context — is it user-visible (must update) or an internal-name reference (camera_match filename or path — must stay)? If the latter shows up unexpectedly, the file may have a typo where someone wrote "Camera Match" when they meant "camera_match" — fix to lowercase if so.

Caveat: don't break the doc's structure. This is a string-replacement-only task. Do NOT rewrite paragraphs, restructure sections, or add changelog entries. The "Folded Todos" section in the doc (if any) and any Phase X.Y SUMMARY references stay verbatim.
  </action>
  <verify>
    <automated>cd /Users/cnoellert/Documents/GitHub/forge-calibrator && grep -c "FORGE → Export Camera to Blender" docs/seamless-bridge.md && grep -c "Camera Calibrator" docs/seamless-bridge.md</automated>
  </verify>
  <acceptance_criteria>
    - Run `grep -c "Camera Match" docs/seamless-bridge.md` — outputs `0` (zero user-visible mentions).
    - Run `grep -c "Camera Calibrator" docs/seamless-bridge.md` — outputs at least `4` (tool name appears in install description, hook loading description, and at least two troubleshooting symptom lines).
    - Run `grep -c "FORGE → Export Camera to Blender" docs/seamless-bridge.md` — outputs at least `4` (menu path appears in workflow description and multiple troubleshooting recipes per the audit table).
    - Run `grep -c "FORGE → Open Camera Calibrator" docs/seamless-bridge.md` — outputs at least `0` (acceptable if the doc doesn't reference the Open Camera Calibrator menu path; only required if the doc covers that workflow).
    - Run `grep -c "Camera Match → Export Camera to Blender" docs/seamless-bridge.md` — outputs `0`.
    - Run `grep -c "Camera Match → Open Camera Match" docs/seamless-bridge.md` — outputs `0`.
    - Run `grep -c "Import Camera from Blender" docs/seamless-bridge.md` — outputs `0` (the Flame-side menu path is gone; if the doc had a workflow paragraph mentioning it, that paragraph is rewritten or removed).
    - Run `grep -c "camera_match_hook.py" docs/seamless-bridge.md` — outputs at least `1` (internal filename reference is preserved per D-13).
    - Run `grep -c "/opt/Autodesk/shared/python/camera_match/" docs/seamless-bridge.md` — outputs at least `1` (install path reference is preserved per D-13).
    - Manual review: read the doc top-to-bottom. The narrative flow about "send a camera back from Blender to Flame" describes the forge_sender N-panel button workflow, NOT a Flame-side menu workflow. No orphan reference to a Flame Import Camera menu.
    - Run `python -m pytest tests/ -x -q` — exits 0 (sanity — should be unaffected since this plan only touches docs).
  </acceptance_criteria>
  <done>All R-08 string updates applied to docs/seamless-bridge.md, zero user-visible "Camera Match" remnants, zero "Import Camera from Blender" Flame-menu references, internal filename / install path references preserved per D-13, full pytest suite green (sanity).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | This plan modifies only a documentation Markdown file. No code, no network, no user input crosses any boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.4-05 | — | Wave 3 docs update | accept | No new threat surface introduced — string replacements in a Markdown file have no security implications. The user-Action-name `repr()` injection mitigation is owned by Plan 04.4-04 (forge_sender transport). Relies on the existing 127.0.0.1 bridge binding (BRG-03). |

</threat_model>

<verification>
After this task lands:

- `grep -c "Camera Match" docs/seamless-bridge.md` outputs 0.
- All R-08 audit-table replacements applied.
- The "Import Camera from Blender" Flame-side workflow no longer appears in the doc.
- Internal filename / install-path references unchanged per D-13.
- Cross-file string sync: bridge-unreachable error symptoms and preflight error symptoms in the doc MATCH the strings the addon emits after Plan 04.4-04's updates.
- `python -m pytest tests/ -x -q` exits 0 (sanity).
</verification>

<success_criteria>
- All grep assertions in the task acceptance criteria PASS.
- Manual readthrough confirms the doc presents the new menu structure and Camera Calibrator brand consistently.
- No regression in any code or test (no code was modified in this plan).
</success_criteria>

<output>
After completion, create `.planning/phases/04.4-tester-rollout-polish-forge-ui-style-on-multi-camera-picker-/04.4-05-SUMMARY.md` per the standard summary template.
</output>
