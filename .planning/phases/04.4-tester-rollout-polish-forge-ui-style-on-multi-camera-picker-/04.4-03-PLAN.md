---
phase: "04.4"
plan: "03"
type: execute
wave: 2
depends_on:
  - "04.4-02"
files_modified:
  - flame/camera_match_hook.py
autonomous: false
requirements:
  - D-04
  - D-06
  - D-11
  - D-12
  - D-13
  - D-14
  - D-15
  - P-01
  - P-02
  - R-08
must_haves:
  truths:
    - "Right-clicking a Clip in Batch shows the FORGE → Camera nested menu with a single 'Open Camera Calibrator' leaf under it — the old 'Camera Match' group is gone. Two-level nesting is supported on Flame 2026.2.1 per RESEARCH §P-01 Live-Test Correction 2026-04-26."
    - "Right-clicking an Action in Batch shows the FORGE → Camera nested menu with a single 'Export Camera to Blender' leaf under it — there is NO 'Import Camera from Blender' entry."
    - "Right-clicking a Camera node inside an Action's schematic shows a root-level 'Export Camera to Blender' menu item (via the new get_action_custom_ui_actions function)."
    - "_import_camera_from_blender function is deleted from camera_match_hook.py; no QFileDialog references remain in the hook file."
    - "User-visible 'Camera Match' strings in the hook (window title, error dialog titles, menu group, module docstring) are replaced with 'Camera Calibrator' or 'FORGE' per R-08; INTERNAL filenames (camera_match_hook.py, install path /opt/Autodesk/shared/python/camera_match/) are unchanged per D-13."
  artifacts:
    - path: flame/camera_match_hook.py
      provides: "Restructured get_batch_custom_ui_actions (two-level nested FORGE → Camera group per D-12 verbatim, P-01 corrected by live test), new get_action_custom_ui_actions function, deleted _import_camera_from_blender handler, R-08 user-visible string updates"
      contains: "def get_action_custom_ui_actions"
  key_links:
    - from: flame/camera_match_hook.py
      to: flame/camera_match_hook.py
      via: "get_action_custom_ui_actions wires _scope_action_camera + _export_camera_from_action_selection"
      pattern: "_scope_action_camera and _export_camera_from_action_selection are both referenced inside get_action_custom_ui_actions"
    - from: flame/camera_match_hook.py
      to: flame/camera_match_hook.py
      via: "get_batch_custom_ui_actions uses 'FORGE' group name with two leaves only"
      pattern: "name FORGE anchor + name Camera with hierarchy: ['FORGE'] holding Open Camera Calibrator and Export Camera to Blender as the only actions"
---

<objective>
Wave 2 — restructure the menu surface in `flame/camera_match_hook.py`. This is
the single Flame-restart this phase requires (RESEARCH §R-07 / Pitfall 4 —
menu changes need a full Flame restart; live-reload doesn't refresh the menu
dispatch table). All hook-side code is touched in one wave so a single
install + restart verifies every menu surface change at once.

Concretely, this plan:
1. Restructures `get_batch_custom_ui_actions` to a single flat `FORGE` group
   with two leaves: `Open Camera Calibrator` (Clip-scope) and
   `Export Camera to Blender` (Action-scope). The old `Camera Match` group
   is gone (D-14 hard cut). The `Import Camera from Blender` leaf is
   deleted (D-06).
2. Adds a new `get_action_custom_ui_actions` function that exposes
   `Export Camera to Blender` on Camera-node selection inside an Action's
   schematic (RESEARCH §P-02). Wired to `_scope_action_camera` +
   `_export_camera_from_action_selection` from Plan 04.4-02.
3. Deletes the entire `_import_camera_from_blender` function body (lines
   2366-2495 approximate) — no shim, no fallback (D-14).
4. Applies R-08 user-visible string updates inside the hook file:
   `"Camera Match"` → `"Camera Calibrator"` in dialog/window titles, and
   `"Open Camera Match"` → `"Open Camera Calibrator"` in the menu leaf.
   Module docstring updated to reference the new menu path
   `"FORGE > Open Camera Calibrator"`. Internal symbol names
   (filename `camera_match_hook.py`, install path) stay untouched per D-13.

Purpose: Land the user-visible menu rebrand in one atomic edit so a single
Flame restart verifies the entire new menu surface. Plans 04.4-04 (addon)
and 04.4-05 (docs) follow R-08 across the rest of the surface.

Output: `flame/camera_match_hook.py` with the new menu structure, the new
hook function for action-schematic right-clicks, the deleted import
handler, and the R-08 user-visible string updates inside the hook file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/04.4-tester-rollout-polish-forge-ui-style-on-multi-camera-picker-/04.4-CONTEXT.md
@.planning/phases/04.4-tester-rollout-polish-forge-ui-style-on-multi-camera-picker-/04.4-RESEARCH.md
@.planning/phases/04.4-tester-rollout-polish-forge-ui-style-on-multi-camera-picker-/04.4-PATTERNS.md
@.planning/phases/04.4-tester-rollout-polish-forge-ui-style-on-multi-camera-picker-/04.4-UI-SPEC.md

@flame/camera_match_hook.py

<interfaces>
Existing functions this plan modifies or removes:

- `get_batch_custom_ui_actions()` at line 2498-2520 — restructure to two-level nested FORGE → Camera group (per CONTEXT D-12 verbatim, P-01 corrected by Live-Test 2026-04-26), drop Import leaf.
- `_import_camera_from_blender(selection)` at lines 2366-2495 — DELETE entirely.
- `_launch_camera_match` at line 1660 — referenced by the new menu but not modified.
- `_export_camera_to_blender` at line 2080 — referenced by the new menu but not modified (Plan 04.4-02 already shrunk its body).

Symbols added in Plan 04.4-02 that this plan WIRES INTO the menu:

- `_scope_action_camera` — `isVisible` callback for the new action-hook menu item.
- `_export_camera_from_action_selection` — handler for the new action-hook menu item.

New function this plan ADDS:

- `get_action_custom_ui_actions()` — sibling to `get_batch_custom_ui_actions`; exposes the new Camera-node right-click menu surface.

R-08 string update locations (from RESEARCH §R-08 audit):

- Line 1169 — `setWindowTitle("FORGE — Camera Match")` → `setWindowTitle("FORGE — Camera Calibrator")`
- Line 1199 — `QtWidgets.QLabel("Camera Match")` → `QtWidgets.QLabel("Camera Calibrator")`
- Line 266 — error dialog `title="Camera Match"` → `title="Camera Calibrator"`
- Line 1677 — error dialog `title="Camera Match"` → `title="Camera Calibrator"`
- Line 2501 — menu group `"name": "Camera Match"` → handled by the menu restructure (becomes `"FORGE"`)
- Line 2504 — menu leaf `"name": "Open Camera Match"` → `"name": "Open Camera Calibrator"`
- Line 2 (module docstring) — `"Flame Camera Match"` → `"Flame Camera Calibrator"`
- Line 6 (module docstring) — `"Camera Match > Open Camera Match"` → `"FORGE > Open Camera Calibrator"`
- Line 1661 — `"""Launch the Camera Match window..."""` → `"""Launch the Camera Calibrator window..."""`

Internal symbols that MUST NOT change per D-13:

- File path `flame/camera_match_hook.py` (no rename).
- Install path `/opt/Autodesk/shared/python/camera_match/` (no rename).
- Module name `camera_match_hook` and any `import camera_match_hook` references.
- Function names `_launch_camera_match`, `_open_camera_match`, etc.
- The `camera_match` package directory and stub `__init__.py`.
</interfaces>

</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Delete _import_camera_from_blender + restructure get_batch_custom_ui_actions to two-level nested FORGE → Camera group</name>
  <files>flame/camera_match_hook.py</files>
  <read_first>
    - flame/camera_match_hook.py lines 2366-2495 (`_import_camera_from_blender` body — to be deleted entirely)
    - flame/camera_match_hook.py lines 2498-2520 (current `get_batch_custom_ui_actions` — to be restructured)
    - .planning/phases/04.4-tester-rollout-polish-forge-ui-style-on-multi-camera-picker-/04.4-PATTERNS.md (`get_batch_custom_ui_actions` MODIFY block — verbatim adaptation)
    - .planning/phases/04.4-tester-rollout-polish-forge-ui-style-on-multi-camera-picker-/04.4-UI-SPEC.md §A-2 (Batch Right-Click Menu — historical flat FORGE structure; superseded by P-01 Live-Test Correction 2026-04-26 to two-level nested FORGE → Camera shape)
    - .planning/phases/04.4-tester-rollout-polish-forge-ui-style-on-multi-camera-picker-/04.4-RESEARCH.md §P-01 (original "no nested submenus" finding plus 2026-04-26 Live-Test Correction confirming two-level nesting IS supported on Flame 2026.2.1 — shipped shape is two-level nested)
    - .planning/phases/04.4-tester-rollout-polish-forge-ui-style-on-multi-camera-picker-/04.4-CONTEXT.md (D-06 remove Import; D-11/D-12 FORGE group + Camera sub-tier dropped; D-14 hard cut)
    - /opt/Autodesk/shared/python/encompass_selected_nodes/encompass_selected_nodes.py lines 245-261 (peer FORGE group analog — read if accessible from the dev machine)
  </read_first>
  <behavior>
    - Test 1 (existing): `python -m pytest tests/ -x -q` passes (no regression — the import handler was not test-covered, but deletion must not break collection).
    - Test 2 (manual UAT, after install + Flame restart): Right-click a Clip in Batch → menu shows ONE top-level FORGE group → "Open Camera Calibrator" leaf inside. No "Camera Match" group anywhere.
    - Test 3 (manual UAT): Right-click an Action in Batch → menu shows the FORGE group → "Export Camera to Blender" leaf. NO "Import Camera from Blender" entry.
  </behavior>
  <action>
**Note (D-12 verbatim, P-01 Live-Test Correction 2026-04-26):** CONTEXT D-12 specified two-level `FORGE / Camera / leaf` nesting. The original RESEARCH §P-01 finding (two-level nesting unsupported) was based on absence-of-evidence — installed tools at the time of research did not demonstrate the shape. The Phase 04.4-03 executor located sibling tool `forge_cv_align` shipping exactly the nested shape, and live UAT on Flame 2026.2.1 confirmed Flame renders it correctly. The two-level nested structure (`FORGE → Camera → leaf`) is the binding shape per CONTEXT D-12 verbatim. See RESEARCH §P-01 Live-Test Correction 2026-04-26 for the full record. (Plan body originally prescribed a flat group; that prescription is superseded by the live-test outcome — Tasks 1-2 below ship the nested shape; the existing acceptance criteria are intentionally permissive about the inner dict shape.)

Step 1 — Delete `_import_camera_from_blender(selection)` (D-06):

Delete the entire function body, currently at approximately lines 2366-2495. The function starts with `def _import_camera_from_blender(selection):` and ends just before the next `def` (likely `def get_batch_custom_ui_actions():` at line 2498). Delete the docstring, all import statements scoped inside it, and every line of the body.

Side-effects to verify:

- The function uses `from PySide6 import QtWidgets` and references `QtWidgets.QFileDialog` — these were lazy imports inside the deleted function body, so no top-level import cleanup is needed.
- After deletion, `grep -c "QFileDialog" flame/camera_match_hook.py` should output `0` — `_import_camera_from_blender` was the only `QFileDialog` use site in the file.
- After deletion, `grep -c "_import_camera_from_blender" flame/camera_match_hook.py` should output `0` — the menu entry referencing it is also being removed in Step 2 below.

Step 2 — Restructure `get_batch_custom_ui_actions` (D-11, D-12, D-14, P-01):

Replace the current implementation (line 2498-2520 approx) with the two-level nested FORGE → Camera group structure per CONTEXT D-12 verbatim (P-01 corrected by Live-Test 2026-04-26):

```python
def get_batch_custom_ui_actions():
    """Right-click menu registration for Batch schematic.

    Flat FORGE group — two-level nesting (FORGE / Camera / leaf) is NOT
    supported on Flame 2026.2.1 (RESEARCH §P-01: verified via code
    inspection of installed tools and logik.tv FR FI-02123). The
    'Camera' sub-tier from CONTEXT D-12 is dropped. Menu is flat under
    the FORGE group name.

    'Import Camera from Blender' was removed (D-06): the Blender-side
    forge_sender addon is now the sole inbound path. Hard cut on
    install per D-14 — no backwards-compat shim.
    """
    return [
        {
            "name": "FORGE",
            "actions": [
                {
                    "name": "Open Camera Calibrator",
                    "isVisible": _scope_batch_clip,
                    "execute": _launch_camera_match,
                },
                {
                    "name": "Export Camera to Blender",
                    "isVisible": _scope_batch_action,
                    "execute": _export_camera_to_blender,
                },
            ],
        }
    ]
```

Critical: the third dict entry from the original (the Import leaf) is GONE. The two remaining leaves are renamed per R-08 (Open Camera Match → Open Camera Calibrator). The group name is `"FORGE"` not `"Camera Match"`.

Pitfall reminder (paste as a comment above get_batch_custom_ui_actions):

```python
# RESEARCH §Pitfall 4: menu structure changes require a full Flame
# restart. The gc/exec live-reload pattern refreshes module globals but
# NOT Flame's cached menu dispatch table. Test this menu surface only
# after `bash install.sh` + Flame restart.
```
  </action>
  <verify>
    <automated>cd /Users/cnoellert/Documents/GitHub/forge-calibrator && python -c "import ast, pathlib; ast.parse(pathlib.Path('flame/camera_match_hook.py').read_text())" && python -m pytest tests/ -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Run `grep -c "_import_camera_from_blender" flame/camera_match_hook.py` — outputs `0` (function deleted, menu entry deleted).
    - Run `grep -c "QFileDialog" flame/camera_match_hook.py` — outputs `0` (no QFileDialog references remain).
    - Run `grep -c "Import Camera from Blender" flame/camera_match_hook.py` — outputs `0` (no user-visible string remains).
    - Run `grep -nc '"name": "FORGE"' flame/camera_match_hook.py` — outputs `1` (the new menu group).
    - Run `grep -c '"name": "Open Camera Calibrator"' flame/camera_match_hook.py` — outputs `1`.
    - Run `grep -c '"name": "Camera Match"' flame/camera_match_hook.py` — outputs `0` (old menu group gone).
    - Run `grep -c '"name": "Open Camera Match"' flame/camera_match_hook.py` — outputs `0` (old leaf gone).
    - Run `python -c "import ast, pathlib; ast.parse(pathlib.Path('flame/camera_match_hook.py').read_text())"` — exits 0.
    - Run `python -m pytest tests/ -x -q` — exits 0.
    - Manual UAT (deferred to Task 4 sign-off — install + Flame restart): see Task 4 verification steps.
  </acceptance_criteria>
  <done>_import_camera_from_blender deleted, get_batch_custom_ui_actions returns the two-level nested FORGE → Camera group with two leaves only under the Camera sub-group (Open Camera Calibrator + Export Camera to Blender), no QFileDialog references in the file, full pytest suite green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add get_action_custom_ui_actions function (Camera-node right-click surface)</name>
  <files>flame/camera_match_hook.py</files>
  <read_first>
    - flame/camera_match_hook.py (the new) `_scope_action_camera` and `_export_camera_from_action_selection` from Plan 04.4-02 — these are wired into the new menu function
    - flame/camera_match_hook.py (modified) `get_batch_custom_ui_actions` from Task 1 above — sibling pattern to follow
    - .planning/phases/04.4-tester-rollout-polish-forge-ui-style-on-multi-camera-picker-/04.4-PATTERNS.md (`get_action_custom_ui_actions` NEW block — verbatim)
    - .planning/phases/04.4-tester-rollout-polish-forge-ui-style-on-multi-camera-picker-/04.4-UI-SPEC.md §A-3 (Action-Schematic Right-Click Menu)
    - .planning/phases/04.4-tester-rollout-polish-forge-ui-style-on-multi-camera-picker-/04.4-RESEARCH.md §P-02 (verified: get_action_custom_ui_actions is a new function distinct from get_batch_custom_ui_actions)
    - /opt/Autodesk/shared/python/encompass_selected_nodes/encompass_selected_nodes.py lines 263-279 (peer get_action_custom_ui_actions analog — hierarchy: [] root-level shape)
    - /opt/Autodesk/shared/python/axis_to_point_locators/axis_to_point_locators.py lines 454-470 (alternate analog with minimumVersion key)
  </read_first>
  <behavior>
    - Test 1 (existing): `python -m pytest tests/ -x -q` passes (no regression).
    - Test 2 (manual UAT, after install + Flame restart): Open an Action with a non-Perspective Camera node in its schematic. Right-click the Camera node. Menu shows a root-level "Export Camera to Blender" item. Trigger it; the export pipeline runs against the right-clicked camera (picker is bypassed); Blender opens on the resolved camera.
    - Test 3 (manual UAT): Right-click the Perspective camera node inside an Action. Menu does NOT show "Export Camera to Blender" (Perspective filter in `_scope_action_camera`).
    - Test 4 (manual UAT): Right-click an Axis or other non-Camera node. Menu does NOT show "Export Camera to Blender".
  </behavior>
  <action>
Add a new module-level function alongside (just below) `get_batch_custom_ui_actions`. Use the verbatim adaptation from PATTERNS.md `get_action_custom_ui_actions` NEW block:

```python
def get_action_custom_ui_actions():
    """Right-click menu on camera nodes inside an Action's schematic.

    Distinct from get_batch_custom_ui_actions — fires when the user
    right-clicks a PyCoNode inside an Action's node graph, not when
    they right-click the Action node itself in the Batch schematic
    (RESEARCH §P-02 — verified live 2026-04-25 via forge-bridge).

    Uses hierarchy: [] (root level) because two-level nesting in
    action context is unverified on Flame 2026.2.1 (RESEARCH §P-01).
    The leaf appears as a top-level item in the right-click menu.
    """
    return [
        {
            "hierarchy": [],
            "actions": [
                {
                    "name": "Export Camera to Blender",
                    "isVisible": _scope_action_camera,
                    "execute": _export_camera_from_action_selection,
                },
            ],
        }
    ]
```

Optional keys per peer-tool analogs (`'order': 1`, `'separator': 'below'`, `'minimumVersion': '2026'`) are NOT included in this initial implementation — they're discretionary per PATTERNS.md and not required for the Phase 04.4 scope. Adding them is a v2 polish if menu ordering becomes a concern.

Pitfall reminders (paste as a comment block above the function):

```python
# RESEARCH §Pitfall 1: _scope_action_camera uses item.type == "Camera"
# (PLAIN STR, not PyAttribute) — see Plan 04.4-02 Task 2 for the helper
# definition. Do NOT switch to .get_value() here; the Wave 0 test
# test_scope_action_camera_does_not_call_get_value_on_type guards it.
# RESEARCH §Pitfall 3: this is a NEW Flame hook function that must
# coexist with get_batch_custom_ui_actions. Both fire on different
# selection surfaces. Do NOT consolidate them.
# RESEARCH §Pitfall 4: requires a full Flame restart after `bash
# install.sh` for Flame's menu dispatch table to pick up the new
# function. Live-reload (gc/exec) does NOT refresh the dispatch table.
# RESEARCH §P-02 OQ-2: cam.parent returning the containing PyActionNode
# is verified via bridge but not yet in hook callback context. If UAT
# shows the parent path failing, fallback to scanning flame.batch.nodes
# for the Action containing the right-clicked camera.
```
  </action>
  <verify>
    <automated>cd /Users/cnoellert/Documents/GitHub/forge-calibrator && python -c "import ast, pathlib; ast.parse(pathlib.Path('flame/camera_match_hook.py').read_text())" && python -m pytest tests/ -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Run `grep -nc '^def get_action_custom_ui_actions' flame/camera_match_hook.py` — outputs `1`.
    - Run `grep -nc '^def get_batch_custom_ui_actions' flame/camera_match_hook.py` — outputs `1` (still exists alongside).
    - Run `grep -c '"isVisible": _scope_action_camera' flame/camera_match_hook.py` — outputs `1` (the new menu item wires _scope_action_camera).
    - Run `grep -c '"execute": _export_camera_from_action_selection' flame/camera_match_hook.py` — outputs `1`.
    - Run `grep -c '"hierarchy": \[\]' flame/camera_match_hook.py` — outputs `1` (root-level placement per UI-SPEC §A-3).
    - Run `python -c "import ast, pathlib; ast.parse(pathlib.Path('flame/camera_match_hook.py').read_text())"` — exits 0.
    - Run `python -m pytest tests/ -x -q` — exits 0.
    - Manual UAT (deferred to Task 4 sign-off): see Task 4 verification.
  </acceptance_criteria>
  <done>get_action_custom_ui_actions exists as a new module-level function below get_batch_custom_ui_actions, wires _scope_action_camera + _export_camera_from_action_selection, uses hierarchy: [] for root-level placement, full pytest suite green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Apply R-08 user-visible string updates inside camera_match_hook.py</name>
  <files>flame/camera_match_hook.py</files>
  <read_first>
    - flame/camera_match_hook.py line 2 (module docstring — `"Flame Camera Match"` → `"Flame Camera Calibrator"`)
    - flame/camera_match_hook.py line 6 (module docstring usage path — `"Camera Match > Open Camera Match"` → `"FORGE > Open Camera Calibrator"`)
    - flame/camera_match_hook.py line 266 (error dialog `title="Camera Match"`)
    - flame/camera_match_hook.py line 1169 (`setWindowTitle("FORGE — Camera Match")`)
    - flame/camera_match_hook.py line 1199 (`QtWidgets.QLabel("Camera Match")`)
    - flame/camera_match_hook.py line 1661 (docstring of `_launch_camera_match`)
    - flame/camera_match_hook.py line 1677 (error dialog `title="Camera Match"`)
    - .planning/phases/04.4-tester-rollout-polish-forge-ui-style-on-multi-camera-picker-/04.4-RESEARCH.md §R-08 (full string audit table)
    - .planning/phases/04.4-tester-rollout-polish-forge-ui-style-on-multi-camera-picker-/04.4-UI-SPEC.md §Copywriting Contract — Flame-Side Label Changes
    - .planning/phases/04.4-tester-rollout-polish-forge-ui-style-on-multi-camera-picker-/04.4-CONTEXT.md (D-13: user-facing label rename only; internal names unchanged)
  </read_first>
  <behavior>
    - Test 1 (existing): `python -m pytest tests/ -x -q` passes — strings are user-visible only; no test asserts against them inside the hook.
    - Test 2 (manual UAT after install + restart): Camera Calibrator main window's title bar reads "FORGE — Camera Calibrator" (em-dash). The "Camera Calibrator" header label appears at the top of the line-drawing window. Error dialogs that previously said "Camera Match" now say "Camera Calibrator". The module docstring at the top of the file references the new menu path.
    - Test 3 (grep verification): NO user-visible "Camera Match" string remains in the file. INTERNAL references (`camera_match_hook` module name, `camera_match` install path mentions in install-script comments) are exempt and stay.
  </behavior>
  <action>
Apply the following exact line-by-line replacements per RESEARCH §R-08 audit. Use the Read tool first to confirm current state, then Edit each occurrence:

| File | Old String (verbatim) | New String (verbatim) | Notes |
|------|----------------------|----------------------|-------|
| flame/camera_match_hook.py line 2 (module docstring) | `Flame Camera Match — PySide2 VP line tool with live camera solve.` | `Flame Camera Calibrator — PySide2 VP line tool with live camera solve.` | First line of module docstring |
| flame/camera_match_hook.py line 6 (module docstring) | `Right-click a Clip in Batch > Camera Match > Open Camera Match` | `Right-click a Clip in Batch > FORGE > Open Camera Calibrator` | Usage path in docstring |
| flame/camera_match_hook.py line 266 (error dialog title) | `title="Camera Match"` | `title="Camera Calibrator"` | Error dialog title |
| flame/camera_match_hook.py line 1169 (window title) | `self.setWindowTitle("FORGE — Camera Match")` | `self.setWindowTitle("FORGE — Camera Calibrator")` | Em-dash preserved (U+2014) |
| flame/camera_match_hook.py line 1199 (header label) | `header = QtWidgets.QLabel("Camera Match")` | `header = QtWidgets.QLabel("Camera Calibrator")` | Header inside the line-drawing window |
| flame/camera_match_hook.py line 1661 (docstring) | `Launch the Camera Match window for the selected clip.` | `Launch the Camera Calibrator window for the selected clip.` | First line of `_launch_camera_match`'s docstring |
| flame/camera_match_hook.py line 1677 (error dialog title) | `title="Camera Match"` | `title="Camera Calibrator"` | Error dialog title |

Caveat (W-07 reword): These line numbers are PRE-04.4-02 reference only — locate each string by content, not line. Plan 04.4-02 Task 1's `_FORGE_SS` hoist + Task 3's `_export_camera_pipeline` extract have shifted every line number after the edit point. Read the current state of each line with the Read tool before editing. The string-content match is what matters.

D-13 internal-name guard — DO NOT change any of these:

- Module-level filename comments referencing `camera_match_hook.py` (filename is unchanged).
- Install-path comments referencing `/opt/Autodesk/shared/python/camera_match/` (path is unchanged).
- The `camera_match` package name anywhere it's used in `import camera_match` style references.
- Any function name `_launch_camera_match`, `_open_camera_match`, etc. (internal symbols only).
- The `from camera_match import ...` style references in install scripts.

After all replacements, run a grep audit:

- `grep -n "Camera Match" flame/camera_match_hook.py` — should output ONLY internal-reference lines (e.g. comments mentioning the install path or the module filename), if any. Every USER-VISIBLE occurrence (window titles, dialog titles, menu strings, docstring usage paths) should be gone.
- Manually inspect the grep output line-by-line. If any line is a string literal that ends up rendered to the user, it must be in the replacement table above.

Pitfall reminder (no comment to paste — but the executor must be careful):

The string `"Camera Match"` may appear inside `_FORGE_SS` or other PySide6 stylesheets in non-user-visible contexts. Inspect each grep hit before editing. If unsure, check the surrounding context — strings inside `setWindowTitle`, `QLabel`, dialog `title=` kwargs, and docstrings are user-visible; strings inside install-path comments and module-name comments are internal.
  </action>
  <verify>
    <automated>cd /Users/cnoellert/Documents/GitHub/forge-calibrator && python -c "import ast, pathlib; ast.parse(pathlib.Path('flame/camera_match_hook.py').read_text())" && python -m pytest tests/ -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Run `grep -c "FORGE — Camera Calibrator" flame/camera_match_hook.py` — outputs at least `1` (the new window title).
    - Run `grep -c '"Camera Calibrator"' flame/camera_match_hook.py` — outputs at least `4` (window title, header label, two error dialog titles; possibly more if any docstring uses quoted form).
    - Run `grep -n "Camera Match" flame/camera_match_hook.py` — manual review: every remaining hit (if any) must be an INTERNAL reference (e.g. comment about the install path, NOT a string literal rendered to the user). The Task 4 sign-off lists the exact remaining-line count to confirm.
    - Run `grep -c '"Open Camera Calibrator"' flame/camera_match_hook.py` — outputs `1` (the menu leaf from Task 1).
    - Run `grep -c '"Open Camera Match"' flame/camera_match_hook.py` — outputs `0`.
    - Run `grep -c "^_FORGE_SS = " flame/camera_match_hook.py` — outputs `1` (Plan 04.4-02 Task 1 hoisted _FORGE_SS to module level; the R-08 sweep here MUST NOT touch it — assertion guards against accidental rename of the FORGE-styled stylesheet block).
    - Run `grep -cE '"FORGE — Camera Match"|"Open Camera Match"|Flame Camera Match' flame/camera_match_hook.py` — outputs `0` (composite assertion — every R-08 user-visible string fully eliminated).
    - Run `python -c "import ast, pathlib; ast.parse(pathlib.Path('flame/camera_match_hook.py').read_text())"` — exits 0.
    - Run `python -m pytest tests/ -x -q` — exits 0.
    - Manual UAT (deferred to Task 4 sign-off): see Task 4 verification.
  </acceptance_criteria>
  <done>All seven user-visible string replacements applied, INTERNAL symbols (camera_match_hook.py filename, camera_match install path, function names) untouched, grep-audit confirms no user-visible "Camera Match" string remains, full pytest suite green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Wave 2 sign-off — install + restart Flame + verify the entire new menu surface</name>

  <files>—</files>
  <action>Human verification only — no Claude-driven code changes. The verification steps live under <how-to-verify> below; the checkpoint pauses execution until the user types "approved" via the <resume-signal>. The standard <files>/<action>/<verify>/<done> fields are kept here as parser-required placeholders.</action>
  <verify><manual>Follow the <how-to-verify> steps above; type the resume-signal when complete.</manual></verify>
  <done>User has typed "approved" (or equivalent) per the resume-signal contract.</done>
  <what-built>Restructured `get_batch_custom_ui_actions` (two-level nested FORGE → Camera group per D-12 verbatim, P-01 corrected by Live-Test 2026-04-26, dropped Import leaf), new `get_action_custom_ui_actions` for Camera-node right-clicks, deleted `_import_camera_from_blender` handler, and applied R-08 user-visible string updates inside the hook. This is the single Flame-restart cycle this phase requires.</what-built>
  <how-to-verify>
1. Run `bash install.sh` to deploy the updated hook to `/opt/Autodesk/shared/python/camera_match/`.
2. Quit Flame fully (not just close the project — fully quit). Restart Flame and reload the previous batch (or any batch with a Clip + an Action with 2+ non-Perspective cameras).
3. Right-click a Clip node in the Batch schematic. Verify the menu shows:
   - A `FORGE` group at the top (or wherever Flame places custom groups).
   - Inside FORGE: a single `Open Camera Calibrator` leaf.
   - NO `Camera Match` group anywhere.
   - NO `Open Camera Match` leaf anywhere.
   Click `Open Camera Calibrator` — the camera-calibrator window opens with title bar `FORGE — Camera Calibrator` and header label `Camera Calibrator`.
4. Right-click an Action node in the Batch schematic. Verify the menu shows:
   - The `FORGE` group.
   - Inside FORGE: a single `Export Camera to Blender` leaf.
   - NO `Import Camera from Blender` leaf.
   Click `Export Camera to Blender` — picker dialog appears (FORGE-styled per Plan 04.4-02), selecting a camera proceeds through `_export_camera_pipeline`.
5. Open one of the Actions (double-click to enter its schematic). Right-click a non-Perspective Camera node inside the Action's schematic. Verify the menu shows:
   - A root-level `Export Camera to Blender` item (no FORGE group nesting in action context per RESEARCH §P-01).
   Click it — the picker is BYPASSED; the right-clicked camera is resolved directly via `cam.parent`; Blender opens on that camera.
6. Right-click the Perspective camera inside the Action's schematic. Verify `Export Camera to Blender` does NOT appear (Perspective filter in `_scope_action_camera`).
7. Right-click any non-camera node (Axis, etc.) inside the Action's schematic. Verify `Export Camera to Blender` does NOT appear.
8. Trigger any Camera Calibrator error path (e.g. right-click an Action with no non-Perspective camera). Verify the error dialog title says `Camera Calibrator`, not `Camera Match`.
9. Run `python -m pytest tests/ -x -q` from the dev machine — full suite must be green.

If any of steps 1-9 fail, do NOT mark this checkpoint approved. Investigate via forge-bridge or Flame's terminal log. Common pitfalls:
- If the menu doesn't update: confirm `bash install.sh` actually deployed the new hook (check timestamps in `/opt/Autodesk/shared/python/camera_match/`); confirm Flame was fully quit and restarted (not just project-reloaded).
- If `_scope_action_camera` returns False unexpectedly (no menu on Camera right-click): RESEARCH §P-02 OQ-2 — `cam.parent` may behave differently in hook callback context vs. bridge context. Check via bridge: `repr(selection[0].parent)`. If `None` or unexpected, file a follow-up to add the `flame.batch.nodes` scan fallback.
- If `Perspective` still appears in the Camera-node menu: confirm `_scope_action_camera` is checking `item.name.get_value() != "Perspective"`. The `.name` field IS a PyAttribute (with `.get_value()`); only `.type` is a plain string in the action-hook context.

10. Once all 9 verification steps pass, type "approved" to proceed to Wave 3 (Plans 04.4-04 + 04.4-05 in parallel).
  </how-to-verify>
  <resume-signal>Type "approved" to proceed to Wave 3 (Plans 04.4-04 + 04.4-05 in parallel), or describe any issue found.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Flame action-schematic right-click | User-controlled selection of a PyCoNode flows into `_scope_action_camera` and `_export_camera_from_action_selection`. The selection objects come from Flame's own scene graph — no user keystrokes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.4-02 | — | Wave 2 menu restructure | accept | No new threat surface introduced — this plan only restructures the menu registration table and deletes the QFileDialog-based Import handler (REDUCING attack surface — fewer user-input dialogs). The new hook function `get_action_custom_ui_actions` reads selection objects from Flame's scene graph; no user-typed string crosses any boundary. The user-Action-name `repr()` injection mitigation lives in Plan 04.4-04 (forge_sender transport). Relies on the existing 127.0.0.1 bridge binding (BRG-03). |

</threat_model>

<verification>
After all tasks land:

- `python -m pytest tests/ -x -q` exits 0.
- `flame/camera_match_hook.py` parses cleanly via `ast.parse`.
- Grep confirms `_import_camera_from_blender`, `QFileDialog`, `Import Camera from Blender`, `"Camera Match"` (user-visible), `"Open Camera Match"` are all absent.
- Grep confirms `"name": "FORGE"`, `"name": "Open Camera Calibrator"`, `def get_action_custom_ui_actions`, `"FORGE — Camera Calibrator"` are all present.
- Wave 2 sign-off (Task 4) — Flame restart confirms the entire new menu surface renders correctly across all four right-click contexts (Clip, Action, Camera-node, non-Camera node).
</verification>

<success_criteria>
- All grep / pytest assertions in task acceptance criteria PASS.
- Flame restart verifies: FORGE group on Clip + Action right-clicks, NO old Camera Match group, NO Import Camera from Blender, root-level Export Camera to Blender on non-Perspective Camera nodes inside Action schematics, Perspective camera correctly filtered out, all user-visible strings show "Camera Calibrator" or "FORGE".
- Hook file remains importable in Python (no Flame dependency at module load time).
- No regression in any existing test.
</success_criteria>

<output>
After completion, create `.planning/phases/04.4-tester-rollout-polish-forge-ui-style-on-multi-camera-picker-/04.4-03-SUMMARY.md` per the standard summary template.
</output>
