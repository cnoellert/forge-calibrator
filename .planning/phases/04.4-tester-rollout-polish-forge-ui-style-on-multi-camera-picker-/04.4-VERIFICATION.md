---
phase: 04.4-tester-rollout-polish
verified: 2026-04-26T00:00:00Z
status: gaps_found
score: 7/9 must-haves verified
overrides_applied: 0
gaps:
  - truth: "Right-clicking a Clip/Action in Batch shows a top-level FORGE group with leaves directly under it (flat structure per RESEARCH §P-01)"
    status: failed
    reason: "Phase goal and Plan 04.4-03 PLAN both specify a *flat* FORGE group ('FORGE > {Open Camera Calibrator, Export Camera to Blender}' per RESEARCH §P-01: two-level nesting unsupported on Flame 2026.2.1). Plan 04.4-03 SUMMARY also claims a flat group landed. The actual code in flame/camera_match_hook.py:2592-2614 is a two-level nested hierarchy ('FORGE > Camera > leaf'), achieved via a peer 'Camera' group with `hierarchy: ['FORGE']`. This contradicts the ROADMAP goal sentence verbatim. The deviation reverses the explicit P-01 correction baked into the plan and the executor's commit message says 'flat FORGE menu' while the diff lands a nested structure."
    artifacts:
      - path: flame/camera_match_hook.py
        issue: "Lines 2592-2614 declare two top-level dict entries: one empty 'FORGE' anchor group AND one 'Camera' child group with hierarchy: ['FORGE']. The leaves live under 'Camera', producing a 3-segment menu path 'FORGE → Camera → leaf' rather than the planned 2-segment 'FORGE → leaf'."
    missing:
      - "Either: (a) flatten get_batch_custom_ui_actions to a single dict with name='FORGE', hierarchy=[], and the two leaves in actions=[] (matches PLAN/ROADMAP/SUMMARY claim); OR (b) update ROADMAP goal sentence + Plan 04.4-03 SUMMARY claim + RESEARCH §P-01 stance to acknowledge that two-level nesting *is* in fact supported on 2026.2.1 (sibling-tool evidence in code comment), and refresh dependent strings (preflight + docs) so they match the actual menu."
      - "If option (a): re-run UAT to confirm Flame renders the flat group correctly; preflight + doc strings already match this option."
      - "If option (b): update preflight.py:56 and preflight.py:62 to read 'FORGE → Camera → Export Camera to Blender'; update docs/seamless-bridge.md lines 86, 131, 140, 148, 164 to insert the 'Camera' segment; update tests/test_forge_sender_preflight.py:test_check_error_uses_new_menu_path_string_after_r08_rename to assert the 3-segment string."

  - truth: "User-visible 'Camera Match' strings are replaced consistently across the codebase per R-08 (Plan must-have: 'is Flame running with the Camera Calibrator hook loaded?' is the canonical bridge-unreachable copy)"
    status: partial
    reason: "Three of four bridge-unreachable error sites in tools/blender/forge_sender/__init__.py emit the new 'Camera Calibrator hook loaded' string (lines 386, 439, 508 — all in FORGE_OT_send_to_flame_choose_action and helper paths added by Plan 04.4-04). The original FORGE_OT_send_to_flame.execute path at line 265 still emits 'is Flame running with the Camera Match hook loaded?'. This is a user-visible Phase 04.4 R-08 string that was missed; docs/seamless-bridge.md lines 100, 184 reproduce the *new* wording so a user who hits this stale popup and searches docs will not find a matching troubleshooting recipe. Already documented in deferred-items.md as a follow-up but is properly Phase 04.4 R-08 scope."
    artifacts:
      - path: tools/blender/forge_sender/__init__.py
        issue: "Line 265: ConnectionError/Timeout handler in FORGE_OT_send_to_flame.execute still emits legacy 'Camera Match hook loaded?' wording while three sibling sites use the new 'Camera Calibrator hook loaded?' wording."
    missing:
      - "One-line edit at tools/blender/forge_sender/__init__.py:265 — replace 'Camera Match hook loaded' with 'Camera Calibrator hook loaded' to align with lines 386/439/508 and with docs/seamless-bridge.md lines 100/184."

deferred:  # Items intentionally addressed via human UAT — not actionable as automated gaps
  - truth: "Plan 04.4-04 Task 4 — repackage v1.3.0 zip, reinstall in Blender, run 11 UAT scenarios (pick-existing, create-new happy path, name-collision, empty-name, bridge-unreachable, empty-Batch, stamped-camera regression, etc.)"
    addressed_in: "Human UAT (Plan 04.4-04 checkpoint:human-verify)"
    evidence: "Plan 04.4-04 Task 4 is type=checkpoint:human-verify with explicit how-to-verify steps; Wave 0/1/3 automated guards all pass (414 passed / 2 skipped). Cannot be verified statically."

human_verification:
  - test: "Repackage tools/blender/forge_sender/ as v1.3.0 zip, uninstall existing v1.2.0 in Blender Preferences, install the new zip, restart Blender, then run the 11 UAT scenarios from Plan 04.4-04 Task 4 (pick-existing, create-new happy path, name-collision, empty-name, bridge-unreachable, empty-Batch, stamped-camera regression)."
    expected: "Each scenario behaves as Plan 04.4-04 §<how-to-verify> steps 5-11 describe. Specifically: dialog dropdown lists live Actions + '-- Create New --'; collision surfaces clear 'An Action named X already exists' Tier-1 popup; bridge-unreachable surfaces immediately without showing dialog."
    why_human: "Cross-process flow Blender→forge-bridge→Flame, modal dialog UX, and live Flame Action list cannot be tested without a running Flame instance with the bridge listening; addon installation and Blender Preferences UI cannot be exercised headless."
  - test: "Install the v04.4 hook (`bash install.sh`), fully quit and restart Flame, then verify each of the four right-click contexts: (1) Clip → menu shows FORGE group leading to 'Open Camera Calibrator'; (2) Action → menu leads to 'Export Camera to Blender'; (3) Camera node inside Action schematic → root-level 'Export Camera to Blender' that bypasses the picker; (4) non-Camera node inside Action schematic → no Export Camera entry."
    expected: "Menu renders consistently with whichever resolution is chosen for the WR-02 gap (flat FORGE/leaf OR nested FORGE/Camera/leaf). Camera node selection bypasses the picker and runs the export pipeline directly via cam.parent. Perspective camera is filtered out by _scope_action_camera."
    why_human: "Flame menu callback dispatch, custom hook UI rendering, and the cam.parent resolution in hook callback context (vs. bridge context) cannot be unit-tested. RESEARCH §P-02 OQ-2 explicitly notes cam.parent is only verified via bridge probe — UAT in actual hook context is required."
  - test: "Visually confirm the FORGE-styled multi-camera picker: open the Apply Camera flow inside the line-tool calibration window with 2+ non-Perspective cameras present. Verify dialog uses #282c34 background, #E87E24 accent, em-dash window title 'FORGE — Select Camera', double-click-to-accept, Enter accepts, Escape cancels."
    expected: "Dialog matches FORGE palette per memory/forge_ui_style.md. No QInputDialog appears anywhere in the hook flow."
    why_human: "Visual styling check; pixel comparison brittle vs. real rendering. PySide6 QDialog cannot be exercised without a running PySide6 event loop and a host process."
---

# Phase 04.4: Tester-rollout polish Verification Report

**Phase Goal:** Pre-tester UX polish on the Flame Batch right-click menu surface plus a coupled Blender-addon scope expansion. Four cohesive items: (1) FORGE-styled multi-camera picker (folds 999.1), (2) camera-node-scope right-click entry point in Action schematics (new `get_action_custom_ui_actions`), (3) removal of Flame-side "Import Camera from Blender" + extension of `forge_sender` addon to push cameras without bake metadata (new `FORGE_OT_send_to_flame_choose_action` operator), and (4) menu reorganization under a new top-level `FORGE` group (`FORGE > {Open Camera Calibrator, Export Camera to Blender}` flat per RESEARCH §P-01). User-visible label rename "Camera Match" → "Camera Calibrator" / "FORGE" (D-13: internal filenames + install paths unchanged).

**Verified:** 2026-04-26
**Status:** gaps_found
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| #   | Truth   | Status     | Evidence       |
| --- | ------- | ---------- | -------------- |
| 1   | FORGE-styled multi-camera picker replaces QInputDialog with FORGE QDialog (D-01) | VERIFIED | `_pick_camera` at flame/camera_match_hook.py:1942-1995 uses QDialog, sets em-dash window title `FORGE — {dialog_title}`, applies module-level `_FORGE_SS`, wires double-click-to-accept. `grep -c "QInputDialog" flame/camera_match_hook.py` = 0. Apply Camera flow also restyled (Plan 04.4-03 Rule 3 deviation, fixed in commit 728726a). |
| 2   | Camera-node-scope right-click entry point in Action schematics (`get_action_custom_ui_actions` + `_scope_action_camera` + `_export_camera_from_action_selection`) | VERIFIED | `get_action_custom_ui_actions` at line 2631; `_scope_action_camera` wired as isVisible (line 2649); `_export_camera_from_action_selection` wired as execute (line 2650). All 5 Wave-0 `_scope_action_camera_*` tests PASS. Pitfall-1 canary `grep -c "item.type.get_value()" flame/camera_match_hook.py` = 0 confirms plain-str `item.type` discipline. |
| 3   | Flame-side `_import_camera_from_blender` deleted (D-06 hard cut) | VERIFIED | `grep -c "_import_camera_from_blender" flame/camera_match_hook.py` = 0. `grep -c "QFileDialog" flame/camera_match_hook.py` = 0. `grep -c "Import Camera from Blender" flame/camera_match_hook.py` = 0. `grep -c "Import Camera from Blender" docs/seamless-bridge.md` = 0. |
| 4   | New `FORGE_OT_send_to_flame_choose_action` operator with invoke()/draw()/execute() handles pick-existing AND create-new paths with collision detection | VERIFIED | `class FORGE_OT_send_to_flame_choose_action` at tools/blender/forge_sender/__init__.py:329, registered in `_CLASSES` tuple at line 611. `transport.list_batch_actions()` called inside invoke() at line 381. `transport.make_create_code()` called inside execute() at line 430. Collision substring match at line 452 (`"Could not set Batch node name" in err`). All 7 Wave-0 transport tests PASS. |
| 5   | `transport.list_batch_actions` + `transport.make_create_code` exist with repr() injection-safe name embedding | VERIFIED | `_CODE_LIST_ACTIONS` constant at transport.py:306; `list_batch_actions` at line 313 (POST → ast.literal_eval → defensive isinstance check); `make_create_code` at line 366 with `{name!r}` repr-conversion at line 387 (test_make_create_code_embeds_name_via_repr asserts single-quoted form, test_make_create_code_repr_escapes_quotes_and_special_chars round-trips adversarial input through ast.literal_eval). |
| 6   | Menu reorganization places leaves under a top-level FORGE group, "Camera Match" group is gone (D-11/D-12/D-14) | FAILED | `grep -c '"name": "FORGE"' flame/camera_match_hook.py` = 1 (anchor group exists). `grep -c '"name": "Camera Match"' flame/camera_match_hook.py` = 0 (old group removed). HOWEVER the structure is **two-level nested** (`FORGE → Camera → leaf`) at lines 2592-2614, NOT the flat `FORGE → leaf` per the phase goal sentence and Plan 04.4-03's verbatim "P-01 correction to D-12". See WR-02 in REVIEW.md and gap below. |
| 7   | User-visible "Camera Match" → "Camera Calibrator" rename per R-08, internal filenames unchanged per D-13 | PARTIAL | Hook strings updated (window title, header label, error dialog titles, module docstring, `_launch_camera_match` docstring). Internal symbols preserved: `camera_match_hook.py` filename intact, `/opt/Autodesk/shared/python/camera_match/` install path preserved in docs (1 occurrence), `camera_match_hook.py` referenced in docs (2 occurrences). Three of four `__init__.py` bridge-unreachable strings updated (lines 386, 439, 508) but **line 265 still emits "Camera Match hook loaded?"** (deferred-items.md line 5 documents this; properly Phase 04.4 R-08 scope per the plan must-haves). |
| 8   | bl_info version bumped from (1, 2, 0) → (1, 3, 0) for addon repackaging | VERIFIED | `tools/blender/forge_sender/__init__.py:64` reads `"version": (1, 3, 0)`. `grep -c '"version": (1, 2, 0)' tools/blender/forge_sender/__init__.py` = 0. |
| 9   | docs/seamless-bridge.md updated to FORGE menu paths, Camera Calibrator brand, no Flame-side Import Camera workflow references (R-08) | VERIFIED | `grep -c "Camera Match" docs/seamless-bridge.md` = 0. `grep -c "Camera Calibrator" docs/seamless-bridge.md` = 4. `grep -c "FORGE → Export Camera to Blender" docs/seamless-bridge.md` = 4. `grep -c "Import Camera from Blender" docs/seamless-bridge.md` = 0. Internal references preserved: `camera_match_hook.py` (2x), `/opt/Autodesk/shared/python/camera_match/` (1x). NB: doc strings encode the **two-segment** menu path; correctness of these strings depends on which arm of WR-02 resolution is taken. |

**Score:** 7/9 truths verified (1 FAILED, 1 PARTIAL, 7 VERIFIED)

### Required Artifacts

| Artifact | Expected | Status | Details |
| -------- | -------- | ------ | ------- |
| `flame/camera_match_hook.py` | `_FORGE_SS` module-level + FORGE-styled `_pick_camera` + `_scope_action_camera` + `_first_camera_in_action_selection` + `_export_camera_pipeline` + `_export_camera_from_action_selection` + `get_action_custom_ui_actions` + restructured `get_batch_custom_ui_actions` (flat FORGE) + R-08 hook strings | PARTIAL | All ten new symbols present (verified via `grep -nc` on each `^def ...` declaration). `_FORGE_SS` at module scope (line 251). All R-08 hook-side strings landed (window title, header label, error dialog titles). HOWEVER `get_batch_custom_ui_actions` at lines 2592-2614 implements a two-level nested hierarchy, NOT the planned flat structure. AST parses cleanly. |
| `flame/camera_match_hook.py` (deleted code) | `_import_camera_from_blender` deleted, no QFileDialog refs | VERIFIED | `grep -c "_import_camera_from_blender"` = 0; `grep -c "QFileDialog"` = 0; `grep -c "Import Camera from Blender"` = 0. |
| `tools/blender/forge_sender/transport.py` | `list_batch_actions`, `make_create_code`, `_CODE_LIST_ACTIONS` with repr() name embedding via `{name!r}` and 'Could not set Batch node name' canary | VERIFIED | All three new symbols present at lines 306/313/366. `{name!r}` at line 387. Canary string in docstring. AST parses. All 7 Wave-0 transport tests PASS. |
| `tools/blender/forge_sender/__init__.py` | New operator `FORGE_OT_send_to_flame_choose_action`, panel extension with no-metadata branch, bl_info bump to (1,3,0), `import ast` top-level | PARTIAL | New operator + panel extension + version bump + ast import all present. HOWEVER line 265 still emits stale "Camera Match hook loaded?" string in the original `FORGE_OT_send_to_flame` ConnectionError handler — R-08 sweep miss within this phase scope. |
| `tools/blender/forge_sender/preflight.py` | R-08 string updates: 'Camera Match → Export Camera to Blender' → 'FORGE → Export Camera to Blender' | VERIFIED | `grep -c "Camera Match" tools/blender/forge_sender/preflight.py` = 0. Two error message paths now read 'FORGE → Export Camera to Blender'. Wave-0 `test_check_error_uses_new_menu_path_string_after_r08_rename` PASSES. *NB: the asserted two-segment string disagrees with the actual three-segment menu structure — see WR-02 gap.* |
| `docs/seamless-bridge.md` | R-08 string sweep, no 'Camera Match', no 'Import Camera from Blender' Flame-menu references, internal refs preserved | VERIFIED | Acceptance grep table from Plan 04.4-05 SUMMARY confirmed: 0 'Camera Match' hits, 4 'Camera Calibrator' hits, 4 'FORGE → Export Camera to Blender' hits, 2 `camera_match_hook.py` internal refs preserved, 1 install path preserved. |

### Key Link Verification

| From | To  | Via | Status | Details |
| ---- | --- | --- | ------ | ------- |
| `flame/camera_match_hook.py:get_action_custom_ui_actions` | `_scope_action_camera`, `_export_camera_from_action_selection` | Direct function references in dict literal | WIRED | Lines 2649/2650; both helpers defined at module level (1856/2273) and pass Wave-0 tests. |
| `flame/camera_match_hook.py:_export_camera_from_action_selection` | `_export_camera_pipeline` | Tail call after `_first_camera_in_action_selection` resolves (action_node, cam_node) | WIRED | Line 2298: `_export_camera_pipeline(action_node, cam_node, label)`. Pipeline shared with `_export_camera_to_blender` (Action-scope path). |
| `flame/camera_match_hook.py:_pick_camera` | `_FORGE_SS` (module-level) | `dialog.setStyleSheet(_FORGE_SS)` | WIRED | Line 1963 references hoisted module-level `_FORGE_SS` (line 251). |
| `tools/blender/forge_sender/__init__.py:FORGE_OT_send_to_flame_choose_action.invoke` | `transport.list_batch_actions()` | Module-level call before showing props dialog | WIRED | Line 381. Result cached on `self._cached_actions` for EnumProperty items lambda. |
| `tools/blender/forge_sender/__init__.py:FORGE_OT_send_to_flame_choose_action.execute (create-new path)` | `transport.make_create_code(name)` + bridge POST + collision substring match + `ast.literal_eval` | Multi-step bridge call with two error branches | WIRED | Lines 430/452/470. Collision branch keys on 'Could not set Batch node name' substring (RESEARCH §P-03; brittle per WR-04). |
| `tools/blender/forge_sender/__init__.py:VIEW3D_PT_forge_sender.draw` | `forge.send_to_flame` (legacy disabled) + `forge.send_to_flame_choose_action` (enabled) | Two button rows in no-metadata state | WIRED | Lines 580-617 (verified via grep — both bl_idnames referenced; 'No Flame metadata' warning row present). |
| `tools/blender/forge_sender/preflight.py:check` (metadata-absent path) | Tier-1 error string referencing 'FORGE → Export Camera to Blender' | Direct string return | WIRED | Lines 56/62; both Wave-0 preflight tests PASS. |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
| -------- | ------------- | ------ | ------------------ | ------ |
| `FORGE_OT_send_to_flame_choose_action.invoke` | `self._cached_actions` (list[str]) | `transport.list_batch_actions()` POSTs `_CODE_LIST_ACTIONS` to forge-bridge; parses repr-list via `ast.literal_eval` | YES | Wave-0 `test_list_batch_actions_parses_repr_list` PASSES — round-trips `"['action1', 'action2']"` to `['action1', 'action2']`. Empty Batch returns `[]` (defensive `.get("result") or "[]"` at transport.py:333). FLOWING. |
| `FORGE_OT_send_to_flame_choose_action.execute (create-new)` | `target_name` (str) | `transport.make_create_code(name)` builds bridge code; POST returns `repr(name)`; `ast.literal_eval(envelope["result"])` recovers original | YES | Wave-0 `test_make_create_code_embeds_name_via_repr` + `test_make_create_code_repr_escapes_quotes_and_special_chars` lock the contract end-to-end including adversarial inputs. FLOWING. |
| `_export_camera_from_action_selection` | `(action_node, cam_node)` tuple | `_first_camera_in_action_selection(selection)` walks selection list, predicates on `isinstance(item, flame.PyCoNode) and item.type == "Camera" and item.name.get_value() != "Perspective"`, returns `(item.parent, item)` | UNKNOWN (UAT-bound) | RESEARCH §P-02 OQ-2 explicitly flags `cam.parent` as verified only in bridge context, not in hook callback context. Cannot verify statically; deferred to UAT. Helper itself is correct (Wave-0 tests PASS). |
| `_pick_camera` | List of selectable `(action, cam, label)` tuples | Caller pre-resolves cameras via `_find_action_cameras` and passes tuple list; dialog renders labels and returns selected tuple via `cameras[lst.currentRow()]` | YES | Existing Action-scope path passes the resolved camera list (verified by `tests/test_hook_export_camera_to_blender.py` — 4/4 PASS). FLOWING. |
| `VIEW3D_PT_forge_sender.draw (no-metadata branch)` | Two operator buttons | Direct `layout.operator(bl_idname, ...)` calls; both bl_idnames register at `_CLASSES` registration time | YES | Verified in `_CLASSES` tuple at line 611. FLOWING. |
| `get_batch_custom_ui_actions` | Menu dict literal | Returns hard-coded list of dicts referencing wired callbacks | YES (data flows but structure is wrong) | Callbacks wired correctly; structure deviates from phase intent (see WR-02 gap). |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| -------- | ------- | ------ | ------ |
| All Python files parse cleanly | `python -c "import ast; ast.parse(open('flame/camera_match_hook.py').read())"` (and equivalent for transport/init) | All three exit 0 with output `ALL_PARSE_OK` | PASS |
| Full test suite green | `python -m pytest tests/ -q` | 414 passed, 2 skipped (the 2 skips are pre-existing wave-pointer skips unrelated to Phase 04.4) | PASS |
| Wave-0 transport tests all PASSING (no SKIPs) | `python -m pytest tests/test_forge_sender_transport.py -v` | All 41 PASS, 0 SKIPped (was 34/7 at Wave 0 baseline) | PASS |
| Wave-0 preflight tests PASSING | `python -m pytest tests/test_forge_sender_preflight.py -v` | 12 passed, 0 skipped (R-08 canary flipped from SKIP→PASS) | PASS |
| Wave-0 `_scope_action_camera_*` tests PASSING | `python -m pytest tests/test_camera_match_hook.py -v` | All 5 PASS | PASS |
| Hook export-pipeline contract preserved through refactor | `python -m pytest tests/test_hook_export_camera_to_blender.py -v` | All tests PASS | PASS |

### Requirements Coverage

Phase requirements come from `04.4-CONTEXT.md` decision IDs (D-01..D-15) and probe IDs (P-01..P-03) plus refactor/string-rename markers (R-06, R-08). No formal `REQ-` IDs apply to this rollout-polish phase (per the phase note "no formal REQ-IDs — rollout polish phase"). Coverage is reported per decision ID across plans.

| Requirement | Source Plan(s) | Description | Status | Evidence |
| ----------- | ---------- | ----------- | ------ | -------- |
| D-01 | 04.4-01, 04.4-02 | Palette-only restyle of multi-camera picker | SATISFIED | `_pick_camera` is a FORGE-styled QDialog at line 1942; `_FORGE_SS` hoisted to module scope; em-dash window title; QInputDialog gone from picker path |
| D-02 | 04.4-02 | 999.1 polish items deferred to v2 (thumbnails, sort order, etc.) | SATISFIED | No 999.1 polish work landed; only the styling bullet folded per CONTEXT note |
| D-03 | 04.4-01, 04.4-02 | Two entry points, one handler — Action-scope keeps picker, Camera-scope bypasses | SATISFIED | `get_batch_custom_ui_actions` retains Action-scope `_export_camera_to_blender` path; `get_action_custom_ui_actions` uses `_export_camera_from_action_selection` to bypass picker |
| D-04 | 04.4-03 | Both menu items remain visible, isVisible callbacks separate them | SATISFIED | Both functions co-exist; `_scope_batch_action` and `_scope_action_camera` are independent isVisible callbacks |
| D-05 | 04.4-02 | Single resolution helper extracted from `_export_camera_to_blender` | SATISFIED | `_export_camera_pipeline` exists at line 2301, called from both `_export_camera_to_blender` and `_export_camera_from_action_selection` |
| D-06 | 04.4-03 | Remove Flame-side "Import Camera from Blender" + handler | SATISFIED | `_import_camera_from_blender` deleted; QFileDialog gone; Import leaf gone from menu |
| D-07 | 04.4-04 | Extend forge_sender addon for cameras lacking forge bake metadata | SATISFIED | `FORGE_OT_send_to_flame_choose_action` exists with full operator lifecycle |
| D-08 | 04.4-04 | List Actions in current Batch via forge-bridge `/exec` payload | SATISFIED | `_CODE_LIST_ACTIONS` POSTs `flame.batch.nodes` filtered by `type == 'Action'`; scope is current Batch only |
| D-09 (corrected) | 04.4-04 | Create-new-Action path; collision detection (RuntimeError per RESEARCH P-03 — NOT auto-suffix) | SATISFIED | `make_create_code` builds the create code; execute() detects 'Could not set Batch node name' substring (line 452); surfaces clear collision message. (NB: orphan auto-named node on collision is a known limitation per deferred-items deferral; fall-through behavior is acceptable per RESEARCH §P-03.) |
| D-10 | 04.4-04 | Tier-1 popup error UX matches forge_sender patterns | SATISFIED | Bridge unreachable, malformed result, collision, empty-name all surface via `_popup` / `_popup_multiline` consistent with existing operator |
| D-11 | 04.4-03 | Top-level menu becomes 'FORGE' (future home for all FORGE tools) | SATISFIED | `'name': 'FORGE'` group registered (anchor entry at lines 2592-2597) |
| D-12 (with P-01 correction) | 04.4-03 | `FORGE / {Open Camera Calibrator, Export Camera to Blender}` flat per P-01 | **FAILED** | Phase goal and PLAN/SUMMARY all specify a flat FORGE group with two leaves. Actual code is two-level nested `FORGE / Camera / leaf`. See gap WR-02 in REVIEW.md. |
| D-13 | 04.4-03, 04.4-05 | User-facing label rename only; internal symbols unchanged | PARTIAL | All hook-side internal symbols preserved (filename, install path, function names). Doc internal references preserved. **One stale 'Camera Match hook' user-visible string remains at `__init__.py:265`** (deferred-items.md item 1) — this is a R-08 sweep miss within phase scope, not an internal-symbol exception. |
| D-14 | 04.4-03 | Hard cut on old 'Camera Match' submenu — no shim | SATISFIED | `grep -c '"name": "Camera Match"' flame/camera_match_hook.py` = 0; old menu group fully removed |
| D-15 | 04.4-03 | Camera-node-scope items live at root level (`hierarchy: []`) per get_action_custom_ui_actions | SATISFIED | Verified at line 2643 — `hierarchy: []` for the action-context menu (RESEARCH §P-01 motivation: action-context nesting unverified) |
| P-01 | 04.4-03 | Flame 2026.2.1 nested submenu probe | **NEEDS HUMAN** | Plan 04.4-03's P-01 correction said two-level nesting was unsupported and mandated flat structure. SUMMARY 04.4-03 claims flat group landed. Actual code is **two-level nested** with a docstring claiming "sibling tools confirm the dict shape and Flame 2026.2.1 renders it". The two stories conflict. Whether the actual rendering works on 2026.2.1 must be confirmed via UAT (Plan 04.4-03 Task 4 sign-off). |
| P-02 | 04.4-02, 04.4-03 | get_action_custom_ui_actions vs get_batch_custom_ui_actions; cam.parent resolution | PARTIAL | `get_action_custom_ui_actions` is registered as a sibling function (verified). cam.parent resolution path is wired but RESEARCH §P-02 OQ-2 flags it as unverified in hook callback context. Needs human UAT. |
| P-03 | 04.4-04 | Flame API for create_node('Action'); RuntimeError on name collision | SATISFIED | Implementation matches RESEARCH P-03 finding (RuntimeError, not auto-suffix). Collision handling is wired and tested. Orphan-on-collision known limitation accepted per deferred-items. |
| R-06 | 04.4-04 | repr() injection-safety for `make_create_code` user-typed name | SATISFIED | `{name!r}` at transport.py:387 + Wave-0 `test_make_create_code_repr_escapes_quotes_and_special_chars` round-trips adversarial input through `ast.literal_eval` |
| R-08 | 04.4-03, 04.4-04, 04.4-05 | User-visible 'Camera Match' → 'Camera Calibrator' / 'FORGE' rename across hook + addon + docs | PARTIAL | Hook fully updated; preflight fully updated; docs fully updated; `__init__.py:265` still emits stale 'Camera Match hook loaded?' (3 of 4 sites updated; this is a goal-relevant gap, see Truth #7). |

**ORPHANED requirements:** None — every D-*/P-*/R-* ID declared in any plan's `requirements:` field maps to evidence in the codebase.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| ---- | ---- | ------- | -------- | ------ |
| `tools/blender/forge_sender/__init__.py` | 265 | Hard-coded R-08 stale string ("Camera Match hook loaded") | Warning | Cross-file drift: doc symptom strings and three sibling popups read "Camera Calibrator hook loaded"; users following the doc will not match this popup. (WR-01 in REVIEW; deferred-items.md item 1.) |
| `flame/camera_match_hook.py` | 2592-2614 | Two-level nested menu hierarchy contradicts ROADMAP/PLAN/SUMMARY claim of flat structure; docstring asserts "sibling tools confirm the dict shape and Flame 2026.2.1 renders it" but RESEARCH §P-01 (which the executor was contracted to honor) said the opposite | Blocker | Cross-file inconsistency: preflight + docs encode 2-segment menu paths but the implementation renders 3-segment paths. Users following error/doc copy will not find the menu Flame renders. (WR-02 in REVIEW.) |
| `tools/blender/forge_sender/__init__.py` | 469-478 | `ast.literal_eval` inside `try/except (ValueError, SyntaxError, KeyError)` does NOT catch `TypeError` | Warning | If bridge ever returns `{"result": null}`, `ast.literal_eval(None)` raises uncaught TypeError; surfaces as red traceback in Blender console rather than Tier-1 popup. (WR-03 in REVIEW.) |
| `tools/blender/forge_sender/__init__.py` | 451-452 | Substring match on Flame error text 'Could not set Batch node name' for collision detection | Info | Wave-0 source-text canary guards against rename in our source, but cannot detect Autodesk renaming the RuntimeError text in a future Flame release. (WR-04 in REVIEW.) |
| `flame/camera_match_hook.py` | 1958 | Unused `from PySide6.QtCore import Qt  # noqa: F401  (kept for future use; harmless)` | Info | Dead code; QtCore already imported module-level. (IN-01 in REVIEW.) |
| `flame/camera_match_hook.py` | 1949-1950 | Defensive `if not cameras: return None` is unreachable from production paths | Info | Doc-vs-code drift only. (IN-02 in REVIEW.) |
| `flame/camera_match_hook.py` | 2592-2597 | Empty top-level FORGE entry (`actions: []`) reads as suspicious without an explanatory comment | Info | (IN-03 in REVIEW.) |
| `flame/camera_match_hook.py` | 2643-2654 | `get_action_custom_ui_actions` dict omits the `name` key while `get_batch_custom_ui_actions` always includes one | Info | Asymmetry; UAT will determine whether Flame renders an unnamed group separator. (IN-04 in REVIEW.) |
| `tools/blender/forge_sender/__init__.py` | 365-376 | EnumProperty identifiers built from raw Flame Action names — fails on non-ASCII / path separators | Info | Low-likelihood in this internal VFX tooling context. (IN-05 in REVIEW.) |

### Human Verification Required

#### 1. Repackage and reinstall v1.3.0 forge_sender addon, run 11 UAT scenarios

**Test:** Repackage `tools/blender/forge_sender/` as v1.3.0 zip; uninstall existing v1.2.0; install new zip; restart Blender; run the 11 UAT scenarios from Plan 04.4-04 Task 4 §<how-to-verify> steps 5-11 (pick-existing, create-new happy path, name-collision, empty-name, bridge-unreachable, empty-Batch, stamped-camera regression).
**Expected:** Each scenario behaves as Plan 04.4-04 prescribes. Specifically: dialog dropdown lists live Actions + '-- Create New --'; collision surfaces clear 'An Action named X already exists' Tier-1 popup; bridge-unreachable surfaces immediately without showing dialog; stamped-camera path remains unchanged.
**Why human:** Cross-process flow Blender→forge-bridge→Flame, modal dialog UX, and live Flame Action list cannot be tested without a running Flame instance with the bridge listening; Blender Preferences → Add-ons UI cannot be exercised headless.

#### 2. Install hook, restart Flame, verify all four right-click contexts

**Test:** Run `bash install.sh`, fully quit and restart Flame, then verify: (a) Clip → menu shows FORGE-rooted entry to "Open Camera Calibrator"; (b) Action → menu shows FORGE-rooted entry to "Export Camera to Blender" (NOT to "Import Camera from Blender" — that leaf must be gone); (c) Camera node inside Action schematic → root-level "Export Camera to Blender" that bypasses the picker and runs the export pipeline directly; (d) non-Camera node inside Action schematic → no Export Camera entry.
**Expected:** All four contexts behave per Plan 04.4-03 Task 4 §<how-to-verify> steps 3-7. Resolution to WR-02 (flat vs nested) is validated by what Flame renders for the Clip and Action paths.
**Why human:** Flame menu callback dispatch, custom hook UI rendering, and the cam.parent resolution in hook callback context (vs. bridge context — RESEARCH §P-02 OQ-2 explicitly notes this hasn't been verified in hook context).

#### 3. Visually confirm FORGE-styled multi-camera picker

**Test:** With 2+ non-Perspective cameras present, open the Apply Camera flow inside the line-tool calibration window. Verify dialog uses #282c34 background, #E87E24 accent, em-dash window title `FORGE — Select Camera`, double-click-to-accept, Enter/Escape semantics.
**Expected:** Dialog matches FORGE palette per `memory/forge_ui_style.md`. No QInputDialog appears anywhere in the hook flow.
**Why human:** Visual styling check; pixel-comparison brittle without a real PySide6 event loop and host window manager.

### Gaps Summary

Two real gaps — both anchored in cross-file inconsistencies that would directly mislead a tester:

1. **WR-02: Menu structure conflicts with documented intent (Blocker).** Phase goal sentence in ROADMAP says "FORGE > {Open Camera Calibrator, Export Camera to Blender} flat per RESEARCH §P-01 — two-level nesting unsupported on Flame 2026.2.1". Plan 04.4-03's PLAN body codifies this as a "P-01 correction to D-12". Plan 04.4-03 SUMMARY claims the flat group landed. The actual code at `flame/camera_match_hook.py:2592-2614` implements a two-level nested hierarchy (`FORGE > Camera > leaf`) and includes a comment asserting "sibling tools confirm the dict shape and Flame 2026.2.1 renders it" — directly contradicting the RESEARCH §P-01 finding the executor was contracted to honor. Downstream artifacts (preflight error copy at lines 56/62; docs/seamless-bridge.md menu paths at lines 86/131/140/148/164; Wave-0 `test_check_error_uses_new_menu_path_string_after_r08_rename`) all encode the **two-segment** path. A user who reads the addon error or follows the docs and looks for `FORGE → Export Camera to Blender` will not find that path — the actual menu path is `FORGE → Camera → Export Camera to Blender`. Either the implementation must flatten to match the documented intent, or the documented intent (and downstream strings + tests) must be updated to acknowledge the validated three-segment structure.

2. **WR-01: Stale 'Camera Match hook loaded' string (Warning).** Three of four bridge-unreachable error sites in `tools/blender/forge_sender/__init__.py` were updated to the new "Camera Calibrator hook loaded?" wording (lines 386, 439, 508 — added in Plan 04.4-04). The fourth site at line 265 (in the legacy `FORGE_OT_send_to_flame.execute` ConnectionError handler) still emits the stale "Camera Match hook loaded?" wording. Documented in `deferred-items.md` as a follow-up because Plan 04.4-05 was scope-limited to docs, but the deferred-items entry itself notes this is properly Phase 04.4 R-08 scope. Cross-file drift: docs/seamless-bridge.md lines 100/184 reproduce the **new** wording; a tester who hits this stale popup and searches the docs will find no matching troubleshooting recipe.

Three additional advisory items (WR-03, WR-04, IN-* family) are noted in the Anti-Patterns table but do not affect goal achievement; flag them at the next milestone audit.

The ROADMAP-level success-criteria array is empty for Phase 04.4; the phase-goal sentence is the de-facto contract. With the WR-02 menu-structure gap unresolved, the goal sentence's "flat per P-01" clause is unmet.

---

_Verified: 2026-04-26T00:00:00Z_
_Verifier: Claude (gsd-verifier)_
