---
phase: 04.4-tester-rollout-polish
verified: 2026-04-26T21:00:00Z
status: human_needed
score: 9/9 must-haves verified
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 7/9
  gaps_closed:
    - "WR-01: Stale 'Camera Match hook loaded' string at tools/blender/forge_sender/__init__.py:265 — closed by Plan 04.4-06 Task 1 (commit d465d34). All four bridge-unreachable sites (lines 265, 386, 439, 508) now uniformly emit 'Camera Calibrator hook loaded?'."
    - "WR-02: Menu-structure inconsistency between shipped two-level nested code (flame/camera_match_hook.py:2592-2614) and the planned/documented flat structure — resolved via arm (b) (keep nested code, update everything else to match). Plan 04.4-06 Tasks 2-4 switched preflight.py + tests + docs/seamless-bridge.md + ROADMAP goal sentence + RESEARCH §P-01 + 04.4-03-PLAN + 04.4-03-SUMMARY to describe the shipped 3-segment 'FORGE → Camera → Export Camera to Blender' path."
  gaps_remaining: []
  regressions: []
human_verification:
  - test: "Repackage tools/blender/forge_sender/ as v1.3.0 zip, uninstall existing v1.2.0 in Blender Preferences, install the new zip, restart Blender, then run the 11 UAT scenarios from Plan 04.4-04 Task 4 §<how-to-verify> steps 5-11 (pick-existing, create-new happy path, name-collision, empty-name, bridge-unreachable, empty-Batch, stamped-camera regression)."
    expected: "Each scenario behaves as Plan 04.4-04 prescribes: dialog dropdown lists live Actions + '-- Create New --'; collision surfaces clear 'An Action named X already exists' Tier-1 popup; bridge-unreachable surfaces immediately without showing dialog; stamped-camera path remains unchanged."
    why_human: "Cross-process flow Blender→forge-bridge→Flame, modal dialog UX, and live Flame Action list cannot be tested without a running Flame instance with the bridge listening; Blender Preferences → Add-ons UI cannot be exercised headless."
  - test: "Install the v04.4 hook (`bash install.sh`), fully quit and restart Flame, then verify each of the four right-click contexts: (1) Clip → menu shows FORGE → Camera → 'Open Camera Calibrator'; (2) Action → menu shows FORGE → Camera → 'Export Camera to Blender' (NOT 'Import Camera from Blender' — that leaf must be gone); (3) Camera node inside Action schematic → root-level 'Export Camera to Blender' that bypasses the picker and runs the export pipeline directly; (4) non-Camera node inside Action schematic → no Export Camera entry."
    expected: "All four contexts behave per Plan 04.4-03 Task 4 §<how-to-verify> steps 3-7. Two-level nested FORGE → Camera shape renders correctly on Flame 2026.2.1 (already confirmed in live UAT 2026-04-26 — that confirmation is what drove the WR-02 arm (b) decision)."
    why_human: "Flame menu callback dispatch, custom hook UI rendering, and the cam.parent resolution in hook callback context (vs. bridge context — RESEARCH §P-02 OQ-2 explicitly notes this hasn't been verified in hook context)."
  - test: "Visually confirm the FORGE-styled multi-camera picker: open the Apply Camera flow inside the line-tool calibration window with 2+ non-Perspective cameras present. Verify dialog uses #282c34 background, #E87E24 accent, em-dash window title 'FORGE — Select Camera', double-click-to-accept, Enter accepts, Escape cancels."
    expected: "Dialog matches FORGE palette per memory/forge_ui_style.md. No QInputDialog appears anywhere in the hook flow."
    why_human: "Visual styling check; pixel comparison brittle vs. real rendering. PySide6 QDialog cannot be exercised without a running PySide6 event loop and a host process."
deferred:
  - truth: "WR-03: ast.literal_eval inside try/except (ValueError, SyntaxError, KeyError) does NOT catch TypeError if bridge returns {\"result\": null}"
    addressed_in: "Next milestone audit (advisory; explicitly out of scope for Plan 04.4-06 per the gap-closure directive)"
    evidence: "tools/blender/forge_sender/__init__.py:469-478 — narrow except clause; pre-existing in Plan 04.4-04, not introduced by Phase 04.4 gap-closure work."
  - truth: "WR-04: Substring match on Flame error text 'Could not set Batch node name' for collision detection is brittle to Autodesk renaming the RuntimeError text"
    addressed_in: "Next milestone audit (advisory; explicitly out of scope for Plan 04.4-06 per the gap-closure directive)"
    evidence: "tools/blender/forge_sender/__init__.py:451-452 — substring guarded by Wave-0 source-text canary in tests/test_forge_sender_transport.py but cannot detect upstream rename."
  - truth: "IN-01..IN-05: minor info-only items from prior REVIEW (unused Qt import, unreachable defensive guard, empty FORGE anchor without comment, get_action_custom_ui_actions name omission, EnumProperty identifier non-ASCII tolerance)"
    addressed_in: "Optional polish; status: clean per post-gap-closure REVIEW.md (commit 0fe5040)"
    evidence: "Post-gap-closure REVIEW.md status: clean (0 critical, 0 warning, 3 info-only). The three info-level findings (IN-01 stale UI-SPEC pointer, IN-02 historical skip-message, IN-03 type-hint tightening) are all explicitly marked optional and non-blocking."
---

# Phase 04.4: Tester-rollout polish Verification Report (Re-Verification)

**Phase Goal:** Pre-tester UX polish on the Flame Batch right-click menu surface plus a coupled Blender-addon scope expansion. Four cohesive items: (1) FORGE-styled multi-camera picker (folds 999.1), (2) camera-node-scope right-click entry point in Action schematics (new `get_action_custom_ui_actions`), (3) removal of Flame-side "Import Camera from Blender" + extension of `forge_sender` addon to push cameras without bake metadata (new `FORGE_OT_send_to_flame_choose_action` operator), and (4) menu reorganization under a new top-level `FORGE` group with a `Camera` sub-group (rendered path: `FORGE → Camera → {Open Camera Calibrator, Export Camera to Blender}` per CONTEXT D-12 verbatim — RESEARCH §P-01 Live-Test Correction 2026-04-26 confirms two-level nesting IS supported on Flame 2026.2.1; shipped in `flame/camera_match_hook.py:2592-2614`). User-visible label rename "Camera Match" → "Camera Calibrator" / "FORGE" (D-13: internal filenames + install paths unchanged).

**Verified:** 2026-04-26T21:00:00Z
**Status:** human_needed
**Re-verification:** Yes — after Plan 04.4-06 gap-closure landed (6 atomic commits: d465d34, 7ce734e, baca717, dc9668c, 1b0c4bb, 863f796 + REVIEW refresh 0fe5040)

## Re-Verification Summary

The prior verification (2026-04-26 initial) returned `gaps_found 7/9` with two real gaps:

- **WR-01 (Warning):** Stale "Camera Match hook loaded" wording at `tools/blender/forge_sender/__init__.py:265`.
- **WR-02 (Blocker):** Menu structure conflict — shipped code at `flame/camera_match_hook.py:2592-2614` was a two-level nested `FORGE → Camera → leaf`, but PLAN/ROADMAP/SUMMARY/preflight/tests/docs all encoded a flat `FORGE → leaf` shape per RESEARCH §P-01.

User chose **resolution arm (b)** for WR-02: keep the shipped nested code (live UAT 2026-04-26 confirmed Flame 2026.2.1 renders it; sibling `forge_cv_align` ships the same shape) and update everything else to match. Plan 04.4-06 was authored as a `gap_closure: true` plan and executed in 6 atomic commits, closing both WR-01 and WR-02 arm (b).

This re-verification confirms:

- **WR-01 closed:** `grep -rn "Camera Match hook loaded" tools/blender/forge_sender/` returns 0 hits. All four bridge-unreachable sites (lines 265, 386, 439, 508) emit the canonical `"Camera Calibrator hook loaded?"` wording.
- **WR-02 closed (arm b):** `grep "FORGE → Export Camera to Blender" preflight.py docs/seamless-bridge.md` returns 0 hits; `grep "FORGE → Camera → Export Camera to Blender" preflight.py docs/seamless-bridge.md` returns 6 hits (2 preflight + 4 docs single-line + line 86's bold-split form manually verified). ROADMAP goal sentence rewritten; RESEARCH §P-01 has the Live-Test Correction subsection; 04.4-03-PLAN must_haves + body claims and 04.4-03-SUMMARY claims describe the shipped nested shape.
- **No regressions:** Full pytest suite remains 414 passed / 2 skipped (same baseline as prior verification).

All 9 truths now VERIFIED (was 7/9 + 1 PARTIAL + 1 FAILED). The three `human_verification` items remain — they were always going to require live Flame/Blender testing and were flagged in the prior verification.

## Goal Achievement

### Observable Truths

| #   | Truth   | Status     | Evidence       |
| --- | ------- | ---------- | -------------- |
| 1   | FORGE-styled multi-camera picker replaces QInputDialog with FORGE QDialog (D-01) | VERIFIED | `_pick_camera` at `flame/camera_match_hook.py` uses QDialog, sets em-dash window title `FORGE — {dialog_title}`, applies module-level `_FORGE_SS`, wires double-click-to-accept. `grep -c "QInputDialog" flame/camera_match_hook.py` = 0. `grep -c '"FORGE — '` = 2 (main window + picker). |
| 2   | Camera-node-scope right-click entry point in Action schematics (`get_action_custom_ui_actions` + `_scope_action_camera` + `_export_camera_from_action_selection`) | VERIFIED | All five new symbols present at module level: `_scope_action_camera`, `_first_camera_in_action_selection`, `_export_camera_from_action_selection`, `_export_camera_pipeline`, `get_action_custom_ui_actions`. Pitfall-1 canary `grep -c "item.type.get_value()" flame/camera_match_hook.py` = 0 (plain-str discipline confirmed). All 5 Wave-0 `_scope_action_camera_*` tests PASS. |
| 3   | Flame-side `_import_camera_from_blender` deleted (D-06 hard cut) | VERIFIED | `grep -c "_import_camera_from_blender" flame/camera_match_hook.py` = 0. `grep -c "QFileDialog" flame/camera_match_hook.py` = 0. `grep -c "Import Camera from Blender" flame/camera_match_hook.py` = 0. `grep -c "Import Camera from Blender" docs/seamless-bridge.md` = 0. |
| 4   | New `FORGE_OT_send_to_flame_choose_action` operator with invoke()/draw()/execute() handles pick-existing AND create-new paths with collision detection | VERIFIED | `class FORGE_OT_send_to_flame_choose_action` present in `tools/blender/forge_sender/__init__.py`. Both `transport.list_batch_actions` and `transport.make_create_code` referenced inside the operator. Collision substring `"Could not set Batch node name"` present (2 hits — operator + transport docstring). Operator listed in `_CLASSES` tuple. All 7 Wave-0 transport tests PASS. |
| 5   | `transport.list_batch_actions` + `transport.make_create_code` exist with repr() injection-safe name embedding | VERIFIED | All three new symbols present in `tools/blender/forge_sender/transport.py`: `_CODE_LIST_ACTIONS` (module constant), `list_batch_actions` (POST → ast.literal_eval → defensive isinstance check), `make_create_code` (with `{name!r}` repr-conversion). `grep -c "name!r"` = 1; Wave-0 `test_make_create_code_repr_escapes_quotes_and_special_chars` round-trips adversarial input through `ast.literal_eval`. |
| 6   | Menu reorganization places leaves under a top-level FORGE group, "Camera Match" group is gone (D-11/D-12/D-14, P-01 corrected by Live-Test 2026-04-26) | VERIFIED | `grep -c '"name": "FORGE"' flame/camera_match_hook.py` = 1 (anchor entry); `grep -c '"name": "Camera"' flame/camera_match_hook.py` = 1 (child group with `hierarchy: ["FORGE"]`); `grep -c '"name": "Camera Match"' flame/camera_match_hook.py` = 0 (old group removed). Shipped two-level nested shape `FORGE → Camera → leaf` matches CONTEXT D-12 verbatim per the Live-Test Correction. ROADMAP goal sentence describes this shape; RESEARCH §P-01 Live-Test Correction subsection records the supersedence. |
| 7   | User-visible "Camera Match" → "Camera Calibrator" rename per R-08, internal filenames unchanged per D-13 | VERIFIED | Hook strings updated (`grep -c "FORGE — Camera Calibrator" flame/camera_match_hook.py` = 1). Internal symbols preserved: `camera_match_hook.py` filename intact, `/opt/Autodesk/shared/python/camera_match/` install path preserved (1 hit in docs), `camera_match_hook.py` referenced in docs (2 hits). All four `__init__.py` bridge-unreachable strings now emit `"Camera Calibrator hook loaded?"` (4 hits — line 265 fixed by Plan 04.4-06 Task 1, joining sibling sites at 386/439/508). `grep -c "Camera Match" docs/seamless-bridge.md` = 0; `grep -rn "Camera Match hook loaded" tools/blender/forge_sender/` = 0. |
| 8   | bl_info version bumped from (1, 2, 0) → (1, 3, 0) for addon repackaging | VERIFIED | `tools/blender/forge_sender/__init__.py:64` reads `"version": (1, 3, 0)`. `grep -c '"version": (1, 2, 0)' tools/blender/forge_sender/__init__.py` = 0. |
| 9   | docs/seamless-bridge.md updated to FORGE menu paths, Camera Calibrator brand, no Flame-side Import Camera workflow references (R-08); 3-segment menu path matches shipped code | VERIFIED | `grep -c "Camera Match" docs/seamless-bridge.md` = 0. `grep -c "Camera Calibrator" docs/seamless-bridge.md` = 4. `grep -c "FORGE → Camera → Export Camera to Blender" docs/seamless-bridge.md` = 4 (single-line); 5th hit on line 86 confirmed via manual review (markdown-bold-split form). `grep -c "FORGE → Export Camera to Blender" docs/seamless-bridge.md` = 0 (legacy 2-segment fully removed). `grep -c "Import Camera from Blender" docs/seamless-bridge.md` = 0. Internal references preserved: `camera_match_hook.py` (2x), `/opt/Autodesk/shared/python/camera_match/` (1x). Doc strings now match the shipped 3-segment path Flame renders. |

**Score:** 9/9 truths verified (was 7/9 prior — 2 gaps closed by Plan 04.4-06).

### Required Artifacts

| Artifact | Expected | Status | Details |
| -------- | -------- | ------ | ------- |
| `flame/camera_match_hook.py` | `_FORGE_SS` module-level + FORGE-styled `_pick_camera` + `_scope_action_camera` + `_first_camera_in_action_selection` + `_export_camera_pipeline` + `_export_camera_from_action_selection` + `get_action_custom_ui_actions` + restructured `get_batch_custom_ui_actions` (two-level nested FORGE → Camera per D-12 verbatim, P-01 corrected by Live-Test) + R-08 hook strings | VERIFIED | All ten new symbols present. `_FORGE_SS` at module scope. All R-08 hook-side strings landed (window title, header label, error dialog titles, module docstring). Menu structure ships two-level nested FORGE → Camera shape (lines 2592-2614) per the Live-Test Correction. AST parses cleanly. |
| `flame/camera_match_hook.py` (deleted code) | `_import_camera_from_blender` deleted, no QFileDialog refs, no Import Camera from Blender strings | VERIFIED | `grep -c "_import_camera_from_blender"` = 0; `grep -c "QFileDialog"` = 0; `grep -c "Import Camera from Blender"` = 0. |
| `tools/blender/forge_sender/transport.py` | `list_batch_actions`, `make_create_code`, `_CODE_LIST_ACTIONS` with repr() name embedding via `{name!r}` and 'Could not set Batch node name' canary | VERIFIED | All three new symbols present. `{name!r}` at the make_create_code body. Canary string in docstring. AST parses. All 7 Wave-0 transport tests PASS. |
| `tools/blender/forge_sender/__init__.py` | New operator `FORGE_OT_send_to_flame_choose_action`, panel extension with no-metadata branch, bl_info bump to (1,3,0), `import ast` top-level, all 4 bridge-unreachable sites emit canonical "Camera Calibrator hook loaded?" wording | VERIFIED | New operator + panel extension + version bump + ast import all present. WR-01 closed by Plan 04.4-06 Task 1: line 265 now emits the canonical string; `grep -c "is Flame running with the Camera Calibrator hook loaded?" tools/blender/forge_sender/__init__.py` = 4 (one per bridge-unreachable site). `grep -rn "Camera Match hook loaded" tools/blender/forge_sender/` = 0. |
| `tools/blender/forge_sender/preflight.py` | R-08 string updates: 'Camera Match → Export Camera to Blender' → 'FORGE → Camera → Export Camera to Blender' (3-segment path matching shipped nested menu) | VERIFIED | `grep -c "Camera Match" tools/blender/forge_sender/preflight.py` = 0. Two error message paths now read 'FORGE → Camera → Export Camera to Blender' (`grep -c` = 2). Wave-0 `test_check_error_uses_new_menu_path_string_after_r08_rename` PASSES on the 3-segment assertion. |
| `tests/test_forge_sender_preflight.py` | R-08 canary asserts the 3-segment shipped path, not the legacy 2-segment string | VERIFIED | Test at line 201 asserts `"FORGE → Camera → Export Camera to Blender" in err`; line 202 asserts `"Camera Match" not in err`. Plan 04.4-06 Task 2 updated this from the 2-segment form. Test PASSES. |
| `docs/seamless-bridge.md` | R-08 string sweep, no 'Camera Match', no 'Import Camera from Blender' Flame-menu references, internal refs preserved, 3-segment menu path matches shipped code | VERIFIED | All five named lines (86, 131, 140, 148, 164) encode `FORGE → Camera → Export Camera to Blender`. Single-line grep counts 4 hits (line 86's `**Export Camera to** Blender` markdown-bold-split form does not match single-line grep but is correctly edited). `grep -c "FORGE → Export Camera to Blender" docs/seamless-bridge.md` = 0 (legacy 2-segment fully removed). |
| `.planning/ROADMAP.md` | Phase 04.4 goal sentence describes the shipped two-level nested FORGE → Camera shape | VERIFIED | `grep -c "FORGE → Camera" .planning/ROADMAP.md` = 3. `grep -c "flat per RESEARCH §P-01" .planning/ROADMAP.md` = 0 (legacy clause removed). `grep -c "two-level nesting unsupported on Flame 2026.2.1" .planning/ROADMAP.md` = 0. The corrected goal sentence cites RESEARCH §P-01 Live-Test Correction 2026-04-26 and the shipped line range `flame/camera_match_hook.py:2592-2614`. |
| `.planning/phases/04.4-…/04.4-RESEARCH.md` | §P-01 Live-Test Correction subsection records the supersedence | VERIFIED | `grep -c "Live-Test Correction" .planning/phases/04.4-…/04.4-RESEARCH.md` = 2 (subsection heading + body reference). `grep -c "Live-Test Correction (2026-04-26)" …` = 1. Original Finding/Workaround Options/Recommendation text preserved verbatim per Plan 04.4-06 Task 4 Edit 1 (research-process artifact). |
| `.planning/phases/04.4-…/04.4-03-PLAN.md` and `04.4-03-SUMMARY.md` | must_haves + body claims describe the shipped two-level nested shape | VERIFIED | `grep -c "FORGE → Camera" 04.4-03-PLAN.md` = 10; `grep -c "two-level nested" 04.4-03-PLAN.md` = 9; `grep -c "flat FORGE group" 04.4-03-PLAN.md` = 0. SUMMARY: `grep -c "FORGE → Camera"` = 5; `grep -c "two-level nested"` = 5; historical commit-message quotation at line 84 preserved verbatim per Plan 04.4-06 Edit 4(g). |

### Key Link Verification

| From | To  | Via | Status | Details |
| ---- | --- | --- | ------ | ------- |
| `flame/camera_match_hook.py:get_action_custom_ui_actions` | `_scope_action_camera`, `_export_camera_from_action_selection` | Direct function references in dict literal | WIRED | Lines 2649/2650; both helpers defined at module level and pass Wave-0 tests. |
| `flame/camera_match_hook.py:_export_camera_from_action_selection` | `_export_camera_pipeline` | Tail call after `_first_camera_in_action_selection` resolves (action_node, cam_node) | WIRED | `_export_camera_pipeline(action_node, cam_node, label)`. Pipeline shared with `_export_camera_to_blender` (Action-scope path). |
| `flame/camera_match_hook.py:_pick_camera` | `_FORGE_SS` (module-level) | `dialog.setStyleSheet(_FORGE_SS)` | WIRED | Hoisted module-level reference resolves; setStyleSheet call confirmed. |
| `tools/blender/forge_sender/__init__.py:FORGE_OT_send_to_flame_choose_action.invoke` | `transport.list_batch_actions()` | Module-level call before showing props dialog | WIRED | Result cached on `self._cached_actions` for EnumProperty items lambda. |
| `tools/blender/forge_sender/__init__.py:FORGE_OT_send_to_flame_choose_action.execute (create-new path)` | `transport.make_create_code(name)` + bridge POST + collision substring match + `ast.literal_eval` | Multi-step bridge call with two error branches | WIRED | Collision branch keys on 'Could not set Batch node name' substring (RESEARCH §P-03). |
| `tools/blender/forge_sender/__init__.py:VIEW3D_PT_forge_sender.draw` | `forge.send_to_flame` (legacy disabled) + `forge.send_to_flame_choose_action` (enabled) | Two button rows in no-metadata state | WIRED | Both bl_idnames referenced; 'No Flame metadata' warning row present. |
| `tools/blender/forge_sender/preflight.py:check` (metadata-absent path) | Tier-1 error string referencing 'FORGE → Camera → Export Camera to Blender' | Direct string return | WIRED | Both Wave-0 preflight tests PASS — including the R-08 canary that now asserts the 3-segment shipped path. |
| `tools/blender/forge_sender/__init__.py:FORGE_OT_send_to_flame.execute` (legacy ConnectionError handler at line 265) | Canonical 'Camera Calibrator hook loaded?' wording matching sibling sites | Tier-1 popup string | WIRED | WR-01 closed by Plan 04.4-06 Task 1. All four bridge-unreachable sites uniformly emit the canonical wording. |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
| -------- | ------------- | ------ | ------------------ | ------ |
| `FORGE_OT_send_to_flame_choose_action.invoke` | `self._cached_actions` (list[str]) | `transport.list_batch_actions()` POSTs `_CODE_LIST_ACTIONS` to forge-bridge; parses repr-list via `ast.literal_eval` | YES | Wave-0 `test_list_batch_actions_parses_repr_list` PASSES — round-trips `"['action1', 'action2']"` to `['action1', 'action2']`. Empty Batch returns `[]` (defensive `.get("result") or "[]"`). FLOWING. |
| `FORGE_OT_send_to_flame_choose_action.execute (create-new)` | `target_name` (str) | `transport.make_create_code(name)` builds bridge code; POST returns `repr(name)`; `ast.literal_eval(envelope["result"])` recovers original | YES | Wave-0 `test_make_create_code_embeds_name_via_repr` + `test_make_create_code_repr_escapes_quotes_and_special_chars` lock the contract end-to-end including adversarial inputs. FLOWING. |
| `_export_camera_from_action_selection` | `(action_node, cam_node)` tuple | `_first_camera_in_action_selection(selection)` walks selection list, predicates on `isinstance(item, flame.PyCoNode) and item.type == "Camera" and item.name.get_value() != "Perspective"`, returns `(item.parent, item)` | UNKNOWN (UAT-bound) | RESEARCH §P-02 OQ-2 explicitly flags `cam.parent` as verified only in bridge context, not in hook callback context. Cannot verify statically; deferred to UAT (Human Verification item #2). Helper itself is correct (Wave-0 tests PASS). |
| `_pick_camera` | List of selectable `(action, cam, label)` tuples | Caller pre-resolves cameras via `_find_action_cameras` and passes tuple list; dialog renders labels and returns selected tuple via `cameras[lst.currentRow()]` | YES | Existing Action-scope path passes the resolved camera list (verified by `tests/test_hook_export_camera_to_blender.py` — all PASS). FLOWING. |
| `VIEW3D_PT_forge_sender.draw (no-metadata branch)` | Two operator buttons | Direct `layout.operator(bl_idname, ...)` calls; both bl_idnames register at `_CLASSES` registration time | YES | Verified in `_CLASSES` tuple. FLOWING. |
| `get_batch_custom_ui_actions` | Menu dict literal | Returns hard-coded list of dicts referencing wired callbacks | YES | Two-level nested FORGE → Camera shape; callbacks wired correctly. Live UAT 2026-04-26 confirmed Flame 2026.2.1 renders the dict shape. FLOWING. |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| -------- | ------- | ------ | ------ |
| All Python files parse cleanly | `python -c "import ast; ast.parse(...)"` for hook + transport + init | All exit 0 | PASS |
| Full test suite green | `python -m pytest tests/ -q` | 414 passed, 2 skipped | PASS |
| Wave-0 transport tests all PASSING (no SKIPs) | `python -m pytest tests/test_forge_sender_transport.py -v` | 41 PASS, 0 SKIP | PASS |
| Wave-0 preflight tests PASSING (incl. 3-segment R-08 canary) | `python -m pytest tests/test_forge_sender_preflight.py -v` | 12 passed, 0 skipped | PASS |
| Wave-0 `_scope_action_camera_*` tests PASSING | `python -m pytest tests/test_camera_match_hook.py -v` | All 5 PASS | PASS |
| Hook export-pipeline contract preserved through refactor | `python -m pytest tests/test_hook_export_camera_to_blender.py -v` | All tests PASS | PASS |
| Gap-closure Gate 2 — no stale 'Camera Match hook loaded' | `grep -rn "Camera Match hook loaded" tools/blender/forge_sender/ \| wc -l` | 0 | PASS |
| Gap-closure Gate 3 — no legacy 2-segment menu path | `grep -n "FORGE → Export Camera to Blender" preflight.py docs/seamless-bridge.md \| wc -l` | 0 | PASS |
| Gap-closure Gate 4 — 3-segment menu path landed | `grep -n "FORGE → Camera → Export Camera to Blender" preflight.py docs/seamless-bridge.md \| wc -l` | 6 (≥ 6) | PASS |

### Requirements Coverage

Phase requirements come from `04.4-CONTEXT.md` decision IDs (D-01..D-15) and probe IDs (P-01..P-03) plus refactor/string-rename markers (R-06, R-08). No formal `REQ-` IDs apply to this rollout-polish phase. Coverage is reported per decision ID across plans.

| Requirement | Source Plan(s) | Description | Status | Evidence |
| ----------- | ---------- | ----------- | ------ | -------- |
| D-01 | 04.4-01, 04.4-02 | Palette-only restyle of multi-camera picker | SATISFIED | `_pick_camera` is a FORGE-styled QDialog; `_FORGE_SS` hoisted to module scope; em-dash window title; QInputDialog gone from picker path. |
| D-02 | 04.4-02 | 999.1 polish items deferred to v2 (thumbnails, sort order, etc.) | SATISFIED | No 999.1 polish work landed beyond the styling bullet. |
| D-03 | 04.4-01, 04.4-02 | Two entry points, one handler — Action-scope keeps picker, Camera-scope bypasses | SATISFIED | `get_batch_custom_ui_actions` retains Action-scope `_export_camera_to_blender` path; `get_action_custom_ui_actions` uses `_export_camera_from_action_selection` to bypass picker. |
| D-04 | 04.4-03 | Both menu items remain visible, isVisible callbacks separate them | SATISFIED | Both functions co-exist; `_scope_batch_action` and `_scope_action_camera` are independent isVisible callbacks. |
| D-05 | 04.4-02 | Single resolution helper extracted from `_export_camera_to_blender` | SATISFIED | `_export_camera_pipeline` exists, called from both `_export_camera_to_blender` and `_export_camera_from_action_selection`. |
| D-06 | 04.4-03 | Remove Flame-side "Import Camera from Blender" + handler | SATISFIED | `_import_camera_from_blender` deleted; QFileDialog gone; Import leaf gone from menu and from docs. |
| D-07 | 04.4-04 | Extend forge_sender addon for cameras lacking forge bake metadata | SATISFIED | `FORGE_OT_send_to_flame_choose_action` exists with full operator lifecycle. |
| D-08 | 04.4-04 | List Actions in current Batch via forge-bridge `/exec` payload | SATISFIED | `_CODE_LIST_ACTIONS` POSTs `flame.batch.nodes` filtered by `type == 'Action'`; scope is current Batch only. |
| D-09 (corrected) | 04.4-04 | Create-new-Action path; collision detection (RuntimeError per RESEARCH P-03 — NOT auto-suffix) | SATISFIED | `make_create_code` builds the create code; execute() detects 'Could not set Batch node name' substring; surfaces clear collision message. |
| D-10 | 04.4-04 | Tier-1 popup error UX matches forge_sender patterns | SATISFIED | Bridge unreachable, malformed result, collision, empty-name all surface via `_popup` / `_popup_multiline` consistent with existing operator. |
| D-11 | 04.4-03 | Top-level menu becomes 'FORGE' (future home for all FORGE tools) | SATISFIED | `'name': 'FORGE'` group registered (anchor entry; child group `Camera` with `hierarchy: ["FORGE"]` holds the leaves). |
| D-12 (P-01 Live-Test Correction 2026-04-26) | 04.4-03, 04.4-06 | `FORGE / Camera / {Open Camera Calibrator, Export Camera to Blender}` two-level nested per CONTEXT D-12 verbatim | SATISFIED | Shipped at `flame/camera_match_hook.py:2592-2614`. RESEARCH §P-01 Live-Test Correction subsection records the supersedence (original "nesting unsupported" finding was based on absence-of-evidence). User-decision arm (b) honored: keep nested code, update everything else (preflight, tests, docs, ROADMAP, RESEARCH, 04.4-03 PLAN/SUMMARY) to match. |
| D-13 | 04.4-03, 04.4-05, 04.4-06 | User-facing label rename only; internal symbols unchanged | SATISFIED | All hook-side internal symbols preserved (filename, install path, function names). Doc internal references preserved (`camera_match_hook.py` 2x, install path 1x). All four bridge-unreachable sites in `__init__.py` now emit "Camera Calibrator hook loaded?" (WR-01 closed by Plan 04.4-06 Task 1). |
| D-14 | 04.4-03 | Hard cut on old 'Camera Match' submenu — no shim | SATISFIED | `grep -c '"name": "Camera Match"' flame/camera_match_hook.py` = 0; old menu group fully removed. |
| D-15 | 04.4-03 | Camera-node-scope items live at root level (`hierarchy: []`) per get_action_custom_ui_actions | SATISFIED | Verified — `hierarchy: []` for the action-context menu (RESEARCH §P-01 motivation: action-context nesting unverified at research time; could be re-evaluated post-Live-Test if desired but not in scope this phase). |
| P-01 (Live-Test Correction 2026-04-26) | 04.4-03, 04.4-06 | Flame 2026.2.1 nested submenu probe | SATISFIED | Original Finding (nesting unsupported, based on absence-of-evidence) is superseded by Live-Test Correction subsection in RESEARCH §P-01. Live UAT 2026-04-26 confirmed Flame 2026.2.1 renders the two-level nested shape correctly; sibling tool `forge_cv_align` ships the same shape. The shipped `flame/camera_match_hook.py:2592-2614` matches CONTEXT D-12 verbatim. |
| P-02 | 04.4-02, 04.4-03 | get_action_custom_ui_actions vs get_batch_custom_ui_actions; cam.parent resolution | PARTIAL | `get_action_custom_ui_actions` is registered as a sibling function (verified). cam.parent resolution path is wired but RESEARCH §P-02 OQ-2 flags it as unverified in hook callback context. Needs human UAT (Human Verification item #2). |
| P-03 | 04.4-04 | Flame API for create_node('Action'); RuntimeError on name collision | SATISFIED | Implementation matches RESEARCH P-03 finding (RuntimeError, not auto-suffix). Collision handling is wired and tested. Orphan-on-collision known limitation accepted per deferred-items. |
| R-06 | 04.4-04 | repr() injection-safety for `make_create_code` user-typed name | SATISFIED | `{name!r}` at transport.py + Wave-0 `test_make_create_code_repr_escapes_quotes_and_special_chars` round-trips adversarial input through `ast.literal_eval`. |
| R-08 | 04.4-03, 04.4-04, 04.4-05, 04.4-06 | User-visible 'Camera Match' → 'Camera Calibrator' / 'FORGE' rename across hook + addon + docs; menu paths match shipped 3-segment shape | SATISFIED | Hook fully updated; preflight fully updated to 3-segment path; docs fully updated to 3-segment path; all four `__init__.py` bridge-unreachable sites emit canonical "Camera Calibrator hook loaded?" (WR-01 closed). All cross-file consistency restored. |
| WR-01 (gap-closure) | 04.4-06 | Stale 'Camera Match hook loaded' string at __init__.py:265 | RESOLVED | Plan 04.4-06 Task 1 (commit `d465d34`) replaced the legacy substring with 'Camera Calibrator hook loaded'. `grep -rn "Camera Match hook loaded" tools/blender/forge_sender/` = 0. |
| WR-02 (gap-closure, arm b) | 04.4-06 | Menu structure conflict between shipped nested code and planned/documented flat shape | RESOLVED | Plan 04.4-06 Tasks 2-4 switched preflight + tests + docs + ROADMAP + RESEARCH + 04.4-03 PLAN/SUMMARY to describe the shipped 3-segment 'FORGE → Camera → Export Camera to Blender' path. Shipped code at `flame/camera_match_hook.py:2592-2614` is unchanged per arm (b) decision. |

**ORPHANED requirements:** None — every D-*/P-*/R-*/WR-* ID declared in any plan's `requirements:` field maps to evidence in the codebase. WR-01 and WR-02 (introduced in Plan 04.4-06 frontmatter as `gap_closure: true` requirements) both RESOLVED.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| ---- | ---- | ------- | -------- | ------ |
| `tools/blender/forge_sender/__init__.py` | 469-478 | `ast.literal_eval` inside `try/except (ValueError, SyntaxError, KeyError)` does NOT catch `TypeError` | Info (deferred) | Pre-existing from Plan 04.4-04, NOT a regression. If bridge ever returns `{"result": null}`, `ast.literal_eval(None)` raises uncaught TypeError. Explicitly out of scope per Plan 04.4-06 directive — deferred to next milestone audit. |
| `tools/blender/forge_sender/__init__.py` | 451-452 | Substring match on Flame error text 'Could not set Batch node name' for collision detection | Info (deferred) | Pre-existing from Plan 04.4-04, NOT a regression. Wave-0 source-text canary guards against rename in our source, but cannot detect Autodesk renaming the RuntimeError text in a future Flame release. Explicitly out of scope per Plan 04.4-06 directive — deferred to next milestone audit. |
| `flame/camera_match_hook.py` | 1958 | Unused `from PySide6.QtCore import Qt  # noqa: F401  (kept for future use; harmless)` | Info (advisory) | Dead code; QtCore already imported module-level. (Prior IN-01 in REVIEW; status: clean per post-gap-closure REVIEW.md.) |
| `flame/camera_match_hook.py` | 1949-1950 | Defensive `if not cameras: return None` is unreachable from production paths | Info (advisory) | Doc-vs-code drift only. (Prior IN-02.) |
| `flame/camera_match_hook.py` | 2592-2597 | Empty top-level FORGE entry (`actions: []`) reads as suspicious without an explanatory comment | Info (advisory) | Module-level docstring above the function (lines ~2580-2591) now explains the anchor + child shape. (Prior IN-03 mostly addressed by the docstring rewrite in Plan 04.4-06 Task 4.) |
| `flame/camera_match_hook.py` | 2643-2654 | `get_action_custom_ui_actions` dict omits the `name` key while `get_batch_custom_ui_actions` always includes one | Info (advisory) | Asymmetry; Live UAT confirmed Flame renders the unnamed root-level entry correctly. (Prior IN-04.) |
| `tools/blender/forge_sender/__init__.py` | 365-376 | EnumProperty identifiers built from raw Flame Action names — fails on non-ASCII / path separators | Info (advisory) | Low-likelihood in this internal VFX tooling context. (Prior IN-05.) |

**No blockers, no warnings.** Post-gap-closure REVIEW.md (commit 0fe5040) status: clean (0 critical, 0 warning, 3 info-only).

### Human Verification Required

#### 1. Repackage and reinstall v1.3.0 forge_sender addon, run 11 UAT scenarios

**Test:** Repackage `tools/blender/forge_sender/` as v1.3.0 zip; uninstall existing v1.2.0; install new zip; restart Blender; run the 11 UAT scenarios from Plan 04.4-04 Task 4 §<how-to-verify> steps 5-11 (pick-existing, create-new happy path, name-collision, empty-name, bridge-unreachable, empty-Batch, stamped-camera regression).
**Expected:** Each scenario behaves as Plan 04.4-04 prescribes. Specifically: dialog dropdown lists live Actions + '-- Create New --'; collision surfaces clear 'An Action named X already exists' Tier-1 popup; bridge-unreachable surfaces immediately without showing dialog; stamped-camera path remains unchanged.
**Why human:** Cross-process flow Blender→forge-bridge→Flame, modal dialog UX, and live Flame Action list cannot be tested without a running Flame instance with the bridge listening; Blender Preferences → Add-ons UI cannot be exercised headless.

#### 2. Install hook, restart Flame, verify all four right-click contexts

**Test:** Run `bash install.sh`, fully quit and restart Flame, then verify: (a) Clip → menu shows FORGE → Camera → "Open Camera Calibrator"; (b) Action → menu shows FORGE → Camera → "Export Camera to Blender" (NOT "Import Camera from Blender" — that leaf must be gone); (c) Camera node inside Action schematic → root-level "Export Camera to Blender" that bypasses the picker and runs the export pipeline directly via cam.parent; (d) non-Camera node inside Action schematic → no Export Camera entry.
**Expected:** All four contexts behave per Plan 04.4-03 Task 4 §<how-to-verify> steps 3-7. Two-level nested FORGE → Camera shape renders correctly on Flame 2026.2.1 (already confirmed in live UAT 2026-04-26 — that confirmation is what drove the WR-02 arm (b) decision).
**Why human:** Flame menu callback dispatch, custom hook UI rendering, and the cam.parent resolution in hook callback context (vs. bridge context — RESEARCH §P-02 OQ-2 explicitly notes this hasn't been verified in hook context).

#### 3. Visually confirm FORGE-styled multi-camera picker

**Test:** With 2+ non-Perspective cameras present, open the Apply Camera flow inside the line-tool calibration window. Verify dialog uses #282c34 background, #E87E24 accent, em-dash window title `FORGE — Select Camera`, double-click-to-accept, Enter/Escape semantics.
**Expected:** Dialog matches FORGE palette per `memory/forge_ui_style.md`. No QInputDialog appears anywhere in the hook flow.
**Why human:** Visual styling check; pixel-comparison brittle without a real PySide6 event loop and host window manager.

### Gaps Summary

**No real gaps remain.** Plan 04.4-06 closed both real gaps from the prior verification:

- **WR-01 RESOLVED:** `tools/blender/forge_sender/__init__.py:265` now emits the canonical "Camera Calibrator hook loaded?" wording. All four bridge-unreachable sites in the addon are uniformly worded. (Plan 04.4-06 Task 1, commit `d465d34`.)

- **WR-02 RESOLVED (arm b):** The shipped two-level nested menu in `flame/camera_match_hook.py:2592-2614` is unchanged; everything else (preflight, tests, docs, ROADMAP, RESEARCH §P-01, 04.4-03 PLAN/SUMMARY) was switched to describe the shipped 3-segment "FORGE → Camera → Export Camera to Blender" path. Cross-file consistency restored — a tester following preflight error copy or doc walkthroughs now sees the exact menu path Flame renders. (Plan 04.4-06 Tasks 2-4, commits `7ce734e` / `baca717` / `dc9668c` / `1b0c4bb`.)

**Three items remain in `human_verification`** — these were always going to require live Flame/Blender testing and were flagged in the prior verification:

1. Repackage v1.3.0 addon zip + run 11 UAT scenarios (Blender→forge-bridge→Flame cross-process flow).
2. Install hook + restart Flame + verify all four right-click contexts (Flame menu dispatch + cam.parent in hook context).
3. Visually confirm FORGE-styled picker (PySide6 event loop required).

**Three items remain `deferred` (out of scope for Plan 04.4-06 per the gap-closure directive):**

- WR-03: narrow `except (ValueError, SyntaxError, KeyError)` does not catch TypeError around `ast.literal_eval` (pre-existing from Plan 04.4-04; advisory).
- WR-04: substring-match brittleness on Flame error text 'Could not set Batch node name' (pre-existing; advisory).
- IN-01..IN-05: minor style/info items from prior REVIEW (post-gap-closure REVIEW.md is `status: clean`).

**Phase 04.4 status:** All 9 must-haves now VERIFIED. Phase code+docs+planning artifacts are internally consistent. The remaining work is human UAT (3 items) — once those pass, Phase 04.4 can ship to testers.

---

_Verified: 2026-04-26T21:00:00Z_
_Verifier: Claude (gsd-verifier)_
_Re-verification of: prior gaps_found 7/9 → now 9/9 verified, status human_needed pending live UAT_
