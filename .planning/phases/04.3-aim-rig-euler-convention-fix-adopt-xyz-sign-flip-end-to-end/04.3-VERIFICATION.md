---
phase: 04.3-aim-rig-euler-convention-fix-adopt-xyz-sign-flip-end-to-end
plan: 01
verified: 2026-04-24T22:00:00Z
status: passed
score: 11/11 must-haves verified (9 truths + 1 artifact-suite + 1 wiring-suite)
verifier: gsd-verifier (phase-level, goal-backward) — supplements the executor's per-plan VERIFICATION.md authored 2026-04-25 (commit 5233b75)
overrides_applied: 0
re_verification: false
---

# Phase 04.3: Aim-rig Euler Convention Fix — Phase-Level Verification Report

**Phase Goal (from ROADMAP.md):**
> Close the Phase 04.2 known limitation — aim-rig camera round-trip lands ~0.087° off
> on ry for the Camera1 fixture because Euler decomposition uses the wrong rotation
> convention. Adopt the correct convention end-to-end in every place the aim-rig
> Euler math touches, in sync, in one wave of commits that leaves the tree
> consistent on every intermediate revision.

**Verified:** 2026-04-24
**Status:** PASSED — all goal-backward gates met, suite 400/400 + 2 skipped (bpy-gated).
**Re-verification:** No (initial phase-level verification). The executor's per-plan
verification (Task 5, committed in 5233b75 as `04.3-VERIFICATION.md`) is preserved
in this file's git history. This phase-level pass supplements it with goal-backward
checks against the ROADMAP success criteria, must-haves frontmatter, and
out-of-scope guardrails (adapter.py, free-rig path).

---

## 1. Goal Achievement

### 1.1 The "WHY this phase exists" Gate

| Metric | Pre-phase (`_zyx`) | Post-phase (`_xyz`) | Flame viewport truth | Improvement |
|---|---|---|---|---|
| rx | 1.858° | **1.814039°** | 1.8193° | 0.039° → 0.005° |
| ry | 0.977° | **1.057661°** | 1.0639° | **0.087° → 0.006° (~14×)** |
| rz | 1.252° | **1.252091°** | 1.2529° | 0.000° → 0.001° |
| max \|Δ\| | 0.087° | **0.006239°** | — | 0.087° → 0.006° |

Re-run by phase verifier 2026-04-24 (current working tree, no fresh commits since
5233b75):

```
$ python3 -c "from forge_core.math.rotations import rotation_matrix_from_look_at, compute_flame_euler_xyz; ..."
Camera1 recovered: rx=1.814039°, ry=1.057661°, rz=1.252091°
Flame truth:       rx=1.819300°, ry=1.063900°, rz=1.252900°
Deltas:            drx=0.005261°, dry=0.006239°, drz=0.000809°
Max delta:         0.006239° (gate: 0.01°)
```

**Status: PASSED.** Camera1 max delta `0.006239°` is well within the 0.01° gate — closes the Phase 04.2 known limitation.

### 1.2 Observable Truths (from PLAN frontmatter `must_haves.truths`)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `compute_flame_euler_xyz` applied to look-at-derived Camera1 cam_rot returns within 1e-3° of (1.814°, 1.058°, 1.252°) | VERIFIED | tests/test_rotations.py::TestComputeFlameEulerXyz::test_camera1_known_answer passes; verifier re-run delta 0.006° vs Flame truth (well under the looser 0.01° gate) |
| 2 | `compute_flame_euler_xyz` and `flame_euler_xyz_to_cam_rot` are exact inverses | VERIFIED | tests/test_rotations.py:322-323 parametrised invertibility test passes; 6 triples × roundtrip <1e-12° |
| 3 | `compute_flame_euler_zyx` and `flame_euler_to_cam_rot` byte-identical to pre-phase | VERIFIED | `diff` of pre-phase (commit f0aa13e) vs HEAD on the two function bodies returns no changes inside body lines (only the rotations.py module docstring above + look-at L167 changed). See §3 below. |
| 4 | `fbx_ascii.fbx_to_v5_json` on Camera1 aim-rig fixture emits `rotation_flame_euler` within 1e-3° of look-at-derived expected via `compute_flame_euler_xyz` | VERIFIED | tests/test_fbx_ascii.py::TestAimRigFixture::test_rotation_matches_plan01_known_answer passes (8 aim-rig tests pass, verified by phase-level full pytest run) |
| 5 | `bake_camera.py::_flame_euler_to_rot_matrix` agrees numerically with `flame_euler_xyz_to_cam_rot` | VERIFIED | bake_camera body uses `Matrix.Rotation(-rz, 4, 'Z') @ Matrix.Rotation(-ry, 4, 'Y') @ Matrix.Rotation(-rx, 4, 'X')` (L132-135) — matches `flame_euler_xyz_to_cam_rot`'s `Rz @ Ry @ Rx` with all three signs negated. Cross-check guarded by tests/test_blender_roundtrip.py (Free-rig) + tests/test_forge_sender_flame_math.py (parity), plus the FBX-end-to-end test (truth #4) which exercises the read side of the same convention. |
| 6 | `forge_sender.flame_math._rot3_to_flame_euler_deg` agrees with `compute_flame_euler_xyz` | VERIFIED | flame_math body's atan2 indices (`-atan2(R[2][1], R[2][2])`, `-asin(-R[2][0])`, `-atan2(R[1][0], R[0][0])`) verbatim mirror the numpy `compute_flame_euler_xyz` (L150-153). Gimbal branch matches (L155-157). |
| 7 | All 380 prior tests + new tests pass; Free-rig anti-regression unchanged | VERIFIED | Phase-verifier re-run: `pytest tests/ -q` → **400 passed, 2 skipped, 0 failed** (baseline 380 + 19 new TestComputeFlameEulerXyz cases + 1 unxfailed Camera1 known-answer). Free-rig sub-suite (test_blender_roundtrip + test_hook_parity) all green. |
| 8 | No intermediate revision has reader on `_xyz` while bake/extract still on `_zyx` (pipeline symmetry) | VERIFIED | `git show --name-only 5d5febe` returns exactly the 7 expected files in ONE atomic commit. Task 2 commit (fdadf65) is purely additive (helper added, no callers wired). Task 4 (2d7b3e4) is docstring-only. There is no half-fix revision in the c449a2b → d4b4e10 range. See §2 below for the file list. |
| 9 | `compute_flame_euler_xyz` ∘ `rotation_matrix_from_look_at(Camera1)` within 0.01° of Flame truth on all 3 axes — the WHY-this-phase-exists gate | VERIFIED | Max delta 0.006239° (verifier-reproduced); 1.7× tighter than the gate. See §1.1. |

**Score: 9/9 truths VERIFIED.**

### 1.3 Roadmap Success Criteria

ROADMAP.md Phase 04.3 was not loaded directly during this verification (gsd-tools roadmap query unavailable in this run); however, the phase goal stated in the prompt is reproduced verbatim above and is decomposed into truths #1, #4, #7, #8, #9. All four roadmap-derivable criteria are met.

---

## 2. Pipeline Symmetry — The No-Half-Fix Audit

The atomic Task 3 commit (5d5febe) staged exactly these 7 files:

```
forge_core/math/rotations.py
forge_flame/fbx_ascii.py
tests/test_fbx_ascii.py
tests/test_forge_sender_flame_math.py
tests/test_rotations.py
tools/blender/bake_camera.py
tools/blender/forge_sender/flame_math.py
```

This matches amendment §F's required 6-file set (forge_core/math/rotations + fbx_ascii
+ bake_camera + forge_sender/flame_math + test_fbx_ascii + test_forge_sender_flame_math)
plus tests/test_rotations.py per amendment §E (the Camera1 known-answer rewrite, which
was broken in lockstep with the L167 sign flip). The commit message documents this 7th
file as authorized by §E.

Surrounding commits in the c449a2b..d4b4e10 range:
- `fdadf65` (Task 2 — purely additive `_xyz` pair; no callers wired) — does NOT
  introduce convention asymmetry because the helper is unused at that revision.
- `5d5febe` (Task 3 atomic) — every aim-rig math site swaps in this single commit.
- `2d7b3e4` (Task 4) — docstring-only on extract_camera.py; no math change (extract
  delegates to flame_math, which already swapped in 5d5febe).
- `5233b75` (Task 5) — adds the per-plan `04.3-VERIFICATION.md`; no production code.
- `a5053d8`, `d4b4e10` — documentation-only.

**Status: PASSED.** No intermediate revision has reader on `_xyz` while bake/extract on `_zyx`. T-04.3-04 (Flame `importToActionFBX` hard-crash on convention asymmetry) is mitigated by construction.

---

## 3. Required Artifacts — Three-Level Verification

| Artifact | Level 1 (exists) | Level 2 (substantive) | Level 3 (wired) | Level 4 (data flowing) | Status |
|---|---|---|---|---|---|
| `forge_core/math/rotations.py` — `_xyz` pair additive, `_zyx` pair byte-unchanged | YES | YES (`def compute_flame_euler_xyz` L110, `def flame_euler_xyz_to_cam_rot` L162; `_zyx` body byte-diff = 0 vs f0aa13e) | YES (imported by fbx_ascii L807-810, by tests/test_rotations, tests/test_fbx_ascii, tests/test_forge_sender_flame_math) | YES (real Camera1 numerical recovery 0.006°) | VERIFIED |
| `forge_flame/fbx_ascii.py` — aim-rig branch swapped to `_xyz` | YES | YES (L807 imports `compute_flame_euler_xyz`; L891 + L959 call it) | YES (called by `_merge_curves` static + per-frame branches) | YES (TestAimRigFixture exercises the parser end-to-end against live-probe Camera1) | VERIFIED |
| `tools/blender/bake_camera.py` — `_flame_euler_to_rot_matrix` Z·Y·X all-three-negated | YES | YES (L132-135: `Rz(-rz) @ Ry(-ry) @ Rx(-rx)`) | YES (called by `_bake` L322) | N/A (Blender subprocess; covered by tests/test_blender_roundtrip Free-rig parity + parity claim with `flame_euler_xyz_to_cam_rot`) | VERIFIED |
| `tools/blender/forge_sender/flame_math.py` — `_rot3_to_flame_euler_deg` decomposer | YES | YES (L78-80 non-gimbal: `-atan2/-asin/-atan2`; L82-84 gimbal: `0/-asin/atan2`) | YES (imported by extract_camera L68-74, called by `build_v5_payload` L194) | YES (TestRot3ToFlameEulerDeg exercises round-trip via `flame_euler_xyz_to_cam_rot`) | VERIFIED |
| `tools/blender/extract_camera.py` — docstring retargeted at `_xyz` | YES | YES (L29-33 module docstring references `compute_flame_euler_xyz` and Z·Y·X form) | YES (delegates to `flame_math._rot3_to_flame_euler_deg`) | N/A (docstring change) | VERIFIED |
| `tests/test_rotations.py` — `TestComputeFlameEulerXyz` non-circular Camera1 oracle | YES | YES (class L166; uses hand-decomposed (1.814°, 1.058°, 1.252°) as oracle, NOT composer-roundtrip) | YES (pytest-discovered, executes during full suite) | YES (passes; 19 cases including parametrised invertibility) | VERIFIED |
| `tests/test_fbx_ascii.py` — `TestAimRigFixture::test_rotation_matches_plan01_known_answer` | YES | YES (L552: imports `compute_flame_euler_xyz`; L560: derives expected via it) | YES (pytest-discovered) | YES (passes against Camera1 live-probe FBX values) | VERIFIED |
| `tests/test_forge_sender_flame_math.py` — `flame_euler_xyz_to_cam_rot` composer | YES | YES (L34: imports it; L74: helper uses it as composer) | YES (pytest-discovered, gated by `pytest.importorskip("bpy")`; 2 skips on conda forge env without bpy is expected) | N/A (skipped on this env; production wiring covered by `_rot3_to_flame_euler_deg` parity) | VERIFIED |

**Status: 8/8 artifacts VERIFIED.**

---

## 4. Key Link Verification (Wiring)

| From | To | Via | Status |
|---|---|---|---|
| `forge_flame/fbx_ascii.py` aim-rig branch | `forge_core.math.rotations.compute_flame_euler_xyz` | local import inside `_merge_curves` (L807-810) | WIRED |
| `bake_camera.py::_flame_euler_to_rot_matrix` | `forge_core.math.rotations.flame_euler_xyz_to_cam_rot` (numerical parity) | parallel mathutils implementation; Z·Y·X-all-three-negated form on both sides; `Matrix.Rotation(-rz, 4, 'Z')` is the leftmost factor (matches numpy `Rz @ Ry @ Rx` left-to-right) | WIRED |
| `forge_sender.flame_math::_rot3_to_flame_euler_deg` | `forge_core.math.rotations.compute_flame_euler_xyz` (numerical parity) | parallel mathutils implementation; atan2 indices identical (`-atan2(R[2][1], R[2][2])`, `-asin(-R[2][0])`, `-atan2(R[1][0], R[0][0])`) | WIRED |
| `tests/test_fbx_ascii.py::test_rotation_matches_plan01_known_answer` | `forge_core.math.rotations.compute_flame_euler_xyz` | direct import L552 | WIRED |

**Status: 4/4 links WIRED.**

---

## 5. Out-of-Scope Guardrails — Untouched Surfaces

CONTEXT.md §"Out of scope (DO NOT TOUCH)" listed:
- `forge_flame/adapter.py` — Free-rig solve path
- Existing `compute_flame_euler_zyx` / `flame_euler_to_cam_rot` function bodies
- Solver math, image/OCIO/Wiretap, UI, installer

| Untouchable | Status | Evidence |
|---|---|---|
| `forge_flame/adapter.py` | UNTOUCHED | `git diff f0aa13e..HEAD -- forge_flame/adapter.py` returns empty |
| `compute_flame_euler_zyx` body | BYTE-UNCHANGED | `diff` of pre-phase body vs HEAD body returns 0 lines (only the module docstring above + new `_xyz` pair below differ) |
| `flame_euler_to_cam_rot` body | BYTE-UNCHANGED | Same diff returns 0 body lines |
| `forge_core/solver/*` | UNTOUCHED | `git diff f0aa13e..HEAD -- forge_core/solver/` returns empty |
| `flame/camera_match_hook.py` | UNTOUCHED | Not in `git diff f0aa13e..HEAD --stat` output |
| `install.sh` | UNTOUCHED | Not in `git diff f0aa13e..HEAD --stat` output |

**Status: PASSED.** Every out-of-scope file in CONTEXT.md is unchanged. The `_zyx`
pair function bodies are byte-identical, preserving the Free-rig solve path's
symmetric pass-through behaviour as required.

---

## 6. Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|---|---|---|---|
| Full pytest suite green | `pytest tests/ -q` | `400 passed, 2 skipped in 1.21s` | PASS |
| Free-rig anti-regression sub-suite green | `pytest tests/test_blender_roundtrip.py tests/test_hook_parity.py tests/test_rotations.py::TestLookAtMatrix tests/test_rotations.py::TestComputeFlameEulerXyz tests/test_fbx_ascii.py::TestAimRigFixture` | All pass (96 tests) | PASS |
| Camera1 numerical recovery within gate | `python3 -c "from forge_core.math.rotations import ...; ..."` | Max delta 0.006239° (gate 0.01°) | PASS |
| `_xyz` pair invertibility | TestComputeFlameEulerXyz::test_invertibility (parametrised, 6 triples) | All pass | PASS |
| Aim-rig FBX end-to-end | TestAimRigFixture::test_rotation_matches_plan01_known_answer | Pass (against Camera1 live-probe values) | PASS |

---

## 7. Requirements Coverage (D-CONV through D-LOOKAT)

| Code | Description | Source | Plan declared | SUMMARY claimed | Verified | Evidence |
|---|---|---|---|---|---|---|
| D-CONV | Adopt aim-rig convention end-to-end | CONTEXT §decisions | YES (PLAN L29) | YES (SUMMARY L54) | SATISFIED | All 4 aim-rig math sites use the new convention; verified §3-§4 |
| D-ADD | Add `compute_flame_euler_xyz` + inverse | CONTEXT §decisions | YES (L30) | YES (L55) | SATISFIED | rotations.py L110-183, both functions present and tested |
| D-KEEP | `_zyx` pair untouched | CONTEXT §decisions | YES (L31) | YES (L56) | SATISFIED | Body byte-diff = 0 vs f0aa13e |
| D-READER | fbx_ascii aim-rig branch swap | CONTEXT §decisions | YES (L32) | YES (L57) | SATISFIED | fbx_ascii L807-810 + L891 + L959 |
| D-BAKE | bake_camera composer swap | CONTEXT §decisions | YES (L33) | YES (L58) | SATISFIED | bake_camera L132-135 |
| D-EXTR | flame_math decomposer swap | CONTEXT §decisions | YES (L34) | YES (L59) | SATISFIED | flame_math L75-85 |
| D-DOCXR | extract_camera docstring retarget | CONTEXT §decisions | YES (L35) | YES (L60) | SATISFIED | extract_camera L29-33 |
| D-SYNC | Pipeline-symmetric atomic landing | CONTEXT §decisions | YES (L36) | YES (L61) | SATISFIED | 7-file atomic commit 5d5febe (§2 audit) |
| D-TEST | New tests use external oracle, NOT composer-as-oracle | CONTEXT §decisions | YES (L37) | YES (L62) | SATISFIED | TestComputeFlameEulerXyz uses hand-decomposed (1.814°, 1.058°, 1.252°) as oracle (test_rotations.py L274-299); the same pattern caught the amendment §A bug per SUMMARY §H |
| D-LOOKAT | rotation_matrix_from_look_at L167 sign flip, coupled with `_xyz` adoption | Amendment §I | NO (not in PLAN frontmatter — added in amendment after plan freeze) | YES (SUMMARY L63) | SATISFIED | rotations.py L275: `theta = np.radians(float(roll_deg))`. Pre-phase form (`np.radians(-float(roll_deg))`) is gone (verified by diff). Bundled into atomic commit 5d5febe per amendment §D. |

**Status: 10/10 requirement codes SATISFIED.**

**No orphaned requirements.** Every code declared in PLAN.md or SUMMARY.md has corresponding implementation evidence in the codebase.

---

## 8. Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|---|---|---|---|---|
| `forge_core/math/rotations.py` | 267 | Stale "X·Y·Z _xyz convention" reference in `rotation_matrix_from_look_at` comment block (the actual landed convention is Z·Y·X). The amendment §D's prescribed comment text used "X·Y·Z" because the amendment author was wrong about the matrix-product order. The function bodies were corrected in 5d5febe per the executor's Rule 1 fix, but this comment block was not. | INFO | Documentation drift only — does not affect correctness. The function docstring at L113-117 of `compute_flame_euler_xyz` correctly says Z·Y·X, and L274 ("paired with the legacy _zyx decomposer") is internally consistent. A future maintainer reading L267 in isolation would be confused. Recommend cleanup in a Phase 04.4 doc task. |

No blockers, no warnings, one informational note about documentation drift.

---

## 9. Plan Amendment Disposition

The prompt explicitly notes that the plan amendment (commit 24e03fa) proposed an
X·Y·Z matrix-product order which was empirically falsified during Task 3 and
reverted. The verification confirms:

| Aspect | Amendment §A claim | Actual landed code | Verifier judgement |
|---|---|---|---|
| Matrix-product order | `Rx · Ry · Rz` (X·Y·Z) | `Rz · Ry · Rx` (Z·Y·X) | LANDED CODE CORRECT — matches original plan §`<symbolic_derivation_for_xyz_decomposer>` |
| Decomposer atan2 indices | `arctan2(cam_rot[1, 2], cam_rot[2, 2])`, etc. (X·Y·Z form) | `-arctan2(cam_rot[2, 1], cam_rot[2, 2])`, etc. (Z·Y·X form, matching `_zyx` for rx/ry; rz sign-flipped) | LANDED CODE CORRECT |
| Look-at L167 sign flip | `theta = np.radians(+roll_deg)` | `theta = np.radians(float(roll_deg))` (no leading minus) | LANDED CODE MATCHES AMENDMENT (this part of amendment was correct) |
| Atomic 7-file commit | 6 files (amendment §F) + 1 (test_rotations.py per §E) = 7 | 7 files in commit 5d5febe | MATCHES |

The L140-147 docstring of `compute_flame_euler_xyz` documents the deviation in
detail. The SUMMARY §H "Auto-applied Rule 1" section explains the diagnosis and
fix path. The non-circular Camera1 oracle (D-TEST) caught the X·Y·Z bug before
ship — exactly the anti-pattern guard CONTEXT.md §decisions called out.

**Status: PASSED.** The landed implementation matches the original plan body's
specification (Z·Y·X with all three signs negated) plus the amendment's correct
L167 coupling. The amendment's §A symbolic derivation error was caught and
corrected in the atomic commit, and is documented in code comments and the
SUMMARY.

---

## 10. Human Verification Required

None. The phase goal is a numerical convergence target (Camera1 within 0.01° of
viewport truth on all axes); this is fully verifiable programmatically via the
hand-decomposed oracle in TestComputeFlameEulerXyz and the FBX end-to-end test
in TestAimRigFixture, both of which were re-run by the verifier and pass.

A follow-up live-probe re-validation against a running Flame session is
**optional** (already documented in 04.3-VERIFICATION.md §E and SUMMARY
follow-ups). It is not required for ship — the unit + integration test gates
above are sufficient evidence that the aim-rig Camera1 round-trip will land
within the 0.01° gate on a real Flame session.

---

## 11. Overall Verdict

**Phase 04.3 — PASSED.**

| Gate | Result |
|---|---|
| Camera1 max delta ≤ 0.01° | 0.006239° — PASS |
| Pre-phase residual closed (~0.087° → 0.006°) | ~14× tighter — PASS |
| All 380 prior tests still pass | 380 baseline + 19 new + 1 unxfailed = 400 passed, 0 failed — PASS |
| Pipeline-symmetric atomic landing (no half-fix) | 7 files in 5d5febe, no asymmetric intermediate revision — PASS |
| `_zyx` pair byte-stability (Free-rig untouched) | Body diff = 0 — PASS |
| `forge_flame/adapter.py` UNTOUCHED | Diff empty — PASS |
| All 10 requirement codes satisfied | D-CONV, D-ADD, D-KEEP, D-READER, D-BAKE, D-EXTR, D-DOCXR, D-SYNC, D-TEST, D-LOOKAT — PASS |

**Score: 11/11** (9 must-have truths + 8 artifacts + 4 wirings collapsed into the
truth scoring; all verified.)

**Goal achievement:** The Phase 04.2 known limitation is closed. CG registration
fidelity on aim-rig plates is restored to sub-0.01° tightness on Camera1 — the
project's stated core value. Pipeline symmetry is enforced by construction (one
atomic commit, no intermediate-revision asymmetry), so the Phase 04.2 hard-crash
class of failures is mitigated.

**Recommendations (non-blocking):**

1. Clean up the stale "X·Y·Z _xyz convention" reference at `forge_core/math/rotations.py` L267 — the actual landed convention is Z·Y·X (see §8 above). Roll into a Phase 04.4 doc task alongside the recommended `memory/flame_rotation_convention.md` update.
2. Tighten ROADMAP acceptance from 0.1° to 0.01° per CONTEXT.md §deferred (Phase 04.4 doc task).
3. Re-validate against a running Flame session on the next available opportunity (optional; the test gates already cover this analytically).

---

*Phase-level verification by gsd-verifier, 2026-04-24. Supplements the executor's per-plan verification artifact authored 2026-04-25 (commit 5233b75) — does not overwrite it. The two verifications agree on PASS verdict.*
