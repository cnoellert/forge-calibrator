---
phase: 02-blender-addon
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - memory/flame_batch_frame_rate.md
  - .planning/todos/pending/2026-04-21-verify-multi-camera-picker-in-live-uat.md
  - .planning/todos/completed/2026-04-21-verify-multi-camera-picker-in-live-uat.md
  - .planning/STATE.md
autonomous: false
requirements: ["IMP-03", "IMP-04"]
must_haves:
  truths:
    - "Bridge probe confirms the shape of flame.batch.frame_rate before any implementation code is written"
    - "Probe findings are saved to a memory doc so future sessions don't re-probe"
    - "The multi-camera picker from Phase 1 (FOLDED-01) is verified in live UAT against all 4 checks"
    - "If the probe fails (D-19 contingency), the deviation is recorded so Plan 02/03 can adapt before they run"
  artifacts:
    - path: "memory/flame_batch_frame_rate.md"
      provides: "Findings from the D-18 bridge probe — API shape, return type, sample string value, match against _FPS_FROM_FRAME_RATE"
      contains: "flame.batch.frame_rate"
    - path: ".planning/todos/completed/2026-04-21-verify-multi-camera-picker-in-live-uat.md"
      provides: "FOLDED-01 sweep results — 4 checks marked pass/fail with live observations"
      contains: "Perspective absent"
  key_links:
    - from: "memory/flame_batch_frame_rate.md"
      to: "forge_flame/fbx_ascii._FPS_FROM_FRAME_RATE"
      via: "Probed string MUST be one of these keys; otherwise D-19 contingency triggers"
      pattern: "_FPS_FROM_FRAME_RATE"
    - from: "Completed todo"
      to: "flame/camera_match_hook.py:1783-1794 (_pick_camera)"
      via: "Live UAT exercises the picker path that Phase 1 only tested single-camera"
      pattern: "_pick_camera|_find_action_cameras"
---

<objective>
Before any Phase 2 implementation code is written, confirm two live-Flame assumptions that gate the rest of the phase:

1. **D-18 frame-rate probe:** confirm that `flame.batch.frame_rate.get_value()` exists, returns a string, and that string is one of the keys in `forge_flame.fbx_ascii._FPS_FROM_FRAME_RATE`. If it returns a float or a non-matching string, D-19 contingency (stamp `forge_bake_frame_rate` as a Phase 1 supplement) must be triggered BEFORE Plan 02/03 proceed.
2. **FOLDED-01 multi-camera picker sweep:** the Phase 1 `_pick_camera` path (`flame/camera_match_hook.py:1783-1794`) was only exercised against single-camera Actions during UAT. Phase 2's round-trip imports onto whichever camera Phase 1 exported — if the picker silently picks the wrong camera in a multi-camera Action, Phase 2 would land on the wrong Flame target. Run the 4-check live sweep from the pending todo before any addon code ships.

Purpose: de-risk the two assumptions that, if wrong, would cause silent downstream breakage. Both are mandatory pre-work per CONTEXT §D-18 and §FOLDED-01.
Output: `memory/flame_batch_frame_rate.md` with probe findings; the FOLDED-01 pending todo moved to completed with sweep results; STATE.md updated to note Wave 1 complete.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-blender-addon/02-CONTEXT.md
@.planning/phases/02-blender-addon/02-PATTERNS.md
@.planning/phases/02-blender-addon/02-UI-SPEC.md
@.planning/todos/pending/2026-04-21-verify-multi-camera-picker-in-live-uat.md
@$HOME/.claude/projects/-Users-cnoellert-Documents-GitHub-forge-calibrator/memory/flame_bridge.md
@$HOME/.claude/projects/-Users-cnoellert-Documents-GitHub-forge-calibrator/memory/flame_bridge_probing.md

<interfaces>
<!-- Frame-rate key set the probed string must match (D-18 acceptance bar) -->

From forge_flame/fbx_ascii.py:57-69 — _FPS_FROM_FRAME_RATE keys (authoritative):
```python
# Keys are the exact strings Flame emits for frame_rate. The probe's
# return value MUST match one of these to pass; otherwise D-19 triggers.
"23.976 fps"   # -> 23.976
"24 fps"       # -> 24.0
"25 fps"       # -> 25.0
"29.97 fps df" # -> 29.97
"29.97 fps nd" # -> 29.97
"30 fps"       # -> 30.0
"50 fps"       # -> 50.0
"59.94 fps df" # -> 59.94
"59.94 fps nd" # -> 59.94
"60 fps"       # -> 60.0
```

Bridge payload contract (memory/flame_bridge_probing.md 2026-04-19 update):
```bash
curl -sS -X POST http://127.0.0.1:9999/exec \
  -H "Content-Type: application/json" \
  -d '{"code": "<python>"}'
# Response: {"result": ..., "stdout": ..., "stderr": ..., "error": ..., "traceback": ...}
```
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Ping-only bridge liveness check</name>
  <read_first>
    - $HOME/.claude/projects/-Users-cnoellert-Documents-GitHub-forge-calibrator/memory/flame_bridge_probing.md (one-probe-per-request discipline, ping-first rule)
    - $HOME/.claude/projects/-Users-cnoellert-Documents-GitHub-forge-calibrator/memory/flame_bridge.md (payload contract)
  </read_first>
  <action>This is a human-verification checkpoint. The concrete steps are in the &lt;how-to-verify&gt; block below; this tag is included for planner-tooling schema compliance.</action>
  <what-built>Ping bridge to confirm it is reachable before running any real probe. Per memory/flame_bridge_probing.md: "First call in any probe chain: ping-only (print(2+2))." Nothing is changed on the Flame side — this is a pure liveness check.</what-built>
  <how-to-verify>
    1. Confirm Flame 2026.2.1 is running with the Camera Match hook loaded
    2. Run this exact command from any shell:
       ```bash
       curl -sS -X POST http://127.0.0.1:9999/exec \
         -H "Content-Type: application/json" \
         -d '{"code": "print(2+2)"}'
       ```
    3. Expected response: JSON envelope with `"stdout": "4\n"` and `"error": null` (or missing/empty)
    4. If the request hangs, returns a connection error, or `error` is non-empty: STOP. Report the failure — do not proceed to Task 2. Recovery is outside this plan's scope.
  </how-to-verify>
  <verify>
    <automated>MISSING — blocking checkpoint; see &lt;how-to-verify&gt; above. Validation is human-verify only; executor pauses for the resume-signal.</automated>
  </verify>
  <resume-signal>Type "bridge reachable" to proceed, or paste the error envelope to abort Wave 1.</resume-signal>
  <acceptance_criteria>
    - The curl command returns HTTP 200
    - The response JSON contains `stdout` with the value `"4\n"` (or equivalent, includes the digit 4)
    - The response JSON's `error` field is null, missing, or empty string
    - No Flame restart is required to reach this state
  </acceptance_criteria>
  <done>Bridge confirmed live; safe to probe the unknown frame_rate API surface on the next task.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: D-18 probe — flame.batch.frame_rate shape</name>
  <read_first>
    - $HOME/.claude/projects/-Users-cnoellert-Documents-GitHub-forge-calibrator/memory/flame_bridge_probing.md (one probe per request; save findings IMMEDIATELY if a probe crashes)
    - forge_flame/fbx_ascii.py (lines 57-69 — _FPS_FROM_FRAME_RATE key set the probed value must match)
    - .planning/phases/02-blender-addon/02-CONTEXT.md (D-18 acceptance bar, D-19 contingency)
  </read_first>
  <action>This is a human-verification checkpoint. The concrete steps are in the &lt;how-to-verify&gt; block below; this tag is included for planner-tooling schema compliance.</action>
  <what-built>One isolated bridge call that reads `flame.batch.frame_rate.get_value()` and prints its `repr()`. Findings are written to `memory/flame_batch_frame_rate.md` BEFORE asking the user for any restart, per the probing discipline ("save what you learned to memory IMMEDIATELY").</what-built>
  <how-to-verify>
    1. Run this exact probe (ONE call, no bundling):
       ```bash
       curl -sS -X POST http://127.0.0.1:9999/exec \
         -H "Content-Type: application/json" \
         -d '{"code": "import flame\nfr = flame.batch.frame_rate.get_value()\nprint(repr(fr))\nprint(type(fr).__name__)"}'
       ```
    2. Capture the response verbatim. The `stdout` field should have two lines: first `repr()` of the value, second the type name.
    3. Write findings to `memory/flame_batch_frame_rate.md` with this structure:
       ```markdown
       ---
       name: Flame batch frame_rate API shape (D-18 probe)
       description: Confirmed shape of flame.batch.frame_rate for Phase 2 addon round-trip
       type: reference
       ---
       **Probed:** 2026-04-21 (or today's date)
       **Call:** `flame.batch.frame_rate.get_value()`
       **Return type:** <type name from probe>
       **Sample value:** <repr from probe>
       **Matches _FPS_FROM_FRAME_RATE keys?** <yes / no — list which key>

       **Decision trail:**
       - If match: Plan 02 transport template uses `frame_rate=flame.batch.frame_rate.get_value()` verbatim (D-17 path).
       - If mismatch: D-19 contingency — Plan 02 stamps `forge_bake_frame_rate` in Phase 1 supplement.
       ```
    4. If the probe crashed Flame: save what you observed to `memory/flame_batch_frame_rate.md` as a `PROBE CRASHED` note BEFORE the user restarts; then stop and report. Do NOT re-probe without explicit user approval.
    5. If the return type is a string matching one of the `_FPS_FROM_FRAME_RATE` keys: D-17 is confirmed; note in the memory doc and proceed to Task 3.
    6. If the return type is NOT a string, OR is a string but not a matching key: D-19 contingency triggers. Note in the memory doc, flag the next wave plans, and STOP — ask the user whether to (a) abort Phase 2, (b) run a quick-task supplement to Phase 1 that stamps `forge_bake_frame_rate` on the Blender camera at bake time, or (c) hand-wire a hard-coded frame rate for first-pass validation.
  </how-to-verify>
  <verify>
    <automated>MISSING — blocking checkpoint; see &lt;how-to-verify&gt; above. Validation is human-verify only; executor pauses for the resume-signal.</automated>
  </verify>
  <resume-signal>Paste the probe's stdout/error output and the path you wrote to `memory/flame_batch_frame_rate.md`. If D-19 contingency triggers, state which option the user chose.</resume-signal>
  <acceptance_criteria>
    - File `memory/flame_batch_frame_rate.md` exists and is non-empty
    - `memory/flame_batch_frame_rate.md` contains the literal string "flame.batch.frame_rate"
    - `memory/flame_batch_frame_rate.md` contains the literal string "Return type:"
    - `memory/flame_batch_frame_rate.md` contains the literal string "Matches _FPS_FROM_FRAME_RATE keys?"
    - The probe is a single isolated bridge call — grep/read confirms the payload `code` field has at most one `flame.batch.frame_rate` reference and no unrelated introspection bundled in
    - If D-17 path confirmed: the document explicitly states "D-17 confirmed" or equivalent; downstream plans reference frame rate via `flame.batch.frame_rate.get_value()`
    - If D-19 triggered: the document explicitly states "D-19 contingency triggered" with the chosen recovery option
  </acceptance_criteria>
  <done>Frame-rate API shape is known and recorded; Plan 02 and Plan 03 have the information they need to encode the bridge template correctly.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: FOLDED-01 multi-camera picker 4-check sweep</name>
  <read_first>
    - .planning/todos/pending/2026-04-21-verify-multi-camera-picker-in-live-uat.md (the 4 checks, verbatim)
    - flame/camera_match_hook.py (lines 1695-1723, 1783-1794 — `_find_action_cameras` and `_pick_camera`)
    - $HOME/.claude/projects/-Users-cnoellert-Documents-GitHub-forge-calibrator/memory/flame_perspective_camera.md (exclusion rule)
  </read_first>
  <action>This is a human-verification checkpoint. The concrete steps are in the &lt;how-to-verify&gt; block below; this tag is included for planner-tooling schema compliance.</action>
  <what-built>Live UAT of the Phase 1 multi-camera picker path that was never exercised in Phase 1's single-camera UAT. Not new code — verification only. The 4 checks from the pending todo, run against a real Flame batch containing an Action with 3+ cameras (Perspective + at least two solver-output cameras).</what-built>
  <how-to-verify>
    1. In Flame, build or identify a Batch with an Action holding at least 3 cameras: built-in Perspective + two solver-output cameras (e.g. from two separate `Open Camera Match` runs).
    2. Right-click the Action → Camera Match → Export Camera to Blender.
    3. When the picker dialog appears, confirm check-by-check:
       - **Check 1:** Perspective is NOT in the dropdown list (visible Python filter at `_find_action_cameras` line 1720).
       - **Check 2:** The remaining cameras appear with labels of the form `{action_name} > {cam_name}`. Note the order and confirm it is deterministic across two runs (take a screenshot or write it down, close the dialog without picking, reopen — order must match).
       - **Check 3:** Hit Cancel on the dialog. Confirm: no error dialog appears, no temp dir is left behind under `/tmp/forge_bake_*` (run `ls /tmp/forge_bake_* 2>/dev/null` — must be empty or only unrelated).
       - **Check 4:** Reopen the dialog. Pick one camera explicitly. Let the bake complete. Open the resulting `.blend` in Blender (or use `python -c "import bpy; bpy.ops.wm.open_mainfile(filepath='...blend'); print(bpy.data.objects['<name>'].data['forge_bake_camera_name'])"`). The `forge_bake_camera_name` custom property MUST equal the camera you selected in the picker — not either of the others.
    4. Move the todo file from `.planning/todos/pending/` to `.planning/todos/completed/` and append a results section:
       ```markdown
       ## Results (YYYY-MM-DD)
       - Check 1 (Perspective absent): PASS / FAIL (notes)
       - Check 2 (deterministic order): PASS / FAIL — order observed: [list]
       - Check 3 (cancel clean): PASS / FAIL
       - Check 4 (stamped name matches pick): PASS / FAIL — picked <name>, stamped <name>
       - Overall: PASS / NEEDS QUICK-TASK
       ```
    5. If any check fails, STOP — promote to a quick-task per the todo's "If any of those fail, promote to a real quick task" clause. Do not proceed to Plan 02 until the picker is fixed or the planner is reconvened.
  </how-to-verify>
  <verify>
    <automated>MISSING — blocking checkpoint; see &lt;how-to-verify&gt; above. Validation is human-verify only; executor pauses for the resume-signal.</automated>
  </verify>
  <resume-signal>Paste the 4-check results table and confirm the todo has been moved to `.planning/todos/completed/`.</resume-signal>
  <acceptance_criteria>
    - File `.planning/todos/pending/2026-04-21-verify-multi-camera-picker-in-live-uat.md` NO LONGER exists
    - File `.planning/todos/completed/2026-04-21-verify-multi-camera-picker-in-live-uat.md` EXISTS
    - The completed file contains the literal string "## Results"
    - The completed file contains one PASS/FAIL line for each of: Perspective absent, deterministic order, cancel clean, stamped name matches pick
    - No check is marked FAIL without a follow-up quick-task linked or Phase 2 explicitly paused
  </acceptance_criteria>
  <done>FOLDED-01 closed; the picker path is verified against the 4 checks; Phase 2 implementation is unblocked on this front.</done>
</task>

<task type="auto">
  <name>Task 4: Update STATE.md with Wave 1 findings</name>
  <read_first>
    - .planning/STATE.md (current state — keep structure intact; only add/update the relevant sections)
    - memory/flame_batch_frame_rate.md (probe findings from Task 2)
    - .planning/todos/completed/2026-04-21-verify-multi-camera-picker-in-live-uat.md (sweep results from Task 3)
  </read_first>
  <action>Update `.planning/STATE.md` in place to record Wave 1 completion. Specifically:

1. Set the top-level `stopped_at:` frontmatter value to `"Phase 2 Wave 1 complete — probes done"` (or similar if session continues).
2. Set `last_updated:` to the current ISO-8601 timestamp.
3. In the `## Accumulated Context → ### Decisions` list, add one bullet capturing the D-18 probe outcome. Two possible forms:
   - If D-17 path confirmed: `- D-17 confirmed: flame.batch.frame_rate.get_value() returns a string matching _FPS_FROM_FRAME_RATE (probed 2026-04-21; see memory/flame_batch_frame_rate.md)`
   - If D-19 contingency: `- D-19 triggered: flame.batch.frame_rate shape unexpected (<describe>); recovery plan: <option a/b/c the user chose>; see memory/flame_batch_frame_rate.md`
4. In `### Pending Todos`, REMOVE the `Verify multi-camera picker in live UAT` line (it is now completed). If the list becomes empty, leave it as an empty list header (do not remove the heading).
5. If any Wave 1 check failed (D-19 triggered or a picker-sweep check FAILed), add a bullet to `### Blockers/Concerns` naming the blocker and its impact on subsequent plans.
6. Do NOT alter `progress`, `milestone`, or any other section not listed above.

Keep the file valid YAML frontmatter + Markdown. Preserve comment and section order exactly.</action>
  <verify>
    <automated>grep -q "D-17 confirmed\|D-19 triggered" .planning/STATE.md &amp;&amp; grep -qv "Verify multi-camera picker in live UAT" .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/STATE.md` contains exactly one of the literals `D-17 confirmed` or `D-19 triggered`
    - `.planning/STATE.md` does NOT contain the literal `Verify multi-camera picker in live UAT` (pending reference removed)
    - `.planning/STATE.md` `last_updated` frontmatter is set to today's date
    - `.planning/STATE.md` YAML frontmatter is still parseable (opening `---` and closing `---` intact)
  </acceptance_criteria>
  <done>STATE.md reflects Wave 1 outcomes; downstream waves have the signal they need to choose the frame-rate path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| probe shell → forge-bridge (`127.0.0.1:9999/exec`) | Probe shell (operator's terminal) crosses into Flame's Python runtime. Bridge is bound to 127.0.0.1 (confirmed in PROJECT.md); not reachable over network. Trust level: same-user. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-01 | Tampering / Elevation | Bridge `/exec` endpoint accepts arbitrary Python | accept | Bridge binds `127.0.0.1` only (same-user workstation per PROJECT.md Constraints). This plan only reads values (`get_value()`, `repr()`) — no writes, no `set_value`, no state mutation. One probe per request per `memory/flame_bridge_probing.md`. |
| T-02-01-02 | Denial of Service | Probe hangs Flame / crashes session | mitigate | Ping-only first (Task 1); save findings to memory IMMEDIATELY after each probe (Task 2) so progress is not lost if a later probe crashes Flame. No `help()` calls (crash history per memory/flame_bridge_probing.md). |
| T-02-01-03 | Information Disclosure | Probe output written to memory doc | accept | `memory/flame_batch_frame_rate.md` lives in the user's `.claude/projects/.../memory/` tree — already the repository for local API findings. No PII, no secrets in frame-rate strings. |

**No new attack surface is opened by this plan — it only runs read-only probes and updates planning metadata.**
</threat_model>

<verification>
- Run: `ls memory/flame_batch_frame_rate.md` — file exists
- Run: `ls .planning/todos/pending/2026-04-21-verify-multi-camera-picker-in-live-uat.md` — must NOT exist (moved to completed)
- Run: `ls .planning/todos/completed/2026-04-21-verify-multi-camera-picker-in-live-uat.md` — must exist
- Run: `grep -c "flame.batch.frame_rate" memory/flame_batch_frame_rate.md` — returns ≥1
- Run: `grep "D-17 confirmed\|D-19 triggered" .planning/STATE.md` — returns exactly one line
</verification>

<success_criteria>
- D-18 probe run and findings saved to `memory/flame_batch_frame_rate.md`
- FOLDED-01 todo moved to `.planning/todos/completed/` with 4-check results appended
- STATE.md updated with the Wave 1 outcome signal (D-17 confirmed or D-19 contingency)
- If D-19 triggered, the user has chosen and approved a recovery option before Plan 02 starts
- Plans 02, 03, 04 can now encode the frame-rate path correctly without guessing
</success_criteria>

<output>
After completion, create `.planning/phases/02-blender-addon/02-01-SUMMARY.md` documenting:
- Exact probe command run and its stdout
- Which path (D-17 or D-19) is confirmed
- The 4-check sweep results in a compact table
- Any follow-up quick-tasks spawned (with file paths)
</output>
