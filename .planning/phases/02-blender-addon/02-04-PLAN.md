---
phase: 02-blender-addon
plan: 04
type: execute
wave: 4
depends_on: ["02-03"]
files_modified:
  - tools/blender/forge_sender-v1.0.0.zip
  - .planning/phases/02-blender-addon/02-04-SUMMARY.md
autonomous: false
requirements: ["IMP-01", "IMP-02", "IMP-03", "IMP-04", "IMP-05", "IMP-06"]
must_haves:
  truths:
    - "An installable addon zip tools/blender/forge_sender-v1.0.0.zip exists, built by the CONTEXT D-12 shell command (cd tools/blender && zip -r forge_sender-v1.0.0.zip forge_sender/), NOT by install.sh"
    - "The addon installs via Blender Preferences → Add-ons → Install from file with no errors; the 'Forge' tab appears in the N-panel (IMP-01)"
    - "Happy-path E2E round-trip succeeds: bake a test camera via Phase 1 → edit in Blender → click Send to Flame → new camera with keyframes appears in the target Flame Action (IMP-06)"
    - "Failure path Tier 1: sending from a camera without forge metadata shows the preflight popup naming the first missing key verbatim (IMP-02)"
    - "Failure path Tier 2: sending while forge-bridge is unreachable shows the transport popup with the http://127.0.0.1:9999 URL"
    - "Failure path Tier 3: sending to a deleted/renamed target Action shows the remote-tier popup containing the bridge's error string + traceback"
  artifacts:
    - path: "tools/blender/forge_sender-v1.0.0.zip"
      provides: "Installable Blender addon zip — artists drop into Blender Preferences"
      contains: "forge_sender/__init__.py"
    - path: ".planning/phases/02-blender-addon/02-04-SUMMARY.md"
      provides: "E2E validation record: happy path + three failure paths verified, with observed Flame-side camera name, created-node count, and any discretion calls"
      contains: "IMP-06"
  key_links:
    - from: "tools/blender/forge_sender-v1.0.0.zip"
      to: "Blender 4.5 Preferences → Add-ons → Install from file"
      via: "Standard Blender addon install flow (IMP-01 literal requirement)"
      pattern: "bl_info|Install from file"
    - from: "Blender addon (live) Send to Flame button"
      to: "Flame Action — new camera created under the stamped forge_bake_action_name"
      via: "forge-bridge /exec → v5_json_str_to_fbx → import_fbx_to_action"
      pattern: "v5_json_str_to_fbx.*import_fbx_to_action"
---

<objective>
Close Phase 2 by building the installable addon zip (CONTEXT D-12) and validating the full round-trip live on Flame 2026.2.1 + Blender 4.5+. Every Success Criterion from ROADMAP Phase 2 is verified with a human-in-the-loop check. The three failure tiers are exercised deliberately so the error taxonomy (D-09) is proven, not assumed.

Scope boundaries:
- Install.sh MUST NOT be touched (D-12).
- `flame/camera_match_hook.py` MUST NOT be touched (Phase 1 closed).
- `tools/blender/bake_camera.py` MUST NOT be touched (stamped metadata contract sufficient).
- DOC-01 E2E smoke test and DOC-02 user-facing docs belong to Phase 4 — this plan validates the round-trip works, not that it is documented for artists.

Authority: the round-trip behavior is locked by Plans 01-03; this plan's job is to verify it runs and surfaces errors correctly in the live environment.

Purpose: prove the addon is installable, functional on the happy path, and fails loudly on the three designed failure tiers.
Output: a committed zip artifact + a SUMMARY.md recording the validation matrix.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-blender-addon/02-CONTEXT.md
@.planning/phases/02-blender-addon/02-UI-SPEC.md
@.planning/phases/02-blender-addon/02-PATTERNS.md
@.planning/phases/02-01-SUMMARY.md
@.planning/phases/02-02-SUMMARY.md
@.planning/phases/02-03-SUMMARY.md
@memory/flame_batch_frame_rate.md
@$HOME/.claude/projects/-Users-cnoellert-Documents-GitHub-forge-calibrator/memory/flame_bridge.md
@$HOME/.claude/projects/-Users-cnoellert-Documents-GitHub-forge-calibrator/memory/flame_bridge_probing.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the installable addon zip</name>
  <files>tools/blender/forge_sender-v1.0.0.zip</files>
  <read_first>
    - .planning/phases/02-blender-addon/02-CONTEXT.md (D-12 — EXACT shell command; D-11 — the four files that must be inside the zip)
    - tools/blender/forge_sender/__init__.py (Plan 03 output — confirm bl_info version matches the zip filename)
  </read_first>
  <action>Build the zip via the CONTEXT D-12 locked shell command. The zip MUST NOT include `__pycache__`, MUST NOT include tests, and MUST NOT include stray files from the forge_sender directory.

1. Remove any stale bytecode:
   ```bash
   rm -rf tools/blender/forge_sender/__pycache__
   ```

2. Sanity-check the zip contents list BEFORE zipping:
   ```bash
   find tools/blender/forge_sender -type f
   ```
   Expect exactly these four files (from Plans 02 and 03):
   - `tools/blender/forge_sender/__init__.py`
   - `tools/blender/forge_sender/flame_math.py`
   - `tools/blender/forge_sender/preflight.py`
   - `tools/blender/forge_sender/transport.py`
   If any extra file is present (e.g., `operator.py`, `panel.py`) because the executor split the classes, include them too — D-11 allows the split. Reject only `__pycache__`, `.DS_Store`, or editor swap files.

3. Build the zip using the EXACT D-12 command:
   ```bash
   cd tools/blender && zip -r forge_sender-v1.0.0.zip forge_sender/ -x '*__pycache__*' -x '*.DS_Store' -x '*.pyc'
   ```
   (The CONTEXT command is `cd tools/blender && zip -r forge_sender-v1.0.0.zip forge_sender/` — the `-x` excludes are defensive additions, not a scope change.)

4. Verify the zip:
   ```bash
   unzip -l tools/blender/forge_sender-v1.0.0.zip
   ```
   Expect: every line starts with `forge_sender/` (proper top-level directory for Blender's "Install from file"), no `__pycache__` entries, no `.pyc` entries.

5. Do NOT touch `install.sh`. Do NOT add the zip to any deploy script. D-12 is explicit: artists install manually via Blender Preferences; `install.sh` owns Flame-side `/opt/Autodesk/shared/python/` and nothing else.</action>
  <verify>
    <automated>test -f tools/blender/forge_sender-v1.0.0.zip &amp;&amp; unzip -l tools/blender/forge_sender-v1.0.0.zip | grep -q 'forge_sender/__init__.py' &amp;&amp; ! unzip -l tools/blender/forge_sender-v1.0.0.zip | grep -q '__pycache__'</automated>
  </verify>
  <acceptance_criteria>
    - File `tools/blender/forge_sender-v1.0.0.zip` exists
    - `unzip -l tools/blender/forge_sender-v1.0.0.zip` shows an entry for `forge_sender/__init__.py`
    - `unzip -l tools/blender/forge_sender-v1.0.0.zip` shows an entry for `forge_sender/flame_math.py`
    - `unzip -l tools/blender/forge_sender-v1.0.0.zip` shows an entry for `forge_sender/preflight.py`
    - `unzip -l tools/blender/forge_sender-v1.0.0.zip` shows an entry for `forge_sender/transport.py`
    - `unzip -l tools/blender/forge_sender-v1.0.0.zip` shows NO `__pycache__` entries
    - `unzip -l tools/blender/forge_sender-v1.0.0.zip` shows NO `.pyc` entries
    - Every zip entry path starts with `forge_sender/` (top-level directory)
    - `install.sh` is unchanged: `git diff install.sh` produces no output
  </acceptance_criteria>
  <done>Installable zip exists; Task 2 can install it in Blender.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Install the addon in Blender and verify the Forge panel appears</name>
  <read_first>
    - tools/blender/forge_sender-v1.0.0.zip (Task 1 output)
    - .planning/phases/02-blender-addon/02-UI-SPEC.md (§Panel Layout Contract — Row order, tab name, icon choices)
  </read_first>
  <action>This is a human-verification checkpoint. The concrete steps are in the &lt;how-to-verify&gt; block below; this tag is included for planner-tooling schema compliance.</action>
  <what-built>The Task 1 zip is now installed through Blender's standard addon flow. This is the IMP-01 acceptance check: "A 'Send to Flame' button is visible in the N-panel (3D viewport sidebar) after installing the addon via Blender's standard addon install flow."</what-built>
  <how-to-verify>
    1. Launch Blender 4.5+. Open an empty scene (File → New → General).
    2. In Blender: Edit → Preferences → Add-ons → Install... (button at top-right).
    3. Navigate to `tools/blender/forge_sender-v1.0.0.zip` (from the checkout root). Select the file.
    4. After Blender loads it, the addon "Forge: Send Camera to Flame" should appear in the addon list. Enable its checkbox.
    5. Close Preferences.
    6. In the 3D Viewport, press `N` to toggle the sidebar if it is hidden.
    7. Confirm a new tab labeled **Forge** is visible in the sidebar (tab location per UI-SPEC §Panel identity, D-14).
    8. Click the Forge tab. The `Send to Flame` panel should be visible. With an empty scene (no active object), the panel shows the disabled-state row:
       - Warning row: `Not a Flame-baked camera` with an ERROR icon (red triangle)
       - Separator
       - Disabled `Send to Flame` button with an EXPORT icon
    9. If any of these fail: STOP. Common causes: (a) `bl_info.blender` version mismatch → Blender refuses to enable; (b) syntax error in `__init__.py` → Blender pops a Python error; (c) requests import failure → rare on Blender 4.5; D-13 notes the fallback to `urllib` is deferred.
  </how-to-verify>
  <verify>
    <automated>MISSING — blocking checkpoint; see &lt;how-to-verify&gt; above. Validation is human-verify only; executor pauses for the resume-signal.</automated>
  </verify>
  <resume-signal>Type "installed" and confirm what the Forge tab looks like, OR paste the Blender error text to abort.</resume-signal>
  <acceptance_criteria>
    - Blender's Add-ons list shows the addon with enable checkbox ticked
    - Sidebar (N-panel) shows a `Forge` tab
    - Clicking the `Forge` tab shows the `Send to Flame` panel
    - With no active camera, the panel shows the disabled-state row (red warning + disabled Send button), NOT a crash or empty panel
  </acceptance_criteria>
  <done>IMP-01 verified live. Panel is drawn in the sidebar; addon install flow works.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Failure Tier 1 — preflight popup names missing key verbatim</name>
  <read_first>
    - .planning/phases/02-blender-addon/02-UI-SPEC.md (§Preflight Tier 1 copy strings)
    - tools/blender/forge_sender/preflight.py (Plan 03 output)
  </read_first>
  <action>This is a human-verification checkpoint. The concrete steps are in the &lt;how-to-verify&gt; block below; this tag is included for planner-tooling schema compliance.</action>
  <what-built>Exercise each of the four Tier 1 paths live. This is the IMP-02 acceptance check: "Clicking 'Send to Flame' with a camera that lacks forge metadata shows an error popup naming the missing property rather than crashing or silently failing."</what-built>
  <how-to-verify>
    All four sub-checks are done inside the Blender session from Task 2.

    **Tier 1(a) — no active object:**
    1. In the 3D viewport, click in empty space to deselect everything. Confirm `Active Object` is None (check the status bar or Outliner).
    2. Note: the Send button in the panel is disabled, so you cannot click it from the panel. To exercise the keyboard-bypass path, press `F3`, type `Send to Flame`, and press Enter.
    3. Expected: operator reports `{'ERROR'}` and a popup appears with the message:
       `Send to Flame: no active object — select a forge-baked camera in the 3D viewport and try again`

    **Tier 1(b) — active object is not a camera:**
    1. Add a Cube (Shift+A → Mesh → Cube). Leave it selected.
    2. Press `F3` → `Send to Flame` → Enter.
    3. Expected popup: `Send to Flame: active object is not a camera — select a forge-baked camera in the 3D viewport and try again`

    **Tier 1(c) — missing stamped key:**
    1. Delete the cube. Add a Camera (Shift+A → Camera). Leave it selected (it becomes the active object).
    2. The panel shows disabled state (no `forge_bake_*` keys stamped on this camera). Press `F3` → `Send to Flame` → Enter.
    3. Expected popup: `Send to Flame: active camera is missing 'forge_bake_action_name' — this camera was not baked by forge-calibrator. Re-export from Flame via right-click → Camera Match → Export Camera to Blender`
    4. Note the quoted key MUST be `'forge_bake_action_name'` (first in `_REQUIRED_STAMPED_KEYS`).

    **Tier 1(d) — bad provenance:**
    1. In the Properties editor, select the camera's data-block. Open the Custom Properties panel.
    2. Add two custom props manually:
       - `forge_bake_action_name` = `"FakeAction"` (string)
       - `forge_bake_camera_name` = `"FakeCam"` (string)
    3. DO NOT add `forge_bake_source`.
    4. Press `F3` → `Send to Flame` → Enter.
    5. Expected popup: `Send to Flame: active camera was not baked by forge-calibrator (forge_bake_source != 'flame') — re-export from Flame via right-click → Camera Match → Export Camera to Blender`

    Capture screenshots or transcribe the popup text for the SUMMARY in Task 7.
  </how-to-verify>
  <verify>
    <automated>MISSING — blocking checkpoint; see &lt;how-to-verify&gt; above. Validation is human-verify only; executor pauses for the resume-signal.</automated>
  </verify>
  <resume-signal>Type "tier 1 verified" and confirm all four popups matched the UI-SPEC copy, OR paste any copy mismatches.</resume-signal>
  <acceptance_criteria>
    - All four sub-tiers produced popups with the literal UI-SPEC copy strings (no paraphrasing)
    - Tier 1(c) popup named `'forge_bake_action_name'` specifically (not camera_name, not both)
    - No Blender crash, no silent failure, no console-only error
    - The operator returned `{'CANCELLED'}` (no FINISHED on any failure path — Blender's toast at screen-bottom confirms)
  </acceptance_criteria>
  <done>IMP-02 verified live. Preflight Tier 1 error taxonomy is complete and literal.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Failure Tier 2 — transport popup when forge-bridge is unreachable</name>
  <read_first>
    - .planning/phases/02-blender-addon/02-UI-SPEC.md (§Transport Tier — exact copy with the literal URL)
    - tools/blender/forge_sender/transport.py (Plan 03 output — ConnectionError / Timeout handling)
  </read_first>
  <action>This is a human-verification checkpoint. The concrete steps are in the &lt;how-to-verify&gt; block below; this tag is included for planner-tooling schema compliance.</action>
  <what-built>Exercise the Transport Tier path. The addon must fail loud when the bridge is down — no silent retry, no indefinite hang. This is part of IMP-05 (structured response surfaced in a popup) and D-09 Tier 2.</what-built>
  <how-to-verify>
    1. Ensure Flame is either NOT running, OR the Camera Match hook is NOT loaded (so forge-bridge is not listening on 127.0.0.1:9999). Verify:
       ```bash
       curl -sS --max-time 2 http://127.0.0.1:9999/exec -d '{"code":""}'
       ```
       Expect: connection refused or timeout. If it succeeds, Flame is running — close Flame or kill the bridge for this test.
    2. In Blender, from Task 2, create a fake valid camera: add a Camera, then add three custom properties on its data-block:
       - `forge_bake_action_name` = `"TestAction"` (string)
       - `forge_bake_camera_name` = `"TestCamera"` (string)
       - `forge_bake_source` = `"flame"` (string)
    3. The panel should now show the happy-path rows: `Target Action: TestAction` / `Target Camera: TestCamera` / Send button ENABLED. Confirm this visually before proceeding.
    4. Click `Send to Flame` on the panel.
    5. Within 5 seconds, a popup should appear with the exact text:
       `Send to Flame: forge-bridge not reachable at http://127.0.0.1:9999 — is Flame running with the Camera Match hook loaded?`
    6. Blender's status bar should also show the same error as a toast.
  </how-to-verify>
  <verify>
    <automated>MISSING — blocking checkpoint; see &lt;how-to-verify&gt; above. Validation is human-verify only; executor pauses for the resume-signal.</automated>
  </verify>
  <resume-signal>Type "tier 2 verified" and confirm the popup matched, OR paste any discrepancy.</resume-signal>
  <acceptance_criteria>
    - Popup appears within the 5-second default timeout window (D-16)
    - Popup text is the UI-SPEC Transport Tier literal — contains `http://127.0.0.1:9999` verbatim
    - Blender does not hang, does not crash, returns to idle state after dismissing the popup
    - Operator returned `{'CANCELLED'}`
  </acceptance_criteria>
  <done>IMP-05 Transport Tier verified. Bridge-down failure is loud and the artist has a copy-pasteable URL for `curl` debugging.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Happy path — full round-trip bake → edit → send → verify in Flame</name>
  <read_first>
    - .planning/phases/01-export-polish/01-CONTEXT.md (Phase 1 export flow — right-click Action → Camera Match → Export Camera to Blender)
    - .planning/phases/02-blender-addon/02-UI-SPEC.md (§Success popup — exact copy)
    - memory/flame_batch_frame_rate.md (Plan 01 probe outcome — if D-19 was triggered, the round-trip may require the Phase 1 supplement before this task can pass)
  </read_first>
  <action>This is a human-verification checkpoint. The concrete steps are in the &lt;how-to-verify&gt; block below; this tag is included for planner-tooling schema compliance.</action>
  <what-built>The complete Phase 2 round-trip, exercised for real. This is the IMP-03, IMP-04, IMP-05, and IMP-06 joint acceptance check. If this passes, the phase is functionally done.</what-built>
  <how-to-verify>
    1. Start Flame 2026.2.1 and confirm the Camera Match hook loads and the forge-bridge is listening:
       ```bash
       curl -sS --max-time 5 -X POST http://127.0.0.1:9999/exec \
         -H "Content-Type: application/json" \
         -d '{"code":"print(\"ok\")"}'
       ```
       Expect `{"stdout":"ok\n","error":null,...}` or equivalent.

    2. In Flame, open an existing Batch (or create one) that has a plate clip. Open Camera Match, solve a VP camera for the plate, apply it to a new Action. Name the Action something specific (e.g. `UAT_phase2`) so you can spot it from Blender. Confirm the Action has exactly one non-Perspective camera.

    3. Right-click the Action → Camera Match → Export Camera to Blender. Let the Phase 1 export complete. Confirm:
       - Blender opens on the resulting `.blend` (focus-steal behavior per Phase 1 D-05)
       - The camera in Blender is named the Flame camera name (default `Camera`, or whatever Flame named it)
       - The camera's custom properties include `forge_bake_action_name = "UAT_phase2"`, `forge_bake_camera_name = "<the Flame cam name>"`, `forge_bake_source = "flame"` (Properties editor → Object Data Properties → Custom Properties, or `bpy.data.objects["<name>"].data.items()` in the Python console)

    4. In Blender, make a small but visible edit to the camera: nudge its location by 50 cm on X, or change the focal length by 5mm. This is just to prove Blender's edit lands in Flame.

    5. Open the N-panel → Forge tab. Confirm the happy-path rows display the stamped names.

    6. Click `Send to Flame`. Within 1-2 seconds, a popup should appear with the success copy:
       - Singular: `Sent to Flame: camera '<created_name>' in Action 'UAT_phase2'`
       - (Plural form if Flame created >1 camera — shouldn't happen for a single-camera send)

    7. Switch to Flame. Open the UAT_phase2 Action. Confirm:
       - A new camera appears in the Action with the name reported in the Blender popup. Flame auto-renames on collision, so expect `<cam_name>1` or similar if the original name was still there.
       - Scrub the timeline. If the original Blender camera had multiple keyframes, the new Flame camera should also have them. If static (single keyframe), confirm the position matches the edit you made in Blender (±1 pixel tolerance for the 50 cm nudge).
       - The focal length matches the edit.

    8. The geometric fidelity is the core value: confirm in Flame's Action viewport that the camera frames the plate correctly at the start frame (CG would glue). Any obvious drift (frames jumbled, camera pointing wrong way, focal obviously off) is a FAIL — report and stop.
  </how-to-verify>
  <verify>
    <automated>MISSING — blocking checkpoint; see &lt;how-to-verify&gt; above. Validation is human-verify only; executor pauses for the resume-signal.</automated>
  </verify>
  <resume-signal>Type "e2e verified" and report: (1) the popup text observed, (2) the Flame-side created camera name, (3) observation on frame count / keyframes. Or paste any discrepancy.</resume-signal>
  <acceptance_criteria>
    - The round-trip completes end-to-end without the artist visiting Flame's batch menu after step 3 (IMP-06 literal)
    - Success popup text matches `Sent to Flame: camera '{created_name}' in Action '{action_name}'` exactly
    - A new camera is visible in the target Flame Action after send
    - Position and focal edits from Blender are reflected in the Flame camera (within numeric tolerance — exact equality may fail due to Euler gimbal edges; ±1e-3 in degrees is fine)
    - Multi-keyframe cameras bake correctly (if the bake was animated)
    - No Blender crash, no Flame crash, no leaked temp directories under `/tmp/forge_send_*` on success (`ls /tmp/forge_send_* 2>/dev/null` should show empty; D-03 clean-on-success)
  </acceptance_criteria>
  <done>IMP-03, IMP-04, IMP-05, IMP-06 all verified live. Phase 2 achieves its stated goal.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 6: Failure Tier 3 — remote-tier popup when target Action is renamed</name>
  <read_first>
    - .planning/phases/02-blender-addon/02-UI-SPEC.md (§Remote Tier; §Bridge-returned error variants — the "No Action named" and "Ambiguous:" strings)
    - tools/blender/forge_sender/transport.py (Plan 03 — _FLAME_SIDE_TEMPLATE RuntimeError messages)
  </read_first>
  <action>This is a human-verification checkpoint. The concrete steps are in the &lt;how-to-verify&gt; block below; this tag is included for planner-tooling schema compliance.</action>
  <what-built>Exercise the Remote Tier (D-09 Tier 3) path by triggering a bridge-side error. This proves the full error envelope plumbing (Flame traceback → bridge → Blender popup) works. Part of IMP-05 and confirms D-06/D-07/D-08 (exact-name, fail-loud on 0 matches).</what-built>
  <how-to-verify>
    1. Using the same Blender session from Task 5 (addon installed, camera with forge metadata active), do NOT edit anything further.
    2. Switch to Flame. Rename the `UAT_phase2` Action to `UAT_phase2_RENAMED` (right-click on the Action node in Batch → Rename, or however Flame names things — as long as the new name differs from the stamped `forge_bake_action_name` on the Blender camera).
    3. Back in Blender, leave the camera's `forge_bake_action_name` custom property unchanged — it still says `UAT_phase2` but Flame no longer has an Action by that name. Click `Send to Flame`.
    4. Expected: a multi-line popup appears with the UI-SPEC Remote Tier format:
       ```
       Send to Flame failed: No Action named 'UAT_phase2' in current batch — was it renamed or deleted?

       <multi-line Python traceback from Flame>
       ```
    5. Confirm:
       - The popup title is `Send to Flame`
       - Line 1 is the `Send to Flame failed: ...` summary containing the stamped action name
       - A blank line separator
       - Multiple traceback lines, each on its own row (UI-SPEC §Remote Tier "one layout.label per line", pipeline-TD copy-paste friendly)
       - The traceback includes `RuntimeError` (the Flame-side raise type) — verbatim from Flame's stack

    6. (Optional bonus check for D-07 ambiguous path): rename the Action back to `UAT_phase2`, then create a second Action node (copy+paste the existing one) so two Actions share the name `UAT_phase2`. Click Send again. Expected popup summary: `Send to Flame failed: Ambiguous: 2 Actions named 'UAT_phase2' — rename to disambiguate and resend` + traceback. If this bonus check is skipped, record "skipped" in the SUMMARY — D-07 is still validated by the 0-match path above.

    7. After Task 6 completes, the tempdir preserve-on-failure contract (D-03) should leave `/tmp/forge_send_*` dirs behind. `ls /tmp/forge_send_* 2>/dev/null` on the Flame workstation should show at least one. These can be deleted manually after inspection.
  </how-to-verify>
  <verify>
    <automated>MISSING — blocking checkpoint; see &lt;how-to-verify&gt; above. Validation is human-verify only; executor pauses for the resume-signal.</automated>
  </verify>
  <resume-signal>Type "tier 3 verified" and report: (1) the popup summary text, (2) whether the traceback was visible line-per-line, (3) whether the bonus ambiguous check was run. Or paste any discrepancy.</resume-signal>
  <acceptance_criteria>
    - Popup appears with the Remote Tier two-part format (summary + blank line + traceback)
    - Popup summary contains `No Action named 'UAT_phase2'` and `was it renamed or deleted?`
    - Traceback lines are each on their own row in the popup (not a single collapsed line)
    - Traceback contains `RuntimeError` (the Flame-side raise class)
    - At least one `/tmp/forge_send_*` directory is preserved after the failure (D-03 clean-on-success, preserve-on-failure)
    - Operator returned `{'CANCELLED'}`
  </acceptance_criteria>
  <done>IMP-05 Remote Tier verified. D-06/D-07/D-08 exact-match + fail-loud on 0 matches proven in the live stack.</done>
</task>

<task type="auto">
  <name>Task 7: Write 02-04-SUMMARY.md with the validation matrix</name>
  <files>.planning/phases/02-blender-addon/02-04-SUMMARY.md</files>
  <read_first>
    - $HOME/.claude/get-shit-done/templates/summary.md (the SUMMARY template; follow its headings)
    - .planning/phases/02-01-SUMMARY.md, 02-02-SUMMARY.md, 02-03-SUMMARY.md (prior plan summaries — Phase 2 SUMMARY should reference them)
  </read_first>
  <action>Create the Phase 2 closing summary at `.planning/phases/02-blender-addon/02-04-SUMMARY.md`. Follow the template structure. MUST include:

1. **Validation matrix** — a table with one row per Task 2-6 check:
   | Task | IMP Requirement | Verified? | Notes |
   |------|-----------------|-----------|-------|
   | 2 — Panel install | IMP-01 | pass/fail | (Forge tab appears) |
   | 3 — Preflight Tier 1 (a,b,c,d) | IMP-02 | pass/fail × 4 | (each sub-tier with observed copy) |
   | 4 — Transport Tier | IMP-05 | pass/fail | (bridge-down popup URL verbatim) |
   | 5 — Happy-path round-trip | IMP-03, IMP-04, IMP-05, IMP-06 | pass/fail | (created camera name, keyframe count, any drift) |
   | 6 — Remote Tier | IMP-05 (+ D-06/D-07/D-08) | pass/fail | (0-match popup + optional ambiguous check) |

2. **Evidence links** — paths to screenshots (if captured), exact copy of the success popup text, exact copy of Flame-side created camera name, any `/tmp/forge_send_*` dir path observed for D-03.

3. **Discretion calls** — any ambiguity the executor resolved without re-consulting the user (CONTEXT §Claude's Discretion bullets 1-7). Examples: did you split operator/panel into separate files or keep them in `__init__.py`? Did you bump the timeout from 5 s? Did the tab category stay `Forge` or change?

4. **D-17 vs D-19 trail** — reference `memory/flame_batch_frame_rate.md` and note whether the happy path ran via D-17 (frame_rate queried at import time) or the D-19 contingency (stamped `forge_bake_frame_rate`).

5. **Residual risks and follow-ups** — anything that worked but raised a flag for Phase 4 (E2E smoke automation) or future milestones. Examples: round-trip latency, Blender freeze UX, Flame-side error wording improvements, numerical drift.

6. **Requirements closure** — explicitly mark IMP-01, IMP-02, IMP-03, IMP-04, IMP-05, IMP-06 as Verified (or note any that failed and the follow-up quick-task).

Also ensure the phase's status is reflected in `.planning/STATE.md`:
- Update frontmatter `stopped_at:` to `"Phase 2 complete"` (or leave for the calling orchestrator to update).
- Add a one-line entry to `## Accumulated Context → ### Decisions`: `- Phase 2 shipped: Forge Sender v1.0.0 addon; all IMP-01..IMP-06 verified in live UAT (2026-MM-DD)`

Do NOT touch ROADMAP.md progress counts or any other plan's SUMMARY — those updates belong to the orchestrator.</action>
  <verify>
    <automated>test -f .planning/phases/02-blender-addon/02-04-SUMMARY.md &amp;&amp; grep -q "IMP-01\|IMP-02\|IMP-03\|IMP-04\|IMP-05\|IMP-06" .planning/phases/02-blender-addon/02-04-SUMMARY.md &amp;&amp; grep -q "Validation matrix\|validation matrix" .planning/phases/02-blender-addon/02-04-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/02-blender-addon/02-04-SUMMARY.md` exists
    - SUMMARY contains the literals `IMP-01`, `IMP-02`, `IMP-03`, `IMP-04`, `IMP-05`, `IMP-06`
    - SUMMARY contains a validation matrix (table with pass/fail column)
    - SUMMARY references `memory/flame_batch_frame_rate.md` in context of D-17 vs D-19
    - SUMMARY references prior plan SUMMARYs (02-01, 02-02, 02-03) for context
    - `.planning/STATE.md` has a one-line entry noting Phase 2 completion under `### Decisions`
  </acceptance_criteria>
  <done>Phase 2 is documented; all six requirements are marked as verified; the orchestrator has everything it needs to update ROADMAP.md and STATE.md progress.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer machine → forge-bridge (live) | This plan runs live round-trips against Flame. The bridge is still 127.0.0.1-only, same-user workstation. |
| Blender → Flame via bridge (live) | Full chain exercised end-to-end with real camera data. First time the Phase 2 code contacts a live Flame instance. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04-01 | Injection / Elevation | E2E test confirms that crafted custom-property values (e.g., a camera whose `forge_bake_action_name` contains quotes, backslashes, or newlines) don't break the Python code body passed to `/exec`. | mitigate | Task 5 happy path uses the Flame-side-stamped action name, which is known-safe. The test vectors in Plan 03 Task 2 (transport tests) already prove `repr()` handles arbitrary strings. If the live UAT encounters a name with odd characters, the error path would fail closed (remote-tier popup with the Python parse error), not silently execute. No change needed. |
| T-02-04-02 | Information Disclosure | Failure-path tracebacks visible to pipeline-TD audience include internal Flame paths. | accept | Same as T-02-03-04: single-user workstation, asset paths already known to user. |
| T-02-04-03 | Denial of Service | Large animated camera (thousands of keyframes) could timeout the 5 s HTTP request. | mitigate | D-16 allows bumping timeout to 10 s (CONTEXT §Claude's Discretion bullet 4). Task 7 SUMMARY records any observed latency; if p99 > 3 s, planner approves the bump in a follow-up quick-task. |

**No new attack surface in this plan — E2E validation only.** Task 1 (zip build) does not touch `install.sh` (D-12). Tasks 2-6 exercise paths already unit-tested in Plan 03 — live UAT confirms they behave in the real stack.
</threat_model>

<verification>
- `test -f tools/blender/forge_sender-v1.0.0.zip` exits 0
- `unzip -l tools/blender/forge_sender-v1.0.0.zip | grep forge_sender/__init__.py` returns 1 line
- `test -f .planning/phases/02-blender-addon/02-04-SUMMARY.md` exits 0
- `grep -q "IMP-06" .planning/phases/02-blender-addon/02-04-SUMMARY.md` exits 0
- `git diff install.sh` produces no output (D-12 compliance)
- `git diff flame/camera_match_hook.py` produces no output (Phase 1 closed)
- `git diff tools/blender/bake_camera.py` produces no output (stamped contract unchanged)
</verification>

<success_criteria>
- Installable zip exists at `tools/blender/forge_sender-v1.0.0.zip` (built by D-12 shell command, not install.sh)
- Addon installs in Blender 4.5+ via Preferences → Add-ons → Install from file
- Forge tab appears in the 3D viewport N-panel with the exact UI-SPEC row order
- All four Tier 1 failure paths produce the exact UI-SPEC copy
- Transport Tier popup contains the literal `http://127.0.0.1:9999`
- Happy-path round-trip works: new camera appears in Flame Action; edits from Blender are preserved
- Remote Tier popup for 0-match scenario shows summary + blank line + traceback verbatim
- All IMP-01..IMP-06 are marked Verified in `02-04-SUMMARY.md`
- `install.sh` is unchanged
- `flame/camera_match_hook.py` is unchanged
- `tools/blender/bake_camera.py` is unchanged
</success_criteria>

<output>
`.planning/phases/02-blender-addon/02-04-SUMMARY.md` — the validation matrix + evidence + D-17/D-19 trail + residual risks + requirements closure. No other plan creates a SUMMARY in this plan's scope.
</output>
