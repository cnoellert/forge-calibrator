---
status: partial
phase: 04.2-aim-target-rig-camera-orientation-round-trip
source: [04.2-VERIFICATION.md]
started: 2026-04-24T18:35:00Z
updated: 2026-04-25T00:00:00Z
---

## Current Test

[awaiting human testing]

## Tests

### 1. Flame→Blender aim-rig orientation UAT
expected: Export Aim/Target-rig Camera1 from Flame (Batch > right-click Action > Camera Match > Export Camera to Blender). Open the resulting .blend in Blender 4.5+. The Blender camera points at the same world-space target as the Flame original.
result: [pending]

### 2. Blender→Flame return-trip orientation within 0.1° on all three Euler axes
expected: After the Flame→Blender step above, use forge-sender Blender addon (Phase 2) to send the camera back to Flame. The returned camera's rendered orientation matches the original's to within 0.1° on all three Euler axes (rx, ry, rz).
result: pass
resolved: 2026-04-25 by Phase 04.3 (aim-rig-euler-convention-fix). The previously known 0.087° ry residual is closed — the corrected aim-rig Euler convention combined with the `rotation_matrix_from_look_at` roll-sign coupling lands Camera1 within 0.006° per axis of Flame viewport ground truth, ~14× tighter than the 0.1° tolerance this test demands. The `compute_flame_euler_xyz` / `flame_euler_xyz_to_cam_rot` pair (additive) handles aim-rig decomposition; `_zyx` pair untouched on the Free-rig path. Verified by `tests/test_rotations.py::TestComputeFlameEulerXyz::test_camera1_known_answer` and `tests/test_fbx_ascii.py::TestAimRigFixture::test_rotation_matches_plan01_known_answer` (both green at 400/400). End-to-end Flame↔Blender subprocess chain still requires live UAT to confirm the parser-layer correctness propagates through Blender's bpy + forge-sender transport — that part is captured by Test 1 and Test 3 in this file.

### 3. Apply-path smoke test on aim-rig camera (D-10 latent-bug closure)
expected: With an aim-rig Action camera selected in Flame, trigger the solve-apply flow (Camera Match > Open Camera Match on its clip, draw VPs, click Apply). No AttributeError surfaces from the removed `target_mode.set_value(False)` sites; position/rotation/fov set_value calls land correctly.
result: [pending]

## Summary

total: 3
passed: 1
issues: 0
pending: 2
skipped: 0
blocked: 0

## Gaps

[none — Test 2's known-limitation gap was closed by Phase 04.3; Tests 1 and 3 remain pending live-Flame access, not gap-class issues]
