---
status: partial
phase: 04.2-aim-target-rig-camera-orientation-round-trip
source: [04.2-VERIFICATION.md]
started: 2026-04-24T18:35:00Z
updated: 2026-04-25T00:00:00Z
---

## Current Test

[awaiting human testing]

## Tests

### 1. Flame→Blender aim-rig orientation UAT
expected: Export Aim/Target-rig Camera1 from Flame (Batch > right-click Action > Camera Match > Export Camera to Blender). Open the resulting .blend in Blender 4.5+. The Blender camera points at the same world-space target as the Flame original.
result: [pending]

### 2. Blender→Flame return-trip orientation within 0.1° on all three Euler axes
expected: After the Flame→Blender step above, use forge-sender Blender addon (Phase 2) to send the camera back to Flame. The returned camera's rendered orientation matches the original's to within 0.1° on all three Euler axes (rx, ry, rz).
result: pass
verified: |
  2026-04-25 live UAT (post-Probe 4 resolution): orchestrator drove the
  round-trip via forge-bridge with capture instrumentation. User
  triggered Camera Match > Export to Blender on action11.Camera1
  (aim-rig at position (0.0, 57.77, 2113.31), aim (0.36, 57.13, 2093.32))
  → Blender → Send to Flame → returned as action11.Camera3 (Free-rig).

  Numerical match:
    Camera1 → Camera3
    position: (0.0, 57.7747, 2113.3054) → (0.0, 57.7747, 2113.3059)
              Δ ≈ 0.0005 units (sub-pixel)
    rotation: aim-rig (driven by aim/up vectors) → (1.81399°, 1.05764°, +1.25210°)
              Phase 04.3 ground truth on Camera1 = (1.8140°, 1.0576°, +1.2521°)
              Δ < 0.001° on all three axes (well inside 0.1° gate).

  Probe 4 SIGSEGV did NOT recur. See
  .planning/debug/resolved/aim-rig-roundtrip-offset.md "Probe 4 —
  Resolution" section for the full live verification trace +
  capture artifacts at ~/forge_diag/capture/.
reported: |
  Blender→Flame round-trip is hard-crashing Flame with SIGSEGV. User-observed
  symptoms during 2026-04-25 live UAT (two probes, in order):

  Probe 1 (initial report):
      Python encountered ModuleNotFoundError("No module named 'bpy'") in unknown code
      [PYTHON HOOK] An error occurred. Ignoring python hooks from /opt/Autodesk/shared/python/blender/forge_sender/flame_math.py
    followed by a hard crash in Flame when "trying to pass back to flame".

  Probe 2 (after retrying):
      loaded at the same time. /opt/Autodesk/shared/python/forge_flame/__init__.py will be used.
      Python encountered ModuleNotFoundError("No module named 'flame.action_export'; 'flame' is not a package") in unknown code
      [PYTHON HOOK] An error occurred. Ignoring python hooks from /opt/Autodesk/shared/python/apply_solve.py
      Application: flame 2026.2.1 arm64
      Pid: 34180
      Error: abnormal termination, signal = 11
      SIGSEGV - Segmentation Fault
      Signal was generated internally:
      invalid permissions for mapped object at address 0x00001508

  History: this test was briefly marked `pass` on 2026-04-25 based on Phase 04.3's
  unit-test math closing the 0.087° ry residual to 0.006°. That marking was
  premature — the unit-test math is correct, but the user-facing round-trip is
  failing because the production install layout is corrupted on the user's machine.

  Confirmed root cause (Probe 2 evidence):
  Phase 01-VERIFICATION.md Test 1's rsync command —
      rsync -av flame/ forge_flame/ forge_core/ tools/ /opt/Autodesk/shared/python/
  — uses TRAILING SLASHES on every source, which causes rsync to copy the
  CONTENTS of each directory into /opt/Autodesk/shared/python/ rather than the
  directories themselves. The result flattens four packages' contents into one
  directory, leaving:
    - /opt/Autodesk/shared/python/apply_solve.py    (from local flame/)
    - /opt/Autodesk/shared/python/action_export.py  (from local flame/)
    - /opt/Autodesk/shared/python/camera_match_hook.py
    - /opt/Autodesk/shared/python/adapter.py        (from local forge_flame/)
    - /opt/Autodesk/shared/python/fbx_ascii.py      (from local forge_flame/)
    - /opt/Autodesk/shared/python/blender/          (from local tools/)
    - …and a top-level __init__.py from at least one source
  alongside install.sh's correct subdirectories
  (camera_match/, forge_core/, forge_flame/, forge_bridge/).

  Flame's hook scanner picks up apply_solve.py at top level and tries to
  import it. apply_solve.py contains `from flame.action_export import ...`
  which expects `flame` to be a package — but the rsync flattening means
  there's no `flame/` package at top-level (only its CONTENTS), so Python
  resolves `flame` to Flame's bundled non-package `flame` module, which
  doesn't have an `action_export` attribute → ModuleNotFoundError. The
  partial import leaves Flame's Python state inconsistent → SIGSEGV at
  0x00001508 (low-address mapped-object-permissions violation, classic
  null-pointer-class deref / pyobject ref-count corruption).

  install.sh:45-50 deliberately uses subdirectories (camera_match/,
  forge_core/, forge_flame/) and routes tools/blender/ to a SEPARATE path
  (/opt/Autodesk/shared/forge_blender_scripts/) precisely to avoid this
  flattening. The UAT instruction bypasses both safeguards.

  This is a Phase 01 documentation bug, not a Phase 04.x code bug. Fixing
  the docs is in 01-VERIFICATION.md (this commit). Cleaning the user's
  production install is below.

  Triage steps for user (production stack):
    1. cd /opt/Autodesk/shared/python/
    2. ls -la
       Expected to see a flat mix of misplaced top-level files
       (action_export.py, apply_solve.py, camera_match_hook.py, adapter.py,
       fbx_ascii.py, etc.) AND subdirs (blender/, colour/, image/, math/,
       solver/, smoke-test/, templates/) ALONGSIDE the install.sh-correct
       subdirs (camera_match/, forge_core/, forge_flame/, forge_bridge/).
    3. Surgical removal (preserves install.sh subdirs):
         rm -f __init__.py action_export.py apply_solve.py \
               camera_match_hook.py rotation_diagnostic.py solve_and_update.py \
               adapter.py blender_bridge.py camera_io.py fbx_ascii.py \
               fbx_io.py wiretap.py fspy_import.py
         rm -rf blender/ colour/ image/ math/ solver/ smoke-test/ \
                templates/ __pycache__/
    4. Verify only the install.sh subdirs remain at top level:
         ls -la
         (should show camera_match/, forge_core/, forge_flame/, forge_bridge/
          and nothing else from forge-calibrator)
    5. Redeploy cleanly:
         cd ~/Documents/GitHub/forge-calibrator
         ./install.sh --force
    6. Restart Flame, retry the Blender→Flame round-trip.

  Expected after step 5:
    - The "[PYTHON HOOK] Ignoring python hooks from .../apply_solve.py"
      warning disappears.
    - The "'flame' is not a package" ModuleNotFoundError disappears.
    - The SIGSEGV does NOT recur.

  If after step 5 the SIGSEGV persists, escalate to /gsd-debug 04.2 with
  the fresh Flame log captured at the crash time. The bpy hook noise was
  almost certainly hook-scanner symptom — but a separate Phase 04.x code
  bug remains possible.

  Probe 3 (post-cleanup, 2026-04-25):
    User confirmed cleanup + ./install.sh --force redeploy succeeded. The
    SIGSEGV no longer recurs and the round-trip COMPLETES without crash.
    However, the returned camera's frustum is visibly offset from the
    original by SEVERAL DEGREES of rotation plus a translation offset
    (screenshot supplied: thin white rectangle = returned camera's
    projected frame; outer rectangle = original plate edge). This is a
    structural offset, not the 0.006° precision Phase 04.3's unit tests
    measured against Camera1.

    Investigation (orchestrator-side):
      - rotation_matrix_from_look_at (the L167 sign-flip site) is called
        in only two places, both gated `if is_aim_rig:`
        (forge_flame/fbx_ascii.py L885 static, L953 per-frame). Free-rig
        cameras do not touch Phase 04.3 code.
      - Phase 04.3's unit tests use Camera1 (rx=1.8°, ry=1.0°, rz=1.2°) —
        small-angle regime where Z·Y·X and X·Y·Z matrix products are
        numerically indistinguishable. A structural product-order error
        could pass Phase 04.3's tests yet fail at moderate angles.

    Diagnostic data needed before we can root-cause this:
      a. Camera type — aim-rig (Aim+Target connections) or free-rig?
      b. Was the camera animated or static at solve time?
      c. Original camera rotation values from Flame's Camera tab.
      d. Returned camera rotation values from Flame's Camera tab.
      e. forge-bridge / Flame log lines during the round-trip
         (no errors, but any noise is informative).

    Routed to /gsd-debug 04.2 for structured investigation.

severity: resolved

### 3. Apply-path smoke test on aim-rig camera (D-10 latent-bug closure)
expected: With an aim-rig Action camera selected in Flame, trigger the solve-apply flow (Camera Match > Open Camera Match on its clip, draw VPs, click Apply). No AttributeError surfaces from the removed `target_mode.set_value(False)` sites; position/rotation/fov set_value calls land correctly.
result: [pending]

## Summary

total: 3
passed: 1
issues: 0
pending: 2
skipped: 0
blocked: 0

## Gaps

- truth: "Blender→Flame round-trip lands the returned camera in Flame without crashing, with rendered orientation within 0.1° on all axes"
  status: failed
  reason: "User reported on 2026-04-25 during live UAT: ModuleNotFoundError('bpy') from /opt/Autodesk/shared/python/blender/forge_sender/flame_math.py logged as an ignored Flame hook, followed by a hard crash in Flame on the round-trip. Likely cause: the Phase 01 UAT rsync command deploys tools/blender/ inside Flame's hook discovery tree, which install.sh deliberately avoids."
  severity: blocker
  test: 2
  artifacts:
    - "/opt/Autodesk/shared/python/blender/forge_sender/flame_math.py (should not exist; install.sh routes to /opt/Autodesk/shared/forge_blender_scripts/)"
    - ".planning/phases/01-export-polish/01-VERIFICATION.md Test 1 (rsync command is the suspected source)"
    - "install.sh L45-L51 (documents the path-separation contract)"
  missing:
    - "Triage data: ls /opt/Autodesk/shared/python/blender/ before removal"
    - "Flame log line preceding the hard crash (to determine whether bpy hook error and crash share a root cause)"
    - "Confirmation of round-trip behaviour after rm -rf misplaced dir + install.sh redeploy"
