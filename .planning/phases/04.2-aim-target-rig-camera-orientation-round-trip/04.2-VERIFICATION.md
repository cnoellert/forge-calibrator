---
phase: 04.2-aim-target-rig-camera-orientation-round-trip
verified: 2026-04-24T18:30:00Z
status: human_needed
score: 10/10 must-haves verified
overrides_applied: 0
human_verification:
  - test: "Flame->Blender aim-rig orientation UAT"
    expected: "Export Aim/Target-rig Camera1 from Flame (Batch > right-click Action > Camera Match > Export Camera to Blender). Open the resulting .blend in Blender 4.5+. The Blender camera points at the same world-space target as the Flame original."
    why_human: "Requires running Flame 2026.2.1 + Blender 4.5 subprocess with a live aim-rig camera; no programmatic substitute for visual geometric alignment check against the plate."
  - test: "Blender->Flame return-trip orientation within 0.1° on all three Euler axes"
    expected: "After the Flame->Blender step above, use forge-sender Blender addon (Phase 2) to send the camera back to Flame. The returned camera's rendered orientation matches the original's to within 0.1° on all three Euler axes (rx, ry, rz)."
    why_human: "ROADMAP acceptance criterion is an end-to-end round-trip through live Flame + Blender. Parser-layer integration test proves the .blend carries the correct orientation (Plan 01 known-answer matches fixture at ~5e-5°, 1890x tighter than 0.1° tolerance), but the actual round-trip includes Blender's bpy camera math and forge-sender transport that are not exercisable in pytest."
  - test: "Apply-path smoke test on aim-rig camera (D-10 latent-bug closure)"
    expected: "With an aim-rig Action camera selected in Flame, trigger the solve-apply flow (Camera Match > Open Camera Match on its clip, draw VPs, click Apply). No AttributeError surfaces from the removed target_mode.set_value(False) sites; position/rotation/fov set_value calls land correctly."
    why_human: "The D-10 bug only trips when the solve-apply path runs against an aim-rig Flame camera in the live Flame interpreter. Python unit tests cannot construct a real PyCoNode."
---

# Phase 4.2: Aim/Target-rig camera orientation round-trip Verification Report

**Phase Goal:** Aim/Target-rig cameras must round-trip through Blender with their orientation preserved. Today they lose it — Flame's `action.export_fbx(bake_animation=True)` writes `rotation=(0,0,0)` for every frame on aim-rig cameras, discarding the aim+up+roll that defines the camera's real orientation, and the returned camera ends up as a Free-rig camera with rotation=0.

**Acceptance:** Export an Aim/Target-rig camera from Flame → open the .blend in Blender → the Blender camera points at the same world-space target as the Flame original. Send back to Flame → the returned camera's rendered orientation matches the original's to within 0.1° on all three Euler axes.

**Verified:** 2026-04-24T18:30:00Z
**Status:** human_needed (all automated must-haves PASS; end-to-end Flame↔Blender round-trip requires live UAT)
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `rotation_matrix_from_look_at` helper exists in `forge_core/math/rotations.py`, numpy-only, with the locked contract (columns [right \| up_cam \| -forward], theta = np.radians(-roll_deg)) | VERIFIED | `forge_core/math/rotations.py:83-172`: signature matches, docstring references memory/flame_rotation_convention.md, theta negation present at line 167 (`theta = np.radians(-float(roll_deg))`), column stack at `np.column_stack((right_rolled, up_rolled, -forward))`, numpy-only imports |
| 2 | Helper fails loud with "aim-rig resolve:" prefix on degenerate inputs (|aim-pos| <= 1e-6 OR |cross(forward, up)| <= 1e-6) | VERIFIED | `forge_core/math/rotations.py:138-153`: two ValueError raises with "aim-rig resolve: forward vector degenerate" and "aim-rig resolve: up vector parallel to view direction"; reuses 1e-6 threshold; `grep -c "aim-rig resolve" rotations.py` = 4 matches |
| 3 | All three broken `target_mode` call sites removed from production code paths (camera_match_hook.py:1511 set_value, :1578 get_value; camera_io.py:234 set_value) | VERIFIED | `grep -n "target_mode" flame/camera_match_hook.py forge_flame/camera_io.py` = 0 matches in both files. Only remaining reference is `flame/rotation_diagnostic.py:99-103` which is hasattr-guarded and intentionally out of scope per D-10 |
| 4 | Surrounding explicit `position/rotation/fov.set_value` pattern preserved in both affected files (ensures Free-rig pose still defined unambiguously) | VERIFIED | Plan 02 SUMMARY reports 3 matches of `cam.(position\|rotation\|fov).set_value` preserved in camera_match_hook.py and 3 matches of `cam_node.(position\|rotation\|fov).set_value` preserved in camera_io.py. Both files parse cleanly |
| 5 | `forge_flame/fbx_ascii.py` has an aim-rig branch in `_merge_curves` that composes per-frame look-at → Flame ZYX Euler, with roll negated at the parser layer before the look-at helper | VERIFIED | `forge_flame/fbx_ascii.py:827-960`: `is_aim_rig = cam.aim_null_id is not None` detection; static-fallback branch (lines 876-891) and per-frame branch (lines 939-959) both call `rotation_matrix_from_look_at(..., roll_deg=-cam.static_roll)` (static) and `roll_deg=-roll` (per-frame); output fed to `compute_flame_euler_zyx`; local import inside function body (line 807) keeps module-scope numpy-free (0 module-scope numpy imports confirmed) |
| 6 | Aim-rig branch is cleanly gated — fires only when `cam.aim_null_id is not None` AND Lcl Rotation curves are absent or identically zero; free-rig path unchanged | VERIFIED | `forge_flame/fbx_ascii.py:828-832`: non-zero-curve fallback sets `is_aim_rig = False`. `TestFlameRotationNegationContract` (3 tests) passes — free-rig rotation-negation contract preserved bit-for-bit |
| 7 | `_extract_cameras` detects aim Null via LookAtProperty connection, scans null_models, resolves static aim/up/roll from Properties70, locates aim/up/roll AnimCurves via connection walks; fails loud on dangling LookAtProperty references | VERIFIED | `forge_flame/fbx_ascii.py:534-548` (cam_to_aim_null dispatch with dangling-Null ValueError), `:595-668` (per-camera aim-rig resolution reading `Lcl Translation`, `UpVector`, `Roll` from Properties70 and AnimCurveNodes). `_CameraExtract` extended with 6 aim-rig fields at lines 460-471 |
| 8 | `tests/test_fbx_ascii.py::TestAimRigFixture` has 5 integration tests + `TestAimRigFailLoud` has 3 D-14 fail-loud tests; all pass against the Camera1 fixture | VERIFIED | Live pytest run: 8/8 aim-rig tests pass (test_parses_without_error, test_position_matches_live_probe, test_rotation_is_non_zero, test_rotation_matches_plan01_known_answer, test_focal_preserved, test_fails_loud_on_forward_degenerate, test_fails_loud_on_up_parallel_to_view, test_fails_loud_on_dangling_lookat_connection) |
| 9 | `tests/test_fbx_ascii.py::TestAimRigFixture::test_rotation_matches_plan01_known_answer` (D-07 case-1 integration gate) passes — parser pipeline produces Camera1 rx=1.814°, ry=1.058°, rz=-1.252° matching Plan 01 known-answer within 1e-3° | VERIFIED | Live fbx_to_v5_json invocation on fixture produces `rotation_flame_euler = [1.8139858, 1.0576378, -1.2520908]`. Cross-check vs. Plan 01 reference (rx=1.8140389, ry=1.0576613, rz=-1.2520907): deltas are 5.3e-5°, 2.4e-5°, 2.8e-8° respectively — 19x tighter than the 1e-3° test tolerance and 1890x tighter than the 0.1° ROADMAP tolerance |
| 10 | Full pytest suite green (expected 380 passed, 2 skipped); no regressions from aim-rig branch or target_mode removal | VERIFIED | `pytest tests/` reports **380 passed, 2 skipped in 0.61s**. Matches Plan 03 SUMMARY claim. `TestFlameRotationNegationContract` (3 tests) also passes, proving the aim-rig branch is cleanly gated |

**Score:** 10/10 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `forge_core/math/rotations.py` | rotation_matrix_from_look_at helper with column convention [right \| up_cam \| -forward] and theta = np.radians(-roll_deg) | VERIFIED | Exists at lines 83-172; signature accepts tuple-or-ndarray; fail-loud on two invariants; Google-style docstring; numpy-only imports; `-float(roll_deg)` sign flip at line 167 locked by Plan 01's Camera1 TDD iteration (commit be18ce3) |
| `tests/test_rotations.py` | TestLookAtMatrix class with 11 tests (orthonormality, free-rig cross-check x4, Camera1 known-answer, D-15 fail-loud x3, roll-sign) | VERIFIED | File exists with TestLookAtMatrix class; 11 tests pass in 0.03s; Camera1 constants (CAMERA1_POSITION, CAMERA1_AIM, CAMERA1_UP, CAMERA1_ROLL_DEG=-1.252521) at module scope (lines 36-39) |
| `flame/camera_match_hook.py` | No `target_mode` references in solve-apply or trace-readback paths | VERIFIED | `grep -n "target_mode" flame/camera_match_hook.py` = 0 matches; surrounding `cam.(position\|rotation\|fov).set_value` pattern preserved |
| `forge_flame/camera_io.py` | No `target_mode` references in import_json_to_flame_camera | VERIFIED | `grep -n "target_mode" forge_flame/camera_io.py` = 0 matches; surrounding `cam_node.(position\|rotation\|fov).set_value` pattern preserved |
| `forge_flame/fbx_ascii.py` | Aim-rig branch in _extract_cameras (LookAtProperty dispatch + Null scan) + _merge_curves (per-frame look-at composition with roll negated) | VERIFIED | _CameraExtract extended with 6 aim-rig fields (lines 460-471); `_extract_cameras` has Null model scan, LookAtProperty dispatch with dangling-Null ValueError, per-camera aim-rig field resolution; `_merge_curves` has `is_aim_rig` detection, aim/up/roll curve reads, static + per-frame branches both negating roll. Module-scope numpy-free confirmed |
| `tests/test_fbx_ascii.py` | TestAimRigFixture (5 tests) + TestAimRigFailLoud (3 tests) | VERIFIED | Both classes present (lines 481 + 588); 8/8 aim-rig tests pass |
| `tests/fixtures/forge_fbx_aimrig.fbx` | Byte-identical copy of planning-directory Camera1 probe (29401 bytes) | VERIFIED | File exists (29401 bytes); `diff` against `.planning/phases/04.2-.../04.2-aimrig-probe-camera1.fbx` exits 0 (byte-identical); contains LookAtProperty, Camera1_aim, UpVector markers |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| `forge_flame/fbx_ascii.py:_merge_curves` aim-rig branch | `forge_core.math.rotations.rotation_matrix_from_look_at` | local import + call per keyframe | WIRED | Local import at line 807-810; two call sites (static-fallback at line 885, per-frame at line 953), both with `roll_deg=-cam.static_roll`/`-roll` sign flip |
| `forge_flame/fbx_ascii.py:_merge_curves` aim-rig branch | `forge_core.math.rotations.compute_flame_euler_zyx` | composes decomposition after look-at | WIRED | Called immediately after each `rotation_matrix_from_look_at` call (lines 891, 960) to produce Flame ZYX Euler triple written into v5 JSON `rotation_flame_euler` field |
| `tests/test_fbx_ascii.py:TestAimRigFixture` | `tests/fixtures/forge_fbx_aimrig.fbx` + `forge_flame.fbx_ascii.fbx_to_v5_json` | fixture-driven integration test | WIRED | `AIMRIG_FIXTURE` constant declared; 5 tests invoke `fbx_to_v5_json` against the fixture path; all pass |
| `tests/test_rotations.py` | `forge_core.math.rotations` | direct import | WIRED | Module imports `compute_flame_euler_zyx`, `flame_euler_to_cam_rot`, `rotation_matrix_from_look_at` at lines 21-25 |
| `forge_flame/fbx_ascii.py:_extract_cameras` LookAtProperty dispatch | `cam_to_aim_null` → `null_models[aim_null_id]` | connection-edge walk | WIRED | Lines 534-548 dispatch LookAtProperty edges; dangling reference raises `aim-rig resolve: LookAtProperty connection references unknown Null id` |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|----|
| `rotation_flame_euler` in v5 JSON `frames[0]` | per-frame rx/ry/rz | aim-rig branch in `_merge_curves` reads `static_aim_position`, `static_up_vector`, `static_roll` from FBX Properties70 + per-frame curves, feeds `rotation_matrix_from_look_at` then `compute_flame_euler_zyx` | Yes — live invocation on fixture produces `[1.8139858, 1.0576378, -1.2520908]` (non-zero, matches Plan 01 known answer to 5.3e-5°) | FLOWING |
| `static_aim_position` | tuple(x,y,z) | `_get_property70(aim_null, "Lcl Translation")` read during `_extract_cameras` | Yes — read from real FBX bytes in `tests/fixtures/forge_fbx_aimrig.fbx` (29401 bytes, byte-identical to live Flame probe) | FLOWING |
| `static_up_vector` | tuple(x,y,z) | `_get_property70(na_or_model, "UpVector")` with Model-overwrites-NA ordering | Yes (in current fixture) but WR-01 flags a latent precedence hazard — see Anti-Patterns | FLOWING (with concern) |
| `static_roll` | float | `_get_property70(na_or_model, "Roll")` — FBX stores +1.25252, parser negates at line 889 to match Flame-probe -1.25 convention | Yes — Plan 03 SUMMARY documents the sign-flip rationale; Camera1 rz=-1.2521° after negation matches Plan 01 reference to 2.8e-8° | FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|----|
| Aim-rig fixture parse produces non-zero rotation (core proof of goal) | `python -c "from forge_flame.fbx_ascii import fbx_to_v5_json; ... fbx_to_v5_json('tests/fixtures/forge_fbx_aimrig.fbx', out, width=1920, height=1080, camera_name='Camera1')"` → inspect `frames[0]['rotation_flame_euler']` | `[1.8139858, 1.0576378, -1.2520908]` (all three axes non-zero; rz matches input roll sign) | PASS |
| Camera1 rotation matches Plan 01 known answer within 0.1° | Diff live fbx_to_v5_json output against Plan 01 reference `(1.814038919..., 1.057661345..., -1.252090744...)` | rx delta 5.3e-5°, ry delta 2.4e-5°, rz delta 2.8e-8° — all < 0.1° ROADMAP tolerance (1890x margin) | PASS |
| Full pytest suite green | `pytest tests/` | **380 passed, 2 skipped** in 0.61s | PASS |
| Phase-specific tests green | `pytest tests/test_fbx_ascii.py::TestAimRigFixture tests/test_fbx_ascii.py::TestAimRigFailLoud tests/test_fbx_ascii.py::TestFlameRotationNegationContract tests/test_rotations.py` | 22/22 passed in 0.11s | PASS |
| Module-scope numpy-free preserved in fbx_ascii | `grep -cE "^import numpy\|^from numpy" forge_flame/fbx_ascii.py` | 0 matches | PASS |
| target_mode removal clean | `grep -n "target_mode" flame/camera_match_hook.py forge_flame/camera_io.py` | 0 matches in both files (only hasattr-guarded diagnostic remains) | PASS |
| `aim-rig resolve:` fail-loud prefix present at all three invariant sites | `grep -c "aim-rig resolve" forge_core/math/rotations.py forge_flame/fbx_ascii.py` | rotations.py=4, fbx_ascii.py=1 (dangling LookAtProperty) — all 3 invariants (forward-degenerate, up-parallel, dangling Null) covered with tests | PASS |

### Requirements Coverage

No requirement IDs declared for this phase (per plan frontmatter `requirements: []` and CONTEXT.md: "ROADMAP phase goal + acceptance criterion are the must-haves source"). The phase goal + acceptance criterion are tracked via the truths table above.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `forge_flame/fbx_ascii.py` | 625-635, 646-654 | UpVector / Roll precedence: inline comment states "NodeAttribute first (authoritative for live-probe values), then Model" but the code unconditionally overwrites `static_up_vector` / `static_roll` with the Model value when present. If Flame ever emits these on both, Model wins — contradicting the stated intent. | Warning (from REVIEW.md WR-01) | Current fixture does NOT carry UpVector / Roll on the Model, so the precedence is never exercised in tests. A future Flame export that stamps both could silently pick the wrong value, producing a wrong-but-plausible orientation that would not fail loud. Core-value hazard for round-trip fidelity. |
| `tests/test_fbx_ascii.py` | 481-586, fixture | UpVector extraction path is not exercised against a non-default value: the fixture's Model and NodeAttribute Properties70 don't carry UpVector; parser falls through to hardcoded default `(0, 1, 0)` which happens to be parallel to `CAMERA1_UP = (0, 30, 0)` after re-orthogonalization. Any regression to the UpVector read code (e.g. `p[2]` vs `p[0]` typo) would not be caught. | Warning (from REVIEW.md WR-02) | No visible test coverage on the UpVector-fetch code path; related to WR-01 precedence risk. Would not block current fixture's Camera1 round-trip but leaves the branch effectively dead-code in CI. |
| `tests/test_rotations.py` | 130-143 | `test_roll_sign_convention` only asserts the three roll matrices are distinct; does not lock rotation direction. A sign flip in `theta = np.radians(-roll_deg)` would still pass. Overall coverage is adequate because `test_camera1_known_answer` locks direction, but the test's name overstates what it proves. | Info (from REVIEW.md IN-01) | Coverage gap is informational — Camera1 case already locks the sign. Fix would be to rename or strengthen the assertion. |
| `forge_core/math/rotations.py` | 146-153 | Zero-length `up = (0,0,0)` triggers the "up vector parallel to view" error message, which misdirects readers who passed `(0,0,0)` by accident. | Info (from REVIEW.md IN-03) | Misleading diagnostic only — the invariant still fails loud with `aim-rig resolve:` prefix; caller sees a clear failure. No correctness impact. |
| `forge_flame/fbx_ascii.py` | 482-494 | Duplicate `_iter_objects(root, "Model")` walk — once for cameras, once for Nulls. One-pass branch dispatch on `values[2]` would be cleaner. | Info (from REVIEW.md IN-02) | Performance only; not a correctness issue. |
| `forge_flame/fbx_ascii.py` | 807-810 | Local import at _merge_curves body includes `compute_flame_euler_zyx` unconditionally even though it's only used inside `if is_aim_rig:` branches. Numpy cost paid on every reader call. Cost is cache-hit cheap in practice. | Info (from REVIEW.md IN-04) | Informational; does not violate the module-scope numpy-free intent. |

None of the anti-patterns are blockers — all are captured in `04.2-REVIEW.md` as warnings/info with documented fix suggestions. The WR-01/WR-02 precedence hazard is material for long-term fidelity but does not break the current phase's acceptance because the fixture exercises the path that DOES work.

### Human Verification Required

The ROADMAP acceptance criterion is explicitly an **end-to-end Flame↔Blender round-trip**. The parser-layer integration test proves the .blend bake input (v5 JSON) is correct to ~5e-5° (1890x tighter than the 0.1° ROADMAP tolerance). Three checks require live Flame + Blender and cannot be exercised in pytest:

#### 1. Flame→Blender aim-rig orientation UAT

**Test:** Export Aim/Target-rig Camera1 from Flame (Batch → right-click Action → Camera Match → Export Camera to Blender). Open the resulting `.blend` in Blender 4.5+.
**Expected:** The Blender camera points at the same world-space target (aim=(0.36, 57.13, 2093.32)) as the Flame original. Visual inspection against the plate confirms alignment.
**Why human:** Requires running Flame 2026.2.1 + Blender 4.5 subprocess with a live aim-rig camera; no programmatic substitute for the visual geometric alignment check.

#### 2. Blender→Flame return-trip orientation within 0.1°

**Test:** After step 1, use the forge-sender Blender addon (Phase 2) to send the camera back to Flame. Inspect the returned camera's rotation via the hook trace diagnostic or a direct `cam.rotation.get_value()` probe.
**Expected:** Returned camera's Euler triple matches the original within 0.1° on all three axes (rx, ry, rz).
**Why human:** ROADMAP acceptance criterion is an end-to-end round-trip. Parser-layer test proves the .blend carries correct orientation; the actual round-trip additionally exercises Blender's bpy camera math and forge-sender transport that are not exercisable in pytest.

#### 3. Apply-path smoke test on aim-rig camera (D-10 latent-bug closure)

**Test:** With an aim-rig Action camera selected in Flame, trigger the solve-apply flow (Camera Match → Open Camera Match on its clip, draw VPs, click Apply). Also exercise `import_json_to_flame_camera` (JSON import path) against the returned aim-rig camera.
**Expected:** No `AttributeError` surfaces from the removed `target_mode.set_value(False)` sites; `position/rotation/fov.set_value` calls land correctly and the camera adopts the solved Free-rig pose.
**Why human:** The D-10 bug only trips when the solve-apply path runs against a real aim-rig Flame camera in the live Flame interpreter. Python unit tests cannot construct a real PyCoNode.

### Gaps Summary

No gaps blocking goal achievement. All 10 observable truths are VERIFIED:

- Plan 01 (look-at helper) delivered with a locked contract proven by a Camera1 live-probe known-answer test at 4e-4° accuracy.
- Plan 02 (target_mode D-10 fix) clean — three broken call sites removed; 0 remaining `target_mode` references in production paths; the only remaining occurrence is the hasattr-guarded diagnostic tool.
- Plan 03 (FBX aim-rig branch) delivers end-to-end parser-layer resolution: aim Null detection, LookAtProperty dispatch, Properties70 reads, per-frame look-at composition with sign-flipped roll. Parser output matches Plan 01 reference to 5.3e-5° — **1890x tighter than the 0.1° ROADMAP tolerance**.
- 380 tests pass, 2 skipped; no regressions; free-rig rotation-negation contract preserved.
- Anti-patterns surfaced in REVIEW.md (WR-01 UpVector/Roll precedence; WR-02 UpVector extraction untested) are tracked but not blocking — the current fixture path works correctly, and the precedence hazard only materializes in a future FBX export shape that isn't produced by the pinned Flame 2026.2.1.

**Status is `human_needed`** because the ROADMAP acceptance criterion is defined as a live Flame↔Blender round-trip, which is not exercisable in pytest. The automated work has proven the .blend input is correct to 1890x the tolerance; the remaining verification is confirming the full subprocess chain still produces an aligned camera at the receiving end.

---

*Verified: 2026-04-24T18:30:00Z*
*Verifier: Claude (gsd-verifier)*
