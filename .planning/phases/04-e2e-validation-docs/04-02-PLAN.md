---
phase: 04-e2e-validation-docs
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/smoke-test/seamless-bridge-smoke.sh
autonomous: true
requirements: [DOC-01]
tags: [validation, smoke-test, shell, v6.3, human-uat-folded]

must_haves:
  truths:
    - "Running `./tools/smoke-test/seamless-bridge-smoke.sh` exits 0 if and only if all 10 steps pass (6 [mech], 4 [human])."
    - "On any [mech] failure the script exits non-zero with the failure line echoed (D-06)."
    - "On any [human] step answered `n`, the script exits non-zero with the step description + 'report to the troubleshooting section of docs/seamless-bridge.md' message (D-06)."
    - "A full run transcript lands at `/tmp/forge-smoke-YYYYMMDD-HHMMSS.log` with git rev + timestamp in the header (D-05 + D-Claude's-Discretion item 6)."
    - "Phase 3 HUMAN-UAT Test 3 is folded in: step 5 probes bridge-reachable-after-boot, step 9 confirms no-orphan-after-quit (D-15)."
    - "Script is idempotent and non-destructive — safe to re-run on an already-installed workstation without --force (D-04)."
  artifacts:
    - path: "tools/smoke-test/seamless-bridge-smoke.sh"
      provides: "Hybrid [mech]+[human] E2E smoke test for the seamless bridge v6.3 loop"
      contains: "#!/usr/bin/env bash"
      min_lines: 180
      executable: true
  key_links:
    - from: "tools/smoke-test/seamless-bridge-smoke.sh (step 5)"
      to: "forge-bridge HTTP endpoint"
      via: "curl http://localhost:9999/"
      pattern: "curl -s http://localhost:9999"
    - from: "tools/smoke-test/seamless-bridge-smoke.sh (step 3)"
      to: "install.sh (real run)"
      via: "subprocess invocation"
      pattern: "\\./install\\.sh"
    - from: "tools/smoke-test/seamless-bridge-smoke.sh (step 4)"
      to: "/opt/Autodesk/shared/python/forge_bridge/scripts/forge_bridge.py"
      via: "ast.parse syntax check"
      pattern: "ast\\.parse"
    - from: "tools/smoke-test/seamless-bridge-smoke.sh (step 9)"
      to: "forge_bridge.py process (orphan check)"
      via: "pgrep"
      pattern: "pgrep.*forge_bridge"
    - from: "tools/smoke-test/seamless-bridge-smoke.sh (step 10)"
      to: "pytest suite"
      via: "subprocess invocation"
      pattern: "pytest -q"
    - from: "tools/smoke-test/seamless-bridge-smoke.sh (failure messaging)"
      to: "docs/seamless-bridge.md#troubleshooting"
      via: "stderr message string"
      pattern: "docs/seamless-bridge\\.md#troubleshooting"
---

<objective>
Create `tools/smoke-test/seamless-bridge-smoke.sh` — the hybrid shell-script + inline human checklist that closes DOC-01. The script mechanizes what can be automated (working-tree check, install.sh dry-run + live, forge_bridge.py sanity, curl probe, pgrep orphan check, pytest) and prompts the user inline for the Flame/Blender interactive steps (restart Flame, export camera, edit in Blender, click Send to Flame, verify in Flame, quit Flame).

Purpose: Close DOC-01. Give the project a single authoritative gate for cutting v6.3: the script exits 0 iff all 10 steps pass. Folds in Phase 3 HUMAN-UAT Test 3 (bridge reachable after boot + no orphan after quit) per D-15.

Output: One executable bash script. No code changes elsewhere. Idempotent, non-destructive, text-only interactive prompts. Script mirrors `install.sh`'s visual rhythm (`step`, `ok`, `warn`, `err` printer helpers; TTY-gated ANSI colors) and adds a `human` prompt helper per D-Claude's-Discretion item 5.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-e2e-validation-docs/04-CONTEXT.md
@.planning/phases/04-e2e-validation-docs/04-PATTERNS.md
@.planning/phases/03-forge-bridge-deploy/03-HUMAN-UAT.md
@install.sh

<interfaces>
<!-- Extracted from install.sh 2026-04-22. Smoke-test script replicates this visual rhythm and these printer shapes. -->

Shell preamble + colour helpers (install.sh:1-84, reproduce verbatim shape in the smoke test):
- Shebang `#!/usr/bin/env bash` + `set -euo pipefail`
- TTY-gated ANSI colour variables (`C_OK`, `C_WARN`, `C_ERR`, `C_DIM`, `C_END`) only set when `[[ -t 1 ]]`
- Five printer functions: `ok()`, `warn()`, `err()` (stderr), `step()`, `human()`

Curl probe idiom (install.sh:519-522, Done-section — use verbatim in step 5):
```
curl -s http://localhost:9999/ -o /dev/null -w "%{http_code}\n"
# expect: 200
```

y/n prompt idiom (CONTEXT D-Claude's-Discretion item 4 — shortest legible form):
```
read -r -p "  pass? [y/n] " ans
case "${ans,,}" in
  y|yes) ok "step passed" ;;
  *)     err "step failed — see docs/seamless-bridge.md#troubleshooting"
         FAILED_STEPS+=("<step description>")
         HUMAN_FAIL=1 ;;
esac
```

Post-condition guard pattern (install.sh:322-326 — adapt for final exit):
```
if (( HUMAN_FAIL )); then
  err "smoke test failed — the following human-verified steps did not pass:"
  for s in "${FAILED_STEPS[@]}"; do
    err "  - $s"
  done
  err "report to the troubleshooting section of docs/seamless-bridge.md"
  exit 1
fi
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Write tools/smoke-test/seamless-bridge-smoke.sh (preamble + helpers + transcript logging)</name>
  <files>tools/smoke-test/seamless-bridge-smoke.sh</files>
  <read_first>
    - install.sh lines 1-120 (shebang, set -euo pipefail, colour helpers, printer functions, run/eval wrapper)
    - install.sh lines 322-326 (post-condition guard pattern)
    - install.sh lines 519-522 (curl probe idiom — the verbatim command to reuse in step 5)
    - .planning/phases/04-e2e-validation-docs/04-PATTERNS.md section 1 (visual rhythm, helper functions, y/n idiom, idempotence rules, exit-on-error discipline)
    - .planning/phases/04-e2e-validation-docs/04-CONTEXT.md decisions D-01 through D-06 (hybrid shape, location, step list, idempotence, success criterion, failure mode)
    - .planning/phases/04-e2e-validation-docs/04-CONTEXT.md Claude's-Discretion items 4-7 (prompt form, ANSI colors, git-describe in header, lsof portable form — lsof is for the doc not the script)
  </read_first>
  <action>
First, `mkdir -p tools/smoke-test/` if needed (it does not exist yet — `tools/` has `blender/` and `fspy_import.py` only). Create `tools/smoke-test/seamless-bridge-smoke.sh`. Do NOT add a `README.md` inside `tools/smoke-test/` — the CONTEXT is docs-minimal; README.md at repo root + docs/seamless-bridge.md cover the "how to run this" story.

Write the script preamble (the first 70-100 lines of the final file). This task creates the framework that Task 2 fills with steps.

1. Shebang + strict mode (install.sh:29-30 pattern): `#!/usr/bin/env bash` then `set -euo pipefail`.

2. Header docstring (comments — same style as install.sh:1-28):
   - Purpose: "E2E smoke test for the seamless Flame↔Blender bridge (v6.3). Closes DOC-01."
   - Usage: `./tools/smoke-test/seamless-bridge-smoke.sh` (from repo root)
   - What it does: lists the 10 steps in plain language (mirrors the `step` block names from Task 2)
   - Output: transcript tee'd to `/tmp/forge-smoke-YYYYMMDD-HHMMSS.log`
   - Exit codes: `0` if all 10 steps pass; `1` on any [mech] failure or any [human] `n` answer
   - Non-destructive: does not touch `~/forge-bakes/`, `/opt/Autodesk/`, or require `--force`

3. TTY-gated colour variables + five printer functions. ANSI codes only set when `[[ -t 1 ]]`; otherwise empty strings. Printers:
   - `ok()` — green check prefix, stdout
   - `warn()` — yellow bang prefix, stdout
   - `err()` — red cross prefix, stderr
   - `step()` — dim `>` prefix + newline, section header
   - `human()` — yellow `[HUMAN]` prefix + blank line, for interactive prompts
   Keep the printf shapes identical to install.sh:81-84 (ok/warn/err/step) plus the new `human()` line per PATTERNS.md section 1.

4. Transcript logging (D-05) — AFTER the printer functions are defined but BEFORE the first `step` call:
   ```
   LOG="/tmp/forge-smoke-$(date +%Y%m%d-%H%M%S).log"
   exec > >(tee -a "$LOG") 2>&1
   ```
   This redirects all subsequent stdout/stderr through `tee` so the terminal shows live output AND the full run lands in `$LOG`.

5. Log header (D-Claude's-Discretion item 6) — print timestamp, log path, git rev, git describe, host uname. Trap git failures with `|| echo unknown` so the script runs outside a checkout too.

6. Failure-tracking globals:
   ```
   HUMAN_FAIL=0
   FAILED_STEPS=()
   ```
   These get consumed by the final-exit guard in Task 3.

7. Helper function `ask_human "description"` — encapsulates the y/n prompt idiom (D-Claude's-Discretion item 4: "pick whichever is shortest and most legible"). On `y`/`yes` it calls `ok "$desc"`. On anything else it calls `err`, appends to `FAILED_STEPS`, and sets `HUMAN_FAIL=1`. The `err` message format: `"$desc — see docs/seamless-bridge.md#troubleshooting"`.

**Do NOT** (per PATTERNS.md section 1 / Pattern to avoid):
- Source or import install.sh
- Add a `--force` or `--dry-run` flag to the smoke test itself
- Require root / sudo
- `rm -rf` anywhere
- Write to `/opt/Autodesk/` directly
- Delete `~/forge-bakes/` or any user-visible state

At the end of this task, `chmod +x tools/smoke-test/seamless-bridge-smoke.sh`. File should be 70-100 lines after this task; steps 1-10 arrive in Task 2.
  </action>
  <verify>
    <automated>test -f tools/smoke-test/seamless-bridge-smoke.sh && test -x tools/smoke-test/seamless-bridge-smoke.sh && head -2 tools/smoke-test/seamless-bridge-smoke.sh | grep -qF "#!/usr/bin/env bash" && head -5 tools/smoke-test/seamless-bridge-smoke.sh | grep -qF "set -euo pipefail" && grep -qF 'LOG="/tmp/forge-smoke-' tools/smoke-test/seamless-bridge-smoke.sh && grep -qF "ask_human" tools/smoke-test/seamless-bridge-smoke.sh && grep -qF "FAILED_STEPS=()" tools/smoke-test/seamless-bridge-smoke.sh && grep -qF "HUMAN_FAIL=0" tools/smoke-test/seamless-bridge-smoke.sh && bash -n tools/smoke-test/seamless-bridge-smoke.sh</automated>
  </verify>
  <acceptance_criteria>
    - `tools/smoke-test/` directory exists
    - `tools/smoke-test/seamless-bridge-smoke.sh` exists and is executable (+x)
    - Shebang is `#!/usr/bin/env bash`
    - `set -euo pipefail` appears before any other executable line
    - Five printer functions defined: `ok`, `warn`, `err`, `step`, `human`
    - ANSI colour variables gated on `[[ -t 1 ]]` (TTY-aware)
    - Transcript logging redirects to `/tmp/forge-smoke-YYYYMMDD-HHMMSS.log` via `tee` with `exec > >(tee -a ...)` pattern
    - Log header prints timestamp, log path, git rev (falls back to 'unknown'), git describe, host uname
    - `ask_human` helper defined with `read -r -p "  pass? [y/n] "` and records failures to `FAILED_STEPS`
    - `HUMAN_FAIL=0` and `FAILED_STEPS=()` initialized
    - No `--force` flag, no `rm -rf`, no sudo, no `/opt/Autodesk/` writes
    - `bash -n` syntax check passes (no subshell executed — script preamble must parse cleanly)
    - File is 70-100 lines at end of this task
  </acceptance_criteria>
  <done>Preamble + helpers + transcript logging in place, script is executable and syntax-valid, Task 2 can append the 10 steps without restructuring.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Append the 10 steps (6 [mech] + 4 [human]) and final-exit guard</name>
  <files>tools/smoke-test/seamless-bridge-smoke.sh</files>
  <read_first>
    - .planning/phases/04-e2e-validation-docs/04-CONTEXT.md D-03 (the 10 steps in order, with mechanization split)
    - .planning/phases/04-e2e-validation-docs/04-CONTEXT.md D-05, D-06, D-15 (success criterion + failure mode + HUMAN-UAT folding)
    - .planning/phases/04-e2e-validation-docs/04-PATTERNS.md section 1 "Section-header idiom" (step-block names that map to D-03 items)
    - install.sh lines 519-522 (curl probe idiom for step 5)
    - install.sh lines 425-426 (D-10 warning — acceptable-continue on step 3 per D-03 item 3)
    - tools/smoke-test/seamless-bridge-smoke.sh (existing preamble from Task 1)
  </read_first>
  <action>
Append the 10 mechanized/human steps to `tools/smoke-test/seamless-bridge-smoke.sh` in CONTEXT D-03 order. Each step opens with a `step "Name"` header. Names to use verbatim (from PATTERNS.md section 1):

1. `step "Working-tree clean"` — [mech]
2. `step "install.sh dry-run"` — [mech]
3. `step "install.sh live"` — [mech] (with D-10 warn-and-continue acceptance)
4. `step "forge_bridge.py sanity"` — [mech]
5. `step "Bridge reachable after Flame boot"` — [human] + [mech] (D-15 fold, part 1)
6. `step "Export Camera to Blender"` — [human]
7. `step "Send to Flame"` — [human]
8. `step "Camera appears in target Action"` — [human]
9. `step "No orphan bridge after Flame quit"` — [human] + [mech] (D-15 fold, part 2)
10. `step "pytest"` — [mech]

Final block: `step "Done"` + exit guard.

**Step 1 — Working-tree clean** (D-03 item 1):
`git status --porcelain` must be empty OR contain only `.claude/` entries. Recommended filter also excludes `.planning/phases/04-e2e-validation-docs/` (because the smoke test may be run during Phase 4 execution before the phase commits land). If `dirty` is non-empty after the exclusion filter, echo the remaining changes and `exit 1`. Otherwise `ok "working tree clean (ignoring .claude/ and in-phase .planning/)"`.

**Step 2 — install.sh dry-run** (D-03 item 2):
Run `./install.sh --dry-run` inside a conditional; on non-zero exit call `err` and `exit 1`. On zero exit call `ok`.

**Step 3 — install.sh live** (D-03 item 3):
Run `./install.sh` (no args). On non-zero `exit 1`. After the installer returns, grep the log (`$LOG`) for the D-10 warning literal `"forge-bridge install skipped"` — if present, call `warn "install.sh completed with forge-bridge install skipped (D-10 warn-and-continue); smoke test continues"`. This does NOT fail the step — D-10 warn-and-continue is acceptable per D-03 item 3.

**Step 4 — forge_bridge.py sanity** (D-03 item 4):
Set `BRIDGE_PY="/opt/Autodesk/shared/python/forge_bridge/scripts/forge_bridge.py"`. If the file does not exist:
- If the D-10 skip warning was recorded in step 3 (grep `$LOG` for `"forge-bridge install skipped"`), `warn` and move on (acceptable per D-03).
- Otherwise `err` + `exit 1`.
If the file exists, run `python3 -c "import ast, sys; ast.parse(open(sys.argv[1]).read())" "$BRIDGE_PY"`; on failure `err` + `exit 1`; on success `ok`.

**Step 5 — Bridge reachable after Flame boot** (D-03 item 5 + D-15 first half):
`human "Restart Flame now. When the batch view is up and the Camera Match hook has loaded, press Enter."` then `read -r _`. Then probe the bridge with the verbatim install.sh:519-522 command:
```
code=$(curl -s http://localhost:9999/ -o /dev/null -w "%{http_code}\n" || echo "000")
```
If `code == 200` call `ok "bridge reachable (HTTP $code)"`. Otherwise `err "bridge unreachable (HTTP $code) — see docs/seamless-bridge.md#troubleshooting recipe 1"`, append to `FAILED_STEPS`, set `HUMAN_FAIL=1`.

**Step 6 — Export Camera to Blender** (D-03 item 6):
`human "In Flame: right-click the Action → Camera Match → Export Camera to Blender. Blender should open on the baked camera (zero dialogs)."` then `ask_human "Export Camera to Blender"`.

**Step 7 — Send to Flame** (D-03 item 7):
`human "In Blender: edit the camera (move/rotate, scrub or add a keyframe). Open the N-panel (N key) → Forge tab → click 'Send to Flame'. Expect the success popup 'Sent to Flame: camera X in Action Y'."` then `ask_human "Send to Flame"`.

**Step 8 — Camera appears in target Action** (D-03 item 8):
`human "In Flame: verify the new camera appears in the target Action with keyframes preserved (scrub the batch timeline)."` then `ask_human "Camera appears in target Action"`.

**Step 9 — No orphan bridge after Flame quit** (D-03 item 9 + D-15 second half):
`human "Quit Flame now. When Flame is fully closed, press Enter."` then `read -r _`. Then `orphan=$(pgrep -f forge_bridge.py || true)`. If `orphan` is empty, `ok`. Otherwise `err "orphan forge_bridge.py process(es) still running: $orphan — see docs/seamless-bridge.md#troubleshooting recipe 5"`, append to `FAILED_STEPS`, set `HUMAN_FAIL=1`.

**Step 10 — pytest** (D-03 item 10):
Run `pytest -q` in a conditional; on non-zero `err` + `exit 1`; on success `ok "pytest -q all tests passed"`.

**Final-exit guard** (post-condition pattern from install.sh:322-326):
`step "Done"`. If `HUMAN_FAIL` is non-zero:
- `err "smoke test FAILED — the following human-verified steps did not pass:"`
- Loop `FAILED_STEPS` and `err "  - $s"` for each
- `err "report to the troubleshooting section of docs/seamless-bridge.md"` (verbatim — D-06 requires this message)
- `err "full transcript: $LOG"`
- `exit 1`

Otherwise:
- `ok "smoke test PASSED — all 10 steps green"`
- `ok "transcript: $LOG"`
- `exit 0`

**Idempotence check (D-04)** — add a comment at the top of the step block: `# All steps are idempotent + non-destructive (D-04).` Walk-through:
- Steps 1, 4, 5 (curl), 9 (pgrep), 10 (pytest): read-only.
- Steps 2: `--dry-run` — no side effects by design.
- Step 3: `install.sh` is idempotent (`rsync -a --delete`, `mkdir -p`, `cp` overwrite). Safe to re-run.
- Steps 6, 7, 8: human actions in Flame/Blender; no destructive script operations.

**Do NOT** (PATTERNS.md section 1 Pattern to avoid + CONTEXT D-04):
- Invoke `install.sh --force`
- `rm -rf ~/forge-bakes/` or any other delete
- Touch `/opt/Autodesk/` directly (only through `./install.sh`)
- Import install.sh (no `source install.sh`)
- Add pytest markers or fixtures (step 10 is a subprocess invocation; nothing is added to `tests/`)
- Abbreviate `[human]` prompt text — artists need the exact action described
  </action>
  <verify>
    <automated>bash -n tools/smoke-test/seamless-bridge-smoke.sh && grep -qF 'step "Working-tree clean"' tools/smoke-test/seamless-bridge-smoke.sh && grep -qF 'step "install.sh dry-run"' tools/smoke-test/seamless-bridge-smoke.sh && grep -qF 'step "install.sh live"' tools/smoke-test/seamless-bridge-smoke.sh && grep -qF 'step "forge_bridge.py sanity"' tools/smoke-test/seamless-bridge-smoke.sh && grep -qF 'step "Bridge reachable after Flame boot"' tools/smoke-test/seamless-bridge-smoke.sh && grep -qF 'step "Export Camera to Blender"' tools/smoke-test/seamless-bridge-smoke.sh && grep -qF 'step "Send to Flame"' tools/smoke-test/seamless-bridge-smoke.sh && grep -qF 'step "Camera appears in target Action"' tools/smoke-test/seamless-bridge-smoke.sh && grep -qF 'step "No orphan bridge after Flame quit"' tools/smoke-test/seamless-bridge-smoke.sh && grep -qF 'step "pytest"' tools/smoke-test/seamless-bridge-smoke.sh && grep -qF 'step "Done"' tools/smoke-test/seamless-bridge-smoke.sh && grep -qF "curl -s http://localhost:9999" tools/smoke-test/seamless-bridge-smoke.sh && grep -qF "pgrep -f forge_bridge.py" tools/smoke-test/seamless-bridge-smoke.sh && grep -qF "ast.parse" tools/smoke-test/seamless-bridge-smoke.sh && grep -qF "pytest -q" tools/smoke-test/seamless-bridge-smoke.sh && grep -qF "docs/seamless-bridge.md#troubleshooting" tools/smoke-test/seamless-bridge-smoke.sh && ! grep -qF "rm -rf" tools/smoke-test/seamless-bridge-smoke.sh && ! grep -qF "install.sh --force" tools/smoke-test/seamless-bridge-smoke.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n` syntax check passes (no runtime errors at parse)
    - All 11 `step "..."` headers present in order: Working-tree clean, install.sh dry-run, install.sh live, forge_bridge.py sanity, Bridge reachable after Flame boot, Export Camera to Blender, Send to Flame, Camera appears in target Action, No orphan bridge after Flame quit, pytest, Done
    - Step 5 uses the verbatim curl probe `curl -s http://localhost:9999/ -o /dev/null -w "%{http_code}\n"` (install.sh:519-522 idiom)
    - Step 9 uses `pgrep -f forge_bridge.py` for orphan detection
    - Step 4 uses `ast.parse` (via `python3 -c`) for syntax sanity
    - Step 10 runs `pytest -q` as subprocess
    - Step 3 records the D-10 "forge-bridge install skipped" warn-and-continue path (not a fail)
    - Final guard checks `HUMAN_FAIL` and emits both the FAILED_STEPS list AND the pointer `report to the troubleshooting section of docs/seamless-bridge.md` (D-06)
    - Exit 0 only on all-green (D-05); exit 1 on any mech failure or any human `n` (D-06)
    - No `rm -rf`, no `install.sh --force`, no sudo, no writes outside `/tmp/` (the log) and whatever install.sh itself writes
    - HUMAN-UAT Test 3 folded: step 5 = bridge-reachable-after-boot, step 9 = no-orphan-after-quit (D-15)
    - File is 180-280 lines total after this task
  </acceptance_criteria>
  <done>All 10 steps + final guard appended, `bash -n` parses cleanly, every grep assertion in verify passes, script is runnable end-to-end on a prepared workstation.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Cross-plan agreement + static verification sweep</name>
  <files>(read-only verification — no files modified)</files>
  <read_first>
    - tools/smoke-test/seamless-bridge-smoke.sh (just-written)
    - README.md (from Plan 01 Task 1 — may or may not exist yet if plans run in parallel)
    - docs/seamless-bridge.md (from Plan 01 Task 2 — may or may not exist yet if plans run in parallel)
  </read_first>
  <action>
This task is a lightweight cross-plan consistency + static-verification check. Plans 01 and 02 are in the same wave and share no files, but they refer to each other by path convention:

- Plan 01's README.md references `tools/smoke-test/seamless-bridge-smoke.sh` as the validation command.
- Plan 02's smoke test references `docs/seamless-bridge.md#troubleshooting` in its failure messages.

Verify both directions. If Plan 01 executed before Plan 02 (executor serialized), the README already mentions the smoke-test path. If Plan 01 has not yet run (parallel, Plan 02 lands first), skip the README-side check and note it in the SUMMARY so the execute-phase wrap-up can confirm after Plan 01 writes its files.

Run the following verification block:

1. Plan-02-owned invariant — smoke test must reference the doc anchor (D-06 requires the exact pointer message):
   ```
   grep -qF "docs/seamless-bridge.md#troubleshooting" tools/smoke-test/seamless-bridge-smoke.sh
   ```

2. Cross-plan invariant — if README exists, it must reference this plan's script path:
   ```
   if [[ -f README.md ]]; then
     grep -qF "tools/smoke-test/seamless-bridge-smoke.sh" README.md
   fi
   ```
   If README does not exist yet, echo: `INFO: README.md not yet written — cross-link check deferred to Plan 01 verification.`

3. Cross-plan invariant — if docs/seamless-bridge.md exists, it must have a Troubleshooting anchor for the script's failure messages to link against:
   ```
   if [[ -f docs/seamless-bridge.md ]]; then
     grep -qF "## Troubleshooting" docs/seamless-bridge.md
   fi
   ```

4. Idempotence + non-destructive audit (D-04) — all must be absent:
   ```
   ! grep -qF "rm -rf" tools/smoke-test/seamless-bridge-smoke.sh
   ! grep -qF "install.sh --force" tools/smoke-test/seamless-bridge-smoke.sh
   ! grep -qF "sudo " tools/smoke-test/seamless-bridge-smoke.sh
   ! grep -qF "> /opt/Autodesk/" tools/smoke-test/seamless-bridge-smoke.sh
   ```

5. Executability + syntax:
   ```
   test -x tools/smoke-test/seamless-bridge-smoke.sh
   bash -n tools/smoke-test/seamless-bridge-smoke.sh
   ```

This task does NOT execute the smoke test itself — the smoke test requires a live Flame + Blender + forge-bridge stack. Static verification (syntax + grep anchors + idempotence audit) is what Plan 02 can assert; the actual runtime validation is what the artist/TD performs when they run the script to close DOC-01.

Record all check results in the plan SUMMARY so the checker has evidence.
  </action>
  <verify>
    <automated>bash -n tools/smoke-test/seamless-bridge-smoke.sh && test -x tools/smoke-test/seamless-bridge-smoke.sh && grep -qF "docs/seamless-bridge.md#troubleshooting" tools/smoke-test/seamless-bridge-smoke.sh && ! grep -qF "rm -rf" tools/smoke-test/seamless-bridge-smoke.sh && ! grep -qF "install.sh --force" tools/smoke-test/seamless-bridge-smoke.sh && ! grep -qF "sudo " tools/smoke-test/seamless-bridge-smoke.sh && ! grep -qF "> /opt/Autodesk/" tools/smoke-test/seamless-bridge-smoke.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n` returns 0 (script parses)
    - Script is executable (+x)
    - Script contains `docs/seamless-bridge.md#troubleshooting` reference (D-06 pointer message)
    - Idempotence sweep: no `rm -rf`, no `install.sh --force`, no `sudo `, no `> /opt/Autodesk/` write redirects
    - If README.md exists: grep-matches `tools/smoke-test/seamless-bridge-smoke.sh`
    - If docs/seamless-bridge.md exists: has `## Troubleshooting` H2
    - Cross-plan deferrals recorded in SUMMARY when one plan finishes before the other
  </acceptance_criteria>
  <done>All static checks pass. Cross-plan invariants either confirmed (both plans done) or deferred with SUMMARY note.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → smoke-test script | User invokes the script with their own shell privileges; script does not escalate (no sudo). |
| smoke-test script → subprocesses (`install.sh`, `curl`, `pgrep`, `pytest`, `python3`) | Script invokes well-known local tools and the in-repo installer; no untrusted network input consumed. |
| smoke-test script → transcript log at `/tmp/` | Script writes a world-readable (default `umask 022`) transcript to `/tmp`; no secrets expected in the transcript (install.sh does not print tokens). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | Tampering | `install.sh` invoked twice (steps 2 + 3) | accept | install.sh is idempotent (`rsync -a --delete`, `mkdir -p`, `cp` overwrite); re-invocation produces the same filesystem state. Step 2 is `--dry-run` so it cannot tamper. |
| T-04-06 | Denial of Service | `pgrep -f forge_bridge.py` in step 9 | accept | Read-only process scan; no kill issued. User-initiated. Internal single-user tool per CLAUDE.md security posture. |
| T-04-07 | Information Disclosure | Transcript log at `/tmp/forge-smoke-*.log` | accept | Log contains install.sh output (deploy paths, not secrets) + pytest output. Default `/tmp` perms are fine for single-user workstation. Users in shared-workstation contexts already carry this risk via any shell history. |
| T-04-08 | Elevation of Privilege | Script runs with user's shell — no sudo, no setuid | accept (N/A) | Script never escalates. Any privileged operations (write to `/opt/Autodesk/`) are delegated to `install.sh` which handles its own prompting. |
| T-04-09 | Repudiation | Human `y/n` answers not cryptographically attested | accept | Internal tool; the transcript log + git rev in the header is sufficient audit for milestone-cut decisions. No compliance surface. |
</threat_model>

<verification>
Phase-wide plan checks (run at end of plan, before commit):

1. **Artifact presence + executability:**
   - `test -f tools/smoke-test/seamless-bridge-smoke.sh`
   - `test -x tools/smoke-test/seamless-bridge-smoke.sh`

2. **Syntax:**
   - `bash -n tools/smoke-test/seamless-bridge-smoke.sh` returns 0

3. **Step coverage — all 10 CONTEXT D-03 steps + Done header:**
   - 11 grep-F assertions (see Task 2 verify block) — all must match

4. **Idempotence + non-destructive (D-04):**
   - No `rm -rf`, no `install.sh --force`, no `sudo `, no direct writes to `/opt/Autodesk/`

5. **HUMAN-UAT fold-in (D-15):**
   - `grep -F "curl -s http://localhost:9999" tools/smoke-test/seamless-bridge-smoke.sh` (step 5, D-15 first half)
   - `grep -F "pgrep -f forge_bridge.py" tools/smoke-test/seamless-bridge-smoke.sh` (step 9, D-15 second half)

6. **Failure-mode pointer (D-06):**
   - `grep -F "docs/seamless-bridge.md#troubleshooting" tools/smoke-test/seamless-bridge-smoke.sh`
   - `grep -F "report to the troubleshooting section of docs/seamless-bridge.md" tools/smoke-test/seamless-bridge-smoke.sh`

7. **Scope discipline (Phase 4 is docs + validation only):**
   - `git diff --name-only` adds ONLY `tools/smoke-test/seamless-bridge-smoke.sh` (plus any Plan-01-owned files if wave ran in parallel).
   - `git diff install.sh` → empty
   - `git diff forge_core/ forge_flame/ flame/ tools/blender/ tests/` → empty

8. **The smoke test is NOT executed in this plan** — DOC-01 closure is a RUNTIME event that the artist/TD performs. Plan 02 delivers the artifact; Phase 4 SUMMARY/VERIFICATION documents the first successful live run (which is the actual DOC-01 acceptance evidence).
</verification>

<success_criteria>
Plan 02 succeeds when:

- [ ] `tools/smoke-test/seamless-bridge-smoke.sh` exists, is executable, and passes `bash -n`
- [ ] All 10 CONTEXT D-03 steps present in order with correct `step "..."` headers
- [ ] Step 5 uses the verbatim install.sh:519-522 curl probe
- [ ] Step 9 uses `pgrep -f forge_bridge.py`
- [ ] Step 3 accepts D-10 "forge-bridge install skipped" warn-and-continue (does not fail the step)
- [ ] Final-exit guard emits FAILED_STEPS list + "report to the troubleshooting section of docs/seamless-bridge.md" on any human-n (D-06)
- [ ] Script is idempotent and non-destructive per D-04 (no rm -rf, no --force, no sudo, no direct /opt writes)
- [ ] Transcript logs to `/tmp/forge-smoke-YYYYMMDD-HHMMSS.log` with git rev in header (D-05 + discretion item 6)
- [ ] Phase 3 HUMAN-UAT Test 3 folded into steps 5 + 9 (D-15)
- [ ] Cross-plan invariants: smoke test points at doc anchor; README (if present) points at smoke-test path
- [ ] DOC-01 artifact delivered (actual DOC-01 closure is the first green live run, recorded in phase VERIFICATION.md — not in this plan)
</success_criteria>

<output>
After plan completion, create `.planning/phases/04-e2e-validation-docs/04-02-SUMMARY.md` with:
- Artifact created (`tools/smoke-test/seamless-bridge-smoke.sh`) with line count and `bash -n` status
- Step-coverage grep sweep output (all 11 `step "..."` headers matched)
- Idempotence audit output (all 4 negative-grep assertions pass)
- HUMAN-UAT Test 3 fold-in evidence (step 5 curl + step 9 pgrep anchors)
- Cross-plan invariant status: README reference found / deferred; doc Troubleshooting anchor found / deferred
- DOC-01 delivery status: "artifact in place; runtime closure pending first green live run"
- Pointer: `forge-calibrator/tools/smoke-test/seamless-bridge-smoke.sh`
</output>
