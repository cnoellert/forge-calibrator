---
phase: 04-e2e-validation-docs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - docs/seamless-bridge.md
autonomous: true
requirements: [DOC-02]
tags: [docs, user-facing, grep-anchor, v6.3]

must_haves:
  truths:
    - "A new user reading README.md understands what forge-calibrator does, what changed in v6.3, and how to install it in under 60 seconds."
    - "An artist who sees a Tier 1/2/3 error popup can Ctrl-F the verbatim error text and land on the matching troubleshooting recipe in docs/seamless-bridge.md."
    - "A pipeline TD reading docs/seamless-bridge.md#install can deploy the hook + bridge + Blender addon without consulting any other doc."
    - "Every troubleshooting recipe Symptom heading grep-matches the verbatim literal in its source file (4 grep checks pass; recipe 5 shares recipe 1's anchor)."
    - "README.md links to PASSOFF.md for v4→v6.2 history and to docs/seamless-bridge.md for install/troubleshooting deep-dives — no duplication."
  artifacts:
    - path: "README.md"
      provides: "Repo-root overview + v6.3 what's new + install summary + validation pointer + history pointer + troubleshooting link"
      contains: "# forge-calibrator"
      min_lines: 40
    - path: "docs/seamless-bridge.md"
      provides: "Canonical user guide: overview, install (TD + artist subsections), autostart explanation, Send-to-Flame walkthrough, 5 troubleshooting recipes"
      contains: "## Troubleshooting"
      min_lines: 90
  key_links:
    - from: "README.md"
      to: "docs/seamless-bridge.md"
      via: "relative markdown links"
      pattern: "docs/seamless-bridge\\.md"
    - from: "README.md"
      to: "PASSOFF.md"
      via: "relative markdown link in History section"
      pattern: "PASSOFF\\.md"
    - from: "docs/seamless-bridge.md (recipe 1 Symptom)"
      to: "tools/blender/forge_sender/__init__.py:240"
      via: "grep-anchor"
      pattern: "forge-bridge not reachable at http://127\\.0\\.0\\.1:9999"
    - from: "docs/seamless-bridge.md (recipe 2 Symptom)"
      to: "tools/blender/forge_sender/preflight.py:54"
      via: "grep-anchor"
      pattern: "active camera is missing"
    - from: "docs/seamless-bridge.md (recipe 3 Symptom)"
      to: "tools/blender/forge_sender/transport.py:220"
      via: "grep-anchor"
      pattern: "Send to Flame failed:"
    - from: "docs/seamless-bridge.md (recipe 4 Symptom)"
      to: "install.sh:425"
      via: "grep-anchor"
      pattern: "forge-bridge install skipped"
---

<objective>
Create `README.md` (repo root) and `docs/seamless-bridge.md` — the two user-facing doc artifacts that close DOC-02. README is the thin overview/what's-new/install-summary. `docs/seamless-bridge.md` is the canonical user guide with install (TD + artist subsections), autostart explanation, Send-to-Flame walkthrough, and 5 grep-anchored troubleshooting recipes.

Purpose: Close DOC-02. Give artists a Ctrl-F-able troubleshooting guide wired to the exact popup/install-output strings they see at runtime. Give pipeline TDs a single-file walkthrough of the install flow. Give new readers a 60-second orientation from the repo root.

Output: Two markdown files. No code changes. No screenshots/GIFs/video (D-11). No migration section (D-17). All cross-links are relative markdown (no github.com URLs).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-e2e-validation-docs/04-CONTEXT.md
@.planning/phases/04-e2e-validation-docs/04-PATTERNS.md
@PASSOFF.md
@tools/blender/forge_sender/__init__.py
@tools/blender/forge_sender/preflight.py
@tools/blender/forge_sender/transport.py
@install.sh

<interfaces>
<!-- Verbatim error/warning strings the doc MUST grep-match. Extracted from sources 2026-04-22. -->
<!-- Executors: use these EXACT strings as Symptom headings. Do NOT paraphrase. -->

From `tools/blender/forge_sender/__init__.py:240-242` (Tier 2 popup — recipe 1 + recipe 5 share this):
```
Send to Flame: forge-bridge not reachable at http://127.0.0.1:9999 — is Flame running with the Camera Match hook loaded?
```

From `tools/blender/forge_sender/preflight.py:52-57` (Tier 1 popup — recipe 2):
```
Send to Flame: active camera is missing 'forge_bake_action_name' — this camera was not baked by forge-calibrator. Re-export from Flame via right-click → Camera Match → Export Camera to Blender
```
(The `{key}` slot is `'forge_bake_action_name'` OR `'forge_bake_camera_name'` — preflight names only the first-missing key per preflight.py:27-31.)

From `tools/blender/forge_sender/transport.py:220` (Tier 3 envelope — recipe 3):
```
Send to Flame failed: {error}

{traceback}
```
IMPORTANT: Source reads `"Send to Flame failed:"` NOT `"Send failed:"` (the CONTEXT D-12.3 prose paraphrased; source is authoritative per D-14).

From `install.sh:425-426` (D-10 warning — recipe 4):
```
[WARN] forge-bridge install skipped (sibling installer exited non-zero). VP-solve and v6.2 static round-trip still work.
```
(The `(%s)` slot fills from `$BRIDGE_FAIL_REASON`; `sibling installer exited non-zero` is the most common value per install.sh:382, 389.)

From `install.sh:519-522` (Done-section bridge probe — reuse verbatim in README Validation section + doc autostart section):
```
curl -s http://localhost:9999/ -o /dev/null -w "%{http_code}\n"
# expect: 200
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Write README.md (repo-root overview)</name>
  <files>README.md</files>
  <read_first>
    - .planning/PROJECT.md §What This Is + §Core Value (source copy per D-08 items 1-2)
    - PASSOFF.md (tone reference — direct, names the thing, no jargon softening)
    - .planning/phases/04-e2e-validation-docs/04-CONTEXT.md §D-07, §D-08, §D-16, §D-17 (structure + what's new + migration-free)
    - .planning/phases/04-e2e-validation-docs/04-PATTERNS.md §2 (README.md analog + tone excerpt + H2-ordering rules)
  </read_first>
  <action>
Create `README.md` at repo root. Follow CONTEXT D-08 structure exactly (7 sections, H2 level, in this order):

1. **H1** `# forge-calibrator`
2. **H2** `## What this is` — adapted from PROJECT.md:3-5 §What This Is. Keep to ≤3 sentences. Use the exact voice from PROJECT.md. No internal jargon (no "PyActionNode", no "duck-typing", no "namespace-package"). OK to name "Autodesk Flame", "Flame Action node", "Blender".
3. **H2** `## Core value` — a **bold** one-sentence statement verbatim from PROJECT.md:9: **"The solved camera must be geometrically faithful to the plate, and the Flame↔Blender round-trip must preserve that fidelity end-to-end."** Per D-08 item 2 this is verbatim; per PATTERNS.md §2 content excerpt, bold it.
4. **H2** `## What's new in v6.3` — 3 bullets, user-facing capability language per D-Claude's-Discretion last item + D-16:
   - Seamless Flame↔Blender round-trip (no trip to Flame's batch menu for the return leg)
   - forge-bridge autostart (no manual bridge management)
   - Blender "Send to Flame" addon (one-click return trip from the 3D viewport N-panel)
   Each bullet links to `docs/seamless-bridge.md` with a relative link. Do NOT name internal components (`forge_sender`, `camera_match_hook`, `forge_flame.fbx_ascii`) in these bullets.
5. **H2** `## Install` — collapsed summary per D-08 item 4. Three bullets + a "See also" pointer:
   - Run `./install.sh` (macOS/Linux; requires the forge conda env)
   - Install the Blender addon: zip `tools/blender/forge_sender/` → Blender Preferences → Add-ons → Install from file → enable
   - See [docs/seamless-bridge.md#install](docs/seamless-bridge.md#install) for the detailed walkthrough
   Per D-17: no separate "Migration" or "Upgrading from v6.2" section — the what's-new bullets + install.sh idempotence are the release notes.
6. **H2** `## Validation` — 1 paragraph + the smoke-test pointer. Name the script path `tools/smoke-test/seamless-bridge-smoke.sh` (Plan 02's path — agreed convention). One-line command: `./tools/smoke-test/seamless-bridge-smoke.sh`. State that the script exits 0 only if every step passes.
7. **H2** `## History` — one paragraph pointer to `PASSOFF.md` with a relative link `[PASSOFF.md](PASSOFF.md)`. State that it covers the v4→v6.2 backstory and session recaps.
8. **H2** `## Troubleshooting` — one-line pointer: "See [docs/seamless-bridge.md#troubleshooting](docs/seamless-bridge.md#troubleshooting)." Don't duplicate recipes here.

**Tone per PATTERNS.md §Shared Patterns / Tone:** direct, names the thing, offers the fix. No "state-of-the-art", no "leveraging", no marketing language. No emojis.

**Link discipline per CONTEXT <specifics>:** relative markdown links only. Never github.com URLs. `[install section](docs/seamless-bridge.md#install)` is the idiom.

**Length:** 40-80 lines total. If you're past 100, you're over-explaining — trim to the overview + pointers shape.

**Do NOT:**
- Duplicate content from `docs/seamless-bridge.md` (README points, doc explains)
- Add screenshots, GIFs, badges, CI banners (D-11)
- Add a "Changelog" or "Contributing" or "License" section (out of scope for v1)
- Name internal Python modules in the What's-new bullets (D-Claude's-Discretion last item)
- Link to github.com or any absolute URL
  </action>
  <verify>
    <automated>test -f README.md &amp;&amp; grep -q "^# forge-calibrator" README.md &amp;&amp; grep -qF "geometrically faithful to the plate" README.md &amp;&amp; grep -qF "What's new in v6.3" README.md &amp;&amp; grep -qF "docs/seamless-bridge.md" README.md &amp;&amp; grep -qF "PASSOFF.md" README.md &amp;&amp; grep -qF "tools/smoke-test/seamless-bridge-smoke.sh" README.md</automated>
  </verify>
  <acceptance_criteria>
    - README.md exists at repo root
    - H1 is `# forge-calibrator`
    - Contains verbatim core-value sentence "The solved camera must be geometrically faithful to the plate, and the Flame↔Blender round-trip must preserve that fidelity end-to-end."
    - 7 H2 sections in CONTEXT D-08 order: What this is, Core value, What's new in v6.3, Install, Validation, History, Troubleshooting
    - What's-new section has exactly 3 bullets, none of which name `forge_sender`, `camera_match_hook`, or other internal modules
    - Install section has 3 bullets (install.sh, Blender addon, see-also) — NOT a detailed walkthrough
    - Validation section references `tools/smoke-test/seamless-bridge-smoke.sh` (same path Plan 02 creates)
    - History section links to `PASSOFF.md` via relative link
    - Troubleshooting section is a one-line pointer to `docs/seamless-bridge.md#troubleshooting` — no recipes duplicated
    - No github.com URLs in any link
    - No screenshot/GIF/video references
    - No "Migration" or "Upgrading" section
    - No emojis
  </acceptance_criteria>
  <done>README.md exists, renders as a clean overview+pointer doc, all grep assertions pass, 40-80 line range.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Write docs/seamless-bridge.md (canonical user guide)</name>
  <files>docs/seamless-bridge.md</files>
  <read_first>
    - .planning/phases/04-e2e-validation-docs/04-CONTEXT.md §D-09, §D-10, §D-11, §D-12, §D-13, §D-14 (structure + audience split + 5 recipes + format + grep-anchor discipline)
    - .planning/phases/04-e2e-validation-docs/04-PATTERNS.md §3 (full grep-anchor verbatim copy for all 4 source-anchored recipes + recipe format template)
    - tools/blender/forge_sender/__init__.py:240-242 (recipe 1 source)
    - tools/blender/forge_sender/preflight.py:52-57 (recipe 2 source)
    - tools/blender/forge_sender/transport.py:215-220 (recipe 3 source — note: "Send to Flame failed:" NOT "Send failed:")
    - install.sh:425-426 (recipe 4 source)
    - .planning/phases/02-blender-addon/02-CONTEXT.md §D-14 (N-panel tab name is "Forge")
    - .planning/phases/03-forge-bridge-deploy/03-CONTEXT.md §D-10 (warning copy source)
    - memory/flame_bridge.md (informs recipe 3 traceback explanation)
    - memory/flame_module_reload.md (informs autostart explanation — Flame restart required for hook changes)
  </read_first>
  <action>
Create `docs/seamless-bridge.md`. First ensure `docs/` directory exists (mkdir -p if needed). Follow CONTEXT D-09 structure (5 top-level H2 sections in this order):

**H1:** `# Seamless Flame↔Blender bridge`

**H2 `## Overview`** — 1 paragraph (3-5 sentences) per D-09 item 1. Name the three moving parts:
1. Flame hook (`camera_match_hook.py`) — bakes the Action camera to Blender on right-click
2. forge-bridge — 127.0.0.1:9999 HTTP endpoint into Flame's Python, auto-starts with Flame
3. Blender "Send to Flame" addon (N-panel → Forge tab) — posts the edited camera back to the target Action

State the user-facing outcome: right-click Action → edit camera in Blender → click Send to Flame → camera appears in the target Action with keyframes preserved. No alt-tab to Flame's batch menu for the return trip.

**H2 `## Install`** with two H3 subsections per D-10 (audience split inside one file — NOT separate files):

**H3 `### For pipeline TDs`**
Walk through the `./install.sh` flow:
- Preflight checks: forge conda env, Wiretap CLI, PyOpenColorIO from Flame's bundled Python, OCIO config resolvable
- What deploys where (per install.sh section headers — these are grep-able): `> forge_core + forge_flame` → `/opt/Autodesk/shared/python/forge_core` + `forge_flame`; `> forge-bridge` → `/opt/Autodesk/shared/python/forge_bridge/`; `> Install` → `/opt/Autodesk/shared/python/camera_match/`; `> tools/blender` → `/opt/Autodesk/shared/python/tools/blender/`
- Note `FORGE_BRIDGE_REPO=<path>` env var for offline/air-gapped installs (reference recipe 4 for the recovery path)
- Note `FORGE_BRIDGE_VERSION` pins the bridge version

State that install.sh is idempotent — safe to re-run; `--force` only needed for overwriting an existing install.

**H3 `### For artists`** — numbered list, 4-5 steps:
1. Zip `tools/blender/forge_sender/` (or use the pre-built zip if provided by your pipeline TD)
2. Blender → Edit → Preferences → Add-ons → Install from file → pick the zip
3. Enable "Forge Sender" in the Add-ons list
4. Open the 3D viewport's N-panel (press N) — you should see a **"Forge"** tab with a "Send to Flame" button
5. No further setup — forge-bridge starts automatically when Flame boots

Use plain UI-step language. Name the N-panel tab `Forge` explicitly (Phase 2 D-14). No screenshots.

**H2 `## How forge-bridge autostart works`** — 1-2 paragraphs per D-09 item 3:
- Flame boots → loads `camera_match_hook.py` → hook spawns `forge_bridge.py` as a subprocess → bridge binds `127.0.0.1:9999`
- Bridge dies automatically when Flame quits (lifecycle tied to Flame session — no orphan processes, no launchd/systemd)
- Bridge binds `127.0.0.1` only; never exposed to the network (BRG-03)
- No user action required — restarting Flame is the only way to restart the bridge (hook changes require a Flame restart; this is documented in `memory/flame_module_reload.md`)
- Include the probe command verbatim from install.sh:519-522:
  ```
  curl -s http://localhost:9999/ -o /dev/null -w "%{http_code}\n"
  # expect: 200
  ```

**H2 `## Using Send to Flame`** — happy-path walkthrough per D-09 item 4:
Number the steps:
1. In Flame: right-click the target Action → Camera Match → Export Camera to Blender. Blender opens on the baked camera (zero dialogs; no WxH prompt, no save-path prompt per Phase 1 EXP-01).
2. In Blender: edit the camera — move it, rotate it, scrub or add keyframes as needed.
3. Open the N-panel (N key) → **Forge** tab → click **Send to Flame**.
4. Success popup: `Sent to Flame: camera <name> in Action <action-name>`. The new camera appears in the target Action in Flame with keyframes preserved.

State the user never has to alt-tab to Flame's menu to trigger the import (IMP-06). If the popup shows an error, jump to [Troubleshooting](#troubleshooting).

**H2 `## Troubleshooting`** — 5 recipes in CONTEXT D-12 order. Each recipe uses the D-13 three-section format (H3 Symptom / **Likely cause** / **Fix**).

**CRITICAL:** Every Symptom heading is VERBATIM from the source file. Do NOT paraphrase. Do NOT word-wrap mid-string. Recipe 3 uses `"Send to Flame failed:"` (NOT `"Send failed:"` — CONTEXT D-12.3 paraphrased; source is authoritative per D-14).

**Recipe 1** — bridge not reachable (grep anchor: `__init__.py:240`):
```markdown
### Symptom: Send to Flame: forge-bridge not reachable at http://127.0.0.1:9999 — is Flame running with the Camera Match hook loaded?

**Likely cause:** Flame is not running, or forge-bridge did not start when Flame booted.

**Fix:**
1. Confirm Flame is running.
2. Probe the bridge: `curl -s http://localhost:9999/ -o /dev/null -w "%{http_code}\n"` — expect `200`.
3. If the probe returns non-200 or fails, restart Flame (the bridge starts as a Flame-spawned subprocess).
```

**Recipe 2** — missing metadata (grep anchor: `preflight.py:54`):
```markdown
### Symptom: Send to Flame: active camera is missing 'forge_bake_action_name' — this camera was not baked by forge-calibrator. Re-export from Flame via right-click → Camera Match → Export Camera to Blender

**Likely cause:** The active Blender camera was not baked from Flame, or was baked by an older tool that does not stamp metadata. (The error may name `'forge_bake_camera_name'` instead — the missing-key slot surfaces whichever stamped property is absent first.)

**Fix:**
1. In Flame: right-click the target Action → Camera Match → Export Camera to Blender.
2. Use the freshly baked camera in Blender; the stamped metadata is attached automatically.
```

**Recipe 3** — send failure (grep anchor: `transport.py:220` — "Send to Flame failed:"):
```markdown
### Symptom: Send to Flame failed: {error}

{traceback}

**Likely cause:** The Flame-side import crashed, the FBX parse failed, or the target Action was renamed/deleted after the bake.

**Fix:**
1. Read the top line of the traceback — it names the underlying error.
2. Most common cause: the Action was renamed after the bake. Re-bake from the current Action name (right-click Action → Camera Match → Export Camera to Blender).
3. Second most common: the Action has two cameras with the same name. Rename one to disambiguate, then re-send.
```

**Recipe 4** — install skipped (grep anchor: `install.sh:425`):
```markdown
### Symptom: [WARN] forge-bridge install skipped (sibling installer exited non-zero). VP-solve and v6.2 static round-trip still work.

**Likely cause:** `install.sh` could not find a local forge-bridge clone AND the curl-fallback to `raw.githubusercontent.com` failed (usually offline or firewalled).

**Fix:**
- If you have a local forge-bridge clone: `FORGE_BRIDGE_REPO=<path-to-clone> ./install.sh`
- If you have network access: ensure `raw.githubusercontent.com` is reachable, then re-run `./install.sh`.
- VP-solve and the v6.2 static-camera round-trip continue to work without the bridge; only Send-to-Flame is affected.
```

**Recipe 5** — port 9999 conflict (shares recipe 1's Symptom per PATTERNS.md §3):
```markdown
### Symptom: Send to Flame: forge-bridge not reachable at http://127.0.0.1:9999 — is Flame running with the Camera Match hook loaded? (and `lsof -i :9999` shows a non-forge-bridge process listening)

**Likely cause:** Another process (a stale forge-bridge from a previous Flame session that did not shut down cleanly, or an unrelated service) is holding port 9999.

**Fix:**
1. Find the PID: `lsof -i :9999`
2. Kill it: `kill <pid>`
3. Restart Flame — the bridge starts cleanly on the freed port.
4. If the conflict recurs, check for a second Flame instance running on the same workstation.
```

Per D-Claude's-Discretion item 7: `lsof -i :9999` is the more legible form across macOS + Linux. Use it. Do not invent a `systemctl` or `launchctl` variant.

**Footer:** single line `[back to README](../README.md)`.

**Do NOT:**
- Add screenshots, GIFs, video (D-11)
- Use any internal jargon: no `PyActionNode`, `PyAttribute`, `ast.parse`, `namespace-package`, `duck-typing`
- Paraphrase any Symptom heading — grep-anchor discipline per D-14 requires verbatim match
- Add a "See also" appendix or generic "Before you troubleshoot" preamble (simplicity per D-11 + PATTERNS.md §Simplicity preference)
- Link to github.com URLs except inside recipe 4's Fix block (that's the literal retry command from install.sh:425-426 — reproduce it verbatim)
- Invent new install.sh section header names — use the actual ones (`> Install`, `> forge-bridge`, `> forge_core + forge_flame`, `> tools/blender`)
- Add H4 or deeper headings without reason
  </action>
  <verify>
    <automated>test -f docs/seamless-bridge.md &amp;&amp; grep -q "^# Seamless" docs/seamless-bridge.md &amp;&amp; grep -qF "forge-bridge not reachable at http://127.0.0.1:9999" docs/seamless-bridge.md &amp;&amp; grep -qF "active camera is missing" docs/seamless-bridge.md &amp;&amp; grep -qF "Send to Flame failed:" docs/seamless-bridge.md &amp;&amp; grep -qF "forge-bridge install skipped" docs/seamless-bridge.md &amp;&amp; grep -qF "lsof -i :9999" docs/seamless-bridge.md &amp;&amp; grep -qF "Forge" docs/seamless-bridge.md &amp;&amp; grep -qF "### For pipeline TDs" docs/seamless-bridge.md &amp;&amp; grep -qF "### For artists" docs/seamless-bridge.md &amp;&amp; grep -qF "## How forge-bridge autostart works" docs/seamless-bridge.md &amp;&amp; grep -qF "## Using Send to Flame" docs/seamless-bridge.md &amp;&amp; grep -qF "## Troubleshooting" docs/seamless-bridge.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/seamless-bridge.md exists (docs/ directory created)
    - H1 is `# Seamless Flame↔Blender bridge` (or close variant with the ↔ character and "Seamless" prefix)
    - 5 top-level H2 sections in D-09 order: Overview, Install, How forge-bridge autostart works, Using Send to Flame, Troubleshooting
    - Install section has two H3 subsections: `### For pipeline TDs` + `### For artists` (D-10 audience split in one file)
    - For artists section names "Forge" as the N-panel tab (Phase 2 D-14)
    - Autostart section includes the verbatim curl probe command `curl -s http://localhost:9999/ -o /dev/null -w "%{http_code}\n"` and states expected 200
    - 5 troubleshooting recipes in D-12 order
    - Every recipe has three sections: Symptom (H3), Likely cause, Fix (D-13 format)
    - Recipe 1 Symptom contains verbatim `forge-bridge not reachable at http://127.0.0.1:9999 — is Flame running with the Camera Match hook loaded?`
    - Recipe 2 Symptom contains verbatim `active camera is missing 'forge_bake_action_name'` (substring — either stamped key is OK since preflight names only the first-missing)
    - Recipe 3 Symptom contains verbatim `Send to Flame failed:` (NOT `Send failed:` — source override per D-14)
    - Recipe 4 Symptom contains verbatim `forge-bridge install skipped`
    - Recipe 5 Symptom shares recipe 1's anchor AND adds `lsof -i :9999` diagnostic
    - Grep-verification pass: all four source files contain the recipe anchors (enforced by per-recipe grep, see Task 3)
    - No screenshots, GIFs, video references
    - No internal jargon (no PyActionNode / PyAttribute / duck-typing / namespace-package / ast.parse)
    - Footer has `[back to README](../README.md)` relative link
    - 90-200 lines total (over 200 suggests over-explanation; trim)
    - No emojis
  </acceptance_criteria>
  <done>docs/seamless-bridge.md exists, all 13 grep assertions in the automated verify pass, all 5 recipes are grep-anchored to their source files.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Grep-anchor verification sweep</name>
  <files>(read-only verification — no files modified)</files>
  <read_first>
    - Just-created README.md and docs/seamless-bridge.md
  </read_first>
  <action>
Run the grep-anchor verification pass per CONTEXT D-14 + PATTERNS.md §Shared Patterns / Grep-anchor discipline. This task is mandatory and blocks plan completion.

For each of the 4 source-anchored Symptom headings, run BOTH directions of the grep (source has the string; doc has the string):

```bash
# Recipe 1
grep -F "forge-bridge not reachable at http://127.0.0.1:9999" tools/blender/forge_sender/__init__.py
grep -F "forge-bridge not reachable at http://127.0.0.1:9999" docs/seamless-bridge.md

# Recipe 2
grep -F "active camera is missing" tools/blender/forge_sender/preflight.py
grep -F "active camera is missing" docs/seamless-bridge.md

# Recipe 3
grep -F "Send to Flame failed:" tools/blender/forge_sender/transport.py
grep -F "Send to Flame failed:" docs/seamless-bridge.md

# Recipe 4
grep -F "forge-bridge install skipped" install.sh
grep -F "forge-bridge install skipped" docs/seamless-bridge.md
```

All 8 commands must return at least one match. If any fails, the doc has drifted from the source — edit `docs/seamless-bridge.md` to match the source verbatim, re-run, and do NOT proceed until all 8 pass.

Also verify the README↔doc cross-link pair:
```bash
grep -qF "docs/seamless-bridge.md" README.md
grep -qF "../README.md" docs/seamless-bridge.md
grep -qF "PASSOFF.md" README.md
```

And the README↔smoke-test cross-reference (Plan 02 agreement on script path):
```bash
grep -qF "tools/smoke-test/seamless-bridge-smoke.sh" README.md
```

Record the successful grep sweep output in the plan SUMMARY so the checker has evidence of the verification pass.
  </action>
  <verify>
    <automated>grep -qF "forge-bridge not reachable at http://127.0.0.1:9999" tools/blender/forge_sender/__init__.py &amp;&amp; grep -qF "forge-bridge not reachable at http://127.0.0.1:9999" docs/seamless-bridge.md &amp;&amp; grep -qF "active camera is missing" tools/blender/forge_sender/preflight.py &amp;&amp; grep -qF "active camera is missing" docs/seamless-bridge.md &amp;&amp; grep -qF "Send to Flame failed:" tools/blender/forge_sender/transport.py &amp;&amp; grep -qF "Send to Flame failed:" docs/seamless-bridge.md &amp;&amp; grep -qF "forge-bridge install skipped" install.sh &amp;&amp; grep -qF "forge-bridge install skipped" docs/seamless-bridge.md &amp;&amp; grep -qF "docs/seamless-bridge.md" README.md &amp;&amp; grep -qF "../README.md" docs/seamless-bridge.md &amp;&amp; grep -qF "PASSOFF.md" README.md &amp;&amp; grep -qF "tools/smoke-test/seamless-bridge-smoke.sh" README.md</automated>
  </verify>
  <acceptance_criteria>
    - All 4 source-side greps return matches (confirms the anchor strings exist in the production code at the expected locations)
    - All 4 doc-side greps return matches (confirms the doc reproduces the anchors verbatim — Ctrl-F discipline per D-14)
    - README→doc link exists, doc→README link exists, README→PASSOFF link exists, README→smoke-test path exists
    - No additional text changes to source code (Phase 4 is docs-only; if a grep fails on the source side, it is a docs defect, not a source defect — fix the doc)
  </acceptance_criteria>
  <done>All 12 grep commands return matches. If any fails, the failed anchor is fixed in the doc (never in source code — source is authoritative per D-14), and the sweep re-runs until clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| doc reader → repo filesystem | User Ctrl-F searches error strings; no untrusted input crosses into the docs themselves (docs are static markdown). |
| doc reader → external URLs | Recipe 4 contains the `raw.githubusercontent.com` URL from install.sh:425-426; this is the literal retry command and is the only outbound URL in the doc. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | README.md + docs/seamless-bridge.md mention repo paths (`/opt/Autodesk/shared/python/`, `~/forge-bakes/`) | accept | Paths already disclosed in PASSOFF.md (public repo doc); internal tool, no sensitive path info revealed beyond what's already documented. |
| T-04-02 | Information Disclosure | docs/seamless-bridge.md quotes verbatim error strings that could leak if tool were ever open-sourced | accept | These strings are already user-visible at runtime in the Blender popup and install.sh stderr; quoting them in docs adds no new disclosure. D-14 grep-anchor discipline requires verbatim match. |
| T-04-03 | Tampering | Recipe 4 Fix block includes `curl | bash` from raw.githubusercontent.com | accept | The command is reproduced verbatim from install.sh:425-426's D-10 warning — the install.sh author already accepted this risk; docs just document the recovery path. Not a new attack surface. |
| T-04-04 | Spoofing | Recipe 5 Fix asks user to run `kill <pid>` on an `lsof` result | accept | User-initiated diagnostic; attacker would need pre-existing code execution on the workstation to manipulate `lsof` output. Internal single-user tool per CLAUDE.md security posture. |
</threat_model>

<verification>
Phase-wide plan checks (run at end of plan, before commit):

1. **Content structure:**
   - `test -f README.md` + 7 H2 sections present (see Task 1 acceptance)
   - `test -f docs/seamless-bridge.md` + 5 H2 sections present (see Task 2 acceptance)
   - `test -d docs` (directory created)

2. **Grep-anchor sweep (D-14):**
   - All 12 grep commands in Task 3 return matches

3. **Scope discipline (Phase 4 is docs-only):**
   - `git diff --name-only` shows ONLY `README.md` and `docs/seamless-bridge.md` added. Zero changes under `forge_core/`, `forge_flame/`, `flame/`, `forge_sender/`, `install.sh`, `tests/`.
   - `git diff install.sh` → empty
   - `git diff tools/blender/forge_sender/` → empty

4. **No emojis in the two new files:**
   - `grep -P '[\x{1F300}-\x{1F9FF}]' README.md docs/seamless-bridge.md || echo "no emojis — good"` (negative grep; empty match is the pass condition)

5. **Link discipline:**
   - `grep -F "github.com" README.md` → zero matches (README has no github URLs)
   - github.com is allowed in docs/seamless-bridge.md only inside recipe 4's Fix block (the verbatim retry command from install.sh). No other github.com URLs.
</verification>

<success_criteria>
Plan 01 succeeds when all three tasks are done:

- [ ] `README.md` exists at repo root, 40-80 lines, 7 H2 sections in D-08 order, contains verbatim core-value sentence, links to `docs/seamless-bridge.md` + `PASSOFF.md` + smoke-test script path
- [ ] `docs/seamless-bridge.md` exists, 90-200 lines, 5 H2 sections in D-09 order, Install section has TD + artist H3 subsections (D-10), 5 troubleshooting recipes in D-12 order with D-13 three-section format
- [ ] Every troubleshooting recipe Symptom heading grep-matches the verbatim source literal (Task 3 sweep: 8 grep-Fs pass)
- [ ] No source code changes (only README.md + docs/seamless-bridge.md added)
- [ ] No screenshots, GIFs, or video (D-11)
- [ ] No "Migration" section in README (D-17)
- [ ] No internal jargon (PyActionNode / PyAttribute / duck-typing) in either doc
- [ ] DOC-02 closed: a user can install (both TD + artist paths), understand autostart, and follow 5 troubleshooting recipes from this plan's artifacts alone
</success_criteria>

<output>
After plan completion, create `.planning/phases/04-e2e-validation-docs/04-01-SUMMARY.md` with:
- Artifacts created (README.md, docs/seamless-bridge.md) and line counts
- Grep-anchor sweep output (all 12 grep commands + their matches)
- DOC-02 closure evidence (mapping of D-07..D-14 + D-16..D-17 to which file/section satisfies each)
- Any discretion items exercised (smoke-test script path agreement with Plan 02, lsof flag choice, etc.)
- Pointers: `forge-calibrator/README.md`, `forge-calibrator/docs/seamless-bridge.md`
</output>
