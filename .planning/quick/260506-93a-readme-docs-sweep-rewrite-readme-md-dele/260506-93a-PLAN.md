---
quick_id: 260506-93a
status: complete
date: 2026-05-06
---

# README + docs sweep — match calibrator's post-strip identity

**Companion to:** quick-260505-tb3 (Phase A2 Blender strip)

## Context

The Phase A2 strip (commits d07dedf..48d9b42 in quick-260505-tb3) deleted ALL Flame↔Blender camera round-trip code from calibrator. But the README, `docs/seamless-bridge.md`, and `tools/smoke-test/seamless-bridge-smoke.sh` were not in scope for that strip and continued to describe the deleted feature. This sweep brings the artist-facing docs into alignment with calibrator's actual identity (single-purpose VP solve, bridge-free).

**Mode:** inline execution (planner timed out; the work was small enough — three commits, all doc-only — that respawning didn't buy anything).

## Tasks

| # | Task | Result |
|---|---|---|
| 1 | Delete `docs/seamless-bridge.md` (283 LOC) and `tools/smoke-test/seamless-bridge-smoke.sh` (9.6KB), plus filesystem cleanup of orphan `tools/blender/__pycache__/` leftover from the strip | Commit `2322d44` (-507 LOC) |
| 2 | Rewrite `README.md` to match calibrator's post-strip identity | Commit `902c85c` (98 → 60 LOC) |
| 3 | Update `.planning/codebase/STRUCTURE.md` to remove the smoke-test entries from the tools/ tree | Commit `0011c60` |

## Verification

- ✅ `bash -n install.sh` exits 0
- ✅ `pytest tests/ -p no:pytest-blender -q` → 191 passed (no code change; matches post-A2 baseline)
- ✅ No stale references to `seamless-bridge`, `seamless_bridge`, `forge-sender` in `*.md`/`*.sh`/`*.py` outside `.planning/` and `.claude/`
- ⚠️ Three stale `forge_sender` mentions in `flame/camera_match_hook.py:1777-1835` — comments inside an orphan helper (`_resolve_flame_project_fps_label`) that has zero callers in the surviving codebase. Out of scope for this sweep; folded into the existing orphan-helpers follow-up flagged in quick-260505-tb3's SUMMARY.md.
- ⚠️ "Export Camera to Blender" / "Import Camera from Blender" still appear in `CLAUDE.md` (lines 245, 248) — these are inside the GSD-managed Architecture mirror section, already flagged in quick-260505-tb3 as a known follow-up requiring a `/gsd-map-codebase` rerun.
- `PASSOFF.md:30` mention is historical session log (append-only) — fine to keep.

## Out of scope (handled separately)

- Orphan helpers sweep in `flame/camera_match_hook.py` (see flag above + quick-260505-tb3 SUMMARY.md #3)
- CLAUDE.md GSD-managed Architecture/Conventions mirror sections (see quick-260505-tb3 SUMMARY.md #4)
- Any code in `forge_core/`, `forge_flame/`, or `flame/` (post-strip clean from quick-260505-tb3)
- `install.sh` (correct as-is post-A2)
