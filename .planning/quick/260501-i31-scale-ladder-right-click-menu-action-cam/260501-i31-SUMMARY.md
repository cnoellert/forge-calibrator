---
quick_id: 260501-i31
status: complete
verdict: SHIPPED — 5-stop scale ladder right-click menu on Action + Camera surfaces
mode: quick
type: execute
wave: 1
depends_on: [260501-dpa]
started: "2026-05-01T20:01:14Z"
completed: "2026-05-01T20:25:00Z"
duration_min: 24
files_modified:
  - flame/camera_match_hook.py
  - tests/test_hook_export_camera_to_blender.py
key_findings:
  - Refactor route taken — _export_camera_pipeline + both wrappers grew a kw-only flame_to_blender_scale=100.0 parameter; default preserves the existing entry's behavior so the default menu entry needs no factory wrap
  - Factory _make_export_callback(scale, *, camera_scope=False) returns a closure capturing the chosen scale; closure-binding correctness covered by parametrized test over all 5 stops
  - Both menu surfaces (Action node via get_batch_custom_ui_actions, Camera node inside Action via get_action_custom_ui_actions) register 6 Action-scoped entries each — 1 default ("Export Camera to Blender") + 5 ladder ("@ 0.01x" .. "@ 100x")
  - Viewport-nav scale=1000.0 at line ~2979 stays byte-identical (canary test asserts source.count("scale=1000.0") == 2)
  - Resumed from 260501-em8 with reduced scope (kwarg plumbing already shipped in 9265f86 so only menu factory + registration remained)
---

# Quick Task 260501-i31 — Scale Ladder Right-Click Menu — Summary

## One-liner

5 ladder-stop right-click menu entries (`Export to Blender @ 0.01x` .. `@ 100x`) added to BOTH the Action-node right-click menu (Batch surface) and the Camera-node right-click menu inside an Action's schematic (Action surface). The existing default "Export Camera to Blender" entry is preserved on both surfaces and continues to use the studio-default `flame_to_blender_scale=100.0`. No new dialogs, no PySide2 widgets, flat-sibling pattern (no 4-deep nesting).

## Verdict

**SHIPPED.** One atomic commit (`6200771`), full test suite green (508 passed / 0 failed / 2 skipped — was 490/0/2 baseline → +18 net new collected items: 9 test methods, two parametrized × 5 ladder stops). Default-entry regression guarded; viewport-nav canary asserts `scale=1000.0` byte-identity; menu shape asserts 6 Action-scoped entries on both surfaces.

## What was built

**One commit:**

| Commit | Description |
|--------|-------------|
| `6200771` | `feat(quick-260501-i31): add 5-stop scale ladder right-click menu entries on Action + Camera surfaces` |

**Files modified (2):**

| File | Lines | What |
|------|-------|------|
| `flame/camera_match_hook.py` | +143 / −13 | (1) `_export_camera_pipeline`, `_export_camera_to_blender`, `_export_camera_from_action_selection` grew `flame_to_blender_scale: float = 100.0` keyword-only parameter; (2) `_make_export_callback(scale, *, camera_scope=False)` factory added; (3) 5 new entries in `get_batch_custom_ui_actions` Camera subgroup (Batch surface); (4) 5 new entries in `get_action_custom_ui_actions` (Action surface); (5) hardcoded literal at the prior line 2898 became `flame_to_blender_scale=flame_to_blender_scale,` parameter pass-through |
| `tests/test_hook_export_camera_to_blender.py` | +297 / 0 | New test module — `TestLadderMenuFactory` with 9 test methods covering per-stop dispatch (parametrized × 5), camera_scope dispatch (2), default-entry regression (2 — Action and Camera surfaces), menu shape (2 — Batch and Action surfaces), viewport-nav canary (1), no-new-PySide-widgets defense (1), closure-binding correctness (parametrized × 5) |

## Resulting menu structure

**Action node right-click → Camera Match → Camera:**
- Open Camera Calibrator (clip-scoped, unchanged)
- Export Camera to Blender (scale=100.0, default)
- Export to Blender @ 0.01x (NEW)
- Export to Blender @ 0.1x (NEW)
- Export to Blender @ 1x (NEW)
- Export to Blender @ 10x (NEW)
- Export to Blender @ 100x (NEW)

**Camera node inside Action's schematic → root right-click:**
- Export Camera to Blender (scale=100.0, default)
- Export to Blender @ 0.01x (NEW)
- Export to Blender @ 0.1x (NEW)
- Export to Blender @ 1x (NEW)
- Export to Blender @ 10x (NEW)
- Export to Blender @ 100x (NEW)

## Round-trip parity

Unchanged from 260501-dpa — bake-side validator + extract-side `forge_bake_scale` round-trip handle every ladder stop at bit-exact position parity (`max |delta| = 0.0e+00`) and float-epsilon rotation parity (`max |delta| = 1.78e-15`, ~10⁶× below the `atol=1e-9` gate). The new menu entries pass values directly to the validated path; no new parity surface introduced.

## Out of scope (deliberate)

- **Persistent preference** — set-once-and-forget knob (env var or config field) — separate quick if wanted later
- **Calibrated reference-distance UI** — the photogrammetric "right answer" — separate phase
- **Geometry scaling** — only camera positions are scaled
- **Matchbox-side anything** — shelved 2026-05-01

## Manual UAT (required before artist use)

Per `memory/flame_module_reload.md`, Flame caches `get_batch_custom_ui_actions()` and `get_action_custom_ui_actions()` at hook-registration time. The gc/exec live-reload pattern does NOT refresh the menu dispatch table.

To activate the new entries:
```bash
bash install.sh
# Quit and re-launch Flame 2026.2.1 (full restart, not just project switch)
```

Then verify in-Flame:

1. Right-click an **Action node** in the batch schematic → Camera Match → Camera. **Expected:** 7 entries total — the existing "Open Camera Calibrator" + "Export Camera to Blender" plus 5 new "@ Nx" siblings.
2. Pick `@ 0.01x` → opens .blend with camera at ~83km from origin (scenes are enormous).
3. Pick `@ 100x` → opens .blend with camera at ~76m from origin (matches yesterday's hardcode default).
4. Pick the existing "Export Camera to Blender" (no `@`) → opens .blend at the same ~76m as `@ 100x` (the default uses the same value).
5. Right-click a **Camera node inside an Action's schematic** → 6 entries (default + 5 ladder). Same scale verifications as above.

## Tests (18 collected items from 9 methods)

Run with `pytest -p no:pytest-blender tests/test_hook_export_camera_to_blender.py -v`:
- `TestLadderMenuFactory` — 18 collected items, all PASS
- Full suite: 508 passed / 0 failed / 2 skipped (was 490/0/2 baseline → +18 net new)

## Self-check

- [x] Verdict written: SHIPPED
- [x] Default-entry regression guarded (scale=100.0 preserved on both surfaces)
- [x] Viewport-nav scale=1000.0 byte-identical (canary test)
- [x] Both surfaces register 6 Action-scoped entries (menu shape tests)
- [x] No new PySide2 widgets / dialogs / popups (no-new-widgets test)
- [x] Flat-sibling pattern only — no 4-deep nesting attempted
- [x] One atomic commit; full test suite green
- [x] No new dependencies

## Next planning step

Two natural follow-ups (separate quicks or a small phase):

1. **Documentation for artists** — what each ladder stop means in physical terms (per the corrected divisor table from 260501-dpa SUMMARY). User-facing doc, no code.
2. **Calibrated reference-distance UI** — the photogrammetric "right answer" for scale derivation. Artist drags a known-length reference line on a known feature in the plate, enters the real distance in mm/cm/m, derives scale from the projection math. Supersedes manual ladder picking with a measured value; ladder remains as fallback when no measurement is provided. This is phase-sized (~1 day), not a quick.
