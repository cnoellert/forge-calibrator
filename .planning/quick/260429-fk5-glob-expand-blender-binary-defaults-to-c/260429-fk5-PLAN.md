---
phase: quick-260429-fk5
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - forge_flame/blender_bridge.py
  - tests/test_blender_bridge.py
  - .planning/todos/pending/2026-04-29-blender-binary-picker-ui-tweak.md
autonomous: true
requirements:
  - QUICK-260429-FK5-01  # glob-expand _DEFAULT_BLENDER_BINS so cold-install hosts find Blender without env override
  - QUICK-260429-FK5-02  # backlog UI-tweak todo for browse/override affordance

must_haves:
  truths:
    - "On Linux with Blender at /opt/blender-4.5/blender (no FORGE_BLENDER_BIN), resolve_blender_bin() returns that path."
    - "On macOS with Blender at /Applications/Blender 4.5.app/Contents/MacOS/Blender, resolve_blender_bin() returns that path."
    - "When two versioned candidates match, the highest-sorted (e.g. 4.5 > 4.4) is returned."
    - "FORGE_BLENDER_BIN still overrides any glob match."
    - "shutil.which('blender') still fires when every glob misses."
    - "Existing literal-path test cases (env override, platform default with literal path, PATH fallback, error-message format) still pass — no regression."
    - "Backlog todo file exists with the documented frontmatter and explains the UI-affordance follow-up."
  artifacts:
    - path: "forge_flame/blender_bridge.py"
      provides: "Glob+expanduser resolution inside resolve_blender_bin; expanded _DEFAULT_BLENDER_BINS table."
      contains: "glob.glob"
    - path: "tests/test_blender_bridge.py"
      provides: "New TestResolveBlenderBin cases covering glob expansion, multi-match sort order, expanduser, darwin glob, env-override-still-wins, PATH-fallback-on-glob-miss."
      contains: "glob"
    - path: ".planning/todos/pending/2026-04-29-blender-binary-picker-ui-tweak.md"
      provides: "Backlog item for surfacing/overriding the Blender binary path inside the calibrator UI."
      contains: "status: backlog"
  key_links:
    - from: "forge_flame/blender_bridge.py:resolve_blender_bin"
      to: "_DEFAULT_BLENDER_BINS"
      via: "iterate platform list -> os.path.expanduser -> glob.glob -> sort desc -> isfile+X_OK"
      pattern: "glob\\.glob.*sort"
    - from: "tests/test_blender_bridge.py"
      to: "forge_flame.blender_bridge.resolve_blender_bin"
      via: "monkeypatch _DEFAULT_BLENDER_BINS to a tmp_path glob pattern"
      pattern: "monkeypatch\\.setattr.*_DEFAULT_BLENDER_BINS"
---

<objective>
Extend `_DEFAULT_BLENDER_BINS` in `forge_flame/blender_bridge.py` so `resolve_blender_bin()` discovers Blender across the common sideload locations on Linux and macOS without requiring `FORGE_BLENDER_BIN`. Resolution must support `~` expansion and `*` glob patterns, sort multi-match results descending so newer versions win, and preserve every existing fallback (env override, PATH, error-message format). File the backlog UI-tweak todo so the harder "browse for Blender" affordance has a tracked home.

Purpose: On flame-01 (RHEL 9 cold install) and macOS workstations with versioned Blender bundles, the current literal-path table misses common installs and forces every tester to set the env var. The env var stays as the escape hatch; the defaults just have to cover the 95% case.

Output: Updated `forge_flame/blender_bridge.py`, expanded `tests/test_blender_bridge.py`, and a new backlog todo file at `.planning/todos/pending/2026-04-29-blender-binary-picker-ui-tweak.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@forge_flame/blender_bridge.py
@tests/test_blender_bridge.py

<interfaces>
<!-- Key shapes the executor needs. Extracted from forge_flame/blender_bridge.py. -->
<!-- Use these directly — no codebase exploration needed. -->

Module-level constant being extended:
```python
# forge_flame/blender_bridge.py
_DEFAULT_BLENDER_BINS = {
    "darwin": [
        "/Applications/Blender.app/Contents/MacOS/Blender",
    ],
    "linux": [
        "/usr/bin/blender",
        "/usr/local/bin/blender",
    ],
}
```

Function being modified:
```python
# forge_flame/blender_bridge.py:75
def resolve_blender_bin() -> str:
    """Return the absolute path to the Blender binary.
    Raises FileNotFoundError with attempted-locations message."""
    # Order: $FORGE_BLENDER_BIN -> _DEFAULT_BLENDER_BINS[sys.platform] -> shutil.which("blender")
    # Existing per-candidate check: os.path.isfile(c) and os.access(c, os.X_OK)
```

Test-side hook points (already used by existing tests, reuse them):
- `monkeypatch.setattr(blender_bridge, "_DEFAULT_BLENDER_BINS", {sys.platform: [...]})`
- `monkeypatch.setenv("FORGE_BLENDER_BIN", ...)` / `monkeypatch.delenv(...)`
- `monkeypatch.setenv("PATH", ...)` to control PATH fallback
- Pattern for fake-executable: `tmp_path / "blender"; .write_text("#!/bin/sh\n"); .chmod(0o755)`
</interfaces>

<test_runner>
Test command (per memory/forge_pytest_blender_session_exit.md, the pytest-blender plugin
exits the session if Blender isn't on PATH; we disable it explicitly):

    pytest tests/test_blender_bridge.py -p no:pytest-blender -v
</test_runner>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Glob-expand `_DEFAULT_BLENDER_BINS` + extend `resolve_blender_bin` for `~`/`*` resolution; add test coverage</name>
  <files>forge_flame/blender_bridge.py, tests/test_blender_bridge.py</files>
  <behavior>
    Tests to write FIRST (RED), then implement to make them GREEN. All must run under
    `pytest tests/test_blender_bridge.py -p no:pytest-blender -v`.

    New tests in TestResolveBlenderBin:

    - test_glob_expansion_finds_versioned_linux_binary:
        - monkeypatch `sys.platform` to "linux" (or skip the platform-keying by
          patching `_DEFAULT_BLENDER_BINS` to `{sys.platform: [str(tmp_path / "blender-4.5" / "blender")]}`
          using a glob pattern like `str(tmp_path / "blender-*" / "blender")`).
        - Build a single versioned binary on disk: `tmp_path / "blender-4.5" / "blender"`
          (mkdir + write + chmod 0o755).
        - Set `_DEFAULT_BLENDER_BINS = {sys.platform: [str(tmp_path / "blender-*" / "blender")]}`.
        - Clear FORGE_BLENDER_BIN and PATH.
        - Assert `resolve_blender_bin()` returns the 4.5 path.

    - test_glob_multi_match_prefers_highest_sorted:
        - Create both `tmp_path / "blender-4.4" / "blender"` and `tmp_path / "blender-4.5" / "blender"`
          (both executable).
        - Set `_DEFAULT_BLENDER_BINS = {sys.platform: [str(tmp_path / "blender-*" / "blender")]}`.
        - Clear env + PATH.
        - Assert the returned path ends with `blender-4.5/blender` (descending sort wins).

    - test_env_override_still_beats_glob_match:
        - Create both a glob-matching binary AND an env-override binary.
        - Set FORGE_BLENDER_BIN to the env-override path; set `_DEFAULT_BLENDER_BINS`
          to a glob that would otherwise match.
        - Assert resolved path == env override (override remains highest priority).

    - test_path_fallback_when_all_globs_miss:
        - Set `_DEFAULT_BLENDER_BINS = {sys.platform: [str(tmp_path / "no-such-*" / "blender")]}`
          (glob with no matches on disk).
        - Put a real executable on PATH (existing pattern: bindir + chmod).
        - Clear FORGE_BLENDER_BIN.
        - Assert `resolve_blender_bin()` returns the PATH binary.

    - test_darwin_glob_resolves_versioned_app_bundle:
        - Use any `sys.platform` (test is platform-agnostic via monkeypatch on the dict).
        - Build `tmp_path / "Blender 4.5.app" / "Contents" / "MacOS" / "Blender"` (executable).
        - Set `_DEFAULT_BLENDER_BINS = {sys.platform: [str(tmp_path / "Blender*.app" / "Contents" / "MacOS" / "Blender")]}`.
        - Clear env + PATH.
        - Assert the returned path ends with `Blender 4.5.app/Contents/MacOS/Blender`.

    - test_expanduser_handles_tilde_paths:
        - Set HOME to a tmp directory: `monkeypatch.setenv("HOME", str(tmp_path))`.
          (On macOS pytest also needs the cached `os.path.expanduser` to honour HOME —
          this works because expanduser reads $HOME on every call.)
        - Build `tmp_path / "Apps" / "blender-4.5" / "blender"` (executable).
        - Set `_DEFAULT_BLENDER_BINS = {sys.platform: ["~/Apps/blender-*/blender"]}`.
        - Clear FORGE_BLENDER_BIN and PATH.
        - Assert the returned path is the absolute resolved path under tmp_path.

    - Regression sanity: the existing `test_platform_defaults_tried_when_no_override`
      and `test_path_fallback_used_when_defaults_miss` and `test_env_override_wins`
      and `test_error_message_lists_attempted_locations` all keep passing as-is
      (no edits required — `glob.glob` returns `[literal_path]` for an existing
      non-glob path, so literal candidates flow through the same code path).

    Implementation (forge_flame/blender_bridge.py):

    1. Add `import glob` to the top-of-file imports (next to `os`, `shutil`, etc.).
    2. Replace the `_DEFAULT_BLENDER_BINS` dict literal with the expanded table:
       ```python
       _DEFAULT_BLENDER_BINS = {
           "darwin": [
               "/Applications/Blender.app/Contents/MacOS/Blender",
               "/Applications/Blender*.app/Contents/MacOS/Blender",
               "~/Applications/Blender.app/Contents/MacOS/Blender",
               "~/Applications/Blender*.app/Contents/MacOS/Blender",
           ],
           "linux": [
               "/usr/bin/blender",
               "/usr/local/bin/blender",
               "/opt/blender/blender",
               "/opt/blender-*/blender",
               "~/Apps/blender-*/blender",
           ],
       }
       ```
    3. In `resolve_blender_bin()`, replace the inner `for candidate in _DEFAULT_BLENDER_BINS.get(...):`
       block with one that:
       - records the raw candidate string in `tried` (so the error message keeps showing
         the original `~`/`*` form);
       - applies `os.path.expanduser(candidate)`;
       - applies `glob.glob(expanded)`;
       - sorts the resulting list with `reverse=True` (lexicographic desc → 4.5 > 4.4);
       - for each match: `if os.path.isfile(m) and os.access(m, os.X_OK): return m`.
       - DO NOT branch glob-vs-literal. Per the implementation note in the task context:
         `glob.glob` returns `[path]` for an existing literal path with no wildcards,
         and `[]` if it doesn't exist — one code path covers both.
    4. Leave env-override and `shutil.which("blender")` blocks unchanged.
    5. Leave the `FileNotFoundError` message unchanged (it already lists `tried`,
       which now includes the raw glob/tilde patterns — that's the desired user-facing
       message).

    Out of scope (do not touch in this task):
    - Script resolution (`_script_candidates`, `_resolve_script`) — unchanged.
    - macOS `Info.plist` introspection.
    - Persisting an override to a config file (that's the backlog todo).
    - Reordering env vs. PATH vs. defaults priority.
  </behavior>
  <action>
    Step 1 — RED: Add the seven new tests above to `tests/test_blender_bridge.py` inside
    the existing `TestResolveBlenderBin` class. Run the test command and confirm the new
    tests fail (the existing literal table can't satisfy `blender-*/blender` patterns).

    Step 2 — GREEN: Edit `forge_flame/blender_bridge.py`:
      (a) `import glob` at the top.
      (b) Expand `_DEFAULT_BLENDER_BINS` per the table above.
      (c) Rewrite the platform-defaults block of `resolve_blender_bin()` to expanduser →
          glob → sort desc → isfile+X_OK, recording the raw candidate (pre-expansion)
          in `tried` so error messages preserve the `~`/`*` form the user actually
          configured.
      (d) Keep env-override and PATH-fallback blocks byte-identical.

    Step 3 — Run `pytest tests/test_blender_bridge.py -p no:pytest-blender -v`. All
    18 existing tests + 6 new tests must pass (one of the seven listed tests is the
    "expanduser" case which is a single test, total = existing 18 + 6 new = 24 tests;
    actual count comes out of pytest collection — match what's there).

    Step 4 — REFACTOR (only if needed): keep the resolve function readable; extract
    a small `_iter_default_candidates(platform)` helper if the inline logic exceeds
    ~10 lines. Optional, not required for correctness.
  </action>
  <verify>
    <automated>cd /Users/cnoellert/Documents/GitHub/forge-calibrator && pytest tests/test_blender_bridge.py -p no:pytest-blender -v</automated>
  </verify>
  <done>
    - `forge_flame/blender_bridge.py` imports `glob`, has the expanded `_DEFAULT_BLENDER_BINS`
      table, and `resolve_blender_bin()` runs each candidate through expanduser → glob →
      sort-desc → isfile+X_OK before returning.
    - `tests/test_blender_bridge.py` contains the six new test methods listed in <behavior>
      and they all pass.
    - All previously-passing tests in the file still pass (no regressions).
    - The `tried` list in the FileNotFoundError still reflects what the user configured
      (raw `~`/`*` patterns, not their expansions).
  </done>
</task>

<task type="auto">
  <name>Task 2: File backlog todo for in-UI Blender binary picker</name>
  <files>.planning/todos/pending/2026-04-29-blender-binary-picker-ui-tweak.md</files>
  <action>
    Create `.planning/todos/pending/2026-04-29-blender-binary-picker-ui-tweak.md` with
    the exact frontmatter specified in the task context (created, title, status: backlog,
    priority: low, area: ui-hook, files list). Body should explain:

    - The env-var + glob defaults already cover the common cases:
      macOS `/Applications/Blender*.app/Contents/MacOS/Blender` + `~/Applications/...`,
      Linux `/usr/bin/blender`, `/usr/local/bin/blender`, `/opt/blender/blender`,
      `/opt/blender-*/blender`, `~/Apps/blender-*/blender`.
    - This todo captures the harder case: power users on weird install layouts who'd
      rather click than `export FORGE_BLENDER_BIN=...`.
    - Suggested affordance: a "Browse for Blender..." button in a settings/preferences
      area of the calibrator window, OR surface the resolved binary path in a status
      line/tooltip with an override entry.
    - Implementation would persist the override to a config file (e.g.
      `.planning/config.json`) so it survives Flame restarts.
    - Out of scope for current milestone; revisit when tester feedback shows env var +
      globs aren't enough.

    Use the existing pending todo file format (frontmatter + markdown body, no extra
    sections required beyond what fits the body description above).
  </action>
  <verify>
    <automated>test -f /Users/cnoellert/Documents/GitHub/forge-calibrator/.planning/todos/pending/2026-04-29-blender-binary-picker-ui-tweak.md && head -15 /Users/cnoellert/Documents/GitHub/forge-calibrator/.planning/todos/pending/2026-04-29-blender-binary-picker-ui-tweak.md | grep -E "status: backlog|priority: low|area: ui-hook"</automated>
  </verify>
  <done>
    - File exists at `.planning/todos/pending/2026-04-29-blender-binary-picker-ui-tweak.md`.
    - Frontmatter includes the exact fields: `created`, `title`, `status: backlog`,
      `priority: low`, `area: ui-hook`, and `files` listing both
      `flame/camera_match_hook.py` and `forge_flame/blender_bridge.py` with their
      inline comments.
    - Body explains the env+glob coverage, the in-UI affordance suggestion, the
      persistence note, and the out-of-scope rationale.
  </done>
</task>

</tasks>

<verification>
Single command run from the repo root verifies the code change:

    pytest tests/test_blender_bridge.py -p no:pytest-blender -v

All tests must pass. The backlog todo file is verified by existence + frontmatter grep.

No Flame instance, no Blender binary, and no install.sh re-run is required for this
plan's verification — these tests are pure-Python with `monkeypatch` and `tmp_path`
fixtures. UAT on flame-01 (open Action → FORGE → Camera → Export Camera to Blender
without `FORGE_BLENDER_BIN` set) is the orchestrator's post-commit verification, not
this plan's gate.
</verification>

<success_criteria>
- `pytest tests/test_blender_bridge.py -p no:pytest-blender -v` exits 0 with all
  pre-existing tests still passing AND the six new glob/expanduser/multi-match/env-still-wins/PATH-fallback/darwin-glob tests passing.
- `forge_flame/blender_bridge.py` imports `glob`, ships the expanded
  `_DEFAULT_BLENDER_BINS` dict (with all candidates listed in Task 1's behavior block),
  and `resolve_blender_bin()` resolves through expanduser → glob → sort desc → isfile+X_OK.
- `FORGE_BLENDER_BIN` override and `shutil.which("blender")` fallback both behave
  identically to before (verified by their existing tests still passing).
- Error message produced by `resolve_blender_bin()` when nothing resolves still lists
  the attempted patterns in their raw form (the user can see what to set
  `FORGE_BLENDER_BIN` to in order to bypass).
- `.planning/todos/pending/2026-04-29-blender-binary-picker-ui-tweak.md` exists with
  the documented frontmatter and explains the UI-affordance follow-up.
</success_criteria>

<output>
After completion, create `.planning/quick/260429-fk5-glob-expand-blender-binary-defaults-to-c/260429-fk5-SUMMARY.md` per the standard quick-task summary template.
</output>
