---
phase: quick-260505-tb3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - forge_flame/blender_bridge.py
  - forge_flame/camera_io.py
  - forge_flame/fbx_ascii.py
  - forge_flame/fbx_io.py
  - forge_flame/templates/camera_baked.fbx
  - forge_flame/__init__.py
  - tools/blender/bake_camera.py
  - tools/blender/extract_camera.py
  - tools/blender/forge_sender/
  - tools/blender/forge_sender-v1.3.5.zip
  - tools/blender/roundtrip_selftest.sh
  - tools/blender/sample_camera.json
  - flame/scale_picker_dialog.py
  - flame/camera_match_hook.py
  - tests/test_bake_camera.py
  - tests/test_blender_bridge.py
  - tests/test_blender_roundtrip.py
  - tests/test_camera_io.py
  - tests/test_extract_camera.py
  - tests/test_fbx_ascii.py
  - tests/test_fbx_io.py
  - tests/test_forge_sender_fcurves_walk.py
  - tests/test_forge_sender_flame_math.py
  - tests/test_forge_sender_preflight.py
  - tests/test_forge_sender_transport.py
  - tests/test_hook_export_camera_to_blender.py
  - tests/test_scale_picker_dialog.py
  - tests/test_camera_match_hook.py
  - tests/fixtures/forge_fbx_aimrig.fbx
  - tests/fixtures/forge_fbx_animated.fbx
  - tests/fixtures/forge_fbx_baked.fbx
  - tests/fixtures/forge_fbx_probe.fbx
  - install.sh
  - CLAUDE.md
  - .planning/codebase/STACK.md
  - .planning/codebase/STRUCTURE.md
  - .planning/notes/blender-strip-pending.md
  - .planning/config.json
autonomous: true
requirements:
  - PHASE-A2-STRIP
must_haves:
  truths:
    - "forge_flame/ contains exactly three files: __init__.py, adapter.py, wiretap.py"
    - "tools/ directory does not exist anywhere in the repo"
    - "flame/ contains no scale_picker_dialog.py and no Blender export handlers"
    - "Calibrator's Open Camera Calibrator menu still appears under FORGE > Camera in Batch right-click"
    - "No Export Camera to Blender menu entry appears anywhere (Batch nor Action right-click)"
    - "pytest tests/ -p no:pytest-blender -q passes green after the strip"
    - "bash -n install.sh passes (clean syntax) after install.sh edits"
    - "install.sh on a host with stale pre-strip artifacts at /opt/Autodesk/shared/python/scale_picker_dialog.py and /opt/Autodesk/shared/forge_blender_scripts/ self-heals (purges them)"
    - "CLAUDE.md no longer claims a Flame↔Blender round-trip is part of calibrator identity"
  artifacts:
    - path: "forge_flame/__init__.py"
      provides: "Updated package docstring; lists only adapter + wiretap"
      contains: "wiretap"
    - path: "forge_flame/adapter.py"
      provides: "SURVIVES untouched — still the solver→Flame adapter"
    - path: "forge_flame/wiretap.py"
      provides: "SURVIVES untouched — still the Wiretap frame reader"
    - path: "flame/camera_match_hook.py"
      provides: "Hook surgery — 7-function Blender block deleted, get_action_custom_ui_actions deleted, get_batch_custom_ui_actions reduced to Open Camera Calibrator only"
    - path: "install.sh"
      provides: "Blender bits stripped, stale-Blender-artifact purge added (mirrors the Matchbox purge from quick-260505-mrv)"
    - path: "CLAUDE.md"
      provides: "Identity rewritten — pristine VP solve, no Flame↔Blender claims"
    - path: ".planning/codebase/STACK.md"
      provides: "Synced with CLAUDE.md — Blender stack rows removed"
    - path: ".planning/codebase/STRUCTURE.md"
      provides: "File tree refreshed — no Blender entries"
    - path: ".planning/notes/blender-strip-pending.md"
      provides: "Marked Completed; preserves historical decision context"
  key_links:
    - from: "flame/camera_match_hook.py::get_batch_custom_ui_actions"
      to: "_launch_camera_match"
      via: "execute callback"
      pattern: "Open Camera Calibrator"
    - from: "install.sh"
      to: "/opt/Autodesk/shared/python/forge_flame/"
      via: "_sync_dir"
      pattern: "_sync_dir.*forge_flame"
---

<objective>
Strip the entire Flame↔Blender camera round-trip from forge-calibrator. After this plan, calibrator is a single-purpose VP-solve tool: clip → VP lines → solved camera applied to a Flame Action node, period. The export and import directions to Blender are gone; forge-blender Phase 6 (v1.1) will absorb the export half via git-history cherry-pick from this repo when their planning gets there.

Purpose: Honor the architectural commitment locked 2026-05-05 across calibrator + forge-blender sessions — bridge-free, one-way only, Blender round-trip leaves calibrator. Also closes the dead-code-walking debt flagged in `phase_a2_blender_strip_pending.md` and `.planning/notes/blender-strip-pending.md` since 2026-05-05.

Output: ~5,000 LOC net deletion (sources + tests). forge_flame/ shrinks 6 → 3 files (__init__.py, adapter.py, wiretap.py). tools/ directory disappears. Four atomic commits, each independently revertable.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@.planning/notes/blender-strip-pending.md
@.planning/codebase/STACK.md
@.planning/codebase/STRUCTURE.md
@forge_flame/__init__.py
@install.sh

# Hook surgery target — full file is 3186 LOC; the 7-function Blender block sits
# at lines 2573-3081. Tasks reference exact line numbers from the orchestrator's
# scope audit. Confirmed accurate via `wc -l` and `grep -n` in plan-time spike.

# Reference precedent for the install.sh stale-artifact purge pattern:
# git show 5862ba8 — `quick-260505-mrv` added an analogous self-healing purge
# for stale Matchbox-era hooks at $INSTALL_DIR root. We mirror its shape exactly.

<interfaces>
<!-- Survivors after the strip — these are NOT touched by this plan. -->
<!-- Listed so the executor knows the boundary and does not over-delete. -->

forge_flame/adapter.py:
  - solve_for_flame(...) -- packing + Euler decomposition for the solver pipeline
  - All trace helpers
  -> Untouched. No edits.

forge_flame/wiretap.py:
  - get_clip_colour_space(...)
  - extract_frame_bytes(...)
  -> Untouched. No edits.

flame/camera_match_hook.py — surviving public surface after surgery:
  - _launch_camera_match(selection)        (Batch right-click handler)
  - _scope_batch_clip(item)                (visibility predicate)
  - _scope_batch_action(item)              (visibility predicate — used by Apply Camera flow)
  - _scope_action_camera(item)             (visibility predicate — survived for Apply Camera flow)
  - _first_camera_in_action_selection(...) (helper for Apply Camera flow)
  - _pick_camera(...) + _FORGE_SS          (forge-styled picker dialog)
  - _open_camera_match(...) + UI internals (PySide6 line-drawing window)
  - solver glue, OCIO preview, image buffer pipeline
  - get_batch_custom_ui_actions()          (REDUCED — Open Camera Calibrator only)
  -> get_action_custom_ui_actions()        (DELETED — its sole entry was the Blender export handler)

tests/test_camera_match_hook.py — surviving tests:
  - All _scope_* predicate tests
  - All _pick_camera dialog tests
  - All _first_camera_in_action_selection tests (including the .export_fbx
    fallback tests at lines 389/543/619 — these test Flame Action's native
    .export_fbx attribute, NOT our deleted handlers)
  - Wave 0 test infrastructure (_KEEP_INSTALLED set, fakes)
  -> ANY test that calls _export_camera_to_blender*, _export_camera_pipeline,
     _make_export_callback, _launch_blender_on_blend, or scale_picker_dialog
     gets deleted (Task 3).
</interfaces>

<ordering_warning>
**pytest will be RED between Task 1 commit and Task 3 commit.** That is intentional and acceptable for the duration of two commit boundaries:
- After Task 1: forge_flame source modules deleted, but flame/camera_match_hook.py still imports them lazily inside the deleted handlers. The handlers are reachable from `get_batch_custom_ui_actions` and `get_action_custom_ui_actions` at module-load time — but only if Flame actually invokes the menu. pytest's static import of camera_match_hook does NOT trigger the handler bodies, so the hook IMPORT itself stays green. However, tests that exercise the deleted handlers (test_hook_export_camera_to_blender.py, test_scale_picker_dialog.py) will fail.
- After Task 2: those test files are deleted, so the broken-test surface shrinks.
- After Task 3: hook surgery completes; pytest must be GREEN again.

**Do NOT run pytest between Task 1 and Task 3.** Run it once after Task 3 lands and once after Task 4 lands. If you want a sanity gate between commits, use `python -c "import flame.camera_match_hook"` at the repo root after each task — that confirms the hook still imports without exploding, even if individual handler bodies would fail at runtime.
</ordering_warning>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete source files (forge_flame Blender modules + tools/ + scale_picker_dialog)</name>
  <files>
    forge_flame/blender_bridge.py,
    forge_flame/camera_io.py,
    forge_flame/fbx_ascii.py,
    forge_flame/fbx_io.py,
    forge_flame/templates/camera_baked.fbx,
    forge_flame/templates/,
    tools/blender/bake_camera.py,
    tools/blender/extract_camera.py,
    tools/blender/forge_sender/,
    tools/blender/forge_sender-v1.3.5.zip,
    tools/blender/roundtrip_selftest.sh,
    tools/blender/sample_camera.json,
    tools/blender/,
    tools/,
    flame/scale_picker_dialog.py
  </files>
  <action>
    git rm the source files. Use git rm (not plain rm) so the deletions land in the index for the commit.

    Commands (run from repo root):
    ```bash
    git rm forge_flame/blender_bridge.py
    git rm forge_flame/camera_io.py
    git rm forge_flame/fbx_ascii.py
    git rm forge_flame/fbx_io.py
    git rm -r forge_flame/templates/
    git rm -r tools/
    git rm flame/scale_picker_dialog.py
    ```

    Note: `git rm -r tools/` removes the entire directory including:
      - tools/blender/bake_camera.py
      - tools/blender/extract_camera.py
      - tools/blender/forge_sender/ (the whole Blender addon subdirectory)
      - tools/blender/forge_sender-v1.3.5.zip
      - tools/blender/roundtrip_selftest.sh
      - tools/blender/sample_camera.json
      - tools/blender/__pycache__/ (if tracked; usually .gitignored)

    After git rm, verify forge_flame/ contains exactly three .py files:
    ```bash
    ls forge_flame/
    # Expected: __init__.py  adapter.py  wiretap.py
    ```

    Then commit. Do NOT run pytest yet — see <ordering_warning> in <context>. The hook still imports these modules lazily inside the soon-to-be-deleted handlers; static import of the hook works (handlers aren't invoked), but tests of the deleted handlers will fail. We fix that in Tasks 2 and 3.

    Commit message:
    ```
    refactor(quick-260505-tb3): delete Blender round-trip source files

    Phase A2 strip — Step 1/4: source deletion.

    Removes:
      forge_flame/blender_bridge.py    (~10.6 KB)
      forge_flame/camera_io.py         (~12 KB)
      forge_flame/fbx_ascii.py         (~70 KB — biggest file in repo)
      forge_flame/fbx_io.py            (~8.7 KB)
      forge_flame/templates/           (camera_baked.fbx)
      tools/blender/                   (bake/extract/forge_sender addon)
      flame/scale_picker_dialog.py

    forge_flame/ shrinks 6 -> 3 files (__init__.py, adapter.py, wiretap.py).
    tools/ directory disappears entirely. Calibrator becomes a single-purpose
    VP-solve tool. forge-blender Phase 6 (v1.1) will absorb the export half
    via cherry-pick from this repo's git history when their planning gets there.

    Architectural commitment locked 2026-05-05 across calibrator +
    forge-blender sessions: bridge-free, one-way only, Blender round-trip
    leaves calibrator. See .planning/notes/blender-strip-pending.md and
    auto-memory entry phase_a2_blender_strip_pending.md.

    Tests + hook surgery follow in commits 2-4 of this quick task.

    Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
    ```
  </action>
  <verify>
    <automated>test ! -e forge_flame/blender_bridge.py && test ! -e forge_flame/camera_io.py && test ! -e forge_flame/fbx_ascii.py && test ! -e forge_flame/fbx_io.py && test ! -e forge_flame/templates && test ! -e tools && test ! -e flame/scale_picker_dialog.py && test -f forge_flame/adapter.py && test -f forge_flame/wiretap.py && test -f forge_flame/__init__.py && echo "Task 1 source deletion verified"</automated>
  </verify>
  <done>All listed source files removed via git rm; commit landed. forge_flame/ contains only __init__.py + adapter.py + wiretap.py. tools/ directory does not exist. flame/scale_picker_dialog.py removed. pytest not yet run (deferred to Task 3 — see ordering warning).</done>
</task>

<task type="auto">
  <name>Task 2: Delete test files + FBX fixtures</name>
  <files>
    tests/test_bake_camera.py,
    tests/test_blender_bridge.py,
    tests/test_blender_roundtrip.py,
    tests/test_camera_io.py,
    tests/test_extract_camera.py,
    tests/test_fbx_ascii.py,
    tests/test_fbx_io.py,
    tests/test_forge_sender_fcurves_walk.py,
    tests/test_forge_sender_flame_math.py,
    tests/test_forge_sender_preflight.py,
    tests/test_forge_sender_transport.py,
    tests/test_hook_export_camera_to_blender.py,
    tests/test_scale_picker_dialog.py,
    tests/fixtures/forge_fbx_aimrig.fbx,
    tests/fixtures/forge_fbx_animated.fbx,
    tests/fixtures/forge_fbx_baked.fbx,
    tests/fixtures/forge_fbx_probe.fbx
  </files>
  <action>
    git rm the 13 test files + 4 FBX fixtures. These tests covered exclusively the deleted Blender flow; no replacement tests are added (the surviving forge_core/forge_flame/hook surface already has its own coverage in test_solver.py, test_hook_parity.py, test_image_buffer.py, test_camera_match_hook.py, etc.).

    Commands (run from repo root):
    ```bash
    git rm tests/test_bake_camera.py
    git rm tests/test_blender_bridge.py
    git rm tests/test_blender_roundtrip.py
    git rm tests/test_camera_io.py
    git rm tests/test_extract_camera.py
    git rm tests/test_fbx_ascii.py
    git rm tests/test_fbx_io.py
    git rm tests/test_forge_sender_fcurves_walk.py
    git rm tests/test_forge_sender_flame_math.py
    git rm tests/test_forge_sender_preflight.py
    git rm tests/test_forge_sender_transport.py
    git rm tests/test_hook_export_camera_to_blender.py
    git rm tests/test_scale_picker_dialog.py
    git rm tests/fixtures/forge_fbx_aimrig.fbx
    git rm tests/fixtures/forge_fbx_animated.fbx
    git rm tests/fixtures/forge_fbx_baked.fbx
    git rm tests/fixtures/forge_fbx_probe.fbx
    ```

    Verify count after deletion:
    ```bash
    ls tests/test_*.py | wc -l
    # Expected: previous count minus 13
    ls tests/fixtures/ 2>/dev/null
    # Expected: empty or only non-FBX fixtures (verify nothing else lives in fixtures/)
    ```

    Commit message:
    ```
    refactor(quick-260505-tb3): delete Blender round-trip test files + FBX fixtures

    Phase A2 strip — Step 2/4: test deletion.

    Removes 13 test modules + 4 FBX fixtures whose sole coverage was the
    deleted Blender flow:
      tests/test_bake_camera.py
      tests/test_blender_bridge.py
      tests/test_blender_roundtrip.py
      tests/test_camera_io.py
      tests/test_extract_camera.py
      tests/test_fbx_ascii.py
      tests/test_fbx_io.py
      tests/test_forge_sender_fcurves_walk.py
      tests/test_forge_sender_flame_math.py
      tests/test_forge_sender_preflight.py
      tests/test_forge_sender_transport.py
      tests/test_hook_export_camera_to_blender.py
      tests/test_scale_picker_dialog.py
      tests/fixtures/forge_fbx_aimrig.fbx
      tests/fixtures/forge_fbx_animated.fbx
      tests/fixtures/forge_fbx_baked.fbx
      tests/fixtures/forge_fbx_probe.fbx

    No replacement: surviving solver / adapter / hook / OCIO / image-buffer
    coverage is unchanged. tests/test_camera_match_hook.py gets surgical
    edits in commit 3 (the camera_io mock and any export-handler test cases
    live in that file).

    Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
    ```
  </action>
  <verify>
    <automated>for f in tests/test_bake_camera.py tests/test_blender_bridge.py tests/test_blender_roundtrip.py tests/test_camera_io.py tests/test_extract_camera.py tests/test_fbx_ascii.py tests/test_fbx_io.py tests/test_forge_sender_fcurves_walk.py tests/test_forge_sender_flame_math.py tests/test_forge_sender_preflight.py tests/test_forge_sender_transport.py tests/test_hook_export_camera_to_blender.py tests/test_scale_picker_dialog.py tests/fixtures/forge_fbx_aimrig.fbx tests/fixtures/forge_fbx_animated.fbx tests/fixtures/forge_fbx_baked.fbx tests/fixtures/forge_fbx_probe.fbx; do test ! -e "$f" || { echo "FAIL: $f still exists"; exit 1; }; done && echo "Task 2 test/fixture deletion verified"</automated>
  </verify>
  <done>13 test files + 4 FBX fixtures removed via git rm; commit landed. pytest still not run (waits until Task 3 closes the hook-surgery loop).</done>
</task>

<task type="auto">
  <name>Task 3: Hook surgery (camera_match_hook.py + test_camera_match_hook.py) + forge_flame/__init__.py</name>
  <files>
    flame/camera_match_hook.py,
    tests/test_camera_match_hook.py,
    forge_flame/__init__.py
  </files>
  <action>
    This is the highest-risk task — surgical edits inside a 3186-LOC monolith. Take it slowly and verify after each sub-step.

    **Step 3a — flame/camera_match_hook.py — delete the 7-function Blender block (lines ~2573-3081):**

    Delete these functions in order, end-to-end (each is a contiguous `def ... return` block):
      - `_launch_blender_on_blend` (starts line 2573)
      - `_export_camera_to_blender` (starts line 2633)
      - `_export_camera_from_action_selection` (starts line 2694)
      - `_make_export_callback` (starts line 2738)
      - `_export_camera_to_blender_with_picker` (starts line 2775)
      - `_export_camera_from_action_selection_with_picker` (starts line 2790)
      - `_export_camera_pipeline` (starts line 2802)

    Also delete the module-level constant supporting them:
      - `_LADDER_MENU_STOPS = (...)` at line 2735, plus the comment block above it (lines ~2733-2734)

    Also delete any orphan comment block immediately above `_launch_blender_on_blend` that documents the about-to-die functions.

    **Practical approach:** find the line containing `def _launch_blender_on_blend` and the line containing `def get_batch_custom_ui_actions`. Everything strictly between them — and the comment blocks immediately above _launch_blender_on_blend that talk about Blender — gets deleted. Preserve any comment block that documents `get_batch_custom_ui_actions` itself (the one starting `# RESEARCH §Pitfall 4: menu structure changes...` at ~line 3083 — KEEP).

    **Step 3b — flame/camera_match_hook.py — surgically edit `get_batch_custom_ui_actions` (~line 3096):**

    Within the "Camera" hierarchy block, REMOVE the second action dict (the one named "Export Camera to Blender" at lines ~3127-3140) and the long comment block immediately above it (lines ~3122-3136 starting `# Quick 260501-knl:`). KEEP the first action dict ("Open Camera Calibrator").

    Also UPDATE the docstring inside `get_batch_custom_ui_actions` — the current docstring claims:
    > "The legacy 'Import' entry (Blender-to-Flame pull) was removed (D-06): the Blender-side forge_sender addon is now the sole inbound path. Hard cut on install per D-14 — no backwards-compat shim."

    Replace with:
    > "The Blender export AND import entries were removed in quick-260505-tb3 (Phase A2 strip — bridge-free, one-way only architecture decision locked 2026-05-05 across calibrator + forge-blender sessions). forge-blender Phase 6 (v1.1) will absorb the export half via cherry-pick from this repo's git history. Calibrator is now a single-purpose VP-solve tool."

    After surgery, the function body returns a list of two dicts:
      1. `{"name": "FORGE", "hierarchy": [], "actions": []}` — KEEP unchanged.
      2. `{"name": "Camera", "hierarchy": ["FORGE"], "actions": [{"name": "Open Camera Calibrator", "isVisible": _scope_batch_clip, "execute": _launch_camera_match}]}` — KEEP only the Open Camera Calibrator action.

    **Step 3c — flame/camera_match_hook.py — DELETE `get_action_custom_ui_actions` entirely (~line 3158):**

    The entire function plus the long preceding comment block (`# RESEARCH §Pitfall 1: _scope_action_camera...` starting ~line 3142) gets removed. Its sole entry was the deleted Blender export handler. Without this function, Flame's hook scanner does not register any right-click menu on cameras inside an Action — which is the desired behavior.

    Note: the `_scope_action_camera` and `_first_camera_in_action_selection` helpers at module scope SURVIVE — they are also referenced by the Apply Camera picker flow inside `_open_camera_match`. Do NOT delete them.

    **Step 3d — flame/camera_match_hook.py — audit and trim top-of-file imports:**

    After the surgery in 3a/3b/3c, walk the top of `flame/camera_match_hook.py` (lines 1-~80) and check each top-level import. Drop any that became unused after the deletion. Likely candidates:
      - `subprocess` — only used by the deleted Blender launcher? Verify with `grep -n subprocess flame/camera_match_hook.py`. If only the `_launch_blender_on_blend` referenced it at file level, drop the import. (Other modules may still import subprocess for OCIO config probing — verify before dropping.)
      - `tempfile` — only used by `_export_camera_pipeline`? Verify with `grep -n tempfile flame/camera_match_hook.py`.
      - `shutil` — only used by Blender launcher? Verify with `grep -n shutil flame/camera_match_hook.py`.
      - `json`, `os`, `sys`, `re` — almost certainly still used elsewhere; verify before touching.

    **Rule:** drop an import ONLY if `grep -n <name> flame/camera_match_hook.py` returns zero hits OUTSIDE the import line itself after Steps 3a-3c land. Never drop on assumption alone.

    **Step 3e — tests/test_camera_match_hook.py — surgical edit:**

    1. Find the line containing `"forge_flame.camera_io": MagicMock()` (around line 55 per orchestrator's audit). DELETE that line — the module no longer exists.
    2. Search for any other deleted-module mocks: `grep -n "forge_flame.fbx_ascii\|forge_flame.fbx_io\|forge_flame.blender_bridge\|forge_flame.camera_io" tests/test_camera_match_hook.py` — delete each matching line if found.
    3. Search for any test cases that touch deleted handlers: `grep -n "_export_camera_to_blender\|_export_camera_pipeline\|_export_camera_from_action_selection\|_make_export_callback\|_launch_blender_on_blend\|scale_picker" tests/test_camera_match_hook.py`. Per plan-time spike (5 hits expected zero, but verify): if any test cases reference these names, delete those test functions entirely (find the `def test_...:` start, find the next `def test_...:` or end of file, delete the function body + decorator).
    4. KEEP all other tests — particularly:
       - `test_first_camera_parent_with_broken_export_fbx_falls_back` (line 389) — tests Flame Action's NATIVE `.export_fbx` attribute behavior in the picker fallback, NOT our deleted handlers
       - `test_first_camera_fallback_skips_action_with_broken_export_fbx` (line 543) — same kind
       - `test_first_camera_promotes_via_get_node_when_parent_export_fbx_missing` (line 619) — same kind
       - All `_scope_*` predicate tests
       - All `_pick_camera` tests
       - All Wave 0 infrastructure (the `_KEEP_INSTALLED` set, fakes)

    **Step 3f — forge_flame/__init__.py — refresh docstring:**

    Current contents (13 lines) claim:
    > "Contents: wiretap.py — get_clip_colour_space(), extract_frame_bytes()"

    Update to reflect that adapter.py is also a public surface (it always was, the docstring just understated). Replacement:

    ```python
    """
    forge_flame — Flame-specific adapters for Forge camera tools.

    Unlike ``forge_core`` which is host-agnostic, everything in this package
    touches Flame's API (PyClip, Wiretap SDK, batch.Action). Not importable
    from trafFIK or any non-Flame tool — the Wiretap SDK only exists inside
    a Flame install.

    Contents:
        adapter.py — solve_for_flame() (packing + Euler decomposition)
        wiretap.py — get_clip_colour_space(), extract_frame_bytes()

    Ships alongside forge_core/ at install time (see install.sh).

    History: pre-quick-260505-tb3 this package also contained
    blender_bridge / fbx_ascii / fbx_io / camera_io modules supporting
    a Flame↔Blender camera round-trip. That surface was stripped 2026-05-05
    (Phase A2) — the export half lives on in forge-blender Phase 6 via
    git-history cherry-pick from this repo. See
    .planning/notes/blender-strip-pending.md for the decision chain.
    """
    ```

    **Step 3g — verify and commit:**

    Run pytest cleanly:
    ```bash
    conda run -n forge pytest tests/ -p no:pytest-blender -q
    ```

    Expected: GREEN. Test count drops from 542 to ~270-300 surviving (per orchestrator's estimate). If pytest is RED, the most likely cause is:
      - A missed reference to a deleted function name in get_batch_custom_ui_actions
      - A missed mock in test_camera_match_hook.py that test setup needs
      - A surviving call site for `_LADDER_MENU_STOPS` we missed

    Also sanity-check the hook still imports:
    ```bash
    conda run -n forge python -c "import sys; sys.path.insert(0, '.'); import flame.camera_match_hook as h; print('hook imports OK; get_batch_custom_ui_actions =', h.get_batch_custom_ui_actions()); print('get_action_custom_ui_actions exists?', hasattr(h, 'get_action_custom_ui_actions'))"
    ```
    Expected output: hook imports OK, `get_batch_custom_ui_actions` returns a list with two dicts (FORGE + Camera), `get_action_custom_ui_actions exists? False`.

    Commit message:
    ```
    refactor(quick-260505-tb3): hook surgery — strip Blender export handlers

    Phase A2 strip — Step 3/4: hook surgery + __init__.py refresh.

    flame/camera_match_hook.py:
      - Delete 7-function Blender block (~lines 2573-3081):
          _launch_blender_on_blend, _export_camera_to_blender,
          _export_camera_from_action_selection, _make_export_callback,
          _export_camera_to_blender_with_picker,
          _export_camera_from_action_selection_with_picker,
          _export_camera_pipeline
      - Delete _LADDER_MENU_STOPS module constant
      - get_batch_custom_ui_actions: drop "Export Camera to Blender" action,
        update docstring (both Import and Export are now gone)
      - DELETE get_action_custom_ui_actions entirely (its sole entry was
        the deleted Blender handler; without the function Flame's hook
        scanner deregisters the camera-inside-Action right-click surface)
      - Trim now-unused top-of-file imports (verified via grep before drop)

    tests/test_camera_match_hook.py:
      - Drop "forge_flame.camera_io" mock (module deleted in commit 1)
      - Drop any test cases referencing the deleted handlers
      - KEEP all _scope_* predicate tests, _pick_camera tests, and the
        three .export_fbx fallback tests at lines 389/543/619 (those test
        Flame Action's native .export_fbx attribute in the Apply Camera
        picker flow, NOT our deleted handlers)

    forge_flame/__init__.py:
      - Update docstring to reflect adapter + wiretap as the only modules
      - Add history note pointing at .planning/notes/blender-strip-pending.md
        for the decision chain

    Verification:
      - `conda run -n forge pytest tests/ -p no:pytest-blender -q` GREEN
      - Hook still imports cleanly; get_batch_custom_ui_actions returns
        the FORGE > Camera > Open Camera Calibrator menu only
      - get_action_custom_ui_actions function no longer exists

    Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
    ```
  </action>
  <verify>
    <automated>conda run -n forge pytest tests/ -p no:pytest-blender -q && conda run -n forge python -c "import sys; sys.path.insert(0, '.'); import flame.camera_match_hook as h; menu = h.get_batch_custom_ui_actions(); assert len(menu) == 2, f'expected 2 top-level groups, got {len(menu)}'; assert menu[0]['name'] == 'FORGE'; assert menu[1]['name'] == 'Camera'; cam_actions = menu[1]['actions']; assert len(cam_actions) == 1, f'expected 1 Camera action (Open Camera Calibrator), got {len(cam_actions)}: {[a[\"name\"] for a in cam_actions]}'; assert cam_actions[0]['name'] == 'Open Camera Calibrator'; assert not hasattr(h, 'get_action_custom_ui_actions'), 'get_action_custom_ui_actions must be deleted'; assert not hasattr(h, '_export_camera_to_blender'), '_export_camera_to_blender must be deleted'; assert not hasattr(h, '_export_camera_pipeline'), '_export_camera_pipeline must be deleted'; assert not hasattr(h, '_LADDER_MENU_STOPS'), '_LADDER_MENU_STOPS must be deleted'; print('Task 3 hook surgery verified')"</automated>
  </verify>
  <done>pytest GREEN. Hook imports cleanly. get_batch_custom_ui_actions returns FORGE > Camera > Open Camera Calibrator only. get_action_custom_ui_actions and all 7 deleted Blender handlers + _LADDER_MENU_STOPS no longer exist on the hook module. forge_flame/__init__.py docstring refreshed. Commit landed.</done>
</task>

<task type="auto">
  <name>Task 4: install.sh + docs (CLAUDE.md, STACK.md, STRUCTURE.md, blender-strip-pending.md, config.json)</name>
  <files>
    install.sh,
    CLAUDE.md,
    .planning/codebase/STACK.md,
    .planning/codebase/STRUCTURE.md,
    .planning/notes/blender-strip-pending.md,
    .planning/config.json
  </files>
  <action>
    **Step 4a — install.sh edits:**

    Apply these surgical edits in order:

    1. **Drop SOURCE_SCALE_PICKER variable + comment (lines ~35-39):**
       Delete lines 35-39 (the comment block "Added by quick 260501-knl..." plus `SOURCE_SCALE_PICKER="$REPO_ROOT/flame/scale_picker_dialog.py"`).

    2. **Drop SOURCE_BLENDER_SCRIPTS variable (line ~42):**
       Delete `SOURCE_BLENDER_SCRIPTS="$REPO_ROOT/tools/blender"` at line 42.

    3. **Drop BLENDER_SCRIPTS_DEST var + comment block (lines ~51-57):**
       Delete the comment block "tools/blender/ ships OUTSIDE..." (lines 51-56) plus `BLENDER_SCRIPTS_DEST="/opt/Autodesk/shared/forge_blender_scripts"` at line 57.

    4. **Drop scale_picker_dialog source preflight (lines ~256-263):**
       Delete the block "# Added by quick 260501-knl..." through `ok "scale picker dialog present (...)"` (lines 256-263).

    5. **Drop tools/blender preflight check (lines ~274-278):**
       Delete the `if [[ ! -d "$SOURCE_BLENDER_SCRIPTS" ]]; then ... ok "tools/blender present (...)"` block (lines 274-278).

    6. **Drop scale_picker_dialog copy block (lines ~513-519):**
       Delete the block "# Added by quick 260501-knl — copy the scale picker..." through `ok "wrote $TARGET_SCALE_PICKER"` (lines 513-519).

    7. **Drop tools/blender sync block (lines ~545-548):**
       Delete the comment block "# tools/blender ships to /opt/Autodesk/shared/forge_blender_scripts/..." (lines 545-547) plus `_sync_dir "$SOURCE_BLENDER_SCRIPTS"  "$BLENDER_SCRIPTS_DEST"  "tools/blender"` at line 548.

    8. **Verify the post-install pycache purge still works:**
       Line ~559 currently reads `find "$INSTALL_DIR" "$FORGE_CORE_DEST" "$FORGE_FLAME_DEST" -name __pycache__ -type d -exec rm -rf {} + 2>/dev/null || true`. This is unchanged — it covers the surviving install. Verify it's still present and unmodified after the edits above.

    9. **ADD a stale-Blender-artifact purge (mirroring the Matchbox purge from quick-260505-mrv at lines ~562-568):**

       Insert the following block immediately AFTER the existing Matchbox purge (after the line `ok "purged stale Matchbox-era scripts (if present)"` at ~line 568) and BEFORE the `# ---- done ----` step:

       ```bash
       # Purge stale Blender round-trip artifacts at $INSTALL_DIR root and
       # /opt/Autodesk/shared/forge_blender_scripts/. These were never copied
       # by the post-strip install.sh, but a pre-strip install left them behind.
       # Source files deleted in quick-260505-tb3. Idempotent: rm survives missing.
       run "rm -f \"$INSTALL_DIR/scale_picker_dialog.py\""
       run "rm -rf /opt/Autodesk/shared/forge_blender_scripts/"
       ok "purged stale Blender round-trip artifacts (if present)"
       ```

       Note the hardcoded `/opt/Autodesk/shared/forge_blender_scripts/` path — `$BLENDER_SCRIPTS_DEST` was deleted in step 4a-3, so we hardcode the path here. This matches the shape of the Matchbox purge above (which also hardcodes its target paths).

    10. **Verify install.sh syntax:**
        ```bash
        bash -n install.sh
        ```
        Must exit 0. If not, fix syntax issues before continuing.

    **Step 4b — CLAUDE.md edits:**

    1. **Project description block:** REMOVE the sentence "Recent work (v6.x) extends this with a Flame↔Blender camera round-trip so solved cameras — static or animated — can be refined in Blender and returned to Flame."

    2. **Core Value statement:** Replace
       > "**The solved camera must be geometrically faithful to the plate, and the Flame↔Blender round-trip must preserve that fidelity end-to-end.** Everything else — UX polish, seamless workflows, automation — is secondary. If the numbers are wrong, the compositing CG won't glue to the plate and the tool fails its purpose."

       with:
       > "**The solved camera must be geometrically faithful to the plate.** Everything else — UX polish, seamless workflows, automation — is secondary. If the numbers are wrong, the compositing CG won't glue to the plate and the tool fails its purpose."

    3. **Constraints section:**
       - From the **Tech stack** bullet: REMOVE "Blender 4.5's FBX importer rejects ASCII FBX — motivates the custom parser in `forge_flame/fbx_ascii.py`."
       - From **Runtime dependencies**: REMOVE "Blender 4.5+ as a subprocess."

    4. **Stack section** (bordered by `<!-- GSD:stack-start source:codebase/STACK.md -->` markers — note: the GSD docs system regenerates this from STACK.md so the surgery on STACK.md in Step 4c will eventually flow back into CLAUDE.md, but make the edits in CLAUDE.md too for the immediate-effect commit):

       - **Languages:** Drop the entire "Blender Python (bundled with Blender 4.5+) - Scripts run in subprocess via `blender --background --python`; uses `bpy` and `mathutils` only, no external forge imports" line.
       - **Runtime:** Drop "Blender 4.5+ - Subprocess invocation only; not a runtime host for the Python library".
       - **Frameworks:** Drop "Blender bpy - For animated camera baking/extraction (Blender's Python API, subprocess-only)".
       - **Key Dependencies:** Drop the entire Blender-side block:
         ```
         - bpy - Scene graph, camera object manipulation, keyframing
         - mathutils - Matrix algebra for Euler/quaternion conversions ...
         ```
       - **Configuration / Environment Variables:** Drop FORGE_BLENDER_BIN and FORGE_BLENDER_SCRIPTS lines.
       - **Configuration / Installation Paths:** Drop `/opt/Autodesk/shared/python/tools/blender/` line.
       - **Build & Deployment / Deployment / Copies:** Change "Copies: `camera_match/`, `forge_core/`, `forge_flame/`, `tools/blender/`" to "Copies: `camera_match/`, `forge_core/`, `forge_flame/`".
       - **Platform Requirements / Development:** Drop "Blender 4.5+ (for Blender→Flame animation testing)".
       - **Platform Requirements:** Drop the entire "**Blender Subprocess:**" subsection (Blender 4.5+ + bpy + mathutils lines).

    **Step 4c — .planning/codebase/STACK.md edits:**

    Apply the EXACT SAME surgical removals as in CLAUDE.md Step 4b-4. The two files must stay in sync per the `<!-- GSD:stack-start source:codebase/STACK.md -->` marker contract.

    **Step 4d — .planning/codebase/STRUCTURE.md edits:**

    1. **Directory Layout tree (~line 7):** Update the tree to reflect deletions:
       - Under `flame/`: drop `scale_picker_dialog.py`
       - Under `forge_flame/`: drop `camera_io.py`, `fbx_io.py`, `fbx_ascii.py`, `blender_bridge.py`, and the entire `templates/` subdirectory line
       - Drop the entire `tools/` block (`tools/blender/` with bake_camera, extract_camera, roundtrip_selftest.sh, sample_camera.json)
       - Under `tests/`: drop `test_camera_io.py`, `test_blender_roundtrip.py`, `test_blender_bridge.py`, `test_fbx_io.py`, `test_fbx_ascii.py`. Update test count from "264 tests" to a post-strip estimate (e.g., "~280 tests" — actual count to be verified by Task 3's pytest run; pick the conservative round number).
       - Under `tests/fixtures/`: drop `forge_fbx_baked.fbx`, `forge_fbx_probe.fbx` (and the others if listed)
       - Update `install.sh` annotation: from "(copies hook + forge_core + forge_flame + tools/blender to /opt)" to "(copies hook + forge_core + forge_flame to /opt)"

    2. **Directory Purposes section:**
       - Update `forge_flame/` description: drop "FBX ASCII parser/writer, Blender orchestration"; replace with the truthful smaller surface ("Solver adapter, Wiretap frame reader").
       - DELETE the entire `tools/blender/` subsection.
       - Update `tests/` description: drop the FBX-ASCII / Blender-roundtrip references; trim "264 tests" to the post-strip count.

    3. **Key File Locations section:**
       - **Entry Points:** Drop the `tools/blender/bake_camera.py` and `tools/blender/extract_camera.py` lines.
       - **Configuration:** Drop the `forge_flame/templates/camera_baked.fbx` line.
       - **Core Logic by Layer / Adapter layer:** Drop the lines for `forge_flame/camera_io.py`, `forge_flame/fbx_io.py`, `forge_flame/fbx_ascii.py`, `forge_flame/blender_bridge.py`. Keep the lines for `forge_flame/adapter.py` and `forge_flame/wiretap.py`.
       - **Hook orchestration:** Update the line "`flame/camera_match_hook.py` — `_launch_camera_match()`, `_export_camera_to_blender()`, `_import_camera_from_blender()`" to drop the export/import handlers: `flame/camera_match_hook.py` — `_launch_camera_match()` (sole user-facing handler post-260505-tb3).
       - **Testing:** Drop the lines for `tests/test_fbx_ascii.py`, `tests/test_camera_io.py`, `tests/test_blender_roundtrip.py`.

    4. **Where to Add New Code section:**
       - DELETE the entire "**Blender-side code:**" subsection.
       - Update "New feature (e.g., third VP mode):" to drop the FBX/Blender bullet if any.

    5. **Special Directories section:**
       - DELETE the entire "**forge_flame/templates:**" subsection.
       - DELETE the entire "**tools/blender:**" subsection.

    6. **Deployment Structure section:**
       - In the dev tree, drop `tools/blender/` line.
       - In the install tree, drop `tools/blender/` line.

    **Step 4e — .planning/notes/blender-strip-pending.md edits:**

    Update in place to mark Completed (preserve the historical decision context — option (a) per orchestrator's lean). Specifically:

    1. **Header line:** change `**Status:** Decided 2026-05-05, blocked on forge-blender Phase 6` to `**Status:** Completed in quick-260505-tb3 (2026-05-05). Originally decided 2026-05-05.`
    2. **Trigger to start line:** change `**Trigger to start:** forge-blender Phase 6 ...` to `**Trigger that fired:** orchestrator decision 2026-05-05 — strip ahead of forge-blender Phase 6 (forge-blender will absorb the export half via git-history cherry-pick from this repo).`
    3. **Estimated effort line:** change `**Estimated effort:** /gsd-quick scope when triggered ...` to `**Actual effort:** quick-260505-tb3, 4 atomic commits (source delete / test delete / hook surgery / install + docs).`
    4. **At the bottom of the file**, append a new section:

       ```markdown
       ## Completion record (2026-05-05)

       Stripped in quick-260505-tb3, 4-commit sequence:
       1. Source deletion (forge_flame Blender modules + tools/ + scale_picker_dialog)
       2. Test deletion (13 test modules + 4 FBX fixtures)
       3. Hook surgery (camera_match_hook.py + test_camera_match_hook.py + forge_flame/__init__.py)
       4. install.sh + docs (CLAUDE.md, STACK.md, STRUCTURE.md, this file)

       Surviving forge_flame/: __init__.py, adapter.py, wiretap.py.
       Surviving menu surface: FORGE > Camera > Open Camera Calibrator (only).
       Test count: 542 -> ~290 (verify exact in commit 3 message).

       The export half is preserved in this repo's git history (commits before
       quick-260505-tb3); forge-blender Phase 6 (v1.1) will cherry-pick when
       their planning gets there.

       The Blender→Flame import direction is fully retired — artists who want
       a refined camera back use Blender's `File → Export → FBX` then Flame's
       native FBX import. Manual but works.
       ```

    **Step 4f — .planning/config.json edit (optional cleanup, planner's call: I lean YES — it fits cleanly into this commit and stops the gsd-tools warning):**

    Remove the `"blender_launch_focus_steal": false,` line (line 6) from `.planning/config.json`. This was config for the deleted Blender launcher; gsd-tools warns about it as an unknown key. Edit must preserve valid JSON (drop the trailing comma on the previous line if needed — but in this file, the previous line is `"search_gitignored": false,` which already has a trailing comma; the line AFTER blender_launch_focus_steal is `"brave_search": false,` which also still needs its leading comma context. Actual edit: just delete the entire line 6, the surrounding JSON stays valid).

    Verify with:
    ```bash
    python3 -c "import json; json.load(open('.planning/config.json'))"
    ```

    **Step 4g — verify and commit:**

    ```bash
    bash -n install.sh                                              # syntax check
    conda run -n forge pytest tests/ -p no:pytest-blender -q        # tests still green
    python3 -c "import json; json.load(open('.planning/config.json'))"  # JSON valid
    ```

    All three must pass.

    **DO NOT touch the auto-memory file at `~/.claude/projects/-Users-cnoellert-Documents-GitHub-forge-calibrator/memory/phase_a2_blender_strip_pending.md`** — that lives outside the repo and the planner/executor cannot edit it. Flag in SUMMARY.md so the orchestrator handles it post-execution.

    **DO NOT touch STATE.md "Last activity" line or "Quick Tasks Completed" table** — orchestrator's Step 7 owns those updates per the standard quick-task workflow.

    Commit message:
    ```
    docs(quick-260505-tb3): install.sh + docs reflect Blender-strip; calibrator identity = pristine VP solve

    Phase A2 strip — Step 4/4: install.sh + docs.

    install.sh:
      - Drop SOURCE_SCALE_PICKER, SOURCE_BLENDER_SCRIPTS, BLENDER_SCRIPTS_DEST vars
      - Drop scale_picker_dialog source preflight + copy block
      - Drop tools/blender preflight + sync block
      - ADD self-healing purge for stale Blender artifacts at $INSTALL_DIR
        root + /opt/Autodesk/shared/forge_blender_scripts/ (mirrors the
        Matchbox purge from quick-260505-mrv shape)
      - bash -n install.sh: clean

    CLAUDE.md + .planning/codebase/STACK.md (kept in sync per GSD marker
    contract):
      - Drop "Recent work (v6.x) extends this with a Flame↔Blender..." sentence
      - Rewrite Core Value: drop the "AND the Flame↔Blender round-trip must
        preserve that fidelity end-to-end" clause; calibrator's identity
        becomes simply "The solved camera must be geometrically faithful to
        the plate."
      - Drop Blender Python from Languages
      - Drop Blender 4.5+ from Runtime
      - Drop bpy + mathutils from Key Dependencies
      - Drop FORGE_BLENDER_BIN + FORGE_BLENDER_SCRIPTS from env vars
      - Drop /opt/Autodesk/shared/python/tools/blender/ from install paths
      - Drop tools/blender from "Copies:" in Build & Deployment
      - Drop "Blender 4.5+ (for Blender→Flame animation testing)" from dev
      - Drop entire Blender Subprocess subsection from Platform Requirements

    .planning/codebase/STRUCTURE.md:
      - Refresh Directory Layout tree (no Blender entries)
      - Update test count estimate
      - Drop forge_flame/templates and tools/blender purposes
      - Drop deleted-module lines from Key File Locations
      - Drop Blender-side code subsection from "Where to Add New Code"
      - Drop forge_flame/templates and tools/blender Special Directories
      - Drop tools/blender from Deployment Structure trees

    .planning/notes/blender-strip-pending.md:
      - Marked Completed (preserves historical decision context per option (a))
      - Appended Completion record section

    .planning/config.json:
      - Drop "blender_launch_focus_steal" key (was config for the deleted
        Blender launcher; gsd-tools was warning about it as unknown)

    Verification:
      - bash -n install.sh: clean
      - pytest tests/ -p no:pytest-blender -q: GREEN
      - .planning/config.json: valid JSON

    NOT touched (out of executor scope; flag in SUMMARY.md):
      - Auto-memory file phase_a2_blender_strip_pending.md (lives outside repo)
      - STATE.md "Last activity" + "Quick Tasks Completed" table (orchestrator
        handles via standard quick-task Step 7)

    Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
    ```
  </action>
  <verify>
    <automated>bash -n install.sh && conda run -n forge pytest tests/ -p no:pytest-blender -q && python3 -c "import json; json.load(open('.planning/config.json'))" && grep -q "single-purpose VP-solve\|geometrically faithful to the plate" CLAUDE.md && ! grep -q "Flame↔Blender round-trip must preserve" CLAUDE.md && ! grep -q "FORGE_BLENDER_BIN" CLAUDE.md && ! grep -q "FORGE_BLENDER_BIN" .planning/codebase/STACK.md && ! grep -q "tools/blender" install.sh && ! grep -q "SOURCE_SCALE_PICKER" install.sh && grep -q "purged stale Blender round-trip artifacts" install.sh && grep -q "Completed in quick-260505-tb3" .planning/notes/blender-strip-pending.md && ! grep -q "blender_launch_focus_steal" .planning/config.json && echo "Task 4 install + docs verified"</automated>
  </verify>
  <done>install.sh syntactically clean, Blender bits removed, stale-artifact purge added. CLAUDE.md and STACK.md synced — Blender removed, calibrator identity rewritten to single-purpose VP solve. STRUCTURE.md tree refreshed. blender-strip-pending.md marked Completed with appended record. config.json no longer has blender_launch_focus_steal key. pytest green. JSON valid. Commit landed.</done>
</task>

</tasks>

<verification>
After all four commits land:

1. **Repo shape audit:**
   ```bash
   ls forge_flame/      # expect: __init__.py adapter.py wiretap.py
   test ! -e tools      # expect: pass (directory gone)
   test ! -e flame/scale_picker_dialog.py  # expect: pass
   ls tests/test_*.py | wc -l  # expect: ~31 tests (was ~44; minus 13)
   ls tests/fixtures/ 2>/dev/null  # expect: empty or non-FBX only
   ```

2. **Test gate:**
   ```bash
   conda run -n forge pytest tests/ -p no:pytest-blender -q
   ```
   Expected: GREEN. ~270-300 surviving tests (orchestrator's estimate).

3. **install.sh sanity:**
   ```bash
   bash -n install.sh                          # syntax OK
   bash install.sh --dry-run 2>&1 | tail -40   # dry-run flow looks sane
   ```

4. **Hook menu surface:**
   ```bash
   conda run -n forge python -c "
   import sys; sys.path.insert(0, '.')
   import flame.camera_match_hook as h
   menu = h.get_batch_custom_ui_actions()
   assert len(menu) == 2 and menu[0]['name']=='FORGE' and menu[1]['name']=='Camera'
   assert [a['name'] for a in menu[1]['actions']] == ['Open Camera Calibrator']
   assert not hasattr(h, 'get_action_custom_ui_actions')
   print('menu surface OK')
   "
   ```

5. **Git log shape:**
   ```bash
   git log --oneline -4
   ```
   Expected: 4 commits with the `quick-260505-tb3` scope, in the order Source / Tests / Hook / Docs.

6. **No surviving references to deleted modules anywhere in source:**
   ```bash
   grep -rn "forge_flame.blender_bridge\|forge_flame.fbx_ascii\|forge_flame.fbx_io\|forge_flame.camera_io\|scale_picker_dialog\|_export_camera_to_blender\|_export_camera_pipeline\|_LADDER_MENU_STOPS" --include='*.py' --include='*.sh' --include='*.md' . | grep -v __pycache__ | grep -v '.planning/notes/blender-strip-pending.md' | grep -v '.planning/quick/260505-tb3' | grep -v '.git/'
   ```
   Expected: zero hits (the only allowed surviving references are in the blender-strip-pending.md historical note and this plan/SUMMARY).

7. **forge-bridge stays Tier-3 dev-only:** confirm CLAUDE.md still describes forge-bridge as a dev probe (the quick-260505-mrv work is preserved; this strip does not touch the forge-bridge classification).
</verification>

<success_criteria>
- forge_flame/ contains exactly 3 .py files: __init__.py, adapter.py, wiretap.py
- tools/ directory does not exist
- flame/scale_picker_dialog.py does not exist
- pytest -p no:pytest-blender -q is GREEN
- bash -n install.sh exits 0
- install.sh ships a self-healing purge for stale pre-strip artifacts at /opt
- get_batch_custom_ui_actions returns FORGE > Camera > Open Camera Calibrator (only)
- get_action_custom_ui_actions function does not exist
- CLAUDE.md no longer claims a Flame↔Blender round-trip is part of calibrator identity
- .planning/codebase/STACK.md and STRUCTURE.md reflect the smaller surface
- .planning/notes/blender-strip-pending.md marked Completed with record appended
- .planning/config.json no longer has blender_launch_focus_steal (no more gsd-tools warning)
- 4 atomic commits land, each independently revertable
- ~5,000 LOC net deletion (sources + tests combined)
</success_criteria>

<output>
After completion, create `.planning/quick/260505-tb3-phase-a2-strip-all-flame-blender-camera-/260505-tb3-SUMMARY.md` with:

1. **What shipped** — 4 commits, file counts, LOC delta, test count delta (542 → actual surviving count)
2. **Verification evidence** — pytest output snippet, bash -n install.sh exit, menu-surface assertion output, repo-shape ls output
3. **Out-of-scope items orchestrator must handle (FLAGS):**
   - **forge_sender disposition (NEW):** the Blender addon at tools/blender/forge_sender/ was deleted as part of the strip. If forge_sender should survive in some form (e.g., as a viable inbound path for some artists), it needs a new home — its own repo OR absorbed into forge-blender separately. NOT decided here. Original architectural decision was to retire the Blender→Flame import direction entirely; if that's actually still desired in some form, this is the surface to revisit.
   - **Auto-memory update needed:** `~/.claude/projects/-Users-cnoellert-Documents-GitHub-forge-calibrator/memory/phase_a2_blender_strip_pending.md` should be updated to "Completed" status or deleted. Memory lives outside the repo so executor cannot touch it. Orchestrator should handle.
   - **STATE.md update:** "Last activity" line + "Quick Tasks Completed" table need the standard quick-task Step 7 update with this task's row.
   - **forge-blender repo coordination (REMINDER, not action):** forge-blender's planning at `.planning/notes/camera-push-architecture-decision.md` and `.planning/research/calibrator-fbx-ascii-lessons.md` already references this strip. No coordination work needed from this side; just a reminder that when forge-blender Phase 6 starts, they will cherry-pick from this repo's pre-strip git history.
4. **What artists temporarily lose** — the "Export Camera to Blender" menu entry is gone until forge-blender v1.1 ships its "Send Camera to Blender" replacement. The Import direction was already gone (D-06 in calibrator). VP-solve flow (the core value) is unchanged.
5. **Follow-up todos surfaced (none expected from this strip itself, but flag any encountered during execution).**
</output>
